var___95__prelude = do_import("standardGrace", gracecode_standardGrace);
if (typeof gctCache !== "undefined")
  gctCache["lexer"] = "classes:\nconfidential:\n accum\n accum:=(accum': Unknown) \u2192 Done\n alphaNums\n asciiLetters\n braceChange\n braceChange:=(braceChange': Unknown) \u2192 Done\n braceDepthAtStartOfInterp\n braceDepthAtStartOfInterp:=(braceDepthAtStartOfInterp': Unknown) \u2192 Done\n brackets\n codepoint\n codepoint:=(codepoint': Unknown) \u2192 Done\n commentSpaceState\n commentState\n currentLineBraceDepth\n currentLineBraceDepth:=(currentLineBraceDepth': Unknown) \u2192 Done\n currentLineIndent\n currentLineIndent:=(currentLineIndent': Unknown) \u2192 Done\n defaultState\n digits\n dotState\n extendedStringState\n identifierState\n inStr\n inStr:=(inStr': Unknown) \u2192 Done\n indentOfLineBeingContinued\n indentOfLineBeingContinued:=(indentOfLineBeingContinued': Unknown) \u2192 Done\n indentStack\n indentStack:=(indentStack': Unknown) \u2192 Done\n indentationState\n inputLines\n interpDepth\n interpDepth:=(interpDepth': Unknown) \u2192 Done\n keywords\n lastNonCommentToken\n lastNonCommentToken:=(lastNonCommentToken': Unknown) \u2192 Done\n lineNumber\n lineNumber:=(lineNumber': Unknown) \u2192 Done\n linePosition\n linePosition:=(linePosition': Unknown) \u2192 Done\n mantissa\n maxIndentOfContinuation\n maxIndentOfContinuation:=(maxIndentOfContinuation': Unknown) \u2192 Done\n nl_cr\n noSuchLine\n numberBaseState\n numberDotState\n numberExponentSignState\n numberExponentState\n numberFractionState\n numberStartState\n operatorChars\n operatorState\n pragmaBodyState\n pragmaPrefixState\n pragmaTrailingWhitespaceState\n priorLineBraceDepth\n priorLineBraceDepth:=(priorLineBraceDepth': Unknown) \u2192 Done\n priorLineIndent\n priorLineIndent:=(priorLineIndent': Unknown) \u2192 Done\n quotedStringEscapedState\n quotedStringState\n rBraceState\n selfModes\n slashState\n spaceChars\n startLine\n startLine:=(startLine': Unknown) \u2192 Done\n startPosition\n startPosition:=(startPosition': Unknown) \u2192 Done\n startState\n state\n state:=(state': Unknown) \u2192 Done\n stringContainsInterp\n stringContainsInterp:=(stringContainsInterp': Unknown) \u2192 Done\n stringStart\n stringStart:=(stringStart': Unknown) \u2192 Done\n tab\n tokenBase\n tokens\n tokens:=(tokens': Unknown) \u2192 Done\n unichars\n unichars:=(unichars': Unknown) \u2192 Done\n unicodeState\n unmatchedLeftBraces\n unmatchedLeftBraces:=(unmatchedLeftBraces': Unknown) \u2192 Done\ndialect:\n standardGrace\nfresh-methods:\n arrowToken\n bindToken\n colonToken\n commaToken\n commentToken(1)\n dotToken\n eofToken\n identifierToken(1)\n keywordToken(1)\n lBraceToken\n lGenericToken\n lParenToken\n lSquareToken\n multiLineStringToken(1)\n numToken(2)\n opToken(1)\n rBraceToken\n rGenericToken\n rParenToken\n rSquareToken\n semicolonToken\n separatorTokenAt(2)\n sofToken\n stringToken(1)\n token\nfresh:arrowToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:bindToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:colonToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:commaToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:commentToken(1):\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:dotToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:eofToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:identifierToken(1):\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:keywordToken(1):\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:lBraceToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:lGenericToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:lParenToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:lSquareToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:multiLineStringToken(1):\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:numToken(2):\n ::(1)\n ==(1)\n asDebugString\n asString\n base\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:opToken(1):\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:rBraceToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:rGenericToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:rParenToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:rSquareToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:semicolonToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:separatorTokenAt(2):\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:sofToken:\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:stringToken(1):\n ::(1)\n ==(1)\n asDebugString\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isMe(1)\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n myIdentityHash\n next\n null\n prev\n size\n value\n \u2260(1)\nfresh:token:\n ::(1)\n ==(1)\n asString\n endLine\n endPos\n hasNext\n hasPrev\n hash\n indent\n isArrow\n isBind\n isColon\n isComma\n isComment\n isDot\n isEof\n isHeader\n isIdentifier\n isKeyword\n isLBrace\n isLGeneric\n isLParen\n isLSquare\n isNum\n isOp\n isRBrace\n isRGeneric\n isRParen\n isRSquare\n isSeparator\n isString\n kind\n line\n linePos\n next\n next:=(_)\n null\n prev\n prev:=(_)\n size\n value\n \u2260(1)\nmodules:\n collectionsPrelude\n errormessages\n fastDict\n io\n standardGrace\n sys\n unicode\n unixFilePath\n util\npath:\n /Users/black/Development/mg/gracelang/minigrace/lexer.grace\npublic:\n advanceTo(1)\n arrowToken\n bindToken\n checkAndRecordIndentStatus(1)\n checkConstantIndent\n checkIndentationReset\n checkSeparator(1)\n checkSeparatorString(1)\n colonToken\n commaToken\n commentToken(1)\n currentLineStartsWithCloseBracket(1)\n dotToken\n emit(1)\n emitNewlineSeparator\n eofToken\n escapedNewlineError\n followingCharacter\n fromBase(2)\n hex4(1)\n hexdecchar(1)\n identifierToken(1)\n initialize\n isBadControl(1)\n isBadSeparator(1)\n isBlockEnd(1)\n isBlockStart(1)\n isBracket(1)\n isContinuationLine\n isDigit(1)\n isIdentifierChar(1)\n isIndentationChangeOne\n isLetter(1)\n isLineEmpty(1)\n isOperatorChar(2)\n isSelfMode(1)\n isSpaceChar(1)\n keywordToken(1)\n lBraceToken\n lGenericToken\n lParenToken\n lSquareToken\n lexInputLines\n lexLines(1)\n lexString(1)\n lexfile(1)\n lexicalError(1)\n mantissa:=(1)\n multiLineStringToken(1)\n newline(1)\n newlineInInterpolationError\n numToken(2)\n opToken(1)\n padl(3)\n priorLineEndsWithOpenBracket\n rBraceToken\n rGenericToken\n rParenToken\n rSquareToken\n recordContinuationStatus\n recordNewIndentation\n reportEmptyStringInterpolation\n rightBrace\n saveDataForPriorLine\n semicolonToken\n separatorTokenAt(2)\n sofToken\n store(1)\n stringToken(1)\n tallyLeftBrace\n terminateContinuationIfNecessary\n token\n tokenBase:=(1)\n unclosedStringError\npublicMethodTypes:\n advanceTo(s)\n arrowToken\n bindToken\n checkAndRecordIndentStatus(currentCharacter)\n checkConstantIndent\n checkIndentationReset\n checkSeparator(c)\n checkSeparatorString(c)\n colonToken\n commaToken\n commentToken(s)\n currentLineStartsWithCloseBracket(currentCharacter)\n dotToken\n emit(t)\n emitNewlineSeparator\n eofToken\n escapedNewlineError\n followingCharacter\n fromBase(str, base)\n hex4(num)\n hexdecchar(c)\n identifierToken(s)\n initialize\n isBadControl(ch)\n isBadSeparator(ch)\n isBlockEnd(currentCharacter)\n isBlockStart(currentCharacter)\n isBracket(ch)\n isContinuationLine\n isDigit(ch)\n isIdentifierChar(ch)\n isIndentationChangeOne\n isLetter(ch)\n isLineEmpty(currentCharacter)\n isOperatorChar(c, ordval)\n isSelfMode(ch)\n isSpaceChar(c)\n keywordToken(v)\n lBraceToken\n lGenericToken\n lParenToken\n lSquareToken\n lexInputLines\n lexLines(input)\n lexString(inputString)\n lexfile(file)\n lexicalError(message)\n mantissa:=(mantissa': Unknown) \u2192 Done\n multiLineStringToken(s)\n newline(currentCharacter)\n newlineInInterpolationError\n numToken(v, b)\n opToken(v)\n padl(s, width, padCh)\n priorLineEndsWithOpenBracket\n rBraceToken\n rGenericToken\n rParenToken\n rSquareToken\n recordContinuationStatus\n recordNewIndentation\n reportEmptyStringInterpolation\n rightBrace\n saveDataForPriorLine\n semicolonToken\n separatorTokenAt(lineNum, pos)\n sofToken\n store(c)\n stringToken(s)\n tallyLeftBrace\n terminateContinuationIfNecessary\n token\n tokenBase:=(tokenBase': Unknown) \u2192 Done\n unclosedStringError\ntypes:\n";
if (typeof originalSourceLines !== "undefined") {
  originalSourceLines["lexer"] = [
    "import \"util\" as util",
    "import \"unicode\" as unicode",
    "import \"errormessages\" as errormessages",
    "import \"fastDict\" as map",
    "",
    "def keywords = map.dictionary.empty",
    "keywords.at \"alias\" put true",
    "keywords.at \"as\" put true",
    "keywords.at \"class\" put true",
    "keywords.at \"def\" put true",
    "keywords.at \"dialect\" put true",
    "keywords.at \"exclude\" put true",
    "keywords.at \"import\" put true",
    "keywords.at \"inherit\" put true",
    "keywords.at \"interface\" put true",
    "keywords.at \"is\" put true",
    "keywords.at \"method\" put true",
    "keywords.at \"object\" put true",
    "keywords.at \"once\" put true",
    "keywords.at \"prefix\" put true",
    "keywords.at \"return\" put true",
    "keywords.at \"trait\" put true",
    "keywords.at \"type\" put true",
    "keywords.at \"use\" put true",
    "keywords.at \"var\" put true",
    "keywords.at \"where\" put true",
    "",
    "def operatorChars = \"-&|:$#\\\\%^@?*/+!~\"",
    "method isOperatorChar(c, ordval) {",
    "    if (operatorChars.contains(c)) then { return true }",
    "    if (unicode.isSymbolMathematical(ordval)) then { return true }",
    "    return false",
    "}",
    "",
    "def spaceChars = \" \\u00A0\"    // ASCII space + non-breaking space",
    "def tab = \"\\t\"",
    "method isBadSeparator(ch) {",
    "    if (spaceChars.contains(ch)) then { return false }",
    "    if (tab == ch) then { return true }",
    "    return unicode.inCategory(ch, \"Z\")",
    "}",
    "",
    "def nl_cr = \"\\n\\r\"      // nl and cr",
    "method isBadControl(ch) {",
    "    if (nl_cr.contains(ch)) then { return false }",
    "    return unicode.inCategory(ch, \"C\")",
    "}",
    "",
    "def selfModes = \"(),\\{}[];⟦⟧\"",
    "method isSelfMode(ch) { selfModes.contains(ch) }",
    "",
    "def brackets = \"()\\{}[]⟦⟧\"",
    "method isBracket(ch) { brackets.contains(ch) }",
    "",
    "def alphaNums = \"_'0123456789abcdefghijklmnopqrstuvwzyzABCDEFGHIJKLMNOPQRSTUVWXYZ\"",
    "method isIdentifierChar(ch) {",
    "    if (alphaNums.contains(ch)) then { return true }",
    "    if (unicode.inCategory(ch, \"L\")) then { return true }",
    "    if (unicode.inCategory(ch, \"N\")) then { return true }",
    "    return false",
    "}",
    "def digits = \"0123456789\"",
    "method isDigit(ch) { digits.contains(ch) }",
    "",
    "def asciiLetters = \"abcdefghijklmnopqrstuvwzyzABCDEFGHIJKLMNOPQRSTUVWXYZ\"",
    "method isLetter(ch) {",
    "    if (asciiLetters.contains(ch)) then { return true }",
    "    if (unicode.inCategory(ch, \"L\")) then { return true }",
    "    return false",
    "}",
    "",
    "method padl(s, width, padCh) {",
    "    if (s.size >= width) then {",
    "        return s",
    "    }",
    "    var s' := s",
    "    while {s'.size < width} do {",
    "        s' := padCh ++ s'",
    "    }",
    "    return s'",
    "}",
    "",
    "method hex4(num) {",
    "    padl(num.inBase 16, 4, \"0\")",
    "}",
    "",
    "def noSuchLine = singleton \"noSuchLine\"",
    "",
    "method initialize {",
    "    lineNumber := 1",
    "    startPosition := 1",
    "    currentLineIndent := 0",
    "    unmatchedLeftBraces := 0",
    "    startLine := 1",
    "    unichars := 0",
    "    currentLineBraceDepth := 0",
    "    indentOfLineBeingContinued := noSuchLine",
    "    indentStack := list.with 0",
    "    priorLineBraceDepth := 0",
    "    priorLineIndent := 0",
    "    accum := \"\"",
    "    inStr := false",
    "    codepoint := 0",
    "    interpDepth := 0",
    "    stringContainsInterp := false",
    "    lastNonCommentToken := sofToken",
    "}",
    "",
    "var tokens                  // a linked-list of output tokens",
    "var lastNonCommentToken     // the most last token pushed that was not a comment",
    "var lineNumber              // the current input line number",
    "var linePosition            // the character position on the input line",
    "var startLine               // the line on which the current token starts",
    "var startPosition           // character position of the start of the current token",
    "var currentLineIndent       // indent of the current line, i.e, the number of leading spaces",
    "var stringStart             // the start position of the current string, if there is one",
    "var unichars                // the number of character remaining in a unicode escape",
    "var braceChange             // the number of { minus the number of } on the current line",
    "var currentLineBraceDepth   // the brace-depth of the current line",
    "var unmatchedLeftBraces     // the number of unmatched left braces on the current line",
    "var indentOfLineBeingContinued  // noSuchLine if there is no continuation in progress",
    "var indentStack             // the indent is the number of leading spaces,",
    "                            // and hence one less than the position",
    "                            // of the first non-space character.",
    "var maxIndentOfContinuation // the maximum indentation of any of the",
    "                            // lines of a continued line",
    "var priorLineBraceDepth",
    "var priorLineIndent",
    "var state                   // the state of the lexer state machie",
    "var accum                   // the accumulated characters of the current token",
    "var inStr                   // are we in a string?",
    "var codepoint               // calcultes the codepoint for a unicode escape",
    "var interpDepth             // string interpolation depth",
    "var stringContainsInterp    // does the current string contain an interpolation?",
    "                            // if so, we prenthesize it.",
    "var braceDepthAtStartOfInterp",
    "    // the brace depth when this string interpolation started",
    "",
    "initialize",
    "",
    "def inputLines = util.lines // an alias for the list util.lines",
    "",
    "method tallyLeftBrace {",
    "    unmatchedLeftBraces := unmatchedLeftBraces + 1",
    "    currentLineBraceDepth := currentLineBraceDepth + 1",
    "}",
    "method rightBrace {",
    "    if (unmatchedLeftBraces > 0) then {",
    "        unmatchedLeftBraces := unmatchedLeftBraces - 1",
    "    }",
    "    currentLineBraceDepth := currentLineBraceDepth - 1",
    "    if ((currentLineBraceDepth < 0)) then {",
    "        errormessages.syntaxError \"there is no opening brace corresponding to this closing brace\"",
    "          atRange (lineNumber, startPosition, linePosition)",
    "    }",
    "}",
    "",
    "class token {",
    "    use equality",
    "    def line is public = lineNumber",
    "    def indent is public = currentLineIndent",
    "    def linePos is public = startPosition",
    "",
    "    def null = object {",
    "        use identityEquality",
    "        method asString { \"⏚\" }",
    "        method isHeader { true }",
    "    }",
    "    var next is public := null",
    "    var prev is public := null",
    "",
    "    method == (other) {",
    "        if (null == other) then {",
    "            false",
    "        } elseif {false == other} then {",
    "            false",
    "        } else {",
    "            (line == other.line) && (linePos == other.linePos)",
    "        }",
    "    }",
    "    method hash { hashCombine(line.hash, linePos.hash) }",
    "    method asString { \"({line}.{linePos}){self.kind} {self.value}\" }",
    "    method value { abstract }",
    "    method size { abstract }",
    "    method kind { abstract }",
    "    method endLine { line }",
    "    method endPos { linePos + size - 1 }",
    "    method isHeader { false }",
    "    method hasPrev { prev.isHeader.not }",
    "    method hasNext { next.isHeader.not }",
    "    method isIdentifier { false }",
    "    method isString { false }",
    "    method isComment { false }",
    "    method isLBrace { false }",
    "    method isRBrace { false }",
    "    method isLParen { false }",
    "    method isRParen { false }",
    "    method isLSquare { false }",
    "    method isRSquare { false }",
    "    method isComma { false }",
    "    method isColon { false }",
    "    method isDot { false }",
    "    method isNum { false }",
    "    method isKeyword { false }",
    "    method isOp { false }",
    "    method isArrow { false }",
    "    method isBind { false }",
    "    method isSeparator { false }",
    "    method isLGeneric { false }",
    "    method isRGeneric { false }",
    "    method isEof { false }",
    "}",
    "",
    "class identifierToken(s) {",
    "    inherit token",
    "    def kind is public = \"identifier\"",
    "    def value is public = s",
    "    def size is public = s.size",
    "    method isIdentifier { true }",
    "}",
    "class stringToken(s) {",
    "    inherit token",
    "    def kind is public = \"string\"",
    "    def value is public = s",
    "    def size is public = linePosition - startPosition + 1",
    "    method isString { true }",
    "}",
    "class multiLineStringToken(s) {",
    "    inherit token",
    "    def kind is public = \"string\"",
    "    def value is public = s",
    "    def size is public = s.size + 2",
    "    def linePos is public = stringStart",
    "    def line is override, public = startLine",
    "    def endLine is override, public = lineNumber",
    "    def endPos is override, public = linePosition",
    "    method isString { true }",
    "}",
    "class commentToken(s) {",
    "    inherit token",
    "    def kind is public = \"comment\"",
    "    def value is public = s",
    "    def size is public = s.size + 2",
    "    method isComment { true }",
    "}",
    "class lBraceToken {",
    "    inherit token",
    "    def kind is public = \"lbrace\"",
    "    def value is public = \"\\{\"",
    "    def size is public = 1",
    "    method isLBrace { true }",
    "}",
    "class rBraceToken {",
    "    inherit token",
    "    def kind is public = \"rbrace\"",
    "    def value is public = \"}\"",
    "    def size is public = 1",
    "    method isRBrace { true }",
    "}",
    "class lParenToken {",
    "    inherit token",
    "    def kind is public = \"lparen\"",
    "    def value is public = \"(\"",
    "    def size is public = 1",
    "    method isLParen { true }",
    "}",
    "class rParenToken {",
    "    inherit token",
    "    def kind is public = \"rparen\"",
    "    def value is public = \")\"",
    "    def size is public = 1",
    "    method isRParen { true }",
    "}",
    "class lSquareToken {",
    "    inherit token",
    "    def kind is public = \"lsquare\"",
    "    def value is public = \"[\"",
    "    def size is public = 1",
    "    method isLSquare { true }",
    "}",
    "class rSquareToken {",
    "    inherit token",
    "    def kind is public = \"rsquare\"",
    "    def value is public = \"]\"",
    "    def size is public = 1",
    "    method isRSquare { true }",
    "}",
    "class commaToken {",
    "    inherit token",
    "    def kind is public = \"comma\"",
    "    def value is public = \",\"",
    "    def size is public = 1",
    "    method isComma { true }",
    "}",
    "class colonToken {",
    "    inherit token",
    "    def kind is public = \"colon\"",
    "    def value is public = \":\"",
    "    def size is public = 1",
    "    method isColon { true }",
    "}",
    "class dotToken {",
    "    inherit token",
    "    def kind is public = \"dot\"",
    "    def value is public = \".\"",
    "    def size is public = 1",
    "    method isDot { true }",
    "}",
    "class numToken(v, b) {",
    "    inherit token",
    "    def kind is public = \"num\"",
    "    def value is public = v",
    "    def base is public = b",
    "    def size is public = linePosition - startPosition + 1",
    "    method isNum { true }",
    "}",
    "class keywordToken(v) {",
    "    inherit token",
    "    def kind is public = \"keyword\"",
    "    def value is public = v",
    "    def size is public = v.size",
    "    method isKeyword { true }",
    "}",
    "class opToken(v) {",
    "    inherit token",
    "    def kind is public = \"op\"",
    "    def value is public = v",
    "    def size is public = v.size",
    "    method isOp { true }",
    "}",
    "class arrowToken {",
    "    inherit token",
    "    def kind is public = \"arrow\"",
    "    def value is public = \"→\"",
    "    def size is public = 1",
    "    method isArrow { true }",
    "}",
    "class bindToken {",
    "    inherit token",
    "    def kind is public = \"bind\"",
    "    def value is public = \":=\"",
    "    def size is public = 2",
    "    method isBind { true }",
    "}",
    "class semicolonToken {",
    "    inherit token",
    "    def kind is public = \"semicolon\"",
    "    def value is public = \";\"",
    "    def size is public = 1",
    "    method isSeparator { true }",
    "}",
    "class separatorTokenAt(lineNum, pos) {",
    "    inherit token",
    "    def kind is public = \"separator\"",
    "    def value is public = \"\\n\"",
    "    def size is public = 1",
    "    method asString { \"({line}.{linePos}){self.kind} \\\\n\" }",
    "    def line is override, public = lineNum",
    "    def linePos is override, public = pos",
    "    def endLine is override, public = lineNumber",
    "    def endPos is override, public = currentLineIndent",
    "    method isSeparator { true }",
    "}",
    "class lGenericToken {",
    "    inherit token",
    "    def kind is public = \"lgeneric\"",
    "    def value is public = \"⟦\"",
    "    def size is public = 1",
    "    method isLGeneric { true }",
    "}",
    "class rGenericToken {",
    "    inherit token",
    "    def kind is public = \"rgeneric\"",
    "    def value is public = \"⟧\"",
    "    def size is public = 1",
    "    method isRGeneric { true }",
    "}",
    "class eofToken {",
    "    inherit token",
    "    def kind is public = \"eof\"",
    "    def value is public = \"\"",
    "    def size is public = 0",
    "    method isEof { true }",
    "}",
    "class sofToken {",
    "    inherit token",
    "    use identityEquality",
    "    def kind is public = \"sof\"",
    "    def value is public = \"⏚\"",
    "    def size is public = 0",
    "    method isHeader { true }",
    "}",
    "",
    "method advanceTo(s) { state := s }",
    "method emit(t) {",
    "    tokens.push(t)",
    "    if (t.isComment.not) then { lastNonCommentToken := t }",
    "    accum := \"\"",
    "}",
    "method emitNewlineSeparator {",
    "    // We have already consumed the leading spaces on the following line.",
    "    // We want the newline's coordinates to be those of the end of the prior line.",
    "    if (tokens.isEmpty) then { return }",
    "    def lastToken = tokens.last",
    "    emit(separatorTokenAt(lineNumber - 1, lastToken.endPos + 1))",
    "}",
    "method store(c) { accum := accum ++ c }",
    "",
    "def startState = object {",
    "    method consume(c){",
    "        if (c == \"#\") then {",
    "            advanceTo(pragmaPrefixState)",
    "        } else {",
    "            advanceTo(indentationState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "",
    "def pragmaPrefixState = object {",
    "    method consume(c){",
    "        checkSeparator(c)",
    "        if (c == \"\\n\") then {",
    "            errormessages.syntaxError \"incomplete pragma definition\"",
    "                atRange(lineNumber, startPosition, linePosition)",
    "        } elseif {isSpaceChar(c)} then {",
    "            if (accum == \"pragma\") then {",
    "                accum := \"\"",
    "                advanceTo(pragmaBodyState)",
    "            } else {",
    "                errormessages.syntaxError \"pragmas must start with #pragma\"",
    "                    atRange(lineNumber, startPosition, linePosition)",
    "            }",
    "        } else {",
    "            store(c)",
    "        }",
    "    }",
    "}",
    "",
    "def pragmaBodyState = object {",
    "    method consume(c){",
    "        checkSeparator(c)",
    "        if ((c == \"\\n\") || isSpaceChar(c)) then {",
    "            //a complete pragma has been found",
    "            util.processExtension(accum)",
    "            accum := \"\"",
    "            advanceTo(pragmaTrailingWhitespaceState)",
    "            state.consume(c)",
    "        } else {",
    "            store(c)",
    "        }",
    "    }",
    "}",
    "",
    "def pragmaTrailingWhitespaceState = object {",
    "    method consume(c) {",
    "        checkSeparator(c)",
    "        if (c == \"\\n\") then {",
    "            advanceTo(startState)",
    "        } elseif {!isSpaceChar(c)} then {",
    "            errormessages.syntaxError \"pragmas must be a single word\"",
    "                atRange(lineNumber, startPosition, linePosition)",
    "        }",
    "    }",
    "}",
    "",
    "def defaultState = object {",
    "    method consume(c) {",
    "        def ordval = c.ord",
    "        checkSeparator(c)",
    "        startPosition := linePosition",
    "        startLine := lineNumber",
    "        if (spaceChars.contains(c)) then {",
    "            //do nothing",
    "        } elseif {c == \"\\\"\"} then {",
    "            inStr := true",
    "            stringStart := linePosition",
    "            advanceTo(quotedStringState)",
    "        } elseif {c == \"‹\"} then {",
    "            inStr := true",
    "            stringStart := linePosition",
    "            advanceTo(extendedStringState)",
    "        } elseif {isDigit(c)} then {",
    "            if ((tokens.size > 0) && {tokens.last.kind == \"num\"}) then {",
    "                def suggestion = errormessages.suggestion.new",
    "                suggestion.deleteRange(tokens.last.linePos+tokens.last.size, linePosition - 1)",
    "                    onLine(lineNumber)",
    "                errormessages.syntaxError \"consecutive numbers are not allowed\"",
    "                    atRange(lineNumber, tokens.last.linePos, linePosition)",
    "                    withSuggestion(suggestion)",
    "            }",
    "            startPosition := linePosition",
    "            advanceTo(numberStartState)",
    "            state.consume(c)",
    "        } elseif {isIdentifierChar(c)} then {",
    "            advanceTo(identifierState)",
    "            state.consume(c)",
    "        } elseif {c == \"/\"} then {",
    "            advanceTo(slashState)",
    "            state.consume(c)",
    "        } elseif {c == \".\"} then {",
    "            advanceTo(dotState)",
    "            state.consume(c)",
    "        } elseif {isOperatorChar(c, ordval)} then {",
    "            advanceTo(operatorState)",
    "            state.consume(c)",
    "        } elseif {c == \",\"} then {",
    "            emit(commaToken)",
    "        } elseif {c == \"\\{\"} then {",
    "            tallyLeftBrace",
    "            emit(lBraceToken)",
    "        } elseif {c == \"}\"} then {",
    "            advanceTo(rBraceState)",
    "            state.consume(c)",
    "        } elseif {c == \"(\"} then {",
    "            emit(lParenToken)",
    "        } elseif {c == \")\"} then {",
    "            if ((tokens.size > 0) && {tokens.last.kind == \"lparen\"}) then {",
    "                errormessages.syntaxError(\"empty parenthesis are not allowed; \" ++",
    "                    \"remove them, or put something between them\")",
    "                    atRange(lineNumber, tokens.last.linePos, linePosition)",
    "            }",
    "            emit(rParenToken)",
    "        } elseif {c == \"[\"} then {",
    "            emit(lSquareToken)",
    "        } elseif {c == \"]\"} then {",
    "            emit(rSquareToken)",
    "        } elseif {c == \"⟦\"} then {",
    "            emit(lGenericToken)",
    "        } elseif {c == \"⟧\"} then {",
    "            emit(rGenericToken)",
    "        } elseif {c == \";\"} then {",
    "            emit(semicolonToken)",
    "        } elseif {c == \"\\n\"} then {",
    "            if (interpDepth > 0) then { newlineInInterpolationError }",
    "            currentLineIndent := 0",
    "            advanceTo(indentationState)",
    "        } else {",
    "            if((unicode.isSeparator(ordval).not) &&",
    "                    (unicode.isControl(ordval).not) &&",
    "                    (ordval != 10) &&",
    "                    (ordval != 13) &&",
    "                    (ordval != 32)) then {",
    "                errormessages.syntaxError(\"{unicode.name(c)} (U+{hex4(ordval)}) \"",
    "                    ++ \"is not a valid character\")",
    "                    atPosition(lineNumber, linePosition)",
    "            }",
    "        }",
    "    }",
    "}",
    "",
    "def rBraceState = object {",
    "    method consume (c){",
    "        if (interpDepth > 0) then {",
    "            if (tokens.last.kind == \"lparen\") then {",
    "                reportEmptyStringInterpolation",
    "            }",
    "            if (currentLineBraceDepth == braceDepthAtStartOfInterp) then {",
    "                interpDepth := interpDepth - 1",
    "            }",
    "            if (interpDepth == 0) then {",
    "                emit (rParenToken)",
    "                emit (opToken(\"++\"))",
    "                inStr := true",
    "                advanceTo(quotedStringState)",
    "                return",
    "            }",
    "        }",
    "        rightBrace",
    "        emit (rBraceToken)",
    "        advanceTo (defaultState)",
    "    }",
    "}",
    "",
    "def slashState = object {",
    "    method consume (c) {",
    "        checkSeparator(c)",
    "        if (c == \"/\") then {",
    "            store(c)",
    "            if (accum == \"//\") then {",
    "                advanceTo(commentSpaceState)",
    "            }",
    "        } elseif {accum == \"/\"} then {",
    "            advanceTo(operatorState)",
    "            state.consume(c)",
    "        } else {",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "",
    "def dotState = object {",
    "    method consume (c) {",
    "        checkSeparator(c)",
    "        if (c == \".\") then {",
    "            store(c)",
    "            if (accum == \"...\") then {",
    "                advanceTo(identifierState)",
    "            }",
    "        } elseif {accum == \"..\"} then {",
    "            advanceTo(operatorState)",
    "            state.consume(c)",
    "        } elseif {isDigit(c)} then {",
    "            accum := \"0.\"",
    "            //should an error be thrown instead?",
    "            advanceTo(numberFractionState)",
    "            state.consume(c)",
    "        } else {",
    "            emit(dotToken)",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "",
    "def identifierState = object {",
    "    method consume (c) {",
    "        checkSeparator(c)",
    "        if (!isIdentifierChar(c)) then {",
    "            if (keywords.containsKey(accum)) then {",
    "                emit(keywordToken(accum))",
    "            } else {",
    "                emit(identifierToken(accum))",
    "            }",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        } else {",
    "            store(c)",
    "        }",
    "    }",
    "}",
    "def quotedStringState = object {",
    "    method consume (c) {",
    "",
    "        checkSeparatorString (c)",
    "        if (c == \"\\\"\") then {               // the end of the string",
    "            emit(stringToken(accum))",
    "            if (stringContainsInterp && (interpDepth == 0)) then {",
    "                emit(rParenToken)",
    "                stringContainsInterp := false",
    "            }",
    "            advanceTo(defaultState)",
    "            inStr := false",
    "        } elseif {c == \"\\n\"} then {",
    "            unclosedStringError",
    "        } elseif {c == \"\\{\"} then {",
    "            def strToken = stringToken(accum)",
    "            startPosition := linePosition",
    "            if (!stringContainsInterp) then {",
    "                emit(lParenToken)",
    "                stringContainsInterp := true",
    "            }",
    "            emit(strToken)",
    "            emit(opToken(\"++\"))",
    "            emit(lParenToken)",
    "            advanceTo(defaultState)",
    "            braceDepthAtStartOfInterp := currentLineBraceDepth",
    "            inStr := false",
    "            interpDepth := interpDepth + 1",
    "        } elseif {c == \"\\\\\"} then {",
    "            advanceTo(quotedStringEscapedState)",
    "        } else {",
    "            store(c)",
    "        }",
    "    }",
    "}",
    "def unicodeState = object {",
    "    method consume(c){",
    "        unichars := unichars - 1",
    "        codepoint := codepoint * 16",
    "        codepoint := codepoint + hexdecchar(c)",
    "        //print \"adding unicode char {c}\"",
    "        if (unichars == 0) then {",
    "            // At the end of the sequence construct",
    "            // the character in the unicode library.",
    "            store(unicode.create(codepoint))",
    "            advanceTo(quotedStringState)",
    "        }",
    "    }",
    "}",
    "def quotedStringEscapedState = object {",
    "    method consume (c) {",
    "        checkSeparatorString (c)",
    "        advanceTo(quotedStringState)",
    "        if (c == \"\\n\") then {",
    "            escapedNewlineError",
    "        } elseif { c == \"n\" } then {",
    "            // Newline escape",
    "            accum := accum ++ \"\\u000a\"",
    "        } elseif { c == \"u\" } then {",
    "            // Beginning of a four-digit Unicode escape",
    "            // (for a BMP codepoint).",
    "            unichars := 4",
    "            codepoint := 0",
    "            advanceTo(unicodeState)",
    "        } elseif { c == \"U\" } then {",
    "            // Beginning of a six-digit Unicode escape",
    "            // (for a general codepoint).",
    "            unichars := 6",
    "            codepoint := 0",
    "            advanceTo(unicodeState)",
    "        } elseif { c == \"t\" } then {",
    "            // Tab escape",
    "            accum := accum ++ \"\\u0009\"",
    "        } elseif { c == \"r\" } then {",
    "            // Carriage return escape",
    "            accum := accum ++ \"\\u000d\"",
    "        } elseif { c == \"b\" } then {",
    "            // Backspace escape",
    "            accum := accum ++ \"\\u0008\"",
    "        } elseif { c == \"l\" } then {",
    "            // LINE SEPARATOR escape",
    "            accum := accum ++ \"\\u2028\"",
    "        } elseif { c == \"f\" } then {",
    "            // Form feed/page down escape",
    "            accum := accum ++ \"\\u000c\"",
    "        } elseif { c == \"e\" } then {",
    "            // Escape escape",
    "            accum := accum ++ \"\\u001b\"",
    "        } elseif { c == \"_\" } then {",
    "            // non-breaking space",
    "            accum := accum ++ \"\\u00a0\"",
    "        } else {",
    "            // For any other character preceded by \\,",
    "            // insert it literally.",
    "            store(c)",
    "        }",
    "    }",
    "}",
    "",
    "def extendedStringState = object {",
    "    method consume (c) {",
    "",
    "        if (c == \"›\") then {",
    "            emit(multiLineStringToken(accum))",
    "            inStr := false",
    "            advanceTo(defaultState)",
    "        } else {",
    "            store(c)",
    "        }",
    "    }",
    "}",
    "",
    "var tokenBase is writable",
    "var mantissa is writable",
    "def numberStartState = object {",
    "    method consume (c) {",
    "        checkSeparator (c)",
    "        if (isDigit(c)) then {",
    "            store(c)",
    "        } elseif { c == \"x\" } then {",
    "            tokenBase := accum.asNumber",
    "            if (tokenBase == 0) then {",
    "                tokenBase := 16",
    "            }",
    "            accum := \"\"",
    "            advanceTo(numberBaseState)",
    "        } elseif {(c == \"e\") || (c == \"E\")} then {",
    "            mantissa := accum.asNumber",
    "            accum := \"\"",
    "            advanceTo(numberExponentSignState)",
    "        } elseif { isLetter(c) } then {",
    "            def suggestion = errormessages.suggestion.new",
    "            suggestion.insert(\".\")atPosition(linePosition)onLine(lineNumber)",
    "            errormessages.syntaxError(\"'{c}' is not a valid digit in base 10. Valid digits are 0..9.\")atRange(",
    "                lineNumber, startPosition, linePosition)withSuggestion(suggestion)",
    "        } elseif { c == \".\" } then {",
    "            advanceTo(numberDotState)",
    "        } else {",
    "            emit(numToken(accum, 10))",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "def numberBaseState = object {",
    "    method consume (c) {",
    "        checkSeparator(c)",
    "        if (isDigit(c) || isLetter(c)) then {",
    "            store(c)",
    "        } elseif {(c == \".\") && (tokenBase == 10)} then {",
    "            advanceTo(numberDotState)",
    "        } else {",
    "            emit(numToken(fromBase(accum, tokenBase).asString, tokenBase))",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "",
    "",
    "def numberDotState = object {",
    "    method consume (c) {",
    "        if (isDigit(c)) then {",
    "            store(\".\")",
    "            advanceTo(numberFractionState)",
    "            state.consume(c)",
    "        } elseif { spaceChars.contains(c) } then {",
    "            //alternatively, throw an error here",
    "            emit(numToken(accum, 10))",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        } else {",
    "            emit(numToken(accum, 10))",
    "            accum := \".\"",
    "            advanceTo(dotState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "def numberFractionState = object {",
    "    method consume (c) {",
    "        checkSeparator(c)",
    "        if (isDigit(c)) then {",
    "            store(c)",
    "        } elseif {(c == \"e\") || (c == \"E\")} then {",
    "            mantissa := accum.asNumber",
    "            accum := \"\"",
    "            advanceTo(numberExponentSignState)",
    "        } elseif {isLetter(c)} then {",
    "            errormessages.syntaxError(\"the fractional part of a number must be in base 10.\")atRange(",
    "                lineNumber, startPosition, linePosition)",
    "        } else {",
    "            emit(numToken(accum, 10))",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "def numberExponentSignState = object {",
    "    method consume (c) {",
    "        checkSeparator(c)",
    "        if (isDigit(c) || (c == \"-\")) then {",
    "            store(c)",
    "            advanceTo(numberExponentState)",
    "        } elseif {isLetter(c)} then {",
    "            errormessages.syntaxError(\"{c} is not an allowed base-10 exponent character\")atRange(",
    "                lineNumber, startPosition, linePosition)",
    "        } else {",
    "            errormessages.syntaxError(\"exponents must have at least one digit\")atRange(",
    "                lineNumber, startPosition, linePosition)",
    "        }",
    "    }",
    "}",
    "def numberExponentState = object {",
    "    method consume (c) {",
    "        checkSeparator(c)",
    "        if (isDigit(c)) then {",
    "            store(c)",
    "        } elseif {isLetter(c)} then {",
    "            errormessages.syntaxError(\"{c} is not an allowed base-10 exponent character\")atRange(",
    "                lineNumber, startPosition, linePosition)",
    "        } else {",
    "            if (accum == \"-\") then {",
    "                errormessages.syntaxError(\"exponents must have at least one digit\")atRange(",
    "                    lineNumber, startPosition, linePosition)",
    "            }",
    "            def exponent = accum.asNumber",
    "            var absExp := exponent",
    "            if (exponent < 0) then {",
    "                absExp := -exponent",
    "            }",
    "            var absMultiplier := 1",
    "            repeat (absExp) times {",
    "                absMultiplier := absMultiplier * 10",
    "            }",
    "            if (exponent >= 0) then {",
    "                emit (numToken((mantissa * absMultiplier).asString, 10))",
    "            } else {",
    "                emit (numToken((mantissa / absMultiplier).asString, 10))",
    "            }",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "",
    "def operatorState = object {",
    "    method consume (c) {",
    "        checkSeparator (c)",
    "        if (isOperatorChar(c, c.ord)) then {",
    "            store(c)",
    "        } else {",
    "            if (accum == \"→\") then {",
    "                emit(arrowToken)",
    "            } elseif { accum == \":=\" } then {",
    "                emit(bindToken)",
    "            } elseif { accum == \":\" } then {",
    "                emit(colonToken)",
    "            } else {",
    "                emit(opToken(accum))",
    "            }",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "",
    "def indentationState = object {",
    "    method consume (c) {",
    "        if (spaceChars.contains(c)) then {",
    "            currentLineIndent := linePosition",
    "        } else {",
    "            newline(c)",
    "            advanceTo(defaultState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "def commentSpaceState = object {",
    "    method consume (c) {",
    "        if (!spaceChars.contains(c)) then {",
    "            accum := \"\"",
    "            advanceTo(commentState)",
    "            state.consume(c)",
    "        }",
    "    }",
    "}",
    "def commentState = object {",
    "    method consume (c) {",
    "        if (c == \"\\n\") then {",
    "            emit (commentToken(accum))",
    "            currentLineIndent := 0",
    "            advanceTo(indentationState)",
    "        } else {",
    "            store(c)",
    "        }",
    "    }",
    "}",
    "",
    "method newline(currentCharacter) {",
    "    // a newline has been matched (including the spaces that follow it).",
    "    // Depending on the state of the lexer, classify it as <whitespace> (when",
    "    // the following line is a continuation line) or a real <newline> token.\"",
    "",
    "    checkAndRecordIndentStatus(currentCharacter)",
    "    if (isLineEmpty(currentCharacter)) then { return }",
    "    if (isIndentationChangeOne) then {",
    "        errormessages.syntaxError \"a change of indentation of 1 is not permitted\"",
    "                        atRange (lineNumber, linePosition, linePosition)",
    "    }",
    "    terminateContinuationIfNecessary",
    "    if (isContinuationLine) then {",
    "        recordContinuationStatus",
    "        saveDataForPriorLine",
    "        return",
    "    }",
    "    if (isBlockStart(currentCharacter)) then {",
    "        recordNewIndentation",
    "    } elseif {isBlockEnd(currentCharacter)} then {",
    "        checkIndentationReset",
    "    } else {",
    "        checkConstantIndent",
    "    }",
    "    saveDataForPriorLine",
    "    if (priorLineEndsWithOpenBracket) then { return }",
    "    if (currentLineStartsWithCloseBracket(currentCharacter)) then { return }",
    "    emitNewlineSeparator",
    "}",
    "",
    "method reportEmptyStringInterpolation {",
    "    def lastPos = tokens.last.linePos",
    "    def lineLength = inputLines.at(lineNumber).size",
    "    def suggestions = list []",
    "    if (lastPos < lineLength) then {",
    "        def suggestion1 = errormessages.suggestion.new",
    "",
    "        suggestion1.deleteRange(lastPos, linePosition)",
    "                        onLine(lineNumber)",
    "        suggestions.add(suggestion1)",
    "        def suggestion2 = errormessages.suggestion.new",
    "        suggestion2.insert \"«expression»\"",
    "                        atPosition (lastPos + 1)",
    "                        onLine(lineNumber)",
    "        suggestions.add(suggestion2)",
    "    }",
    "    errormessages.syntaxError \"a string interpolation cannot be empty\"",
    "          atRange (lineNumber, lastPos, linePosition)",
    "          withSuggestions (suggestions)",
    "}",
    "",
    "method isLineEmpty(currentCharacter) {",
    "    // answers true if the line that we just started is empty, or just a comment.",
    "    // Leading spaces have already been consumed in indentationState, so",
    "    // on a line containing just spaces, currentCharacter will be the next newline.",
    "    // A line containing nothing but a comment is treated as empty,",
    "    // so that comments do not reset the indentation.",
    "",
    "    if (\"\\n\" == currentCharacter) then { return true }",
    "    if (\"/\" ≠ currentCharacter) then { return false }",
    "    return \"/\" == followingCharacter",
    "}",
    "method followingCharacter {",
    "    def currentLine = inputLines.at (lineNumber)",
    "    if (currentLine.size == linePosition) then {",
    "        \"\\n\"",
    "    } else {",
    "        currentLine.at (linePosition + 1)",
    "    }",
    "}",
    "method isIndentationChangeOne {",
    "    def indentationChange = currentLineIndent - priorLineIndent",
    "    return indentationChange.abs == 1",
    "}",
    "method currentLineStartsWithCloseBracket(currentCharacter) {",
    "    \")]\\}⟧\".contains (currentCharacter)",
    "}",
    "method terminateContinuationIfNecessary {",
    "    if (indentOfLineBeingContinued == noSuchLine) then { return }",
    "    if ((braceChange == 0) && { currentLineIndent >= maxIndentOfContinuation }) then {",
    "        maxIndentOfContinuation := currentLineIndent",
    "        return",
    "    }",
    "    // The continuation has ended, either because we have opened a new block,",
    "    // or because currentLineIndent < maxIndentOfContinuation",
    "    priorLineIndent := indentOfLineBeingContinued",
    "    if ((braceChange == 0) && {currentLineIndent ≠ priorLineIndent}) then {",
    "        lexicalError (\"if this line continues the previous line, its indentation \"",
    "            ++ \"must be at least {maxIndentOfContinuation}; if it starts a new \"",
    "            ++ \"statement, its indentation must be {indentOfLineBeingContinued}\")",
    "    }",
    "    indentOfLineBeingContinued := noSuchLine    // record termination of continuation",
    "}",
    "",
    "method checkAndRecordIndentStatus (currentCharacter) {",
    "    if (\"\\t\" == currentCharacter) then {",
    "        lexicalError \"please indent with spaces, not tabs\"",
    "    }",
    "    braceChange := currentLineBraceDepth - priorLineBraceDepth",
    "    if (\"}\" == currentCharacter) then {",
    "        // treat a line that starts with a \"}\" as being at the",
    "        // prior indent level",
    "        braceChange := braceChange - 1",
    "    }",
    "}",
    "method isBlockStart(currentCharacter) {",
    "    (unmatchedLeftBraces > 0) && { currentCharacter ≠ \"\\}\" }",
    "}",
    "method isBlockEnd(currentCharacter) {",
    "    // if we are ending a block that was begun on the current line, we don't",
    "    // consider this to be a block ending, because there are no indented lines,",
    "    // and no entry on the indentStack to remove",
    "    if ((unmatchedLeftBraces > 0) && { currentCharacter ≠ \"\\}\" }) then {",
    "        return false",
    "    }",
    "    return (braceChange < 0)",
    "}",
    "",
    "method recordNewIndentation {",
    "    // One or more blocks have just started.  Record the new indentation(s).",
    "    //",
    "    // Note that it is possible for a block to have started, but for braceChange",
    "    // to be 0, as in",
    "    //     } else {",
    "    // In this case, the indentation need not change.",
    "    if ((braceChange > 0) && { currentLineIndent ≤ priorLineIndent }) then {",
    "        lexicalError \"Please indent the body of a block\"",
    "    }",
    "    if (braceChange > 1) then {",
    "        lexicalError(\"the prior line opened two blocks. There is no way for you \"",
    "            ++ \"to close them correctly! Please split the prior line into two.\")",
    "    }",
    "    repeat (braceChange) times {",
    "        indentStack.addLast (currentLineIndent)",
    "    }",
    "}",
    "method saveDataForPriorLine {",
    "    priorLineBraceDepth := priorLineBraceDepth + braceChange",
    "    priorLineIndent := currentLineIndent",
    "    unmatchedLeftBraces := 0",
    "}",
    "method priorLineEndsWithOpenBracket {",
    "    def tok = lastNonCommentToken",
    "    tok.isLBrace || tok.isLParen || tok.isLSquare || tok.isLGeneric",
    "}",
    "method checkIndentationReset {",
    "    // A block has ended.  Check that the indentation returns to the previous level",
    "    // braceChange may nevertheless be 0, since a new block may have started,",
    "    // e.g., the prior line may be",
    "    //      } else {",
    "",
    "    if (indentStack.size < (1 - braceChange)) then {",
    "        // this can happen when the unmatched closing brace is the first symbol",
    "        // on the next line.  Because it has not yet been lexed, it won't yet have",
    "        // triggered the lexical eror \"no opening brace\" in the rightBrace method.",
    "",
    "        errormessages.",
    "            syntaxError \"there is no opening brace corresponding to this closing brace\"",
    "            atPosition (lineNumber, linePosition)",
    "    }",
    "    repeat (- braceChange) times { indentStack.removeLast }",
    "    if (currentLineIndent ≠ indentStack.last) then {",
    "        lexicalError(\"on closing a block, the indentation must return to that \"",
    "            ++ \"of the line on which the block was opened ({indentStack.last})\")",
    "    }",
    "}",
    "method isContinuationLine {",
    "    if (noSuchLine ≠ indentOfLineBeingContinued) then {",
    "        // we are already in a continuation",
    "        return  true",
    "    }",
    "    if (unmatchedLeftBraces > 0) then { return false }",
    "    currentLineIndent > priorLineIndent",
    "}",
    "method recordContinuationStatus {",
    "    if (noSuchLine == indentOfLineBeingContinued) then {",
    "        indentOfLineBeingContinued := indentStack.last",
    "        maxIndentOfContinuation := currentLineIndent",
    "    }",
    "}",
    "method checkConstantIndent {",
    "    // We have neither started nor ended a block, and this is not a continuation line",
    "    if ((priorLineIndent == currentLineIndent )) then {",
    "        return",
    "    }",
    "    if ((priorLineIndent > currentLineIndent )) then {",
    "        lexicalError \"do not reduce the indentation except when ending a block\"",
    "    }",
    "    lexicalError \"this is not the body of a block, or a continuation line, so it should have indentation {priorLineIndent}, like the previous line\"",
    "}",
    "method lexicalError (message) {",
    "    errormessages.",
    "        syntaxError (message)",
    "        atRange (lineNumber, 1, linePosition - 1)",
    "}",
    "method escapedNewlineError {",
    "    errormessages.syntaxError \"to include a newline in a string use '\\\\n'\"",
    "          atPosition(lineNumber, linePosition - accum.size - 1)",
    "}",
    "",
    "method newlineInInterpolationError {",
    "    errormessages.syntaxError(\"a string interpolation may not \"",
    "          ++ \"contain a newline\")",
    "          atPosition(lineNumber, linePosition)",
    "}",
    "",
    "method unclosedStringError {",
    "    // We have found a newline inside a string.  Report the error",
    "",
    "    if (interpDepth > 0) then {",
    "        // Find closest {.",
    "        var line := lineNumber",
    "        var ix := inputLines.at(line).size",
    "        while { inputLines.at(line).at(ix) != \"\\{\" } do {",
    "            ix := ix - 1",
    "            if (ix == 0) then {",
    "                lineNumber := line - 1",
    "                ix := inputLines.at(line).size",
    "            }",
    "        }",
    "        def suggestions = list []",
    "        var suggestion := errormessages.suggestion.new",
    "        suggestion.insert \"\\\\\" atPosition (ix) onLine (line)",
    "        suggestions.push(suggestion)",
    "        if ((line == lineNumber) && (ix == (linePosition - 2))) then {",
    "            errormessages.syntaxError(\"for a '\\{' character in a string use '\\\\\\{'.\")",
    "                atPosition(line, ix)",
    "                withSuggestions(suggestions)",
    "        } else {",
    "            suggestion := errormessages.suggestion.new",
    "            suggestion.insert(\"}\")",
    "                  atPosition(linePosition - accum.size - 1)",
    "                  onLine(lineNumber)",
    "            suggestions.push(suggestion)",
    "            errormessages.syntaxError(\"a string interpolation must end with a '}';\"",
    "                ++ \"for a '\\{' character in a string use '\\\\\\{'.\")",
    "                atPosition(lineNumber, linePosition - accum.size - 1)",
    "                withSuggestions(suggestions)",
    "        }",
    "    }",
    "",
    "    def errorLine = inputLines.at(lineNumber)",
    "    def nextLine = if (inputLines.size >= (lineNumber + 1)) then {",
    "        inputLines.at(lineNumber + 1)",
    "    } else {",
    "        \"\"",
    "    }",
    "",
    "    // Count the number of literal quotes in nextLine.",
    "    var i := 1",
    "    var count := 0",
    "    while { i <= nextLine.size } do {",
    "        if (nextLine.at(i) == \"\\\"\") then {",
    "            count := count + 1",
    "        } elseif {",
    "            nextLine.at(i) == \"\\\\\"",
    "        } then {",
    "            i := i + 1",
    "        }",
    "        i := i + 1",
    "    }",
    "    if (count.isOdd) then {",
    "        def suggestions = list []",
    "        var suggestion := errormessages.suggestion.new",
    "        suggestion.addLine(lineNumber, errorLine ++ nextLine)",
    "        suggestion.addLine(lineNumber + 1, \"\")",
    "        suggestions.push(suggestion)",
    "        suggestion := errormessages.suggestion.new",
    "        suggestion.addLine(lineNumber, errorLine ++ \"\\\"\")",
    "        suggestion.addLine(lineNumber + 1, \"    ++ \\\"\" ++ nextLine)",
    "        suggestions.push(suggestion)",
    "        suggestion := errormessages.suggestion.new",
    "        suggestion.addLine(lineNumber, errorLine ++ \"\\\\n\" ++ nextLine)",
    "        suggestion.addLine(lineNumber + 1, \"\")",
    "        suggestions.push(suggestion)",
    "        errormessages.syntaxError(\"a string must be terminated by a \\\" before \"",
    "            ++ \"the end of the line. To insert a newline in a string, use '\\\\n'. \"",
    "            ++ \"To spread a string over multiple lines, use several strings \"",
    "            ++ \"joined together with '++'\")",
    "            atPosition(lineNumber, linePosition)",
    "            withSuggestions(suggestions)",
    "    } else {",
    "        def suggestion = errormessages.suggestion.new",
    "        suggestion.addLine(lineNumber, errorLine ++ \"\\\"\")",
    "        errormessages.syntaxError(\"a string must be terminated \" ++",
    "            \"by a \\\" before the end of the line.\")",
    "            atPosition(lineNumber, linePosition)",
    "            withSuggestion(suggestion)",
    "    }",
    "}",
    "",
    "method isSpaceChar(c){",
    "    spaceChars.contains(c)",
    "}",
    "method checkSeparatorString (c) {",
    "    def ordval = c.ord",
    "    if (isBadSeparator(c)) then {",
    "        // Character is whitespace, but not an ASCII space or",
    "        // Unicode NO-BREAK SPACE.  For example, a tab",
    "",
    "        def suggestion = errormessages.suggestion.new",
    "        suggestion.replaceChar(linePosition)with(\" \")onLine(lineNumber)",
    "        if (ordval == 9) then {",
    "            suggestion.replaceRange(linePosition, linePosition)",
    "                with(\"\\\\t\")",
    "                onLine(lineNumber)",
    "            errormessages.syntaxError(\"tabs cannot be \"",
    "                ++ \"written in the source code; \"",
    "                ++ \"use the string escape \\\\t instead\")",
    "                atRange(lineNumber, linePosition, linePosition)",
    "                withSuggestion(suggestion)",
    "            errormessages.syntaxError(\"tabs are not allowed; use spaces instead.\")",
    "                atPosition(lineNumber, linePosition) withSuggestion(suggestion)",
    "        } else {",
    "            suggestion.replaceRange(linePosition, linePosition)",
    "                with(\"\\\\u{hex4(ordval)}\")",
    "                onLine(lineNumber)",
    "            errormessages.syntaxError(\"{unicode.name(c)} (U+{hex4(ordval)})\"",
    "                ++ \" is not a valid whitespace character\"",
    "                ++ \" and cannot be written in the source code;\"",
    "                ++ \" use the Unicode escape \\\\u{hex4(ordval)} instead\")",
    "                atRange(lineNumber, linePosition, linePosition)",
    "                withSuggestion(suggestion)",
    "            errormessages.syntaxError(\"{unicode.name(c)} (U+{hex4(ordval)}) \"",
    "                ++ \"is not a valid whitespace character; use spaces instead.\")",
    "                atPosition(lineNumber, linePosition) withSuggestion(suggestion)",
    "        }",
    "    } elseif {isBadControl(c)} then {",
    "        // Character is a control character other than",
    "        // carriage return or line feed.",
    "        def suggestion = errormessages.suggestion.new",
    "        suggestion.replaceRange(linePosition, linePosition)",
    "            with(\"\\\\u{hex4(ordval)}\")",
    "            onLine(lineNumber)",
    "        errormessages.syntaxError(\"{unicode.name(c)} (U+{hex4(ordval)}) \"",
    "            ++ \"is a control character and cannot be written in the source code; \"",
    "            ++ \"use the Unicode escape \\\\u{hex4(ordval)} instead\")",
    "            atRange(lineNumber, linePosition, linePosition)",
    "            withSuggestion(suggestion)",
    "        suggestion.deleteChar(linePosition)onLine(lineNumber)",
    "        errormessages.syntaxError(\"{unicode.name(c)} (U+{hex4(ordval)}) \"",
    "            ++ \"is a control character and cannot be written in the source code; \"",
    "            ++ \"consider using spaces instead.\")",
    "            atPosition(lineNumber, linePosition) withSuggestion(suggestion)",
    "    }",
    "}",
    "",
    "method checkSeparator (c){",
    "    def ordval = c.ord",
    "    if (isBadSeparator(c)) then {",
    "        // Character is whitespace, but not an ASCII space or",
    "        // Unicode NO-BREAK SPACE.  For example, a tab",
    "",
    "        def suggestion = errormessages.suggestion.new",
    "        suggestion.replaceChar(linePosition)with(\" \")onLine(lineNumber)",
    "        if (ordval == 9) then {",
    "            errormessages.syntaxError(\"tabs are not allowed; use spaces instead.\")",
    "                atPosition(lineNumber, linePosition) withSuggestion(suggestion)",
    "        } else {",
    "            errormessages.syntaxError(\"{unicode.name(c)} (U+{hex4(ordval)}) \"",
    "                ++ \"is not a valid whitespace character; use spaces instead.\")",
    "                atPosition(lineNumber, linePosition) withSuggestion(suggestion)",
    "        }",
    "    } elseif {isBadControl(c)} then {",
    "        // Character is a control character other than",
    "        // carriage return or line feed.",
    "        def suggestion = errormessages.suggestion.new",
    "        suggestion.deleteChar(linePosition)onLine(lineNumber)",
    "        errormessages.syntaxError(\"{unicode.name(c)} (U+{hex4(ordval)}) \"",
    "            ++ \"is a control character and cannot be written in the source \"",
    "            ++ \"code; consider using spaces instead.\")",
    "            atPosition(lineNumber, linePosition) withSuggestion(suggestion)",
    "    }",
    "}",
    "",
    "method fromBase(str, base) {",
    "    var val := 0",
    "    var i := 0",
    "    for (str) do {c->",
    "        def n = c.ord",
    "        val := val * base",
    "        var inc := 0",
    "        if ((n >= 48) && (n <= 57)) then {          // 48 = '0', 57 = '9'",
    "            inc := n - 48 // '0'",
    "        } elseif {(n >= 65) && (n <= 90)} then {    // 65 = 'A', 90 = 'Z'",
    "            inc := n - 55 // 'A' - 10",
    "        } else {",
    "            inc := n - 87 // 'a' - 10",
    "        }",
    "        if (inc >= base) then {",
    "            if((str.first == \"0\") && (inc < 16)) then {",
    "                def suggestion = errormessages.suggestion.new",
    "                suggestion.insert(\"x\")",
    "                    atPosition(linePosition - str.size + 1) onLine(lineNumber)",
    "                errormessages.syntaxError(\"a number in base 16 must start with '0x'.\")",
    "                    atPosition(lineNumber, linePosition - str.size + 1)",
    "                    withSuggestion(suggestion)",
    "            } else {",
    "                def suggestion = errormessages.suggestion.new",
    "                if(str.size == 1) then {",
    "                    suggestion.deleteRange(linePosition - str.size - 1, linePosition - 1)",
    "                        onLine(lineNumber)",
    "                } else {",
    "                    suggestion.deleteChar(linePosition - str.size + i)",
    "                        onLine(lineNumber)",
    "                }",
    "                def validDigits = if(base <= 10) then {",
    "                    \"'0' to '{base - 1}'\"",
    "                } else {",
    "                    \"'0' to '9', 'a' to '{unicode.create(87 + base)}'\"",
    "                }",
    "                errormessages.syntaxError(\"'{c}' is not a valid digit in base {base}. \"",
    "                    ++ \"Valid digits are {validDigits}.\")",
    "                    atRange(lineNumber, linePosition - str.size + i, linePosition - str.size + i)",
    "                    withSuggestion(suggestion)",
    "            }",
    "        }",
    "        val := val + inc",
    "        i := i + 1",
    "    }",
    "    val",
    "}",
    "",
    "method hexdecchar(c) {",
    "// Return the numeric value of the single hexadecimal digit c.",
    "    def AOrd = \"A\".ord",
    "    def aOrd = \"a\".ord",
    "    def zeroOrd = \"0\".ord",
    "    def cOrd = c.ord",
    "    if ( (cOrd >= AOrd) && (cOrd <= \"F\".ord) ) then {",
    "        cOrd - AOrd + 10",
    "    } elseif { (cOrd >= aOrd) && (cOrd <= \"f\".ord) } then {",
    "        cOrd - aOrd + 10",
    "    } elseif { (cOrd >= zeroOrd) && (cOrd <= \"9\".ord) } then {",
    "        cOrd - zeroOrd",
    "    } else {",
    "        errormessages.syntaxError(\"the character '{c}' must be a hexadecimal digit\")",
    "            atPosition(lineNumber, linePosition)",
    "    }",
    "}",
    "",
    "method lexfile(file) {",
    "    // Reads the program text from file; returns a list of tokens.",
    "",
    "    inputLines.clear",
    "    def cLines = util.cLines.clear",
    "        // making them empty is necessary for the browser,",
    "        // where these variables persist from one compilation",
    "        // to the next",
    "    var line := \"\"",
    "    var cLine := \"\"",
    "    var wasCR := false",
    "    for(file.read) do { c ->",
    "        if (c == \"\\r\") then {",
    "            inputLines.push(line)",
    "            cLines.push(cLine)",
    "            line := \"\"",
    "            cLine := \"\"",
    "            wasCR := true",
    "        } elseif {c  == \"\\n\"} then {",
    "            if (wasCR) then {",
    "                // the spec says \"a LINE FEED that immediately",
    "                // follows a CARRIAGE RETURN is ignored\"",
    "            } else {",
    "                inputLines.push(line)",
    "                cLines.push(cLine)",
    "                line := \"\"",
    "                cLine := \"\"",
    "            }",
    "            wasCR := false",
    "        } elseif {c == \"\\l\"} then {",
    "            inputLines.push(line)",
    "            cLines.push(cLine)",
    "            line := \"\"",
    "            cLine := \"\"",
    "            wasCR := false",
    "        } elseif {c == \"\\\\\"} then {",
    "            line := line ++ c",
    "            cLine := cLine ++ \"\\\\\\\\\"",
    "            wasCR := false",
    "        } elseif {c == \"\\\"\"} then {",
    "            line := line ++ c",
    "            cLine := cLine ++ \"\\\\\\\"\"",
    "            wasCR := false",
    "        } else {",
    "            line := line ++ c",
    "            cLine := cLine ++ c",
    "            wasCR := false",
    "        }",
    "    }",
    "    if (line != \"\") then {",
    "        // this deals with an un-terminated final line",
    "        inputLines.push(line)",
    "        cLines.push(cLine)",
    "    }",
    "    lexInputLines",
    "}",
    "",
    "method lexString (inputString) {",
    "    // this method is here to make it easier to test the lexer",
    "    lexLines(inputString.split \"\\n\")",
    "}",
    "",
    "method lexLines (input) {",
    "    def oldInput = util.lines.copy",
    "    try {",
    "        util.lines.clear",
    "        util.lines.addAll(input)",
    "        initialize",
    "        lexInputLines",
    "    } finally {",
    "        util.lines.clear",
    "        util.lines.addAll(oldInput)",
    "    }",
    "}",
    "",
    "method lexInputLines {",
    "    // tokens is a doubly-linked list of tokens.",
    "    tokens := object {",
    "        def header = sofToken",
    "        header.next := header",
    "        var last is readable := header",
    "        var size is readable := 0",
    "",
    "        method first { header.next }",
    "        method first := (nu) is confidential { header.next := nu }",
    "        method isEmpty { header == last }",
    "",
    "        method savePosition {",
    "            return [first, last, size]",
    "        }",
    "",
    "        method restorePosition(saved) {",
    "            first := saved.first",
    "            last := saved.second",
    "            size := saved.third",
    "        }",
    "",
    "        method push(t) {",
    "            // adds t to the end of the list",
    "",
    "            last.next := t",
    "            t.prev := last",
    "            if {(t.indent - last.indent).abs == 1} then {",
    "                def m1 = \"the indentation for this line can't differ \"",
    "                def m2 = \"from that of the previous line by 1.\\n  To start a block, or \"",
    "                def m3 = \"continue a statement on the next line line, indent by 2 or more. \"",
    "                def m4 = \"To end a block, or end the continuation, decrease the indent \"",
    "                def m5 = \"to the prior level. \"",
    "                def m6 = \"Otherwise, use the same indentation as the previous line\"",
    "                def msg = m1 ++ m2 ++ m3 ++ m4 ++ m5 ++ m6",
    "                errormessages.syntaxError(msg) atRange(t.line, 1, t.indent)",
    "            }",
    "            last := t",
    "            size := size + 1",
    "        }",
    "",
    "        method pop {",
    "            // removes the last element and returns it",
    "            if (isEmpty) then {",
    "                ProgrammingError.raise \"attempt to pop from an empty token list\"",
    "            }",
    "            def popped = last",
    "            last := last.prev",
    "            size := size - 1",
    "            popped",
    "        }",
    "",
    "        method poll {",
    "            // removes the first element and returns it",
    "            if (isEmpty) then {",
    "                ProgrammingError.raise \"attempt to poll from an empty token list\"",
    "            }",
    "            def polled = first",
    "            first := first.next",
    "            size := size - 1",
    "            polled",
    "        }",
    "",
    "        class iterator {",
    "            var n := first",
    "            method hasNext {",
    "                header ≠ n",
    "            }",
    "            method next {",
    "                def ret = n",
    "                n := n.next",
    "                ret",
    "            }",
    "        }",
    "",
    "        method do(action) {",
    "            var n := first",
    "            while { n.isHeader.not } do {",
    "                action.apply(n)",
    "                n := n.next",
    "            }",
    "        }",
    "    }",
    "",
    "",
    "    linePosition := 0",
    "    util.log_verbose \"lexing.\"",
    "    state := startState",
    "    for (inputLines) do { eachLine ->",
    "        def charS = eachLine.iterator",
    "        while {charS.hasNext} do {",
    "            def ch = charS.next",
    "            linePosition := linePosition + 1",
    "            if (inStr.not && { \"><![]-\".contains(ch) } && charS.hasNext) then {",
    "                // map to unicode equavalents",
    "                def nextCh = charS.next",
    "                linePosition := linePosition + 1",
    "                def bigraph = ch ++ nextCh",
    "                if (bigraph == \">=\") then { state.consume \"≥\"",
    "                } elseif { bigraph == \"<=\" } then { state.consume \"≤\"",
    "                } elseif { bigraph == \"!=\" } then { state.consume \"≠\"",
    "                } elseif { bigraph == \"[[\" } then { state.consume \"⟦\"",
    "                } elseif { bigraph == \"]]\" } then { state.consume \"⟧\"",
    "                } elseif { bigraph == \"->\" } then { state.consume \"→\"",
    "                } else {",
    "                    linePosition := linePosition - 1",
    "                    state.consume(ch)",
    "                    linePosition := linePosition + 1",
    "                    state.consume(nextCh)",
    "                }",
    "            } else {",
    "                state.consume(ch)",
    "            }",
    "        }",
    "        state.consume(\"\\n\")",
    "        linePosition := 0",
    "        lineNumber := lineNumber + 1",
    "    }",
    "    linePosition := linePosition + 1",
    "    if (inStr) then {",
    "        errormessages.syntaxError(\"a multi-line string must end with a '›'.\\n\" ++",
    "            \"String opened on line {startLine} and unclosed at end of input.\")",
    "            atRange(startLine, stringStart, inputLines.at(startLine).size)",
    "    }",
    "    tokens.push(eofToken)",
    "    tokens",
    "}" ];
}
function gracecode_lexer() {
  importedModules["lexer"] = this;
  var module$lexer = this;
  this.definitionModule = "lexer";
  this.definitionLine = 1;
  var var_prelude = var___95__prelude;
  this.closureKeys = this.closureKeys || [];
  this.closureKeys.push("outer_lexer_1");
  this.outer_lexer_1 = var_prelude;
  // Dialect "standardGrace"
  var_prelude = do_import("standardGrace", gracecode_standardGrace);
  this.outer = var_prelude;
  setLineNumber(1);    // compilenode import
  // Import of "util" as util
  if (typeof gracecode_util == "undefined")
    throw new GraceExceptionPacket(EnvironmentExceptionObject, 
      new GraceString("could not find module util"));
  var var_util = do_import("util", gracecode_util);
  var func0 = function(argcv) {     // accessor method util
    const numArgs = arguments.length - 1;
    if (numArgs > 0) raiseTypeArgError("util", 0, numArgs - 0);
    if (var_util === undefined) raiseUninitializedVariable("util");
    return var_util;
  };    // end of method util
  this.methods["util"] = func0;
  func0.methodName = "util";
  func0.paramCounts = [0];
  func0.paramNames = [];
  func0.definitionLine = 1;
  func0.definitionModule = "lexer";
  func0.debug = "import";
  func0.confidential = true;
  setLineNumber(2);    // compilenode import
  // Import of "unicode" as unicode
  if (typeof gracecode_unicode == "undefined")
    throw new GraceExceptionPacket(EnvironmentExceptionObject, 
      new GraceString("could not find module unicode"));
  var var_unicode = do_import("unicode", gracecode_unicode);
  var func1 = function(argcv) {     // accessor method unicode
    const numArgs = arguments.length - 1;
    if (numArgs > 0) raiseTypeArgError("unicode", 0, numArgs - 0);
    if (var_unicode === undefined) raiseUninitializedVariable("unicode");
    return var_unicode;
  };    // end of method unicode
  this.methods["unicode"] = func1;
  func1.methodName = "unicode";
  func1.paramCounts = [0];
  func1.paramNames = [];
  func1.definitionLine = 2;
  func1.definitionModule = "lexer";
  func1.debug = "import";
  func1.confidential = true;
  setLineNumber(3);    // compilenode import
  // Import of "errormessages" as errormessages
  if (typeof gracecode_errormessages == "undefined")
    throw new GraceExceptionPacket(EnvironmentExceptionObject, 
      new GraceString("could not find module errormessages"));
  var var_errormessages = do_import("errormessages", gracecode_errormessages);
  var func2 = function(argcv) {     // accessor method errormessages
    const numArgs = arguments.length - 1;
    if (numArgs > 0) raiseTypeArgError("errormessages", 0, numArgs - 0);
    if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
    return var_errormessages;
  };    // end of method errormessages
  this.methods["errormessages"] = func2;
  func2.methodName = "errormessages";
  func2.paramCounts = [0];
  func2.paramNames = [];
  func2.definitionLine = 3;
  func2.definitionModule = "lexer";
  func2.debug = "import";
  func2.confidential = true;
  setLineNumber(4);    // compilenode import
  // Import of "fastDict" as map
  if (typeof gracecode_fastDict == "undefined")
    throw new GraceExceptionPacket(EnvironmentExceptionObject, 
      new GraceString("could not find module fastDict"));
  var var_map = do_import("fastDict", gracecode_fastDict);
  var func3 = function(argcv) {     // accessor method map
    const numArgs = arguments.length - 1;
    if (numArgs > 0) raiseTypeArgError("map", 0, numArgs - 0);
    if (var_map === undefined) raiseUninitializedVariable("map");
    return var_map;
  };    // end of method map
  this.methods["map"] = func3;
  func3.methodName = "map";
  func3.paramCounts = [0];
  func3.paramNames = [];
  func3.definitionLine = 4;
  func3.definitionModule = "lexer";
  func3.debug = "import";
  func3.confidential = true;
  var func4 = function(argcv, var_c, var_ordval) {    // method isOperatorChar(_,_), line 29
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("isOperatorChar(_,_)", 0, numArgs - 2);
    }
    var if5 = GraceDone;
    setLineNumber(30);    // compilenode call
    // call case 6: other requests
    if (var_operatorChars === undefined) raiseUninitializedVariable("operatorChars");
    var call6 = request(var_operatorChars, "contains(1)", [1], var_c);
    if (Grace_isTrue(call6)) {
      return GraceTrue;
    }
    var if7 = GraceDone;
    setLineNumber(31);    // compilenode call
    // call case 6: other requests
    if (var_unicode === undefined) raiseUninitializedVariable("unicode");
    var call8 = request(var_unicode, "isSymbolMathematical(1)", [1], var_ordval);
    if (Grace_isTrue(call8)) {
      return GraceTrue;
    }
    setLineNumber(32);    // compilenode return
    return GraceFalse;
  };    // end of method isOperatorChar(_,_)
  this.methods["isOperatorChar(2)"] = func4;
  func4.methodName = "isOperatorChar(2)";
  func4.paramCounts = [2];
  func4.paramNames = ["c", "ordval"];
  func4.definitionLine = 29;
  func4.definitionModule = "lexer";
  var func9 = function(argcv, var_ch) {    // method isBadSeparator(_), line 37
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isBadSeparator(_)", 0, numArgs - 1);
    }
    var if10 = GraceDone;
    setLineNumber(38);    // compilenode call
    // call case 6: other requests
    if (var_spaceChars === undefined) raiseUninitializedVariable("spaceChars");
    var call11 = request(var_spaceChars, "contains(1)", [1], var_ch);
    if (Grace_isTrue(call11)) {
      return GraceFalse;
    }
    var if12 = GraceDone;
    setLineNumber(39);    // compilenode op
    if (var_tab === undefined) raiseUninitializedVariable("tab");
    var opresult13 = request(var_tab, "==(1)", [1], var_ch);
    if (Grace_isTrue(opresult13)) {
      return GraceTrue;
    }
    setLineNumber(40);    // compilenode string
    var string15 = new GraceString("Z");
    // call case 6: other requests
    if (var_unicode === undefined) raiseUninitializedVariable("unicode");
    var call14 = request(var_unicode, "inCategory(2)", [2], var_ch, string15);
    return call14;
  };    // end of method isBadSeparator(_)
  this.methods["isBadSeparator(1)"] = func9;
  func9.methodName = "isBadSeparator(1)";
  func9.paramCounts = [1];
  func9.paramNames = ["ch"];
  func9.definitionLine = 37;
  func9.definitionModule = "lexer";
  var func16 = function(argcv, var_ch) {    // method isBadControl(_), line 44
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isBadControl(_)", 0, numArgs - 1);
    }
    var if17 = GraceDone;
    setLineNumber(45);    // compilenode call
    // call case 6: other requests
    if (var_nl__95__cr === undefined) raiseUninitializedVariable("nl_cr");
    var call18 = request(var_nl__95__cr, "contains(1)", [1], var_ch);
    if (Grace_isTrue(call18)) {
      return GraceFalse;
    }
    setLineNumber(46);    // compilenode string
    var string20 = new GraceString("C");
    // call case 6: other requests
    if (var_unicode === undefined) raiseUninitializedVariable("unicode");
    var call19 = request(var_unicode, "inCategory(2)", [2], var_ch, string20);
    return call19;
  };    // end of method isBadControl(_)
  this.methods["isBadControl(1)"] = func16;
  func16.methodName = "isBadControl(1)";
  func16.paramCounts = [1];
  func16.paramNames = ["ch"];
  func16.definitionLine = 44;
  func16.definitionModule = "lexer";
  var func21 = function(argcv, var_ch) {    // method isSelfMode(_), line 50
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isSelfMode(_)", 0, numArgs - 1);
    }
    setLineNumber(50);    // compilenode call
    // call case 6: other requests
    if (var_selfModes === undefined) raiseUninitializedVariable("selfModes");
    var call22 = request(var_selfModes, "contains(1)", [1], var_ch);
    return call22;
  };    // end of method isSelfMode(_)
  this.methods["isSelfMode(1)"] = func21;
  func21.methodName = "isSelfMode(1)";
  func21.paramCounts = [1];
  func21.paramNames = ["ch"];
  func21.definitionLine = 50;
  func21.definitionModule = "lexer";
  var func23 = function(argcv, var_ch) {    // method isBracket(_), line 53
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isBracket(_)", 0, numArgs - 1);
    }
    setLineNumber(53);    // compilenode call
    // call case 6: other requests
    if (var_brackets === undefined) raiseUninitializedVariable("brackets");
    var call24 = request(var_brackets, "contains(1)", [1], var_ch);
    return call24;
  };    // end of method isBracket(_)
  this.methods["isBracket(1)"] = func23;
  func23.methodName = "isBracket(1)";
  func23.paramCounts = [1];
  func23.paramNames = ["ch"];
  func23.definitionLine = 53;
  func23.definitionModule = "lexer";
  var func25 = function(argcv, var_ch) {    // method isIdentifierChar(_), line 56
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isIdentifierChar(_)", 0, numArgs - 1);
    }
    var if26 = GraceDone;
    setLineNumber(57);    // compilenode call
    // call case 6: other requests
    if (var_alphaNums === undefined) raiseUninitializedVariable("alphaNums");
    var call27 = request(var_alphaNums, "contains(1)", [1], var_ch);
    if (Grace_isTrue(call27)) {
      return GraceTrue;
    }
    var if28 = GraceDone;
    setLineNumber(58);    // compilenode string
    var string30 = new GraceString("L");
    // call case 6: other requests
    if (var_unicode === undefined) raiseUninitializedVariable("unicode");
    var call29 = request(var_unicode, "inCategory(2)", [2], var_ch, string30);
    if (Grace_isTrue(call29)) {
      return GraceTrue;
    }
    var if31 = GraceDone;
    setLineNumber(59);    // compilenode string
    var string33 = new GraceString("N");
    // call case 6: other requests
    if (var_unicode === undefined) raiseUninitializedVariable("unicode");
    var call32 = request(var_unicode, "inCategory(2)", [2], var_ch, string33);
    if (Grace_isTrue(call32)) {
      return GraceTrue;
    }
    setLineNumber(60);    // compilenode return
    return GraceFalse;
  };    // end of method isIdentifierChar(_)
  this.methods["isIdentifierChar(1)"] = func25;
  func25.methodName = "isIdentifierChar(1)";
  func25.paramCounts = [1];
  func25.paramNames = ["ch"];
  func25.definitionLine = 56;
  func25.definitionModule = "lexer";
  var func34 = function(argcv, var_ch) {    // method isDigit(_), line 63
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isDigit(_)", 0, numArgs - 1);
    }
    setLineNumber(63);    // compilenode call
    // call case 6: other requests
    if (var_digits === undefined) raiseUninitializedVariable("digits");
    var call35 = request(var_digits, "contains(1)", [1], var_ch);
    return call35;
  };    // end of method isDigit(_)
  this.methods["isDigit(1)"] = func34;
  func34.methodName = "isDigit(1)";
  func34.paramCounts = [1];
  func34.paramNames = ["ch"];
  func34.definitionLine = 63;
  func34.definitionModule = "lexer";
  var func36 = function(argcv, var_ch) {    // method isLetter(_), line 66
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isLetter(_)", 0, numArgs - 1);
    }
    var if37 = GraceDone;
    setLineNumber(67);    // compilenode call
    // call case 6: other requests
    if (var_asciiLetters === undefined) raiseUninitializedVariable("asciiLetters");
    var call38 = request(var_asciiLetters, "contains(1)", [1], var_ch);
    if (Grace_isTrue(call38)) {
      return GraceTrue;
    }
    var if39 = GraceDone;
    setLineNumber(68);    // compilenode string
    var string41 = new GraceString("L");
    // call case 6: other requests
    if (var_unicode === undefined) raiseUninitializedVariable("unicode");
    var call40 = request(var_unicode, "inCategory(2)", [2], var_ch, string41);
    if (Grace_isTrue(call40)) {
      return GraceTrue;
    }
    setLineNumber(69);    // compilenode return
    return GraceFalse;
  };    // end of method isLetter(_)
  this.methods["isLetter(1)"] = func36;
  func36.methodName = "isLetter(1)";
  func36.paramCounts = [1];
  func36.paramNames = ["ch"];
  func36.definitionLine = 66;
  func36.definitionModule = "lexer";
  var func42 = function(argcv, var_s, var_width, var_padCh) {    // method padl(_,_,_), line 72
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 3) && (numArgs !== 3)) {
        raiseTypeArgError("padl(_,_,_)", 0, numArgs - 3);
    }
    var if43 = GraceDone;
    setLineNumber(73);    // compilenode member
    // call case 6: other requests
    var call44 = request(var_s, "size", [0]);
    var opresult45 = request(call44, "\u2265(1)", [1], var_width);
    if (Grace_isTrue(opresult45)) {
      setLineNumber(74);    // compilenode return
      return var_s;
    }
    setLineNumber(76);    // compilenode vardec
    var var_s__39__ = var_s;
    setLineNumber(77);    // compilenode block
    var block47 = new GraceBlock(this, 77, 0);
    block47.guard = jsTrue;
    block47.real = function block47() {
      setLineNumber(77);    // compilenode member
      // call case 6: other requests
      if (var_s__39__ === undefined) raiseUninitializedVariable("s'");
      var call48 = request(var_s__39__, "size", [0]);
      var opresult49 = request(call48, "<(1)", [1], var_width);
      return opresult49;
    };
    let applyMeth47 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth47.methodName = "apply";
    applyMeth47.paramCounts = [0];
    applyMeth47.paramNames = [];
    applyMeth47.definitionLine = 77;
    applyMeth47.definitionModule = "lexer";
    block47.methods["apply"] = applyMeth47;
    var block50 = new GraceBlock(this, 77, 0);
    block50.guard = jsTrue;
    block50.real = function block50() {
      setLineNumber(78);    // compilenode op
      if (var_s__39__ === undefined) raiseUninitializedVariable("s'");
      var opresult51 = request(var_padCh, "++(1)", [1], var_s__39__);
      var_s__39__ = opresult51;
      return GraceDone;
    };
    let applyMeth50 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth50.methodName = "apply";
    applyMeth50.paramCounts = [0];
    applyMeth50.paramNames = [];
    applyMeth50.definitionLine = 77;
    applyMeth50.definitionModule = "lexer";
    block50.methods["apply"] = applyMeth50;
    // call case 2: outer request
    var call46 = selfRequest(var_prelude, "while(1)do(1)", [1, 1], block47, block50);
    setLineNumber(80);    // compilenode return
    return var_s__39__;
  };    // end of method padl(_,_,_)
  this.methods["padl(3)"] = func42;
  func42.methodName = "padl(3)";
  func42.paramCounts = [3];
  func42.paramNames = ["s", "width", "padCh"];
  func42.definitionLine = 72;
  func42.definitionModule = "lexer";
  var func52 = function(argcv, var_num) {    // method hex4(_), line 83
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("hex4(_)", 0, numArgs - 1);
    }
    setLineNumber(84);    // compilenode num
    // call case 6: other requests
    var call54 = request(var_num, "inBase(1)", [1], new GraceNum(16));
    var string55 = new GraceString("0");
    // call case 2: outer request
    var call53 = selfRequest(importedModules["lexer"], "padl(3)", [3], call54, new GraceNum(4), string55);
    return call53;
  };    // end of method hex4(_)
  this.methods["hex4(1)"] = func52;
  func52.methodName = "hex4(1)";
  func52.paramCounts = [1];
  func52.paramNames = ["num"];
  func52.definitionLine = 83;
  func52.definitionModule = "lexer";
  var func56 = function(argcv) {    // method initialize, line 89
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("initialize", 0, numArgs - 0);
    }
    setLineNumber(90);    // compilenode num
    var_lineNumber = new GraceNum(1);
    setLineNumber(91);    // compilenode num
    var_startPosition = new GraceNum(1);
    setLineNumber(92);    // compilenode num
    var_currentLineIndent = new GraceNum(0);
    setLineNumber(93);    // compilenode num
    var_unmatchedLeftBraces = new GraceNum(0);
    setLineNumber(94);    // compilenode num
    var_startLine = new GraceNum(1);
    setLineNumber(95);    // compilenode num
    var_unichars = new GraceNum(0);
    setLineNumber(96);    // compilenode num
    var_currentLineBraceDepth = new GraceNum(0);
    setLineNumber(97);    // compilenode bind
    if (var_noSuchLine === undefined) raiseUninitializedVariable("noSuchLine");
    var_indentOfLineBeingContinued = var_noSuchLine;
    setLineNumber(98);    // compilenode num
    // call case 6: other requests
    // call case 2: outer request
    var call58 = selfRequest(var_prelude, "list", [0]);
    var call57 = request(call58, "with(1)", [1], new GraceNum(0));
    var_indentStack = call57;
    setLineNumber(99);    // compilenode num
    var_priorLineBraceDepth = new GraceNum(0);
    setLineNumber(100);    // compilenode num
    var_priorLineIndent = new GraceNum(0);
    setLineNumber(101);    // compilenode string
    var string59 = new GraceString("");
    var_accum = string59;
    setLineNumber(102);    // compilenode bind
    var_inStr = GraceFalse;
    setLineNumber(103);    // compilenode num
    var_codepoint = new GraceNum(0);
    setLineNumber(104);    // compilenode num
    var_interpDepth = new GraceNum(0);
    setLineNumber(105);    // compilenode bind
    var_stringContainsInterp = GraceFalse;
    setLineNumber(106);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call60 = selfRequest(this, "sofToken", [0]);
    var_lastNonCommentToken = call60;
    return GraceDone;
  };    // end of method initialize
  this.methods["initialize"] = func56;
  func56.methodName = "initialize";
  func56.paramCounts = [0];
  func56.paramNames = [];
  func56.definitionLine = 89;
  func56.definitionModule = "lexer";
  var func61 = function(argcv) {    // method tallyLeftBrace, line 143
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("tallyLeftBrace", 0, numArgs - 0);
    }
    setLineNumber(144);    // compilenode op
    if (var_unmatchedLeftBraces === undefined) raiseUninitializedVariable("unmatchedLeftBraces");
    var sum62 = request(var_unmatchedLeftBraces, "+(1)", [1], new GraceNum(1));
    var_unmatchedLeftBraces = sum62;
    setLineNumber(145);    // compilenode op
    if (var_currentLineBraceDepth === undefined) raiseUninitializedVariable("currentLineBraceDepth");
    var sum63 = request(var_currentLineBraceDepth, "+(1)", [1], new GraceNum(1));
    var_currentLineBraceDepth = sum63;
    return GraceDone;
  };    // end of method tallyLeftBrace
  this.methods["tallyLeftBrace"] = func61;
  func61.methodName = "tallyLeftBrace";
  func61.paramCounts = [0];
  func61.paramNames = [];
  func61.definitionLine = 143;
  func61.definitionModule = "lexer";
  var func64 = function(argcv) {    // method rightBrace, line 147
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rightBrace", 0, numArgs - 0);
    }
    var if65 = GraceDone;
    setLineNumber(148);    // compilenode op
    if (var_unmatchedLeftBraces === undefined) raiseUninitializedVariable("unmatchedLeftBraces");
    var opresult66 = request(var_unmatchedLeftBraces, ">(1)", [1], new GraceNum(0));
    if (Grace_isTrue(opresult66)) {
      setLineNumber(149);    // compilenode op
      if (var_unmatchedLeftBraces === undefined) raiseUninitializedVariable("unmatchedLeftBraces");
      var diff67 = request(var_unmatchedLeftBraces, "-(1)", [1], new GraceNum(1));
      var_unmatchedLeftBraces = diff67;
      if65 = GraceDone;
    }
    setLineNumber(151);    // compilenode op
    if (var_currentLineBraceDepth === undefined) raiseUninitializedVariable("currentLineBraceDepth");
    var diff68 = request(var_currentLineBraceDepth, "-(1)", [1], new GraceNum(1));
    var_currentLineBraceDepth = diff68;
    var if69 = GraceDone;
    setLineNumber(152);    // compilenode op
    if (var_currentLineBraceDepth === undefined) raiseUninitializedVariable("currentLineBraceDepth");
    var opresult70 = request(var_currentLineBraceDepth, "<(1)", [1], new GraceNum(0));
    if (Grace_isTrue(opresult70)) {
      setLineNumber(153);    // compilenode string
      var string72 = new GraceString("there is no opening brace corresponding to this closing brace");
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call71 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], string72, var_lineNumber, var_startPosition, var_linePosition);
      if69 = call71;
    }
    return if69;
  };    // end of method rightBrace
  this.methods["rightBrace"] = func64;
  func64.methodName = "rightBrace";
  func64.paramCounts = [0];
  func64.paramNames = [];
  func64.definitionLine = 147;
  func64.definitionModule = "lexer";
  var func73 = function(argcv) {    // method token, line 158
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("token", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("token", "lexer", 158);
    var ouc_init = this.methods["token$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method token
  this.methods["token"] = func73;
  func73.methodName = "token";
  func73.paramCounts = [0];
  func73.paramNames = [];
  func73.definitionLine = 158;
  func73.definitionModule = "lexer";
  var func74 = function(argcv, inheritingObject, aliases, exclusions) {    // method token$build(_,_,_), line 158
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("token", 0, numArgs - 0);
    }
    var obj75_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_158");
      this.outer_lexer_158 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(159);    // reuse call
      var initFun76 = selfRequest(var_prelude, "equality$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.line = undefined;
      var reader77_line = function() {  // reader method line
          if (this.data.line === undefined) raiseUninitializedVariable("line");
          return this.data.line;
      };
      reader77_line.isDef = true;
      this.methods["line"] = reader77_line;
      this.data.indent = undefined;
      var reader78_indent = function() {  // reader method indent
          if (this.data.indent === undefined) raiseUninitializedVariable("indent");
          return this.data.indent;
      };
      reader78_indent.isDef = true;
      this.methods["indent"] = reader78_indent;
      this.data.linePos = undefined;
      var reader79_linePos = function() {  // reader method linePos
          if (this.data.linePos === undefined) raiseUninitializedVariable("linePos");
          return this.data.linePos;
      };
      reader79_linePos.isDef = true;
      this.methods["linePos"] = reader79_linePos;
      this.data.null = undefined;
      var reader80_null = function() {  // reader method null
          if (this.data.null === undefined) raiseUninitializedVariable("null");
          return this.data.null;
      };
      reader80_null.isDef = true;
      reader80_null.confidential = true;
      this.methods["null"] = reader80_null;
      this.data.next = undefined;
      var reader81_next = function() {  // reader method next
          if (this.data.next === undefined) raiseUninitializedVariable("next");
          return this.data.next;
      };
      reader81_next.isVar = true;
      this.methods["next"] = reader81_next;
      var writer82_next = function(argcv, n) {   // writer method next:=(_)
        this.data.next = n;
        return GraceDone;
      };
      this.methods["next:=(1)"] = writer82_next;
      this.data.prev = undefined;
      var reader83_prev = function() {  // reader method prev
          if (this.data.prev === undefined) raiseUninitializedVariable("prev");
          return this.data.prev;
      };
      reader83_prev.isVar = true;
      this.methods["prev"] = reader83_prev;
      var writer84_prev = function(argcv, n) {   // writer method prev:=(_)
        this.data.prev = n;
        return GraceDone;
      };
      this.methods["prev:=(1)"] = writer84_prev;
      var func85 = function(argcv, var_other) {    // method ==(_), line 172
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("==(_)", 0, numArgs - 1);
        }
        var if86 = GraceDone;
        setLineNumber(173);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call87 = selfRequest(this, "null", [0]);
        var opresult88 = request(call87, "==(1)", [1], var_other);
        if (Grace_isTrue(opresult88)) {
          if86 = GraceFalse;
        } else {
          var if89 = GraceDone;
          setLineNumber(175);    // compilenode op
          var opresult90 = request(GraceFalse, "==(1)", [1], var_other);
          if (Grace_isTrue(opresult90)) {
            if89 = GraceFalse;
          } else {
            setLineNumber(178);    // compilenode member
            // call case 4: self request with 0 args and 0 typeArgs 
            var call91 = selfRequest(this, "line", [0]);
            // call case 6: other requests
            var call92 = request(var_other, "line", [0]);
            var opresult93 = request(call91, "==(1)", [1], call92);
            // call case 4: self request with 0 args and 0 typeArgs 
            var call94 = selfRequest(this, "linePos", [0]);
            // call case 6: other requests
            var call95 = request(var_other, "linePos", [0]);
            var opresult96 = request(call94, "==(1)", [1], call95);
            var opresult97 = request(opresult93, "&&(1)", [1], opresult96);
            if89 = opresult97;
          }
          if86 = if89;
        }
        return if86;
      };    // end of method ==(_)
      this.methods["==(1)"] = func85;
      func85.methodName = "==(1)";
      func85.paramCounts = [1];
      func85.paramNames = ["other"];
      func85.definitionLine = 172;
      func85.definitionModule = "lexer";
      var func98 = function(argcv) {    // method hash, line 181
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("hash", 0, numArgs - 0);
        }
        setLineNumber(181);    // compilenode member
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call101 = selfRequest(this, "line", [0]);
        var call100 = request(call101, "hash", [0]);
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call103 = selfRequest(this, "linePos", [0]);
        var call102 = request(call103, "hash", [0]);
        // call case 2: outer request
        var call99 = selfRequest(var_prelude, "hashCombine(2)", [2], call100, call102);
        return call99;
      };    // end of method hash
      this.methods["hash"] = func98;
      func98.methodName = "hash";
      func98.paramCounts = [0];
      func98.paramNames = [];
      func98.definitionLine = 181;
      func98.definitionModule = "lexer";
      var func104 = function(argcv) {    // method asString, line 182
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("asString", 0, numArgs - 0);
        }
        setLineNumber(182);    // compilenode string
        var string105 = new GraceString("(");
        // call case 4: self request with 0 args and 0 typeArgs 
        var call106 = selfRequest(this, "line", [0]);
        var opresult107 = request(string105, "++(1)", [1], call106);
        var string108 = new GraceString(".");
        var opresult109 = request(opresult107, "++(1)", [1], string108);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call110 = selfRequest(this, "linePos", [0]);
        var opresult111 = request(opresult109, "++(1)", [1], call110);
        var string112 = new GraceString(")");
        var opresult113 = request(opresult111, "++(1)", [1], string112);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call114 = selfRequest(this, "kind", [0]);
        var opresult115 = request(opresult113, "++(1)", [1], call114);
        var string116 = new GraceString(" ");
        var opresult117 = request(opresult115, "++(1)", [1], string116);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call118 = selfRequest(this, "value", [0]);
        var opresult119 = request(opresult117, "++(1)", [1], call118);
        var string120 = new GraceString("");
        var opresult121 = request(opresult119, "++(1)", [1], string120);
        return opresult121;
      };    // end of method asString
      this.methods["asString"] = func104;
      func104.methodName = "asString";
      func104.paramCounts = [0];
      func104.paramNames = [];
      func104.definitionLine = 182;
      func104.definitionModule = "lexer";
      var func122 = function(argcv) {    // method value, line 183
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("value", 0, numArgs - 0);
        }
        setLineNumber(183);    // compilenode member
        // call case 2: outer request
        var call123 = selfRequest(var_prelude, "abstract", [0]);
        return call123;
      };    // end of method value
      this.methods["value"] = func122;
      func122.methodName = "value";
      func122.paramCounts = [0];
      func122.paramNames = [];
      func122.definitionLine = 183;
      func122.definitionModule = "lexer";
      var func124 = function(argcv) {    // method size, line 184
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("size", 0, numArgs - 0);
        }
        setLineNumber(184);    // compilenode member
        // call case 2: outer request
        var call125 = selfRequest(var_prelude, "abstract", [0]);
        return call125;
      };    // end of method size
      this.methods["size"] = func124;
      func124.methodName = "size";
      func124.paramCounts = [0];
      func124.paramNames = [];
      func124.definitionLine = 184;
      func124.definitionModule = "lexer";
      var func126 = function(argcv) {    // method kind, line 185
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("kind", 0, numArgs - 0);
        }
        setLineNumber(185);    // compilenode member
        // call case 2: outer request
        var call127 = selfRequest(var_prelude, "abstract", [0]);
        return call127;
      };    // end of method kind
      this.methods["kind"] = func126;
      func126.methodName = "kind";
      func126.paramCounts = [0];
      func126.paramNames = [];
      func126.definitionLine = 185;
      func126.definitionModule = "lexer";
      var func128 = function(argcv) {    // method endLine, line 186
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("endLine", 0, numArgs - 0);
        }
        setLineNumber(186);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call129 = selfRequest(this, "line", [0]);
        return call129;
      };    // end of method endLine
      this.methods["endLine"] = func128;
      func128.methodName = "endLine";
      func128.paramCounts = [0];
      func128.paramNames = [];
      func128.definitionLine = 186;
      func128.definitionModule = "lexer";
      var func130 = function(argcv) {    // method endPos, line 187
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("endPos", 0, numArgs - 0);
        }
        setLineNumber(187);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call131 = selfRequest(this, "linePos", [0]);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call132 = selfRequest(this, "size", [0]);
        var sum133 = request(call131, "+(1)", [1], call132);
        var diff134 = request(sum133, "-(1)", [1], new GraceNum(1));
        return diff134;
      };    // end of method endPos
      this.methods["endPos"] = func130;
      func130.methodName = "endPos";
      func130.paramCounts = [0];
      func130.paramNames = [];
      func130.definitionLine = 187;
      func130.definitionModule = "lexer";
      var func135 = function(argcv) {     // accessor method isHeader
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isHeader", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isHeader
      this.methods["isHeader"] = func135;
      func135.methodName = "isHeader";
      func135.paramCounts = [0];
      func135.paramNames = [];
      func135.definitionLine = 188;
      func135.definitionModule = "lexer";
      var func136 = function(argcv) {    // method hasPrev, line 189
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("hasPrev", 0, numArgs - 0);
        }
        setLineNumber(189);    // compilenode member
        // call case 6: other requests
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call139 = selfRequest(this, "prev", [0]);
        var call138 = request(call139, "isHeader", [0]);
        var call137 = request(call138, "not", [0]);
        return call137;
      };    // end of method hasPrev
      this.methods["hasPrev"] = func136;
      func136.methodName = "hasPrev";
      func136.paramCounts = [0];
      func136.paramNames = [];
      func136.definitionLine = 189;
      func136.definitionModule = "lexer";
      var func140 = function(argcv) {    // method hasNext, line 190
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("hasNext", 0, numArgs - 0);
        }
        setLineNumber(190);    // compilenode member
        // call case 6: other requests
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call143 = selfRequest(this, "next", [0]);
        var call142 = request(call143, "isHeader", [0]);
        var call141 = request(call142, "not", [0]);
        return call141;
      };    // end of method hasNext
      this.methods["hasNext"] = func140;
      func140.methodName = "hasNext";
      func140.paramCounts = [0];
      func140.paramNames = [];
      func140.definitionLine = 190;
      func140.definitionModule = "lexer";
      var func144 = function(argcv) {     // accessor method isIdentifier
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isIdentifier", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isIdentifier
      this.methods["isIdentifier"] = func144;
      func144.methodName = "isIdentifier";
      func144.paramCounts = [0];
      func144.paramNames = [];
      func144.definitionLine = 191;
      func144.definitionModule = "lexer";
      var func145 = function(argcv) {     // accessor method isString
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isString", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isString
      this.methods["isString"] = func145;
      func145.methodName = "isString";
      func145.paramCounts = [0];
      func145.paramNames = [];
      func145.definitionLine = 192;
      func145.definitionModule = "lexer";
      var func146 = function(argcv) {     // accessor method isComment
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isComment", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isComment
      this.methods["isComment"] = func146;
      func146.methodName = "isComment";
      func146.paramCounts = [0];
      func146.paramNames = [];
      func146.definitionLine = 193;
      func146.definitionModule = "lexer";
      var func147 = function(argcv) {     // accessor method isLBrace
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isLBrace", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isLBrace
      this.methods["isLBrace"] = func147;
      func147.methodName = "isLBrace";
      func147.paramCounts = [0];
      func147.paramNames = [];
      func147.definitionLine = 194;
      func147.definitionModule = "lexer";
      var func148 = function(argcv) {     // accessor method isRBrace
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isRBrace", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isRBrace
      this.methods["isRBrace"] = func148;
      func148.methodName = "isRBrace";
      func148.paramCounts = [0];
      func148.paramNames = [];
      func148.definitionLine = 195;
      func148.definitionModule = "lexer";
      var func149 = function(argcv) {     // accessor method isLParen
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isLParen", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isLParen
      this.methods["isLParen"] = func149;
      func149.methodName = "isLParen";
      func149.paramCounts = [0];
      func149.paramNames = [];
      func149.definitionLine = 196;
      func149.definitionModule = "lexer";
      var func150 = function(argcv) {     // accessor method isRParen
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isRParen", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isRParen
      this.methods["isRParen"] = func150;
      func150.methodName = "isRParen";
      func150.paramCounts = [0];
      func150.paramNames = [];
      func150.definitionLine = 197;
      func150.definitionModule = "lexer";
      var func151 = function(argcv) {     // accessor method isLSquare
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isLSquare", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isLSquare
      this.methods["isLSquare"] = func151;
      func151.methodName = "isLSquare";
      func151.paramCounts = [0];
      func151.paramNames = [];
      func151.definitionLine = 198;
      func151.definitionModule = "lexer";
      var func152 = function(argcv) {     // accessor method isRSquare
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isRSquare", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isRSquare
      this.methods["isRSquare"] = func152;
      func152.methodName = "isRSquare";
      func152.paramCounts = [0];
      func152.paramNames = [];
      func152.definitionLine = 199;
      func152.definitionModule = "lexer";
      var func153 = function(argcv) {     // accessor method isComma
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isComma", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isComma
      this.methods["isComma"] = func153;
      func153.methodName = "isComma";
      func153.paramCounts = [0];
      func153.paramNames = [];
      func153.definitionLine = 200;
      func153.definitionModule = "lexer";
      var func154 = function(argcv) {     // accessor method isColon
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isColon", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isColon
      this.methods["isColon"] = func154;
      func154.methodName = "isColon";
      func154.paramCounts = [0];
      func154.paramNames = [];
      func154.definitionLine = 201;
      func154.definitionModule = "lexer";
      var func155 = function(argcv) {     // accessor method isDot
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isDot", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isDot
      this.methods["isDot"] = func155;
      func155.methodName = "isDot";
      func155.paramCounts = [0];
      func155.paramNames = [];
      func155.definitionLine = 202;
      func155.definitionModule = "lexer";
      var func156 = function(argcv) {     // accessor method isNum
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isNum", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isNum
      this.methods["isNum"] = func156;
      func156.methodName = "isNum";
      func156.paramCounts = [0];
      func156.paramNames = [];
      func156.definitionLine = 203;
      func156.definitionModule = "lexer";
      var func157 = function(argcv) {     // accessor method isKeyword
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isKeyword", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isKeyword
      this.methods["isKeyword"] = func157;
      func157.methodName = "isKeyword";
      func157.paramCounts = [0];
      func157.paramNames = [];
      func157.definitionLine = 204;
      func157.definitionModule = "lexer";
      var func158 = function(argcv) {     // accessor method isOp
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isOp", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isOp
      this.methods["isOp"] = func158;
      func158.methodName = "isOp";
      func158.paramCounts = [0];
      func158.paramNames = [];
      func158.definitionLine = 205;
      func158.definitionModule = "lexer";
      var func159 = function(argcv) {     // accessor method isArrow
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isArrow", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isArrow
      this.methods["isArrow"] = func159;
      func159.methodName = "isArrow";
      func159.paramCounts = [0];
      func159.paramNames = [];
      func159.definitionLine = 206;
      func159.definitionModule = "lexer";
      var func160 = function(argcv) {     // accessor method isBind
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isBind", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isBind
      this.methods["isBind"] = func160;
      func160.methodName = "isBind";
      func160.paramCounts = [0];
      func160.paramNames = [];
      func160.definitionLine = 207;
      func160.definitionModule = "lexer";
      var func161 = function(argcv) {     // accessor method isSeparator
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isSeparator", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isSeparator
      this.methods["isSeparator"] = func161;
      func161.methodName = "isSeparator";
      func161.paramCounts = [0];
      func161.paramNames = [];
      func161.definitionLine = 208;
      func161.definitionModule = "lexer";
      var func162 = function(argcv) {     // accessor method isLGeneric
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isLGeneric", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isLGeneric
      this.methods["isLGeneric"] = func162;
      func162.methodName = "isLGeneric";
      func162.paramCounts = [0];
      func162.paramNames = [];
      func162.definitionLine = 209;
      func162.definitionModule = "lexer";
      var func163 = function(argcv) {     // accessor method isRGeneric
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isRGeneric", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isRGeneric
      this.methods["isRGeneric"] = func163;
      func163.methodName = "isRGeneric";
      func163.paramCounts = [0];
      func163.paramNames = [];
      func163.definitionLine = 210;
      func163.definitionModule = "lexer";
      var func164 = function(argcv) {     // accessor method isEof
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isEof", 0, numArgs - 0);
        return GraceFalse;
      };    // end of method isEof
      this.methods["isEof"] = func164;
      func164.methodName = "isEof";
      func164.paramCounts = [0];
      func164.paramNames = [];
      func164.definitionLine = 211;
      func164.definitionModule = "lexer";
      this.mutable = true;
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 158;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj75_init = function() {    // init of object on line 158
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        this.data.line = var_lineNumber;
        if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
        this.data.indent = var_currentLineIndent;
        if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
        this.data.linePos = var_startPosition;
        setLineNumber(164);    // compilenode object
        var obj165_build = function(ignore, outerObj, aliases, exclusions) {
          this.closureKeys = this.closureKeys || [];
          this.closureKeys.push("outer_lexer_164");
          this.outer_lexer_164 = outerObj;
          const inheritedExclusions = { };
          for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
              const exMeth = exclusions[eix];
              inheritedExclusions[exMeth] = this.methods[exMeth]; };
          setLineNumber(165);    // reuse call
          var initFun166 = selfRequest(var_prelude, "identityEquality$build(3)", [null], this, [], []);  // compileReuseCall
          var func167 = function(argcv) {    // method asString, line 166
            var returnTarget = invocationCount;
            invocationCount++;
            const numArgs = arguments.length - 1;
            if ((numArgs > 0) && (numArgs !== 0)) {
                raiseTypeArgError("asString", 0, numArgs - 0);
            }
            setLineNumber(166);    // compilenode string
            var string168 = new GraceString("\u23da");
            return string168;
          };    // end of method asString
          this.methods["asString"] = func167;
          func167.methodName = "asString";
          func167.paramCounts = [0];
          func167.paramNames = [];
          func167.definitionLine = 166;
          func167.definitionModule = "lexer";
          var func169 = function(argcv) {     // accessor method isHeader
            const numArgs = arguments.length - 1;
            if (numArgs > 0) raiseTypeArgError("isHeader", 0, numArgs - 0);
            return GraceTrue;
          };    // end of method isHeader
          this.methods["isHeader"] = func169;
          func169.methodName = "isHeader";
          func169.paramCounts = [0];
          func169.paramNames = [];
          func169.definitionLine = 167;
          func169.definitionModule = "lexer";
          const overridenByAliases = { };
          for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
              const a = aliases[aix];
              const newNm = a.newName;
              const oldNm = a.oldName;
              overridenByAliases[newNm] = this.methods[newNm];
              const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
              m.definitionLine = 164;
              m.definitionModule = "lexer";
              this.methods[newNm] = m;
          }
          for (let exName in inheritedExclusions) {
              if (inheritedExclusions.hasOwnProperty(exName)) {
                  if (inheritedExclusions[exName]) {
                      this.methods[exName] = inheritedExclusions[exName];
                  } else {
                      delete this.methods[exName];
                  }
              }
          }
          var obj165_init = function() {    // init of object on line 164
          };
          return obj165_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
        };
        var obj165 = emptyGraceObject("null", "lexer", 164);
        var obj165_init = obj165_build.call(obj165, null, this, [], []);
        obj165_init.call(obj165);  // end of compileobject
        this.data.null = obj165;
        setLineNumber(169);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call170 = selfRequest(this, "null", [0]);
        this.data.next = call170;
        setLineNumber(170);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call171 = selfRequest(this, "null", [0]);
        this.data.prev = call171;
      };
      return obj75_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj75_init = obj75_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj75_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method token$build(_,_,_)
  this.methods["token$build(3)"] = func74;
  func74.methodName = "token$build(3)";
  func74.paramCounts = [0];
  func74.paramNames = [];
  func74.definitionLine = 158;
  func74.definitionModule = "lexer";
  var func172 = function(argcv, var_s) {    // method identifierToken(_), line 214
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("identifierToken(_)", 0, numArgs - 1);
    }
    var ouc = emptyGraceObject("identifierToken(_)", "lexer", 214);
    var ouc_init = this.methods["identifierToken(1)$build(3)"].call(this, null, var_s, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method identifierToken(_)
  this.methods["identifierToken(1)"] = func172;
  func172.methodName = "identifierToken(1)";
  func172.paramCounts = [1];
  func172.paramNames = ["s"];
  func172.definitionLine = 214;
  func172.definitionModule = "lexer";
  var func173 = function(argcv, var_s, inheritingObject, aliases, exclusions) {    // method identifierToken(_)$build(_,_,_), line 214
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("identifierToken(_)", 0, numArgs - 1);
    }
    var obj174_build = function(ignore, var_s, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_214");
      this.outer_lexer_214 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(215);    // reuse call
      var initFun175 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader176_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader176_kind.isDef = true;
      this.methods["kind"] = reader176_kind;
      this.data.value = undefined;
      var reader177_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader177_value.isDef = true;
      this.methods["value"] = reader177_value;
      this.data.size = undefined;
      var reader178_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader178_size.isDef = true;
      this.methods["size"] = reader178_size;
      var func179 = function(argcv) {     // accessor method isIdentifier
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isIdentifier", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isIdentifier
      this.methods["isIdentifier"] = func179;
      func179.methodName = "isIdentifier";
      func179.paramCounts = [0];
      func179.paramNames = [];
      func179.definitionLine = 219;
      func179.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 214;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj174_init = function() {    // init of object on line 214
        initFun175.call(this);
        setLineNumber(216);    // compilenode string
        var string180 = new GraceString("identifier");
        this.data.kind = string180;
        this.data.value = var_s;
        setLineNumber(218);    // compilenode member
        // call case 6: other requests
        var call181 = request(var_s, "size", [0]);
        this.data.size = call181;
      };
      return obj174_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj174_init = obj174_build.call(inheritingObject, null, var_s, this, aliases, exclusions);
    return obj174_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method identifierToken(_)$build(_,_,_)
  this.methods["identifierToken(1)$build(3)"] = func173;
  func173.methodName = "identifierToken(1)$build(3)";
  func173.paramCounts = [1];
  func173.paramNames = ["s"];
  func173.definitionLine = 214;
  func173.definitionModule = "lexer";
  var func182 = function(argcv, var_s) {    // method stringToken(_), line 221
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("stringToken(_)", 0, numArgs - 1);
    }
    var ouc = emptyGraceObject("stringToken(_)", "lexer", 221);
    var ouc_init = this.methods["stringToken(1)$build(3)"].call(this, null, var_s, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method stringToken(_)
  this.methods["stringToken(1)"] = func182;
  func182.methodName = "stringToken(1)";
  func182.paramCounts = [1];
  func182.paramNames = ["s"];
  func182.definitionLine = 221;
  func182.definitionModule = "lexer";
  var func183 = function(argcv, var_s, inheritingObject, aliases, exclusions) {    // method stringToken(_)$build(_,_,_), line 221
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("stringToken(_)", 0, numArgs - 1);
    }
    var obj184_build = function(ignore, var_s, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_221");
      this.outer_lexer_221 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(222);    // reuse call
      var initFun185 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader186_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader186_kind.isDef = true;
      this.methods["kind"] = reader186_kind;
      this.data.value = undefined;
      var reader187_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader187_value.isDef = true;
      this.methods["value"] = reader187_value;
      this.data.size = undefined;
      var reader188_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader188_size.isDef = true;
      this.methods["size"] = reader188_size;
      var func189 = function(argcv) {     // accessor method isString
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isString", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isString
      this.methods["isString"] = func189;
      func189.methodName = "isString";
      func189.paramCounts = [0];
      func189.paramNames = [];
      func189.definitionLine = 226;
      func189.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 221;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj184_init = function() {    // init of object on line 221
        initFun185.call(this);
        setLineNumber(223);    // compilenode string
        var string190 = new GraceString("string");
        this.data.kind = string190;
        this.data.value = var_s;
        setLineNumber(225);    // compilenode op
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
        var diff191 = request(var_linePosition, "-(1)", [1], var_startPosition);
        var sum192 = request(diff191, "+(1)", [1], new GraceNum(1));
        this.data.size = sum192;
      };
      return obj184_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj184_init = obj184_build.call(inheritingObject, null, var_s, this, aliases, exclusions);
    return obj184_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method stringToken(_)$build(_,_,_)
  this.methods["stringToken(1)$build(3)"] = func183;
  func183.methodName = "stringToken(1)$build(3)";
  func183.paramCounts = [1];
  func183.paramNames = ["s"];
  func183.definitionLine = 221;
  func183.definitionModule = "lexer";
  var func193 = function(argcv, var_s) {    // method multiLineStringToken(_), line 228
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("multiLineStringToken(_)", 0, numArgs - 1);
    }
    var ouc = emptyGraceObject("multiLineStringToken(_)", "lexer", 228);
    var ouc_init = this.methods["multiLineStringToken(1)$build(3)"].call(this, null, var_s, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method multiLineStringToken(_)
  this.methods["multiLineStringToken(1)"] = func193;
  func193.methodName = "multiLineStringToken(1)";
  func193.paramCounts = [1];
  func193.paramNames = ["s"];
  func193.definitionLine = 228;
  func193.definitionModule = "lexer";
  var func194 = function(argcv, var_s, inheritingObject, aliases, exclusions) {    // method multiLineStringToken(_)$build(_,_,_), line 228
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("multiLineStringToken(_)", 0, numArgs - 1);
    }
    var obj195_build = function(ignore, var_s, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_228");
      this.outer_lexer_228 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(229);    // reuse call
      var initFun196 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader197_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader197_kind.isDef = true;
      this.methods["kind"] = reader197_kind;
      this.data.value = undefined;
      var reader198_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader198_value.isDef = true;
      this.methods["value"] = reader198_value;
      this.data.size = undefined;
      var reader199_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader199_size.isDef = true;
      this.methods["size"] = reader199_size;
      this.data.linePos = undefined;
      var reader200_linePos = function() {  // reader method linePos
          if (this.data.linePos === undefined) raiseUninitializedVariable("linePos");
          return this.data.linePos;
      };
      reader200_linePos.isDef = true;
      this.methods["linePos"] = reader200_linePos;
      this.data.line = undefined;
      var reader201_line = function() {  // reader method line
          if (this.data.line === undefined) raiseUninitializedVariable("line");
          return this.data.line;
      };
      reader201_line.isDef = true;
      this.methods["line"] = reader201_line;
      this.data.endLine = undefined;
      var reader202_endLine = function() {  // reader method endLine
          if (this.data.endLine === undefined) raiseUninitializedVariable("endLine");
          return this.data.endLine;
      };
      reader202_endLine.isDef = true;
      this.methods["endLine"] = reader202_endLine;
      this.data.endPos = undefined;
      var reader203_endPos = function() {  // reader method endPos
          if (this.data.endPos === undefined) raiseUninitializedVariable("endPos");
          return this.data.endPos;
      };
      reader203_endPos.isDef = true;
      this.methods["endPos"] = reader203_endPos;
      var func204 = function(argcv) {     // accessor method isString
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isString", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isString
      this.methods["isString"] = func204;
      func204.methodName = "isString";
      func204.paramCounts = [0];
      func204.paramNames = [];
      func204.definitionLine = 237;
      func204.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 228;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj195_init = function() {    // init of object on line 228
        initFun196.call(this);
        setLineNumber(230);    // compilenode string
        var string205 = new GraceString("string");
        this.data.kind = string205;
        this.data.value = var_s;
        setLineNumber(232);    // compilenode member
        // call case 6: other requests
        var call206 = request(var_s, "size", [0]);
        var sum207 = request(call206, "+(1)", [1], new GraceNum(2));
        this.data.size = sum207;
        if (var_stringStart === undefined) raiseUninitializedVariable("stringStart");
        this.data.linePos = var_stringStart;
        if (var_startLine === undefined) raiseUninitializedVariable("startLine");
        this.data.line = var_startLine;
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        this.data.endLine = var_lineNumber;
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        this.data.endPos = var_linePosition;
      };
      return obj195_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj195_init = obj195_build.call(inheritingObject, null, var_s, this, aliases, exclusions);
    return obj195_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method multiLineStringToken(_)$build(_,_,_)
  this.methods["multiLineStringToken(1)$build(3)"] = func194;
  func194.methodName = "multiLineStringToken(1)$build(3)";
  func194.paramCounts = [1];
  func194.paramNames = ["s"];
  func194.definitionLine = 228;
  func194.definitionModule = "lexer";
  var func208 = function(argcv, var_s) {    // method commentToken(_), line 239
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("commentToken(_)", 0, numArgs - 1);
    }
    var ouc = emptyGraceObject("commentToken(_)", "lexer", 239);
    var ouc_init = this.methods["commentToken(1)$build(3)"].call(this, null, var_s, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method commentToken(_)
  this.methods["commentToken(1)"] = func208;
  func208.methodName = "commentToken(1)";
  func208.paramCounts = [1];
  func208.paramNames = ["s"];
  func208.definitionLine = 239;
  func208.definitionModule = "lexer";
  var func209 = function(argcv, var_s, inheritingObject, aliases, exclusions) {    // method commentToken(_)$build(_,_,_), line 239
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("commentToken(_)", 0, numArgs - 1);
    }
    var obj210_build = function(ignore, var_s, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_239");
      this.outer_lexer_239 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(240);    // reuse call
      var initFun211 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader212_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader212_kind.isDef = true;
      this.methods["kind"] = reader212_kind;
      this.data.value = undefined;
      var reader213_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader213_value.isDef = true;
      this.methods["value"] = reader213_value;
      this.data.size = undefined;
      var reader214_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader214_size.isDef = true;
      this.methods["size"] = reader214_size;
      var func215 = function(argcv) {     // accessor method isComment
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isComment", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isComment
      this.methods["isComment"] = func215;
      func215.methodName = "isComment";
      func215.paramCounts = [0];
      func215.paramNames = [];
      func215.definitionLine = 244;
      func215.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 239;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj210_init = function() {    // init of object on line 239
        initFun211.call(this);
        setLineNumber(241);    // compilenode string
        var string216 = new GraceString("comment");
        this.data.kind = string216;
        this.data.value = var_s;
        setLineNumber(243);    // compilenode member
        // call case 6: other requests
        var call217 = request(var_s, "size", [0]);
        var sum218 = request(call217, "+(1)", [1], new GraceNum(2));
        this.data.size = sum218;
      };
      return obj210_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj210_init = obj210_build.call(inheritingObject, null, var_s, this, aliases, exclusions);
    return obj210_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method commentToken(_)$build(_,_,_)
  this.methods["commentToken(1)$build(3)"] = func209;
  func209.methodName = "commentToken(1)$build(3)";
  func209.paramCounts = [1];
  func209.paramNames = ["s"];
  func209.definitionLine = 239;
  func209.definitionModule = "lexer";
  var func219 = function(argcv) {    // method lBraceToken, line 246
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lBraceToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("lBraceToken", "lexer", 246);
    var ouc_init = this.methods["lBraceToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method lBraceToken
  this.methods["lBraceToken"] = func219;
  func219.methodName = "lBraceToken";
  func219.paramCounts = [0];
  func219.paramNames = [];
  func219.definitionLine = 246;
  func219.definitionModule = "lexer";
  var func220 = function(argcv, inheritingObject, aliases, exclusions) {    // method lBraceToken$build(_,_,_), line 246
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lBraceToken", 0, numArgs - 0);
    }
    var obj221_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_246");
      this.outer_lexer_246 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(247);    // reuse call
      var initFun222 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader223_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader223_kind.isDef = true;
      this.methods["kind"] = reader223_kind;
      this.data.value = undefined;
      var reader224_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader224_value.isDef = true;
      this.methods["value"] = reader224_value;
      this.data.size = undefined;
      var reader225_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader225_size.isDef = true;
      this.methods["size"] = reader225_size;
      var func226 = function(argcv) {     // accessor method isLBrace
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isLBrace", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isLBrace
      this.methods["isLBrace"] = func226;
      func226.methodName = "isLBrace";
      func226.paramCounts = [0];
      func226.paramNames = [];
      func226.definitionLine = 251;
      func226.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 246;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj221_init = function() {    // init of object on line 246
        initFun222.call(this);
        setLineNumber(248);    // compilenode string
        var string227 = new GraceString("lbrace");
        this.data.kind = string227;
        setLineNumber(249);    // compilenode string
        var string228 = new GraceString("{");
        this.data.value = string228;
        setLineNumber(250);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj221_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj221_init = obj221_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj221_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method lBraceToken$build(_,_,_)
  this.methods["lBraceToken$build(3)"] = func220;
  func220.methodName = "lBraceToken$build(3)";
  func220.paramCounts = [0];
  func220.paramNames = [];
  func220.definitionLine = 246;
  func220.definitionModule = "lexer";
  var func229 = function(argcv) {    // method rBraceToken, line 253
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rBraceToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("rBraceToken", "lexer", 253);
    var ouc_init = this.methods["rBraceToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method rBraceToken
  this.methods["rBraceToken"] = func229;
  func229.methodName = "rBraceToken";
  func229.paramCounts = [0];
  func229.paramNames = [];
  func229.definitionLine = 253;
  func229.definitionModule = "lexer";
  var func230 = function(argcv, inheritingObject, aliases, exclusions) {    // method rBraceToken$build(_,_,_), line 253
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rBraceToken", 0, numArgs - 0);
    }
    var obj231_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_253");
      this.outer_lexer_253 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(254);    // reuse call
      var initFun232 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader233_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader233_kind.isDef = true;
      this.methods["kind"] = reader233_kind;
      this.data.value = undefined;
      var reader234_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader234_value.isDef = true;
      this.methods["value"] = reader234_value;
      this.data.size = undefined;
      var reader235_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader235_size.isDef = true;
      this.methods["size"] = reader235_size;
      var func236 = function(argcv) {     // accessor method isRBrace
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isRBrace", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isRBrace
      this.methods["isRBrace"] = func236;
      func236.methodName = "isRBrace";
      func236.paramCounts = [0];
      func236.paramNames = [];
      func236.definitionLine = 258;
      func236.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 253;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj231_init = function() {    // init of object on line 253
        initFun232.call(this);
        setLineNumber(255);    // compilenode string
        var string237 = new GraceString("rbrace");
        this.data.kind = string237;
        setLineNumber(256);    // compilenode string
        var string238 = new GraceString("}");
        this.data.value = string238;
        setLineNumber(257);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj231_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj231_init = obj231_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj231_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method rBraceToken$build(_,_,_)
  this.methods["rBraceToken$build(3)"] = func230;
  func230.methodName = "rBraceToken$build(3)";
  func230.paramCounts = [0];
  func230.paramNames = [];
  func230.definitionLine = 253;
  func230.definitionModule = "lexer";
  var func239 = function(argcv) {    // method lParenToken, line 260
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lParenToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("lParenToken", "lexer", 260);
    var ouc_init = this.methods["lParenToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method lParenToken
  this.methods["lParenToken"] = func239;
  func239.methodName = "lParenToken";
  func239.paramCounts = [0];
  func239.paramNames = [];
  func239.definitionLine = 260;
  func239.definitionModule = "lexer";
  var func240 = function(argcv, inheritingObject, aliases, exclusions) {    // method lParenToken$build(_,_,_), line 260
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lParenToken", 0, numArgs - 0);
    }
    var obj241_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_260");
      this.outer_lexer_260 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(261);    // reuse call
      var initFun242 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader243_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader243_kind.isDef = true;
      this.methods["kind"] = reader243_kind;
      this.data.value = undefined;
      var reader244_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader244_value.isDef = true;
      this.methods["value"] = reader244_value;
      this.data.size = undefined;
      var reader245_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader245_size.isDef = true;
      this.methods["size"] = reader245_size;
      var func246 = function(argcv) {     // accessor method isLParen
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isLParen", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isLParen
      this.methods["isLParen"] = func246;
      func246.methodName = "isLParen";
      func246.paramCounts = [0];
      func246.paramNames = [];
      func246.definitionLine = 265;
      func246.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 260;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj241_init = function() {    // init of object on line 260
        initFun242.call(this);
        setLineNumber(262);    // compilenode string
        var string247 = new GraceString("lparen");
        this.data.kind = string247;
        setLineNumber(263);    // compilenode string
        var string248 = new GraceString("(");
        this.data.value = string248;
        setLineNumber(264);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj241_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj241_init = obj241_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj241_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method lParenToken$build(_,_,_)
  this.methods["lParenToken$build(3)"] = func240;
  func240.methodName = "lParenToken$build(3)";
  func240.paramCounts = [0];
  func240.paramNames = [];
  func240.definitionLine = 260;
  func240.definitionModule = "lexer";
  var func249 = function(argcv) {    // method rParenToken, line 267
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rParenToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("rParenToken", "lexer", 267);
    var ouc_init = this.methods["rParenToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method rParenToken
  this.methods["rParenToken"] = func249;
  func249.methodName = "rParenToken";
  func249.paramCounts = [0];
  func249.paramNames = [];
  func249.definitionLine = 267;
  func249.definitionModule = "lexer";
  var func250 = function(argcv, inheritingObject, aliases, exclusions) {    // method rParenToken$build(_,_,_), line 267
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rParenToken", 0, numArgs - 0);
    }
    var obj251_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_267");
      this.outer_lexer_267 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(268);    // reuse call
      var initFun252 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader253_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader253_kind.isDef = true;
      this.methods["kind"] = reader253_kind;
      this.data.value = undefined;
      var reader254_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader254_value.isDef = true;
      this.methods["value"] = reader254_value;
      this.data.size = undefined;
      var reader255_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader255_size.isDef = true;
      this.methods["size"] = reader255_size;
      var func256 = function(argcv) {     // accessor method isRParen
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isRParen", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isRParen
      this.methods["isRParen"] = func256;
      func256.methodName = "isRParen";
      func256.paramCounts = [0];
      func256.paramNames = [];
      func256.definitionLine = 272;
      func256.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 267;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj251_init = function() {    // init of object on line 267
        initFun252.call(this);
        setLineNumber(269);    // compilenode string
        var string257 = new GraceString("rparen");
        this.data.kind = string257;
        setLineNumber(270);    // compilenode string
        var string258 = new GraceString(")");
        this.data.value = string258;
        setLineNumber(271);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj251_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj251_init = obj251_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj251_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method rParenToken$build(_,_,_)
  this.methods["rParenToken$build(3)"] = func250;
  func250.methodName = "rParenToken$build(3)";
  func250.paramCounts = [0];
  func250.paramNames = [];
  func250.definitionLine = 267;
  func250.definitionModule = "lexer";
  var func259 = function(argcv) {    // method lSquareToken, line 274
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lSquareToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("lSquareToken", "lexer", 274);
    var ouc_init = this.methods["lSquareToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method lSquareToken
  this.methods["lSquareToken"] = func259;
  func259.methodName = "lSquareToken";
  func259.paramCounts = [0];
  func259.paramNames = [];
  func259.definitionLine = 274;
  func259.definitionModule = "lexer";
  var func260 = function(argcv, inheritingObject, aliases, exclusions) {    // method lSquareToken$build(_,_,_), line 274
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lSquareToken", 0, numArgs - 0);
    }
    var obj261_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_274");
      this.outer_lexer_274 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(275);    // reuse call
      var initFun262 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader263_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader263_kind.isDef = true;
      this.methods["kind"] = reader263_kind;
      this.data.value = undefined;
      var reader264_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader264_value.isDef = true;
      this.methods["value"] = reader264_value;
      this.data.size = undefined;
      var reader265_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader265_size.isDef = true;
      this.methods["size"] = reader265_size;
      var func266 = function(argcv) {     // accessor method isLSquare
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isLSquare", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isLSquare
      this.methods["isLSquare"] = func266;
      func266.methodName = "isLSquare";
      func266.paramCounts = [0];
      func266.paramNames = [];
      func266.definitionLine = 279;
      func266.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 274;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj261_init = function() {    // init of object on line 274
        initFun262.call(this);
        setLineNumber(276);    // compilenode string
        var string267 = new GraceString("lsquare");
        this.data.kind = string267;
        setLineNumber(277);    // compilenode string
        var string268 = new GraceString("[");
        this.data.value = string268;
        setLineNumber(278);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj261_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj261_init = obj261_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj261_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method lSquareToken$build(_,_,_)
  this.methods["lSquareToken$build(3)"] = func260;
  func260.methodName = "lSquareToken$build(3)";
  func260.paramCounts = [0];
  func260.paramNames = [];
  func260.definitionLine = 274;
  func260.definitionModule = "lexer";
  var func269 = function(argcv) {    // method rSquareToken, line 281
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rSquareToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("rSquareToken", "lexer", 281);
    var ouc_init = this.methods["rSquareToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method rSquareToken
  this.methods["rSquareToken"] = func269;
  func269.methodName = "rSquareToken";
  func269.paramCounts = [0];
  func269.paramNames = [];
  func269.definitionLine = 281;
  func269.definitionModule = "lexer";
  var func270 = function(argcv, inheritingObject, aliases, exclusions) {    // method rSquareToken$build(_,_,_), line 281
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rSquareToken", 0, numArgs - 0);
    }
    var obj271_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_281");
      this.outer_lexer_281 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(282);    // reuse call
      var initFun272 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader273_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader273_kind.isDef = true;
      this.methods["kind"] = reader273_kind;
      this.data.value = undefined;
      var reader274_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader274_value.isDef = true;
      this.methods["value"] = reader274_value;
      this.data.size = undefined;
      var reader275_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader275_size.isDef = true;
      this.methods["size"] = reader275_size;
      var func276 = function(argcv) {     // accessor method isRSquare
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isRSquare", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isRSquare
      this.methods["isRSquare"] = func276;
      func276.methodName = "isRSquare";
      func276.paramCounts = [0];
      func276.paramNames = [];
      func276.definitionLine = 286;
      func276.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 281;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj271_init = function() {    // init of object on line 281
        initFun272.call(this);
        setLineNumber(283);    // compilenode string
        var string277 = new GraceString("rsquare");
        this.data.kind = string277;
        setLineNumber(284);    // compilenode string
        var string278 = new GraceString("]");
        this.data.value = string278;
        setLineNumber(285);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj271_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj271_init = obj271_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj271_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method rSquareToken$build(_,_,_)
  this.methods["rSquareToken$build(3)"] = func270;
  func270.methodName = "rSquareToken$build(3)";
  func270.paramCounts = [0];
  func270.paramNames = [];
  func270.definitionLine = 281;
  func270.definitionModule = "lexer";
  var func279 = function(argcv) {    // method commaToken, line 288
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("commaToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("commaToken", "lexer", 288);
    var ouc_init = this.methods["commaToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method commaToken
  this.methods["commaToken"] = func279;
  func279.methodName = "commaToken";
  func279.paramCounts = [0];
  func279.paramNames = [];
  func279.definitionLine = 288;
  func279.definitionModule = "lexer";
  var func280 = function(argcv, inheritingObject, aliases, exclusions) {    // method commaToken$build(_,_,_), line 288
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("commaToken", 0, numArgs - 0);
    }
    var obj281_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_288");
      this.outer_lexer_288 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(289);    // reuse call
      var initFun282 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader283_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader283_kind.isDef = true;
      this.methods["kind"] = reader283_kind;
      this.data.value = undefined;
      var reader284_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader284_value.isDef = true;
      this.methods["value"] = reader284_value;
      this.data.size = undefined;
      var reader285_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader285_size.isDef = true;
      this.methods["size"] = reader285_size;
      var func286 = function(argcv) {     // accessor method isComma
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isComma", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isComma
      this.methods["isComma"] = func286;
      func286.methodName = "isComma";
      func286.paramCounts = [0];
      func286.paramNames = [];
      func286.definitionLine = 293;
      func286.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 288;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj281_init = function() {    // init of object on line 288
        initFun282.call(this);
        setLineNumber(290);    // compilenode string
        var string287 = new GraceString("comma");
        this.data.kind = string287;
        setLineNumber(291);    // compilenode string
        var string288 = new GraceString(",");
        this.data.value = string288;
        setLineNumber(292);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj281_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj281_init = obj281_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj281_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method commaToken$build(_,_,_)
  this.methods["commaToken$build(3)"] = func280;
  func280.methodName = "commaToken$build(3)";
  func280.paramCounts = [0];
  func280.paramNames = [];
  func280.definitionLine = 288;
  func280.definitionModule = "lexer";
  var func289 = function(argcv) {    // method colonToken, line 295
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("colonToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("colonToken", "lexer", 295);
    var ouc_init = this.methods["colonToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method colonToken
  this.methods["colonToken"] = func289;
  func289.methodName = "colonToken";
  func289.paramCounts = [0];
  func289.paramNames = [];
  func289.definitionLine = 295;
  func289.definitionModule = "lexer";
  var func290 = function(argcv, inheritingObject, aliases, exclusions) {    // method colonToken$build(_,_,_), line 295
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("colonToken", 0, numArgs - 0);
    }
    var obj291_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_295");
      this.outer_lexer_295 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(296);    // reuse call
      var initFun292 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader293_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader293_kind.isDef = true;
      this.methods["kind"] = reader293_kind;
      this.data.value = undefined;
      var reader294_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader294_value.isDef = true;
      this.methods["value"] = reader294_value;
      this.data.size = undefined;
      var reader295_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader295_size.isDef = true;
      this.methods["size"] = reader295_size;
      var func296 = function(argcv) {     // accessor method isColon
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isColon", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isColon
      this.methods["isColon"] = func296;
      func296.methodName = "isColon";
      func296.paramCounts = [0];
      func296.paramNames = [];
      func296.definitionLine = 300;
      func296.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 295;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj291_init = function() {    // init of object on line 295
        initFun292.call(this);
        setLineNumber(297);    // compilenode string
        var string297 = new GraceString("colon");
        this.data.kind = string297;
        setLineNumber(298);    // compilenode string
        var string298 = new GraceString(":");
        this.data.value = string298;
        setLineNumber(299);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj291_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj291_init = obj291_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj291_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method colonToken$build(_,_,_)
  this.methods["colonToken$build(3)"] = func290;
  func290.methodName = "colonToken$build(3)";
  func290.paramCounts = [0];
  func290.paramNames = [];
  func290.definitionLine = 295;
  func290.definitionModule = "lexer";
  var func299 = function(argcv) {    // method dotToken, line 302
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("dotToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("dotToken", "lexer", 302);
    var ouc_init = this.methods["dotToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method dotToken
  this.methods["dotToken"] = func299;
  func299.methodName = "dotToken";
  func299.paramCounts = [0];
  func299.paramNames = [];
  func299.definitionLine = 302;
  func299.definitionModule = "lexer";
  var func300 = function(argcv, inheritingObject, aliases, exclusions) {    // method dotToken$build(_,_,_), line 302
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("dotToken", 0, numArgs - 0);
    }
    var obj301_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_302");
      this.outer_lexer_302 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(303);    // reuse call
      var initFun302 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader303_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader303_kind.isDef = true;
      this.methods["kind"] = reader303_kind;
      this.data.value = undefined;
      var reader304_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader304_value.isDef = true;
      this.methods["value"] = reader304_value;
      this.data.size = undefined;
      var reader305_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader305_size.isDef = true;
      this.methods["size"] = reader305_size;
      var func306 = function(argcv) {     // accessor method isDot
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isDot", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isDot
      this.methods["isDot"] = func306;
      func306.methodName = "isDot";
      func306.paramCounts = [0];
      func306.paramNames = [];
      func306.definitionLine = 307;
      func306.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 302;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj301_init = function() {    // init of object on line 302
        initFun302.call(this);
        setLineNumber(304);    // compilenode string
        var string307 = new GraceString("dot");
        this.data.kind = string307;
        setLineNumber(305);    // compilenode string
        var string308 = new GraceString(".");
        this.data.value = string308;
        setLineNumber(306);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj301_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj301_init = obj301_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj301_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method dotToken$build(_,_,_)
  this.methods["dotToken$build(3)"] = func300;
  func300.methodName = "dotToken$build(3)";
  func300.paramCounts = [0];
  func300.paramNames = [];
  func300.definitionLine = 302;
  func300.definitionModule = "lexer";
  var func309 = function(argcv, var_v, var_b) {    // method numToken(_,_), line 309
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("numToken(_,_)", 0, numArgs - 2);
    }
    var ouc = emptyGraceObject("numToken(_,_)", "lexer", 309);
    var ouc_init = this.methods["numToken(2)$build(3)"].call(this, null, var_v, var_b, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method numToken(_,_)
  this.methods["numToken(2)"] = func309;
  func309.methodName = "numToken(2)";
  func309.paramCounts = [2];
  func309.paramNames = ["v", "b"];
  func309.definitionLine = 309;
  func309.definitionModule = "lexer";
  var func310 = function(argcv, var_v, var_b, inheritingObject, aliases, exclusions) {    // method numToken(_,_)$build(_,_,_), line 309
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("numToken(_,_)", 0, numArgs - 2);
    }
    var obj311_build = function(ignore, var_v, var_b, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_309");
      this.outer_lexer_309 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(310);    // reuse call
      var initFun312 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader313_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader313_kind.isDef = true;
      this.methods["kind"] = reader313_kind;
      this.data.value = undefined;
      var reader314_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader314_value.isDef = true;
      this.methods["value"] = reader314_value;
      this.data.base = undefined;
      var reader315_base = function() {  // reader method base
          if (this.data.base === undefined) raiseUninitializedVariable("base");
          return this.data.base;
      };
      reader315_base.isDef = true;
      this.methods["base"] = reader315_base;
      this.data.size = undefined;
      var reader316_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader316_size.isDef = true;
      this.methods["size"] = reader316_size;
      var func317 = function(argcv) {     // accessor method isNum
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isNum", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isNum
      this.methods["isNum"] = func317;
      func317.methodName = "isNum";
      func317.paramCounts = [0];
      func317.paramNames = [];
      func317.definitionLine = 315;
      func317.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 309;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj311_init = function() {    // init of object on line 309
        initFun312.call(this);
        setLineNumber(311);    // compilenode string
        var string318 = new GraceString("num");
        this.data.kind = string318;
        this.data.value = var_v;
        this.data.base = var_b;
        setLineNumber(314);    // compilenode op
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
        var diff319 = request(var_linePosition, "-(1)", [1], var_startPosition);
        var sum320 = request(diff319, "+(1)", [1], new GraceNum(1));
        this.data.size = sum320;
      };
      return obj311_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj311_init = obj311_build.call(inheritingObject, null, var_v, var_b, this, aliases, exclusions);
    return obj311_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method numToken(_,_)$build(_,_,_)
  this.methods["numToken(2)$build(3)"] = func310;
  func310.methodName = "numToken(2)$build(3)";
  func310.paramCounts = [2];
  func310.paramNames = ["v", "b"];
  func310.definitionLine = 309;
  func310.definitionModule = "lexer";
  var func321 = function(argcv, var_v) {    // method keywordToken(_), line 317
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("keywordToken(_)", 0, numArgs - 1);
    }
    var ouc = emptyGraceObject("keywordToken(_)", "lexer", 317);
    var ouc_init = this.methods["keywordToken(1)$build(3)"].call(this, null, var_v, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method keywordToken(_)
  this.methods["keywordToken(1)"] = func321;
  func321.methodName = "keywordToken(1)";
  func321.paramCounts = [1];
  func321.paramNames = ["v"];
  func321.definitionLine = 317;
  func321.definitionModule = "lexer";
  var func322 = function(argcv, var_v, inheritingObject, aliases, exclusions) {    // method keywordToken(_)$build(_,_,_), line 317
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("keywordToken(_)", 0, numArgs - 1);
    }
    var obj323_build = function(ignore, var_v, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_317");
      this.outer_lexer_317 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(318);    // reuse call
      var initFun324 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader325_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader325_kind.isDef = true;
      this.methods["kind"] = reader325_kind;
      this.data.value = undefined;
      var reader326_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader326_value.isDef = true;
      this.methods["value"] = reader326_value;
      this.data.size = undefined;
      var reader327_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader327_size.isDef = true;
      this.methods["size"] = reader327_size;
      var func328 = function(argcv) {     // accessor method isKeyword
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isKeyword", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isKeyword
      this.methods["isKeyword"] = func328;
      func328.methodName = "isKeyword";
      func328.paramCounts = [0];
      func328.paramNames = [];
      func328.definitionLine = 322;
      func328.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 317;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj323_init = function() {    // init of object on line 317
        initFun324.call(this);
        setLineNumber(319);    // compilenode string
        var string329 = new GraceString("keyword");
        this.data.kind = string329;
        this.data.value = var_v;
        setLineNumber(321);    // compilenode member
        // call case 6: other requests
        var call330 = request(var_v, "size", [0]);
        this.data.size = call330;
      };
      return obj323_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj323_init = obj323_build.call(inheritingObject, null, var_v, this, aliases, exclusions);
    return obj323_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method keywordToken(_)$build(_,_,_)
  this.methods["keywordToken(1)$build(3)"] = func322;
  func322.methodName = "keywordToken(1)$build(3)";
  func322.paramCounts = [1];
  func322.paramNames = ["v"];
  func322.definitionLine = 317;
  func322.definitionModule = "lexer";
  var func331 = function(argcv, var_v) {    // method opToken(_), line 324
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("opToken(_)", 0, numArgs - 1);
    }
    var ouc = emptyGraceObject("opToken(_)", "lexer", 324);
    var ouc_init = this.methods["opToken(1)$build(3)"].call(this, null, var_v, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method opToken(_)
  this.methods["opToken(1)"] = func331;
  func331.methodName = "opToken(1)";
  func331.paramCounts = [1];
  func331.paramNames = ["v"];
  func331.definitionLine = 324;
  func331.definitionModule = "lexer";
  var func332 = function(argcv, var_v, inheritingObject, aliases, exclusions) {    // method opToken(_)$build(_,_,_), line 324
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("opToken(_)", 0, numArgs - 1);
    }
    var obj333_build = function(ignore, var_v, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_324");
      this.outer_lexer_324 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(325);    // reuse call
      var initFun334 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader335_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader335_kind.isDef = true;
      this.methods["kind"] = reader335_kind;
      this.data.value = undefined;
      var reader336_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader336_value.isDef = true;
      this.methods["value"] = reader336_value;
      this.data.size = undefined;
      var reader337_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader337_size.isDef = true;
      this.methods["size"] = reader337_size;
      var func338 = function(argcv) {     // accessor method isOp
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isOp", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isOp
      this.methods["isOp"] = func338;
      func338.methodName = "isOp";
      func338.paramCounts = [0];
      func338.paramNames = [];
      func338.definitionLine = 329;
      func338.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 324;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj333_init = function() {    // init of object on line 324
        initFun334.call(this);
        setLineNumber(326);    // compilenode string
        var string339 = new GraceString("op");
        this.data.kind = string339;
        this.data.value = var_v;
        setLineNumber(328);    // compilenode member
        // call case 6: other requests
        var call340 = request(var_v, "size", [0]);
        this.data.size = call340;
      };
      return obj333_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj333_init = obj333_build.call(inheritingObject, null, var_v, this, aliases, exclusions);
    return obj333_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method opToken(_)$build(_,_,_)
  this.methods["opToken(1)$build(3)"] = func332;
  func332.methodName = "opToken(1)$build(3)";
  func332.paramCounts = [1];
  func332.paramNames = ["v"];
  func332.definitionLine = 324;
  func332.definitionModule = "lexer";
  var func341 = function(argcv) {    // method arrowToken, line 331
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("arrowToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("arrowToken", "lexer", 331);
    var ouc_init = this.methods["arrowToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method arrowToken
  this.methods["arrowToken"] = func341;
  func341.methodName = "arrowToken";
  func341.paramCounts = [0];
  func341.paramNames = [];
  func341.definitionLine = 331;
  func341.definitionModule = "lexer";
  var func342 = function(argcv, inheritingObject, aliases, exclusions) {    // method arrowToken$build(_,_,_), line 331
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("arrowToken", 0, numArgs - 0);
    }
    var obj343_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_331");
      this.outer_lexer_331 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(332);    // reuse call
      var initFun344 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader345_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader345_kind.isDef = true;
      this.methods["kind"] = reader345_kind;
      this.data.value = undefined;
      var reader346_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader346_value.isDef = true;
      this.methods["value"] = reader346_value;
      this.data.size = undefined;
      var reader347_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader347_size.isDef = true;
      this.methods["size"] = reader347_size;
      var func348 = function(argcv) {     // accessor method isArrow
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isArrow", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isArrow
      this.methods["isArrow"] = func348;
      func348.methodName = "isArrow";
      func348.paramCounts = [0];
      func348.paramNames = [];
      func348.definitionLine = 336;
      func348.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 331;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj343_init = function() {    // init of object on line 331
        initFun344.call(this);
        setLineNumber(333);    // compilenode string
        var string349 = new GraceString("arrow");
        this.data.kind = string349;
        setLineNumber(334);    // compilenode string
        var string350 = new GraceString("\u2192");
        this.data.value = string350;
        setLineNumber(335);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj343_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj343_init = obj343_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj343_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method arrowToken$build(_,_,_)
  this.methods["arrowToken$build(3)"] = func342;
  func342.methodName = "arrowToken$build(3)";
  func342.paramCounts = [0];
  func342.paramNames = [];
  func342.definitionLine = 331;
  func342.definitionModule = "lexer";
  var func351 = function(argcv) {    // method bindToken, line 338
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("bindToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("bindToken", "lexer", 338);
    var ouc_init = this.methods["bindToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method bindToken
  this.methods["bindToken"] = func351;
  func351.methodName = "bindToken";
  func351.paramCounts = [0];
  func351.paramNames = [];
  func351.definitionLine = 338;
  func351.definitionModule = "lexer";
  var func352 = function(argcv, inheritingObject, aliases, exclusions) {    // method bindToken$build(_,_,_), line 338
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("bindToken", 0, numArgs - 0);
    }
    var obj353_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_338");
      this.outer_lexer_338 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(339);    // reuse call
      var initFun354 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader355_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader355_kind.isDef = true;
      this.methods["kind"] = reader355_kind;
      this.data.value = undefined;
      var reader356_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader356_value.isDef = true;
      this.methods["value"] = reader356_value;
      this.data.size = undefined;
      var reader357_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader357_size.isDef = true;
      this.methods["size"] = reader357_size;
      var func358 = function(argcv) {     // accessor method isBind
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isBind", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isBind
      this.methods["isBind"] = func358;
      func358.methodName = "isBind";
      func358.paramCounts = [0];
      func358.paramNames = [];
      func358.definitionLine = 343;
      func358.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 338;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj353_init = function() {    // init of object on line 338
        initFun354.call(this);
        setLineNumber(340);    // compilenode string
        var string359 = new GraceString("bind");
        this.data.kind = string359;
        setLineNumber(341);    // compilenode string
        var string360 = new GraceString(":=");
        this.data.value = string360;
        setLineNumber(342);    // compilenode num
        this.data.size = new GraceNum(2);
      };
      return obj353_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj353_init = obj353_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj353_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method bindToken$build(_,_,_)
  this.methods["bindToken$build(3)"] = func352;
  func352.methodName = "bindToken$build(3)";
  func352.paramCounts = [0];
  func352.paramNames = [];
  func352.definitionLine = 338;
  func352.definitionModule = "lexer";
  var func361 = function(argcv) {    // method semicolonToken, line 345
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("semicolonToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("semicolonToken", "lexer", 345);
    var ouc_init = this.methods["semicolonToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method semicolonToken
  this.methods["semicolonToken"] = func361;
  func361.methodName = "semicolonToken";
  func361.paramCounts = [0];
  func361.paramNames = [];
  func361.definitionLine = 345;
  func361.definitionModule = "lexer";
  var func362 = function(argcv, inheritingObject, aliases, exclusions) {    // method semicolonToken$build(_,_,_), line 345
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("semicolonToken", 0, numArgs - 0);
    }
    var obj363_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_345");
      this.outer_lexer_345 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(346);    // reuse call
      var initFun364 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader365_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader365_kind.isDef = true;
      this.methods["kind"] = reader365_kind;
      this.data.value = undefined;
      var reader366_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader366_value.isDef = true;
      this.methods["value"] = reader366_value;
      this.data.size = undefined;
      var reader367_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader367_size.isDef = true;
      this.methods["size"] = reader367_size;
      var func368 = function(argcv) {     // accessor method isSeparator
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isSeparator", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isSeparator
      this.methods["isSeparator"] = func368;
      func368.methodName = "isSeparator";
      func368.paramCounts = [0];
      func368.paramNames = [];
      func368.definitionLine = 350;
      func368.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 345;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj363_init = function() {    // init of object on line 345
        initFun364.call(this);
        setLineNumber(347);    // compilenode string
        var string369 = new GraceString("semicolon");
        this.data.kind = string369;
        setLineNumber(348);    // compilenode string
        var string370 = new GraceString(";");
        this.data.value = string370;
        setLineNumber(349);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj363_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj363_init = obj363_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj363_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method semicolonToken$build(_,_,_)
  this.methods["semicolonToken$build(3)"] = func362;
  func362.methodName = "semicolonToken$build(3)";
  func362.paramCounts = [0];
  func362.paramNames = [];
  func362.definitionLine = 345;
  func362.definitionModule = "lexer";
  var func371 = function(argcv, var_lineNum, var_pos) {    // method separatorTokenAt(_,_), line 352
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("separatorTokenAt(_,_)", 0, numArgs - 2);
    }
    var ouc = emptyGraceObject("separatorTokenAt(_,_)", "lexer", 352);
    var ouc_init = this.methods["separatorTokenAt(2)$build(3)"].call(this, null, var_lineNum, var_pos, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method separatorTokenAt(_,_)
  this.methods["separatorTokenAt(2)"] = func371;
  func371.methodName = "separatorTokenAt(2)";
  func371.paramCounts = [2];
  func371.paramNames = ["lineNum", "pos"];
  func371.definitionLine = 352;
  func371.definitionModule = "lexer";
  var func372 = function(argcv, var_lineNum, var_pos, inheritingObject, aliases, exclusions) {    // method separatorTokenAt(_,_)$build(_,_,_), line 352
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("separatorTokenAt(_,_)", 0, numArgs - 2);
    }
    var obj373_build = function(ignore, var_lineNum, var_pos, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_352");
      this.outer_lexer_352 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(353);    // reuse call
      var initFun374 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader375_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader375_kind.isDef = true;
      this.methods["kind"] = reader375_kind;
      this.data.value = undefined;
      var reader376_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader376_value.isDef = true;
      this.methods["value"] = reader376_value;
      this.data.size = undefined;
      var reader377_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader377_size.isDef = true;
      this.methods["size"] = reader377_size;
      var func378 = function(argcv) {    // method asString, line 357
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("asString", 0, numArgs - 0);
        }
        setLineNumber(357);    // compilenode string
        var string379 = new GraceString("(");
        // call case 4: self request with 0 args and 0 typeArgs 
        var call380 = selfRequest(this, "line", [0]);
        var opresult381 = request(string379, "++(1)", [1], call380);
        var string382 = new GraceString(".");
        var opresult383 = request(opresult381, "++(1)", [1], string382);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call384 = selfRequest(this, "linePos", [0]);
        var opresult385 = request(opresult383, "++(1)", [1], call384);
        var string386 = new GraceString(")");
        var opresult387 = request(opresult385, "++(1)", [1], string386);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call388 = selfRequest(this, "kind", [0]);
        var opresult389 = request(opresult387, "++(1)", [1], call388);
        var string390 = new GraceString(" \\n");
        var opresult391 = request(opresult389, "++(1)", [1], string390);
        return opresult391;
      };    // end of method asString
      this.methods["asString"] = func378;
      func378.methodName = "asString";
      func378.paramCounts = [0];
      func378.paramNames = [];
      func378.definitionLine = 357;
      func378.definitionModule = "lexer";
      this.data.line = undefined;
      var reader392_line = function() {  // reader method line
          if (this.data.line === undefined) raiseUninitializedVariable("line");
          return this.data.line;
      };
      reader392_line.isDef = true;
      this.methods["line"] = reader392_line;
      this.data.linePos = undefined;
      var reader393_linePos = function() {  // reader method linePos
          if (this.data.linePos === undefined) raiseUninitializedVariable("linePos");
          return this.data.linePos;
      };
      reader393_linePos.isDef = true;
      this.methods["linePos"] = reader393_linePos;
      this.data.endLine = undefined;
      var reader394_endLine = function() {  // reader method endLine
          if (this.data.endLine === undefined) raiseUninitializedVariable("endLine");
          return this.data.endLine;
      };
      reader394_endLine.isDef = true;
      this.methods["endLine"] = reader394_endLine;
      this.data.endPos = undefined;
      var reader395_endPos = function() {  // reader method endPos
          if (this.data.endPos === undefined) raiseUninitializedVariable("endPos");
          return this.data.endPos;
      };
      reader395_endPos.isDef = true;
      this.methods["endPos"] = reader395_endPos;
      var func396 = function(argcv) {     // accessor method isSeparator
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isSeparator", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isSeparator
      this.methods["isSeparator"] = func396;
      func396.methodName = "isSeparator";
      func396.paramCounts = [0];
      func396.paramNames = [];
      func396.definitionLine = 362;
      func396.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 352;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj373_init = function() {    // init of object on line 352
        initFun374.call(this);
        setLineNumber(354);    // compilenode string
        var string397 = new GraceString("separator");
        this.data.kind = string397;
        setLineNumber(355);    // compilenode string
        var string398 = new GraceString("\n");
        this.data.value = string398;
        setLineNumber(356);    // compilenode num
        this.data.size = new GraceNum(1);
        this.data.line = var_lineNum;
        this.data.linePos = var_pos;
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        this.data.endLine = var_lineNumber;
        if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
        this.data.endPos = var_currentLineIndent;
      };
      return obj373_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj373_init = obj373_build.call(inheritingObject, null, var_lineNum, var_pos, this, aliases, exclusions);
    return obj373_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method separatorTokenAt(_,_)$build(_,_,_)
  this.methods["separatorTokenAt(2)$build(3)"] = func372;
  func372.methodName = "separatorTokenAt(2)$build(3)";
  func372.paramCounts = [2];
  func372.paramNames = ["lineNum", "pos"];
  func372.definitionLine = 352;
  func372.definitionModule = "lexer";
  var func399 = function(argcv) {    // method lGenericToken, line 364
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lGenericToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("lGenericToken", "lexer", 364);
    var ouc_init = this.methods["lGenericToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method lGenericToken
  this.methods["lGenericToken"] = func399;
  func399.methodName = "lGenericToken";
  func399.paramCounts = [0];
  func399.paramNames = [];
  func399.definitionLine = 364;
  func399.definitionModule = "lexer";
  var func400 = function(argcv, inheritingObject, aliases, exclusions) {    // method lGenericToken$build(_,_,_), line 364
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lGenericToken", 0, numArgs - 0);
    }
    var obj401_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_364");
      this.outer_lexer_364 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(365);    // reuse call
      var initFun402 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader403_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader403_kind.isDef = true;
      this.methods["kind"] = reader403_kind;
      this.data.value = undefined;
      var reader404_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader404_value.isDef = true;
      this.methods["value"] = reader404_value;
      this.data.size = undefined;
      var reader405_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader405_size.isDef = true;
      this.methods["size"] = reader405_size;
      var func406 = function(argcv) {     // accessor method isLGeneric
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isLGeneric", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isLGeneric
      this.methods["isLGeneric"] = func406;
      func406.methodName = "isLGeneric";
      func406.paramCounts = [0];
      func406.paramNames = [];
      func406.definitionLine = 369;
      func406.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 364;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj401_init = function() {    // init of object on line 364
        initFun402.call(this);
        setLineNumber(366);    // compilenode string
        var string407 = new GraceString("lgeneric");
        this.data.kind = string407;
        setLineNumber(367);    // compilenode string
        var string408 = new GraceString("\u27e6");
        this.data.value = string408;
        setLineNumber(368);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj401_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj401_init = obj401_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj401_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method lGenericToken$build(_,_,_)
  this.methods["lGenericToken$build(3)"] = func400;
  func400.methodName = "lGenericToken$build(3)";
  func400.paramCounts = [0];
  func400.paramNames = [];
  func400.definitionLine = 364;
  func400.definitionModule = "lexer";
  var func409 = function(argcv) {    // method rGenericToken, line 371
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rGenericToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("rGenericToken", "lexer", 371);
    var ouc_init = this.methods["rGenericToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method rGenericToken
  this.methods["rGenericToken"] = func409;
  func409.methodName = "rGenericToken";
  func409.paramCounts = [0];
  func409.paramNames = [];
  func409.definitionLine = 371;
  func409.definitionModule = "lexer";
  var func410 = function(argcv, inheritingObject, aliases, exclusions) {    // method rGenericToken$build(_,_,_), line 371
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("rGenericToken", 0, numArgs - 0);
    }
    var obj411_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_371");
      this.outer_lexer_371 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(372);    // reuse call
      var initFun412 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader413_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader413_kind.isDef = true;
      this.methods["kind"] = reader413_kind;
      this.data.value = undefined;
      var reader414_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader414_value.isDef = true;
      this.methods["value"] = reader414_value;
      this.data.size = undefined;
      var reader415_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader415_size.isDef = true;
      this.methods["size"] = reader415_size;
      var func416 = function(argcv) {     // accessor method isRGeneric
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isRGeneric", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isRGeneric
      this.methods["isRGeneric"] = func416;
      func416.methodName = "isRGeneric";
      func416.paramCounts = [0];
      func416.paramNames = [];
      func416.definitionLine = 376;
      func416.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 371;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj411_init = function() {    // init of object on line 371
        initFun412.call(this);
        setLineNumber(373);    // compilenode string
        var string417 = new GraceString("rgeneric");
        this.data.kind = string417;
        setLineNumber(374);    // compilenode string
        var string418 = new GraceString("\u27e7");
        this.data.value = string418;
        setLineNumber(375);    // compilenode num
        this.data.size = new GraceNum(1);
      };
      return obj411_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj411_init = obj411_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj411_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method rGenericToken$build(_,_,_)
  this.methods["rGenericToken$build(3)"] = func410;
  func410.methodName = "rGenericToken$build(3)";
  func410.paramCounts = [0];
  func410.paramNames = [];
  func410.definitionLine = 371;
  func410.definitionModule = "lexer";
  var func419 = function(argcv) {    // method eofToken, line 378
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("eofToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("eofToken", "lexer", 378);
    var ouc_init = this.methods["eofToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method eofToken
  this.methods["eofToken"] = func419;
  func419.methodName = "eofToken";
  func419.paramCounts = [0];
  func419.paramNames = [];
  func419.definitionLine = 378;
  func419.definitionModule = "lexer";
  var func420 = function(argcv, inheritingObject, aliases, exclusions) {    // method eofToken$build(_,_,_), line 378
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("eofToken", 0, numArgs - 0);
    }
    var obj421_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_378");
      this.outer_lexer_378 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(379);    // reuse call
      var initFun422 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader423_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader423_kind.isDef = true;
      this.methods["kind"] = reader423_kind;
      this.data.value = undefined;
      var reader424_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader424_value.isDef = true;
      this.methods["value"] = reader424_value;
      this.data.size = undefined;
      var reader425_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader425_size.isDef = true;
      this.methods["size"] = reader425_size;
      var func426 = function(argcv) {     // accessor method isEof
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isEof", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isEof
      this.methods["isEof"] = func426;
      func426.methodName = "isEof";
      func426.paramCounts = [0];
      func426.paramNames = [];
      func426.definitionLine = 383;
      func426.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 378;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj421_init = function() {    // init of object on line 378
        initFun422.call(this);
        setLineNumber(380);    // compilenode string
        var string427 = new GraceString("eof");
        this.data.kind = string427;
        setLineNumber(381);    // compilenode string
        var string428 = new GraceString("");
        this.data.value = string428;
        setLineNumber(382);    // compilenode num
        this.data.size = new GraceNum(0);
      };
      return obj421_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj421_init = obj421_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj421_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method eofToken$build(_,_,_)
  this.methods["eofToken$build(3)"] = func420;
  func420.methodName = "eofToken$build(3)";
  func420.paramCounts = [0];
  func420.paramNames = [];
  func420.definitionLine = 378;
  func420.definitionModule = "lexer";
  var func429 = function(argcv) {    // method sofToken, line 385
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("sofToken", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("sofToken", "lexer", 385);
    var ouc_init = this.methods["sofToken$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method sofToken
  this.methods["sofToken"] = func429;
  func429.methodName = "sofToken";
  func429.paramCounts = [0];
  func429.paramNames = [];
  func429.definitionLine = 385;
  func429.definitionModule = "lexer";
  var func430 = function(argcv, inheritingObject, aliases, exclusions) {    // method sofToken$build(_,_,_), line 385
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("sofToken", 0, numArgs - 0);
    }
    var obj431_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_385");
      this.outer_lexer_385 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(386);    // reuse call
      var initFun432 = selfRequest(importedModules["lexer"], "token$build(3)", [null], this, [], []);  // compileReuseCall
      setLineNumber(387);    // reuse call
      var initFun433 = selfRequest(var_prelude, "identityEquality$build(3)", [null], this, [], []);  // compileReuseCall
      this.data.kind = undefined;
      var reader434_kind = function() {  // reader method kind
          if (this.data.kind === undefined) raiseUninitializedVariable("kind");
          return this.data.kind;
      };
      reader434_kind.isDef = true;
      this.methods["kind"] = reader434_kind;
      this.data.value = undefined;
      var reader435_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader435_value.isDef = true;
      this.methods["value"] = reader435_value;
      this.data.size = undefined;
      var reader436_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader436_size.isDef = true;
      this.methods["size"] = reader436_size;
      var func437 = function(argcv) {     // accessor method isHeader
        const numArgs = arguments.length - 1;
        if (numArgs > 0) raiseTypeArgError("isHeader", 0, numArgs - 0);
        return GraceTrue;
      };    // end of method isHeader
      this.methods["isHeader"] = func437;
      func437.methodName = "isHeader";
      func437.paramCounts = [0];
      func437.paramNames = [];
      func437.definitionLine = 391;
      func437.definitionModule = "lexer";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 385;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj431_init = function() {    // init of object on line 385
        initFun432.call(this);
        setLineNumber(388);    // compilenode string
        var string438 = new GraceString("sof");
        this.data.kind = string438;
        setLineNumber(389);    // compilenode string
        var string439 = new GraceString("\u23da");
        this.data.value = string439;
        setLineNumber(390);    // compilenode num
        this.data.size = new GraceNum(0);
      };
      return obj431_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj431_init = obj431_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj431_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method sofToken$build(_,_,_)
  this.methods["sofToken$build(3)"] = func430;
  func430.methodName = "sofToken$build(3)";
  func430.paramCounts = [0];
  func430.paramNames = [];
  func430.definitionLine = 385;
  func430.definitionModule = "lexer";
  var func440 = function(argcv, var_s) {    // method advanceTo(_), line 394
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("advanceTo(_)", 0, numArgs - 1);
    }
    setLineNumber(394);    // compilenode bind
    var_state = var_s;
    return GraceDone;
  };    // end of method advanceTo(_)
  this.methods["advanceTo(1)"] = func440;
  func440.methodName = "advanceTo(1)";
  func440.paramCounts = [1];
  func440.paramNames = ["s"];
  func440.definitionLine = 394;
  func440.definitionModule = "lexer";
  var func441 = function(argcv, var_t) {    // method emit(_), line 395
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("emit(_)", 0, numArgs - 1);
    }
    setLineNumber(396);    // compilenode call
    // call case 6: other requests
    if (var_tokens === undefined) raiseUninitializedVariable("tokens");
    var call442 = request(var_tokens, "push(1)", [1], var_t);
    var if443 = GraceDone;
    setLineNumber(397);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    var call445 = request(var_t, "isComment", [0]);
    var call444 = request(call445, "not", [0]);
    if (Grace_isTrue(call444)) {
      var_lastNonCommentToken = var_t;
      if443 = GraceDone;
    }
    setLineNumber(398);    // compilenode string
    var string446 = new GraceString("");
    var_accum = string446;
    return GraceDone;
  };    // end of method emit(_)
  this.methods["emit(1)"] = func441;
  func441.methodName = "emit(1)";
  func441.paramCounts = [1];
  func441.paramNames = ["t"];
  func441.definitionLine = 395;
  func441.definitionModule = "lexer";
  var func447 = function(argcv) {    // method emitNewlineSeparator, line 400
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("emitNewlineSeparator", 0, numArgs - 0);
    }
    var if448 = GraceDone;
    setLineNumber(403);    // compilenode member
    // call case 6: other requests
    if (var_tokens === undefined) raiseUninitializedVariable("tokens");
    var call449 = request(var_tokens, "isEmpty", [0]);
    if (Grace_isTrue(call449)) {
      return var_done;
    }
    setLineNumber(404);    // compilenode member
    // call case 6: other requests
    if (var_tokens === undefined) raiseUninitializedVariable("tokens");
    var call450 = request(var_tokens, "last", [0]);
    var var_lastToken = call450;
    setLineNumber(405);    // compilenode op
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    var diff453 = request(var_lineNumber, "-(1)", [1], new GraceNum(1));
    // call case 6: other requests
    var call454 = request(var_lastToken, "endPos", [0]);
    var sum455 = request(call454, "+(1)", [1], new GraceNum(1));
    // call case 2: outer request
    var call452 = selfRequest(importedModules["lexer"], "separatorTokenAt(2)", [2], diff453, sum455);
    // call case 2: outer request
    var call451 = selfRequest(importedModules["lexer"], "emit(1)", [1], call452);
    return call451;
  };    // end of method emitNewlineSeparator
  this.methods["emitNewlineSeparator"] = func447;
  func447.methodName = "emitNewlineSeparator";
  func447.paramCounts = [0];
  func447.paramNames = [];
  func447.definitionLine = 400;
  func447.definitionModule = "lexer";
  var func456 = function(argcv, var_c) {    // method store(_), line 407
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("store(_)", 0, numArgs - 1);
    }
    setLineNumber(407);    // compilenode op
    if (var_accum === undefined) raiseUninitializedVariable("accum");
    var opresult457 = request(var_accum, "++(1)", [1], var_c);
    var_accum = opresult457;
    return GraceDone;
  };    // end of method store(_)
  this.methods["store(1)"] = func456;
  func456.methodName = "store(1)";
  func456.paramCounts = [1];
  func456.paramNames = ["c"];
  func456.definitionLine = 407;
  func456.definitionModule = "lexer";
  var func458 = function(argcv, var_currentCharacter) {    // method newline(_), line 932
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("newline(_)", 0, numArgs - 1);
    }
    setLineNumber(937);    // compilenode call
    // call case 2: outer request
    var call459 = selfRequest(importedModules["lexer"], "checkAndRecordIndentStatus(1)", [1], var_currentCharacter);
    var if460 = GraceDone;
    setLineNumber(938);    // compilenode call
    // call case 2: outer request
    var call461 = selfRequest(importedModules["lexer"], "isLineEmpty(1)", [1], var_currentCharacter);
    if (Grace_isTrue(call461)) {
      return var_done;
    }
    var if462 = GraceDone;
    setLineNumber(939);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call463 = selfRequest(this, "isIndentationChangeOne", [0]);
    if (Grace_isTrue(call463)) {
      setLineNumber(940);    // compilenode string
      var string465 = new GraceString("a change of indentation of 1 is not permitted");
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call464 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], string465, var_lineNumber, var_linePosition, var_linePosition);
      if462 = call464;
    }
    setLineNumber(943);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call466 = selfRequest(this, "terminateContinuationIfNecessary", [0]);
    var if467 = GraceDone;
    setLineNumber(944);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call468 = selfRequest(this, "isContinuationLine", [0]);
    if (Grace_isTrue(call468)) {
      setLineNumber(945);    // compilenode member
      // call case 4: self request with 0 args and 0 typeArgs 
      var call469 = selfRequest(this, "recordContinuationStatus", [0]);
      setLineNumber(946);    // compilenode member
      // call case 4: self request with 0 args and 0 typeArgs 
      var call470 = selfRequest(this, "saveDataForPriorLine", [0]);
      setLineNumber(947);    // compilenode return
      return var_done;
    }
    var if471 = GraceDone;
    setLineNumber(949);    // compilenode call
    // call case 2: outer request
    var call472 = selfRequest(importedModules["lexer"], "isBlockStart(1)", [1], var_currentCharacter);
    if (Grace_isTrue(call472)) {
      setLineNumber(950);    // compilenode member
      // call case 4: self request with 0 args and 0 typeArgs 
      var call473 = selfRequest(this, "recordNewIndentation", [0]);
      if471 = call473;
    } else {
      var if474 = GraceDone;
      setLineNumber(951);    // compilenode call
      // call case 2: outer request
      var call475 = selfRequest(importedModules["lexer"], "isBlockEnd(1)", [1], var_currentCharacter);
      if (Grace_isTrue(call475)) {
        setLineNumber(952);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call476 = selfRequest(this, "checkIndentationReset", [0]);
        if474 = call476;
      } else {
        setLineNumber(954);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call477 = selfRequest(this, "checkConstantIndent", [0]);
        if474 = call477;
      }
      if471 = if474;
    }
    setLineNumber(956);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call478 = selfRequest(this, "saveDataForPriorLine", [0]);
    var if479 = GraceDone;
    setLineNumber(957);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call480 = selfRequest(this, "priorLineEndsWithOpenBracket", [0]);
    if (Grace_isTrue(call480)) {
      return var_done;
    }
    var if481 = GraceDone;
    setLineNumber(958);    // compilenode call
    // call case 2: outer request
    var call482 = selfRequest(importedModules["lexer"], "currentLineStartsWithCloseBracket(1)", [1], var_currentCharacter);
    if (Grace_isTrue(call482)) {
      return var_done;
    }
    setLineNumber(959);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call483 = selfRequest(this, "emitNewlineSeparator", [0]);
    return call483;
  };    // end of method newline(_)
  this.methods["newline(1)"] = func458;
  func458.methodName = "newline(1)";
  func458.paramCounts = [1];
  func458.paramNames = ["currentCharacter"];
  func458.definitionLine = 932;
  func458.definitionModule = "lexer";
  var func484 = function(argcv) {    // method reportEmptyStringInterpolation, line 962
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("reportEmptyStringInterpolation", 0, numArgs - 0);
    }
    setLineNumber(963);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    if (var_tokens === undefined) raiseUninitializedVariable("tokens");
    var call486 = request(var_tokens, "last", [0]);
    var call485 = request(call486, "linePos", [0]);
    var var_lastPos = call485;
    setLineNumber(964);    // compilenode member
    // call case 6: other requests
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    // call case 6: other requests
    if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
    var call488 = request(var_inputLines, "at(1)", [1], var_lineNumber);
    var call487 = request(call488, "size", [0]);
    var var_lineLength = call487;
    setLineNumber(965);    // compilenode array
    var array490 = new GraceSequence([]);
    // call case 2: outer request
    var call489 = selfRequest(var_prelude, "list(1)", [1], array490);
    var var_suggestions = call489;
    var if491 = GraceDone;
    setLineNumber(966);    // compilenode op
    var opresult492 = request(var_lastPos, "<(1)", [1], var_lineLength);
    if (Grace_isTrue(opresult492)) {
      setLineNumber(967);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call494 = request(var_errormessages, "suggestion", [0]);
      var call493 = request(call494, "new", [0]);
      var var_suggestion1 = call493;
      setLineNumber(969);    // compilenode call
      if (var_lastPos === undefined) raiseUninitializedVariable("lastPos");
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      // call case 6: other requests
      if (var_suggestion1 === undefined) raiseUninitializedVariable("suggestion1");
      var call495 = request(var_suggestion1, "deleteRange(2)onLine(1)", [2, 1], var_lastPos, var_linePosition, var_lineNumber);
      setLineNumber(971);    // compilenode call
      if (var_suggestion1 === undefined) raiseUninitializedVariable("suggestion1");
      // call case 6: other requests
      if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
      var call496 = request(var_suggestions, "add(1)", [1], var_suggestion1);
      setLineNumber(972);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call498 = request(var_errormessages, "suggestion", [0]);
      var call497 = request(call498, "new", [0]);
      var var_suggestion2 = call497;
      setLineNumber(973);    // compilenode string
      var string500 = new GraceString("\u00abexpression\u00bb");
      setLineNumber(974);    // compilenode op
      if (var_lastPos === undefined) raiseUninitializedVariable("lastPos");
      var sum501 = request(var_lastPos, "+(1)", [1], new GraceNum(1));
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      // call case 6: other requests
      if (var_suggestion2 === undefined) raiseUninitializedVariable("suggestion2");
      var call499 = request(var_suggestion2, "insert(1)atPosition(1)onLine(1)", [1, 1, 1], string500, sum501, var_lineNumber);
      setLineNumber(976);    // compilenode call
      if (var_suggestion2 === undefined) raiseUninitializedVariable("suggestion2");
      // call case 6: other requests
      if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
      var call502 = request(var_suggestions, "add(1)", [1], var_suggestion2);
      if491 = call502;
    }
    setLineNumber(978);    // compilenode string
    var string504 = new GraceString("a string interpolation cannot be empty");
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
    // call case 6: other requests
    if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
    var call503 = request(var_errormessages, "syntaxError(1)atRange(3)withSuggestions(1)", [1, 3, 1], string504, var_lineNumber, var_lastPos, var_linePosition, var_suggestions);
    return call503;
  };    // end of method reportEmptyStringInterpolation
  this.methods["reportEmptyStringInterpolation"] = func484;
  func484.methodName = "reportEmptyStringInterpolation";
  func484.paramCounts = [0];
  func484.paramNames = [];
  func484.definitionLine = 962;
  func484.definitionModule = "lexer";
  var func505 = function(argcv, var_currentCharacter) {    // method isLineEmpty(_), line 983
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isLineEmpty(_)", 0, numArgs - 1);
    }
    var if506 = GraceDone;
    setLineNumber(990);    // compilenode string
    var string507 = new GraceString("\n");
    var opresult508 = request(string507, "==(1)", [1], var_currentCharacter);
    if (Grace_isTrue(opresult508)) {
      return GraceTrue;
    }
    var if509 = GraceDone;
    setLineNumber(991);    // compilenode string
    var string510 = new GraceString("/");
    var opresult511 = request(string510, "\u2260(1)", [1], var_currentCharacter);
    if (Grace_isTrue(opresult511)) {
      return GraceFalse;
    }
    setLineNumber(992);    // compilenode string
    var string512 = new GraceString("/");
    // call case 4: self request with 0 args and 0 typeArgs 
    var call513 = selfRequest(this, "followingCharacter", [0]);
    var opresult514 = request(string512, "==(1)", [1], call513);
    return opresult514;
  };    // end of method isLineEmpty(_)
  this.methods["isLineEmpty(1)"] = func505;
  func505.methodName = "isLineEmpty(1)";
  func505.paramCounts = [1];
  func505.paramNames = ["currentCharacter"];
  func505.definitionLine = 983;
  func505.definitionModule = "lexer";
  var func515 = function(argcv) {    // method followingCharacter, line 994
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("followingCharacter", 0, numArgs - 0);
    }
    setLineNumber(995);    // compilenode call
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    // call case 6: other requests
    if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
    var call516 = request(var_inputLines, "at(1)", [1], var_lineNumber);
    var var_currentLine = call516;
    var if517 = GraceDone;
    setLineNumber(996);    // compilenode member
    // call case 6: other requests
    var call518 = request(var_currentLine, "size", [0]);
    if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
    var opresult519 = request(call518, "==(1)", [1], var_linePosition);
    if (Grace_isTrue(opresult519)) {
      setLineNumber(997);    // compilenode string
      var string520 = new GraceString("\n");
      if517 = string520;
    } else {
      setLineNumber(999);    // compilenode op
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      var sum522 = request(var_linePosition, "+(1)", [1], new GraceNum(1));
      // call case 6: other requests
      if (var_currentLine === undefined) raiseUninitializedVariable("currentLine");
      var call521 = request(var_currentLine, "at(1)", [1], sum522);
      if517 = call521;
    }
    return if517;
  };    // end of method followingCharacter
  this.methods["followingCharacter"] = func515;
  func515.methodName = "followingCharacter";
  func515.paramCounts = [0];
  func515.paramNames = [];
  func515.definitionLine = 994;
  func515.definitionModule = "lexer";
  var func523 = function(argcv) {    // method isIndentationChangeOne, line 1002
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("isIndentationChangeOne", 0, numArgs - 0);
    }
    setLineNumber(1003);    // compilenode op
    if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
    if (var_priorLineIndent === undefined) raiseUninitializedVariable("priorLineIndent");
    var diff524 = request(var_currentLineIndent, "-(1)", [1], var_priorLineIndent);
    var var_indentationChange = diff524;
    setLineNumber(1004);    // compilenode member
    // call case 6: other requests
    var call525 = request(var_indentationChange, "abs", [0]);
    var opresult526 = request(call525, "==(1)", [1], new GraceNum(1));
    return opresult526;
  };    // end of method isIndentationChangeOne
  this.methods["isIndentationChangeOne"] = func523;
  func523.methodName = "isIndentationChangeOne";
  func523.paramCounts = [0];
  func523.paramNames = [];
  func523.definitionLine = 1002;
  func523.definitionModule = "lexer";
  var func527 = function(argcv, var_currentCharacter) {    // method currentLineStartsWithCloseBracket(_), line 1006
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("currentLineStartsWithCloseBracket(_)", 0, numArgs - 1);
    }
    setLineNumber(1007);    // compilenode call
    // call case 6: other requests
    var string529 = new GraceString(")]}\u27e7");
    var call528 = request(string529, "contains(1)", [1], var_currentCharacter);
    return call528;
  };    // end of method currentLineStartsWithCloseBracket(_)
  this.methods["currentLineStartsWithCloseBracket(1)"] = func527;
  func527.methodName = "currentLineStartsWithCloseBracket(1)";
  func527.paramCounts = [1];
  func527.paramNames = ["currentCharacter"];
  func527.definitionLine = 1006;
  func527.definitionModule = "lexer";
  var func530 = function(argcv) {    // method terminateContinuationIfNecessary, line 1009
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("terminateContinuationIfNecessary", 0, numArgs - 0);
    }
    var if531 = GraceDone;
    setLineNumber(1010);    // compilenode op
    if (var_indentOfLineBeingContinued === undefined) raiseUninitializedVariable("indentOfLineBeingContinued");
    if (var_noSuchLine === undefined) raiseUninitializedVariable("noSuchLine");
    var opresult532 = request(var_indentOfLineBeingContinued, "==(1)", [1], var_noSuchLine);
    if (Grace_isTrue(opresult532)) {
      return var_done;
    }
    var if533 = GraceDone;
    setLineNumber(1011);    // compilenode op
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var opresult534 = request(var_braceChange, "==(1)", [1], new GraceNum(0));
    var block535 = new GraceBlock(this, 1011, 0);
    block535.guard = jsTrue;
    block535.real = function block535() {
      setLineNumber(1011);    // compilenode op
      if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
      if (var_maxIndentOfContinuation === undefined) raiseUninitializedVariable("maxIndentOfContinuation");
      var opresult536 = request(var_currentLineIndent, "\u2265(1)", [1], var_maxIndentOfContinuation);
      return opresult536;
    };
    let applyMeth535 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth535.methodName = "apply";
    applyMeth535.paramCounts = [0];
    applyMeth535.paramNames = [];
    applyMeth535.definitionLine = 1011;
    applyMeth535.definitionModule = "lexer";
    block535.methods["apply"] = applyMeth535;
    var opresult537 = request(opresult534, "&&(1)", [1], block535);
    if (Grace_isTrue(opresult537)) {
      setLineNumber(1012);    // compilenode bind
      if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
      var_maxIndentOfContinuation = var_currentLineIndent;
      setLineNumber(1013);    // compilenode return
      return var_done;
    }
    setLineNumber(1017);    // compilenode bind
    if (var_indentOfLineBeingContinued === undefined) raiseUninitializedVariable("indentOfLineBeingContinued");
    var_priorLineIndent = var_indentOfLineBeingContinued;
    var if538 = GraceDone;
    setLineNumber(1018);    // compilenode op
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var opresult539 = request(var_braceChange, "==(1)", [1], new GraceNum(0));
    var block540 = new GraceBlock(this, 1018, 0);
    block540.guard = jsTrue;
    block540.real = function block540() {
      setLineNumber(1018);    // compilenode op
      if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
      if (var_priorLineIndent === undefined) raiseUninitializedVariable("priorLineIndent");
      var opresult541 = request(var_currentLineIndent, "\u2260(1)", [1], var_priorLineIndent);
      return opresult541;
    };
    let applyMeth540 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth540.methodName = "apply";
    applyMeth540.paramCounts = [0];
    applyMeth540.paramNames = [];
    applyMeth540.definitionLine = 1018;
    applyMeth540.definitionModule = "lexer";
    block540.methods["apply"] = applyMeth540;
    var opresult542 = request(opresult539, "&&(1)", [1], block540);
    if (Grace_isTrue(opresult542)) {
      setLineNumber(1019);    // compilenode string
      var string544 = new GraceString("if this line continues the previous line, its indentation ");
      setLineNumber(1020);    // compilenode string
      var string545 = new GraceString("must be at least ");
      if (var_maxIndentOfContinuation === undefined) raiseUninitializedVariable("maxIndentOfContinuation");
      var opresult546 = request(string545, "++(1)", [1], var_maxIndentOfContinuation);
      var string547 = new GraceString("; if it starts a new ");
      var opresult548 = request(opresult546, "++(1)", [1], string547);
      var opresult549 = request(string544, "++(1)", [1], opresult548);
      setLineNumber(1021);    // compilenode string
      var string550 = new GraceString("statement, its indentation must be ");
      if (var_indentOfLineBeingContinued === undefined) raiseUninitializedVariable("indentOfLineBeingContinued");
      var opresult551 = request(string550, "++(1)", [1], var_indentOfLineBeingContinued);
      var string552 = new GraceString("");
      var opresult553 = request(opresult551, "++(1)", [1], string552);
      var opresult554 = request(opresult549, "++(1)", [1], opresult553);
      // call case 2: outer request
      var call543 = selfRequest(importedModules["lexer"], "lexicalError(1)", [1], opresult554);
      if538 = call543;
    }
    setLineNumber(1023);    // compilenode bind
    if (var_noSuchLine === undefined) raiseUninitializedVariable("noSuchLine");
    var_indentOfLineBeingContinued = var_noSuchLine;
    return GraceDone;
  };    // end of method terminateContinuationIfNecessary
  this.methods["terminateContinuationIfNecessary"] = func530;
  func530.methodName = "terminateContinuationIfNecessary";
  func530.paramCounts = [0];
  func530.paramNames = [];
  func530.definitionLine = 1009;
  func530.definitionModule = "lexer";
  var func555 = function(argcv, var_currentCharacter) {    // method checkAndRecordIndentStatus(_), line 1026
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("checkAndRecordIndentStatus(_)", 0, numArgs - 1);
    }
    var if556 = GraceDone;
    setLineNumber(1027);    // compilenode string
    var string557 = new GraceString("\u0009");
    var opresult558 = request(string557, "==(1)", [1], var_currentCharacter);
    if (Grace_isTrue(opresult558)) {
      setLineNumber(1028);    // compilenode string
      var string560 = new GraceString("please indent with spaces, not tabs");
      // call case 2: outer request
      var call559 = selfRequest(importedModules["lexer"], "lexicalError(1)", [1], string560);
      if556 = call559;
    }
    setLineNumber(1030);    // compilenode op
    if (var_currentLineBraceDepth === undefined) raiseUninitializedVariable("currentLineBraceDepth");
    if (var_priorLineBraceDepth === undefined) raiseUninitializedVariable("priorLineBraceDepth");
    var diff561 = request(var_currentLineBraceDepth, "-(1)", [1], var_priorLineBraceDepth);
    var_braceChange = diff561;
    var if562 = GraceDone;
    setLineNumber(1031);    // compilenode string
    var string563 = new GraceString("}");
    var opresult564 = request(string563, "==(1)", [1], var_currentCharacter);
    if (Grace_isTrue(opresult564)) {
      setLineNumber(1034);    // compilenode op
      if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
      var diff565 = request(var_braceChange, "-(1)", [1], new GraceNum(1));
      var_braceChange = diff565;
      if562 = GraceDone;
    }
    return if562;
  };    // end of method checkAndRecordIndentStatus(_)
  this.methods["checkAndRecordIndentStatus(1)"] = func555;
  func555.methodName = "checkAndRecordIndentStatus(1)";
  func555.paramCounts = [1];
  func555.paramNames = ["currentCharacter"];
  func555.definitionLine = 1026;
  func555.definitionModule = "lexer";
  var func566 = function(argcv, var_currentCharacter) {    // method isBlockStart(_), line 1037
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isBlockStart(_)", 0, numArgs - 1);
    }
    setLineNumber(1038);    // compilenode op
    if (var_unmatchedLeftBraces === undefined) raiseUninitializedVariable("unmatchedLeftBraces");
    var opresult567 = request(var_unmatchedLeftBraces, ">(1)", [1], new GraceNum(0));
    var block568 = new GraceBlock(this, 1038, 0);
    block568.guard = jsTrue;
    block568.real = function block568() {
      setLineNumber(1038);    // compilenode string
      var string569 = new GraceString("}");
      var opresult570 = request(var_currentCharacter, "\u2260(1)", [1], string569);
      return opresult570;
    };
    let applyMeth568 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth568.methodName = "apply";
    applyMeth568.paramCounts = [0];
    applyMeth568.paramNames = [];
    applyMeth568.definitionLine = 1038;
    applyMeth568.definitionModule = "lexer";
    block568.methods["apply"] = applyMeth568;
    var opresult571 = request(opresult567, "&&(1)", [1], block568);
    return opresult571;
  };    // end of method isBlockStart(_)
  this.methods["isBlockStart(1)"] = func566;
  func566.methodName = "isBlockStart(1)";
  func566.paramCounts = [1];
  func566.paramNames = ["currentCharacter"];
  func566.definitionLine = 1037;
  func566.definitionModule = "lexer";
  var func572 = function(argcv, var_currentCharacter) {    // method isBlockEnd(_), line 1040
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isBlockEnd(_)", 0, numArgs - 1);
    }
    var if573 = GraceDone;
    setLineNumber(1044);    // compilenode op
    if (var_unmatchedLeftBraces === undefined) raiseUninitializedVariable("unmatchedLeftBraces");
    var opresult574 = request(var_unmatchedLeftBraces, ">(1)", [1], new GraceNum(0));
    var block575 = new GraceBlock(this, 1044, 0);
    block575.guard = jsTrue;
    block575.real = function block575() {
      setLineNumber(1044);    // compilenode string
      var string576 = new GraceString("}");
      var opresult577 = request(var_currentCharacter, "\u2260(1)", [1], string576);
      return opresult577;
    };
    let applyMeth575 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth575.methodName = "apply";
    applyMeth575.paramCounts = [0];
    applyMeth575.paramNames = [];
    applyMeth575.definitionLine = 1044;
    applyMeth575.definitionModule = "lexer";
    block575.methods["apply"] = applyMeth575;
    var opresult578 = request(opresult574, "&&(1)", [1], block575);
    if (Grace_isTrue(opresult578)) {
      setLineNumber(1045);    // compilenode return
      return GraceFalse;
    }
    setLineNumber(1047);    // compilenode op
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var opresult579 = request(var_braceChange, "<(1)", [1], new GraceNum(0));
    return opresult579;
  };    // end of method isBlockEnd(_)
  this.methods["isBlockEnd(1)"] = func572;
  func572.methodName = "isBlockEnd(1)";
  func572.paramCounts = [1];
  func572.paramNames = ["currentCharacter"];
  func572.definitionLine = 1040;
  func572.definitionModule = "lexer";
  var func580 = function(argcv) {    // method recordNewIndentation, line 1050
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("recordNewIndentation", 0, numArgs - 0);
    }
    var if581 = GraceDone;
    setLineNumber(1057);    // compilenode op
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var opresult582 = request(var_braceChange, ">(1)", [1], new GraceNum(0));
    var block583 = new GraceBlock(this, 1057, 0);
    block583.guard = jsTrue;
    block583.real = function block583() {
      setLineNumber(1057);    // compilenode op
      if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
      if (var_priorLineIndent === undefined) raiseUninitializedVariable("priorLineIndent");
      var opresult584 = request(var_currentLineIndent, "\u2264(1)", [1], var_priorLineIndent);
      return opresult584;
    };
    let applyMeth583 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth583.methodName = "apply";
    applyMeth583.paramCounts = [0];
    applyMeth583.paramNames = [];
    applyMeth583.definitionLine = 1057;
    applyMeth583.definitionModule = "lexer";
    block583.methods["apply"] = applyMeth583;
    var opresult585 = request(opresult582, "&&(1)", [1], block583);
    if (Grace_isTrue(opresult585)) {
      setLineNumber(1058);    // compilenode string
      var string587 = new GraceString("Please indent the body of a block");
      // call case 2: outer request
      var call586 = selfRequest(importedModules["lexer"], "lexicalError(1)", [1], string587);
      if581 = call586;
    }
    var if588 = GraceDone;
    setLineNumber(1060);    // compilenode op
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var opresult589 = request(var_braceChange, ">(1)", [1], new GraceNum(1));
    if (Grace_isTrue(opresult589)) {
      setLineNumber(1061);    // compilenode string
      var string591 = new GraceString("the prior line opened two blocks. There is no way for you ");
      setLineNumber(1062);    // compilenode string
      var string592 = new GraceString("to close them correctly! Please split the prior line into two.");
      var opresult593 = request(string591, "++(1)", [1], string592);
      // call case 2: outer request
      var call590 = selfRequest(importedModules["lexer"], "lexicalError(1)", [1], opresult593);
      if588 = call590;
    }
    setLineNumber(1064);    // compilenode call
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var block595 = new GraceBlock(this, 1064, 0);
    block595.guard = jsTrue;
    block595.real = function block595() {
      setLineNumber(1065);    // compilenode call
      if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
      // call case 6: other requests
      if (var_indentStack === undefined) raiseUninitializedVariable("indentStack");
      var call596 = request(var_indentStack, "addLast(1)", [1], var_currentLineIndent);
      return call596;
    };
    let applyMeth595 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth595.methodName = "apply";
    applyMeth595.paramCounts = [0];
    applyMeth595.paramNames = [];
    applyMeth595.definitionLine = 1064;
    applyMeth595.definitionModule = "lexer";
    block595.methods["apply"] = applyMeth595;
    // call case 2: outer request
    var call594 = selfRequest(var_prelude, "repeat(1)times(1)", [1, 1], var_braceChange, block595);
    return call594;
  };    // end of method recordNewIndentation
  this.methods["recordNewIndentation"] = func580;
  func580.methodName = "recordNewIndentation";
  func580.paramCounts = [0];
  func580.paramNames = [];
  func580.definitionLine = 1050;
  func580.definitionModule = "lexer";
  var func597 = function(argcv) {    // method saveDataForPriorLine, line 1068
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("saveDataForPriorLine", 0, numArgs - 0);
    }
    setLineNumber(1069);    // compilenode op
    if (var_priorLineBraceDepth === undefined) raiseUninitializedVariable("priorLineBraceDepth");
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var sum598 = request(var_priorLineBraceDepth, "+(1)", [1], var_braceChange);
    var_priorLineBraceDepth = sum598;
    setLineNumber(1070);    // compilenode bind
    if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
    var_priorLineIndent = var_currentLineIndent;
    setLineNumber(1071);    // compilenode num
    var_unmatchedLeftBraces = new GraceNum(0);
    return GraceDone;
  };    // end of method saveDataForPriorLine
  this.methods["saveDataForPriorLine"] = func597;
  func597.methodName = "saveDataForPriorLine";
  func597.paramCounts = [0];
  func597.paramNames = [];
  func597.definitionLine = 1068;
  func597.definitionModule = "lexer";
  var func599 = function(argcv) {    // method priorLineEndsWithOpenBracket, line 1073
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("priorLineEndsWithOpenBracket", 0, numArgs - 0);
    }
    setLineNumber(1074);    // compilenode defdec
    if (var_lastNonCommentToken === undefined) raiseUninitializedVariable("lastNonCommentToken");
    var var_tok = var_lastNonCommentToken;
    setLineNumber(1075);    // compilenode member
    // call case 6: other requests
    var call600 = request(var_tok, "isLBrace", [0]);
    // call case 6: other requests
    var call601 = request(var_tok, "isLParen", [0]);
    var opresult602 = request(call600, "||(1)", [1], call601);
    // call case 6: other requests
    var call603 = request(var_tok, "isLSquare", [0]);
    var opresult604 = request(opresult602, "||(1)", [1], call603);
    // call case 6: other requests
    var call605 = request(var_tok, "isLGeneric", [0]);
    var opresult606 = request(opresult604, "||(1)", [1], call605);
    return opresult606;
  };    // end of method priorLineEndsWithOpenBracket
  this.methods["priorLineEndsWithOpenBracket"] = func599;
  func599.methodName = "priorLineEndsWithOpenBracket";
  func599.paramCounts = [0];
  func599.paramNames = [];
  func599.definitionLine = 1073;
  func599.definitionModule = "lexer";
  var func607 = function(argcv) {    // method checkIndentationReset, line 1077
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("checkIndentationReset", 0, numArgs - 0);
    }
    var if608 = GraceDone;
    setLineNumber(1083);    // compilenode member
    // call case 6: other requests
    if (var_indentStack === undefined) raiseUninitializedVariable("indentStack");
    var call609 = request(var_indentStack, "size", [0]);
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var diff610 = request(new GraceNum(1), "-(1)", [1], var_braceChange);
    var opresult611 = request(call609, "<(1)", [1], diff610);
    if (Grace_isTrue(opresult611)) {
      setLineNumber(1089);    // compilenode string
      var string613 = new GraceString("there is no opening brace corresponding to this closing brace");
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call612 = request(var_errormessages, "syntaxError(1)atPosition(2)", [1, 2], string613, var_lineNumber, var_linePosition);
      if608 = call612;
    }
    setLineNumber(1092);    // compilenode call
    // call case 6: other requests
    if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
    var call615 = request(var_braceChange, "prefix-", [0]);
    var block616 = new GraceBlock(this, 1092, 0);
    block616.guard = jsTrue;
    block616.real = function block616() {
      setLineNumber(1092);    // compilenode member
      // call case 6: other requests
      if (var_indentStack === undefined) raiseUninitializedVariable("indentStack");
      var call617 = request(var_indentStack, "removeLast", [0]);
      return call617;
    };
    let applyMeth616 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth616.methodName = "apply";
    applyMeth616.paramCounts = [0];
    applyMeth616.paramNames = [];
    applyMeth616.definitionLine = 1092;
    applyMeth616.definitionModule = "lexer";
    block616.methods["apply"] = applyMeth616;
    // call case 2: outer request
    var call614 = selfRequest(var_prelude, "repeat(1)times(1)", [1, 1], call615, block616);
    var if618 = GraceDone;
    setLineNumber(1093);    // compilenode op
    if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
    // call case 6: other requests
    if (var_indentStack === undefined) raiseUninitializedVariable("indentStack");
    var call619 = request(var_indentStack, "last", [0]);
    var opresult620 = request(var_currentLineIndent, "\u2260(1)", [1], call619);
    if (Grace_isTrue(opresult620)) {
      setLineNumber(1094);    // compilenode string
      var string622 = new GraceString("on closing a block, the indentation must return to that ");
      setLineNumber(1095);    // compilenode string
      var string623 = new GraceString("of the line on which the block was opened (");
      // call case 6: other requests
      if (var_indentStack === undefined) raiseUninitializedVariable("indentStack");
      var call624 = request(var_indentStack, "last", [0]);
      var opresult625 = request(string623, "++(1)", [1], call624);
      var string626 = new GraceString(")");
      var opresult627 = request(opresult625, "++(1)", [1], string626);
      var opresult628 = request(string622, "++(1)", [1], opresult627);
      // call case 2: outer request
      var call621 = selfRequest(importedModules["lexer"], "lexicalError(1)", [1], opresult628);
      if618 = call621;
    }
    return if618;
  };    // end of method checkIndentationReset
  this.methods["checkIndentationReset"] = func607;
  func607.methodName = "checkIndentationReset";
  func607.paramCounts = [0];
  func607.paramNames = [];
  func607.definitionLine = 1077;
  func607.definitionModule = "lexer";
  var func629 = function(argcv) {    // method isContinuationLine, line 1098
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("isContinuationLine", 0, numArgs - 0);
    }
    var if630 = GraceDone;
    setLineNumber(1099);    // compilenode op
    if (var_noSuchLine === undefined) raiseUninitializedVariable("noSuchLine");
    if (var_indentOfLineBeingContinued === undefined) raiseUninitializedVariable("indentOfLineBeingContinued");
    var opresult631 = request(var_noSuchLine, "\u2260(1)", [1], var_indentOfLineBeingContinued);
    if (Grace_isTrue(opresult631)) {
      setLineNumber(1101);    // compilenode return
      return GraceTrue;
    }
    var if632 = GraceDone;
    setLineNumber(1103);    // compilenode op
    if (var_unmatchedLeftBraces === undefined) raiseUninitializedVariable("unmatchedLeftBraces");
    var opresult633 = request(var_unmatchedLeftBraces, ">(1)", [1], new GraceNum(0));
    if (Grace_isTrue(opresult633)) {
      return GraceFalse;
    }
    setLineNumber(1104);    // compilenode op
    if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
    if (var_priorLineIndent === undefined) raiseUninitializedVariable("priorLineIndent");
    var opresult634 = request(var_currentLineIndent, ">(1)", [1], var_priorLineIndent);
    return opresult634;
  };    // end of method isContinuationLine
  this.methods["isContinuationLine"] = func629;
  func629.methodName = "isContinuationLine";
  func629.paramCounts = [0];
  func629.paramNames = [];
  func629.definitionLine = 1098;
  func629.definitionModule = "lexer";
  var func635 = function(argcv) {    // method recordContinuationStatus, line 1106
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("recordContinuationStatus", 0, numArgs - 0);
    }
    var if636 = GraceDone;
    setLineNumber(1107);    // compilenode op
    if (var_noSuchLine === undefined) raiseUninitializedVariable("noSuchLine");
    if (var_indentOfLineBeingContinued === undefined) raiseUninitializedVariable("indentOfLineBeingContinued");
    var opresult637 = request(var_noSuchLine, "==(1)", [1], var_indentOfLineBeingContinued);
    if (Grace_isTrue(opresult637)) {
      setLineNumber(1108);    // compilenode member
      // call case 6: other requests
      if (var_indentStack === undefined) raiseUninitializedVariable("indentStack");
      var call638 = request(var_indentStack, "last", [0]);
      var_indentOfLineBeingContinued = call638;
      setLineNumber(1109);    // compilenode bind
      if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
      var_maxIndentOfContinuation = var_currentLineIndent;
      if636 = GraceDone;
    }
    return if636;
  };    // end of method recordContinuationStatus
  this.methods["recordContinuationStatus"] = func635;
  func635.methodName = "recordContinuationStatus";
  func635.paramCounts = [0];
  func635.paramNames = [];
  func635.definitionLine = 1106;
  func635.definitionModule = "lexer";
  var func639 = function(argcv) {    // method checkConstantIndent, line 1112
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("checkConstantIndent", 0, numArgs - 0);
    }
    var if640 = GraceDone;
    setLineNumber(1114);    // compilenode op
    if (var_priorLineIndent === undefined) raiseUninitializedVariable("priorLineIndent");
    if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
    var opresult641 = request(var_priorLineIndent, "==(1)", [1], var_currentLineIndent);
    if (Grace_isTrue(opresult641)) {
      setLineNumber(1115);    // compilenode return
      return var_done;
    }
    var if642 = GraceDone;
    setLineNumber(1117);    // compilenode op
    if (var_priorLineIndent === undefined) raiseUninitializedVariable("priorLineIndent");
    if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
    var opresult643 = request(var_priorLineIndent, ">(1)", [1], var_currentLineIndent);
    if (Grace_isTrue(opresult643)) {
      setLineNumber(1118);    // compilenode string
      var string645 = new GraceString("do not reduce the indentation except when ending a block");
      // call case 2: outer request
      var call644 = selfRequest(importedModules["lexer"], "lexicalError(1)", [1], string645);
      if642 = call644;
    }
    setLineNumber(1120);    // compilenode string
    var string647 = new GraceString("this is not the body of a block, or a continuation line, so it should have indentation ");
    if (var_priorLineIndent === undefined) raiseUninitializedVariable("priorLineIndent");
    var opresult648 = request(string647, "++(1)", [1], var_priorLineIndent);
    var string649 = new GraceString(", like the previous line");
    var opresult650 = request(opresult648, "++(1)", [1], string649);
    // call case 2: outer request
    var call646 = selfRequest(importedModules["lexer"], "lexicalError(1)", [1], opresult650);
    return call646;
  };    // end of method checkConstantIndent
  this.methods["checkConstantIndent"] = func639;
  func639.methodName = "checkConstantIndent";
  func639.paramCounts = [0];
  func639.paramNames = [];
  func639.definitionLine = 1112;
  func639.definitionModule = "lexer";
  var func651 = function(argcv, var_message) {    // method lexicalError(_), line 1122
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("lexicalError(_)", 0, numArgs - 1);
    }
    setLineNumber(1123);    // compilenode call
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    setLineNumber(1125);    // compilenode op
    if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
    var diff653 = request(var_linePosition, "-(1)", [1], new GraceNum(1));
    // call case 6: other requests
    if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
    var call652 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], var_message, var_lineNumber, new GraceNum(1), diff653);
    return call652;
  };    // end of method lexicalError(_)
  this.methods["lexicalError(1)"] = func651;
  func651.methodName = "lexicalError(1)";
  func651.paramCounts = [1];
  func651.paramNames = ["message"];
  func651.definitionLine = 1122;
  func651.definitionModule = "lexer";
  var func654 = function(argcv) {    // method escapedNewlineError, line 1127
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("escapedNewlineError", 0, numArgs - 0);
    }
    setLineNumber(1128);    // compilenode string
    var string656 = new GraceString("to include a newline in a string use '\\n'");
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    setLineNumber(1129);    // compilenode op
    if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
    // call case 6: other requests
    if (var_accum === undefined) raiseUninitializedVariable("accum");
    var call657 = request(var_accum, "size", [0]);
    var diff658 = request(var_linePosition, "-(1)", [1], call657);
    var diff659 = request(diff658, "-(1)", [1], new GraceNum(1));
    // call case 6: other requests
    if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
    var call655 = request(var_errormessages, "syntaxError(1)atPosition(2)", [1, 2], string656, var_lineNumber, diff659);
    return call655;
  };    // end of method escapedNewlineError
  this.methods["escapedNewlineError"] = func654;
  func654.methodName = "escapedNewlineError";
  func654.paramCounts = [0];
  func654.paramNames = [];
  func654.definitionLine = 1127;
  func654.definitionModule = "lexer";
  var func660 = function(argcv) {    // method newlineInInterpolationError, line 1132
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("newlineInInterpolationError", 0, numArgs - 0);
    }
    setLineNumber(1133);    // compilenode string
    var string662 = new GraceString("a string interpolation may not ");
    setLineNumber(1134);    // compilenode string
    var string663 = new GraceString("contain a newline");
    var opresult664 = request(string662, "++(1)", [1], string663);
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
    // call case 6: other requests
    if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
    var call661 = request(var_errormessages, "syntaxError(1)atPosition(2)", [1, 2], opresult664, var_lineNumber, var_linePosition);
    return call661;
  };    // end of method newlineInInterpolationError
  this.methods["newlineInInterpolationError"] = func660;
  func660.methodName = "newlineInInterpolationError";
  func660.paramCounts = [0];
  func660.paramNames = [];
  func660.definitionLine = 1132;
  func660.definitionModule = "lexer";
  var func665 = function(argcv) {    // method unclosedStringError, line 1138
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("unclosedStringError", 0, numArgs - 0);
    }
    var if666 = GraceDone;
    setLineNumber(1141);    // compilenode op
    if (var_interpDepth === undefined) raiseUninitializedVariable("interpDepth");
    var opresult667 = request(var_interpDepth, ">(1)", [1], new GraceNum(0));
    if (Grace_isTrue(opresult667)) {
      setLineNumber(1143);    // compilenode vardec
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      var var_line = var_lineNumber;
      setLineNumber(1144);    // compilenode member
      // call case 6: other requests
      if (var_line === undefined) raiseUninitializedVariable("line");
      // call case 6: other requests
      if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
      var call669 = request(var_inputLines, "at(1)", [1], var_line);
      var call668 = request(call669, "size", [0]);
      var var_ix = call668;
      setLineNumber(1145);    // compilenode block
      var block671 = new GraceBlock(this, 1145, 0);
      block671.guard = jsTrue;
      block671.real = function block671() {
        setLineNumber(1145);    // compilenode call
        if (var_ix === undefined) raiseUninitializedVariable("ix");
        // call case 6: other requests
        if (var_line === undefined) raiseUninitializedVariable("line");
        // call case 6: other requests
        if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
        var call673 = request(var_inputLines, "at(1)", [1], var_line);
        var call672 = request(call673, "at(1)", [1], var_ix);
        var string674 = new GraceString("{");
        var opresult675 = request(call672, "\u2260(1)", [1], string674);
        return opresult675;
      };
      let applyMeth671 = function apply (argcv) {
          return this.real.apply(this.receiver);
      };
      applyMeth671.methodName = "apply";
      applyMeth671.paramCounts = [0];
      applyMeth671.paramNames = [];
      applyMeth671.definitionLine = 1145;
      applyMeth671.definitionModule = "lexer";
      block671.methods["apply"] = applyMeth671;
      var block676 = new GraceBlock(this, 1145, 0);
      block676.guard = jsTrue;
      block676.real = function block676() {
        setLineNumber(1146);    // compilenode op
        if (var_ix === undefined) raiseUninitializedVariable("ix");
        var diff677 = request(var_ix, "-(1)", [1], new GraceNum(1));
        var_ix = diff677;
        var if678 = GraceDone;
        setLineNumber(1147);    // compilenode op
        if (var_ix === undefined) raiseUninitializedVariable("ix");
        var opresult679 = request(var_ix, "==(1)", [1], new GraceNum(0));
        if (Grace_isTrue(opresult679)) {
          setLineNumber(1148);    // compilenode op
          if (var_line === undefined) raiseUninitializedVariable("line");
          var diff680 = request(var_line, "-(1)", [1], new GraceNum(1));
          var_lineNumber = diff680;
          setLineNumber(1149);    // compilenode member
          // call case 6: other requests
          if (var_line === undefined) raiseUninitializedVariable("line");
          // call case 6: other requests
          if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
          var call682 = request(var_inputLines, "at(1)", [1], var_line);
          var call681 = request(call682, "size", [0]);
          var_ix = call681;
          if678 = GraceDone;
        }
        return if678;
      };
      let applyMeth676 = function apply (argcv) {
          return this.real.apply(this.receiver);
      };
      applyMeth676.methodName = "apply";
      applyMeth676.paramCounts = [0];
      applyMeth676.paramNames = [];
      applyMeth676.definitionLine = 1145;
      applyMeth676.definitionModule = "lexer";
      block676.methods["apply"] = applyMeth676;
      // call case 2: outer request
      var call670 = selfRequest(var_prelude, "while(1)do(1)", [1, 1], block671, block676);
      setLineNumber(1152);    // compilenode array
      var array684 = new GraceSequence([]);
      // call case 2: outer request
      var call683 = selfRequest(var_prelude, "list(1)", [1], array684);
      var var_suggestions = call683;
      setLineNumber(1153);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call686 = request(var_errormessages, "suggestion", [0]);
      var call685 = request(call686, "new", [0]);
      var var_suggestion = call685;
      setLineNumber(1154);    // compilenode string
      var string688 = new GraceString("\\");
      if (var_ix === undefined) raiseUninitializedVariable("ix");
      if (var_line === undefined) raiseUninitializedVariable("line");
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call687 = request(var_suggestion, "insert(1)atPosition(1)onLine(1)", [1, 1, 1], string688, var_ix, var_line);
      setLineNumber(1155);    // compilenode call
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      // call case 6: other requests
      if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
      var call689 = request(var_suggestions, "push(1)", [1], var_suggestion);
      var if690 = GraceDone;
      setLineNumber(1156);    // compilenode op
      if (var_line === undefined) raiseUninitializedVariable("line");
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      var opresult691 = request(var_line, "==(1)", [1], var_lineNumber);
      if (var_ix === undefined) raiseUninitializedVariable("ix");
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      var diff692 = request(var_linePosition, "-(1)", [1], new GraceNum(2));
      var opresult693 = request(var_ix, "==(1)", [1], diff692);
      var opresult694 = request(opresult691, "&&(1)", [1], opresult693);
      if (Grace_isTrue(opresult694)) {
        setLineNumber(1157);    // compilenode string
        var string696 = new GraceString("for a '{' character in a string use '\\{'.");
        if (var_line === undefined) raiseUninitializedVariable("line");
        if (var_ix === undefined) raiseUninitializedVariable("ix");
        if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call695 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestions(1)", [1, 2, 1], string696, var_line, var_ix, var_suggestions);
        if690 = call695;
      } else {
        setLineNumber(1161);    // compilenode member
        // call case 6: other requests
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call698 = request(var_errormessages, "suggestion", [0]);
        var call697 = request(call698, "new", [0]);
        var_suggestion = call697;
        setLineNumber(1162);    // compilenode string
        var string700 = new GraceString("}");
        setLineNumber(1163);    // compilenode op
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        // call case 6: other requests
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        var call701 = request(var_accum, "size", [0]);
        var diff702 = request(var_linePosition, "-(1)", [1], call701);
        var diff703 = request(diff702, "-(1)", [1], new GraceNum(1));
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        // call case 6: other requests
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        var call699 = request(var_suggestion, "insert(1)atPosition(1)onLine(1)", [1, 1, 1], string700, diff703, var_lineNumber);
        setLineNumber(1165);    // compilenode call
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
        var call704 = request(var_suggestions, "push(1)", [1], var_suggestion);
        setLineNumber(1166);    // compilenode string
        var string706 = new GraceString("a string interpolation must end with a '}';");
        setLineNumber(1167);    // compilenode string
        var string707 = new GraceString("for a '{' character in a string use '\\{'.");
        var opresult708 = request(string706, "++(1)", [1], string707);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        setLineNumber(1168);    // compilenode op
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        // call case 6: other requests
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        var call709 = request(var_accum, "size", [0]);
        var diff710 = request(var_linePosition, "-(1)", [1], call709);
        var diff711 = request(diff710, "-(1)", [1], new GraceNum(1));
        if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call705 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestions(1)", [1, 2, 1], opresult708, var_lineNumber, diff711, var_suggestions);
        if690 = call705;
      }
      if666 = if690;
    }
    setLineNumber(1173);    // compilenode call
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    // call case 6: other requests
    if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
    var call712 = request(var_inputLines, "at(1)", [1], var_lineNumber);
    var var_errorLine = call712;
    var if713 = GraceDone;
    setLineNumber(1174);    // compilenode member
    // call case 6: other requests
    if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
    var call714 = request(var_inputLines, "size", [0]);
    if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
    var sum715 = request(var_lineNumber, "+(1)", [1], new GraceNum(1));
    var opresult716 = request(call714, "\u2265(1)", [1], sum715);
    if (Grace_isTrue(opresult716)) {
      setLineNumber(1175);    // compilenode op
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      var sum718 = request(var_lineNumber, "+(1)", [1], new GraceNum(1));
      // call case 6: other requests
      if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
      var call717 = request(var_inputLines, "at(1)", [1], sum718);
      if713 = call717;
    } else {
      setLineNumber(1177);    // compilenode string
      var string719 = new GraceString("");
      if713 = string719;
    }
    var var_nextLine = if713;
    setLineNumber(1181);    // compilenode num
    var var_i = new GraceNum(1);
    setLineNumber(1182);    // compilenode num
    var var_count = new GraceNum(0);
    setLineNumber(1183);    // compilenode block
    var block721 = new GraceBlock(this, 1183, 0);
    block721.guard = jsTrue;
    block721.real = function block721() {
      setLineNumber(1183);    // compilenode op
      if (var_i === undefined) raiseUninitializedVariable("i");
      // call case 6: other requests
      if (var_nextLine === undefined) raiseUninitializedVariable("nextLine");
      var call722 = request(var_nextLine, "size", [0]);
      var opresult723 = request(var_i, "\u2264(1)", [1], call722);
      return opresult723;
    };
    let applyMeth721 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth721.methodName = "apply";
    applyMeth721.paramCounts = [0];
    applyMeth721.paramNames = [];
    applyMeth721.definitionLine = 1183;
    applyMeth721.definitionModule = "lexer";
    block721.methods["apply"] = applyMeth721;
    var block724 = new GraceBlock(this, 1183, 0);
    block724.guard = jsTrue;
    block724.real = function block724() {
      var if725 = GraceDone;
      setLineNumber(1184);    // compilenode call
      if (var_i === undefined) raiseUninitializedVariable("i");
      // call case 6: other requests
      if (var_nextLine === undefined) raiseUninitializedVariable("nextLine");
      var call726 = request(var_nextLine, "at(1)", [1], var_i);
      var string727 = new GraceString("\"");
      var opresult728 = request(call726, "==(1)", [1], string727);
      if (Grace_isTrue(opresult728)) {
        setLineNumber(1185);    // compilenode op
        if (var_count === undefined) raiseUninitializedVariable("count");
        var sum729 = request(var_count, "+(1)", [1], new GraceNum(1));
        var_count = sum729;
        if725 = GraceDone;
      } else {
        var if730 = GraceDone;
        setLineNumber(1187);    // compilenode call
        if (var_i === undefined) raiseUninitializedVariable("i");
        // call case 6: other requests
        if (var_nextLine === undefined) raiseUninitializedVariable("nextLine");
        var call731 = request(var_nextLine, "at(1)", [1], var_i);
        var string732 = new GraceString("\\");
        var opresult733 = request(call731, "==(1)", [1], string732);
        if (Grace_isTrue(opresult733)) {
          setLineNumber(1189);    // compilenode op
          if (var_i === undefined) raiseUninitializedVariable("i");
          var sum734 = request(var_i, "+(1)", [1], new GraceNum(1));
          var_i = sum734;
          if730 = GraceDone;
        }
        if725 = if730;
      }
      setLineNumber(1191);    // compilenode op
      if (var_i === undefined) raiseUninitializedVariable("i");
      var sum735 = request(var_i, "+(1)", [1], new GraceNum(1));
      var_i = sum735;
      return GraceDone;
    };
    let applyMeth724 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth724.methodName = "apply";
    applyMeth724.paramCounts = [0];
    applyMeth724.paramNames = [];
    applyMeth724.definitionLine = 1183;
    applyMeth724.definitionModule = "lexer";
    block724.methods["apply"] = applyMeth724;
    // call case 2: outer request
    var call720 = selfRequest(var_prelude, "while(1)do(1)", [1, 1], block721, block724);
    var if736 = GraceDone;
    setLineNumber(1193);    // compilenode member
    // call case 6: other requests
    var call737 = request(var_count, "isOdd", [0]);
    if (Grace_isTrue(call737)) {
      setLineNumber(1194);    // compilenode array
      var array739 = new GraceSequence([]);
      // call case 2: outer request
      var call738 = selfRequest(var_prelude, "list(1)", [1], array739);
      var var_suggestions = call738;
      setLineNumber(1195);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call741 = request(var_errormessages, "suggestion", [0]);
      var call740 = request(call741, "new", [0]);
      var var_suggestion = call740;
      setLineNumber(1196);    // compilenode call
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_errorLine === undefined) raiseUninitializedVariable("errorLine");
      if (var_nextLine === undefined) raiseUninitializedVariable("nextLine");
      var opresult743 = request(var_errorLine, "++(1)", [1], var_nextLine);
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call742 = request(var_suggestion, "addLine(2)", [2], var_lineNumber, opresult743);
      setLineNumber(1197);    // compilenode op
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      var sum745 = request(var_lineNumber, "+(1)", [1], new GraceNum(1));
      var string746 = new GraceString("");
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call744 = request(var_suggestion, "addLine(2)", [2], sum745, string746);
      setLineNumber(1198);    // compilenode call
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      // call case 6: other requests
      if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
      var call747 = request(var_suggestions, "push(1)", [1], var_suggestion);
      setLineNumber(1199);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call749 = request(var_errormessages, "suggestion", [0]);
      var call748 = request(call749, "new", [0]);
      var_suggestion = call748;
      setLineNumber(1200);    // compilenode call
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_errorLine === undefined) raiseUninitializedVariable("errorLine");
      var string751 = new GraceString("\"");
      var opresult752 = request(var_errorLine, "++(1)", [1], string751);
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call750 = request(var_suggestion, "addLine(2)", [2], var_lineNumber, opresult752);
      setLineNumber(1201);    // compilenode op
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      var sum754 = request(var_lineNumber, "+(1)", [1], new GraceNum(1));
      var string755 = new GraceString("    ++ \"");
      if (var_nextLine === undefined) raiseUninitializedVariable("nextLine");
      var opresult756 = request(string755, "++(1)", [1], var_nextLine);
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call753 = request(var_suggestion, "addLine(2)", [2], sum754, opresult756);
      setLineNumber(1202);    // compilenode call
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      // call case 6: other requests
      if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
      var call757 = request(var_suggestions, "push(1)", [1], var_suggestion);
      setLineNumber(1203);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call759 = request(var_errormessages, "suggestion", [0]);
      var call758 = request(call759, "new", [0]);
      var_suggestion = call758;
      setLineNumber(1204);    // compilenode call
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_errorLine === undefined) raiseUninitializedVariable("errorLine");
      var string761 = new GraceString("\\n");
      var opresult762 = request(var_errorLine, "++(1)", [1], string761);
      if (var_nextLine === undefined) raiseUninitializedVariable("nextLine");
      var opresult763 = request(opresult762, "++(1)", [1], var_nextLine);
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call760 = request(var_suggestion, "addLine(2)", [2], var_lineNumber, opresult763);
      setLineNumber(1205);    // compilenode op
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      var sum765 = request(var_lineNumber, "+(1)", [1], new GraceNum(1));
      var string766 = new GraceString("");
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call764 = request(var_suggestion, "addLine(2)", [2], sum765, string766);
      setLineNumber(1206);    // compilenode call
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      // call case 6: other requests
      if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
      var call767 = request(var_suggestions, "push(1)", [1], var_suggestion);
      setLineNumber(1207);    // compilenode string
      var string769 = new GraceString("a string must be terminated by a \" before ");
      setLineNumber(1208);    // compilenode string
      var string770 = new GraceString("the end of the line. To insert a newline in a string, use '\\n'. ");
      var opresult771 = request(string769, "++(1)", [1], string770);
      setLineNumber(1209);    // compilenode string
      var string772 = new GraceString("To spread a string over multiple lines, use several strings ");
      var opresult773 = request(opresult771, "++(1)", [1], string772);
      setLineNumber(1210);    // compilenode string
      var string774 = new GraceString("joined together with '++'");
      var opresult775 = request(opresult773, "++(1)", [1], string774);
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      if (var_suggestions === undefined) raiseUninitializedVariable("suggestions");
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call768 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestions(1)", [1, 2, 1], opresult775, var_lineNumber, var_linePosition, var_suggestions);
      if736 = call768;
    } else {
      setLineNumber(1214);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call777 = request(var_errormessages, "suggestion", [0]);
      var call776 = request(call777, "new", [0]);
      var var_suggestion = call776;
      setLineNumber(1215);    // compilenode call
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_errorLine === undefined) raiseUninitializedVariable("errorLine");
      var string779 = new GraceString("\"");
      var opresult780 = request(var_errorLine, "++(1)", [1], string779);
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call778 = request(var_suggestion, "addLine(2)", [2], var_lineNumber, opresult780);
      setLineNumber(1216);    // compilenode string
      var string782 = new GraceString("a string must be terminated ");
      setLineNumber(1217);    // compilenode string
      var string783 = new GraceString("by a \" before the end of the line.");
      var opresult784 = request(string782, "++(1)", [1], string783);
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call781 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestion(1)", [1, 2, 1], opresult784, var_lineNumber, var_linePosition, var_suggestion);
      if736 = call781;
    }
    return if736;
  };    // end of method unclosedStringError
  this.methods["unclosedStringError"] = func665;
  func665.methodName = "unclosedStringError";
  func665.paramCounts = [0];
  func665.paramNames = [];
  func665.definitionLine = 1138;
  func665.definitionModule = "lexer";
  var func785 = function(argcv, var_c) {    // method isSpaceChar(_), line 1223
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("isSpaceChar(_)", 0, numArgs - 1);
    }
    setLineNumber(1224);    // compilenode call
    // call case 6: other requests
    if (var_spaceChars === undefined) raiseUninitializedVariable("spaceChars");
    var call786 = request(var_spaceChars, "contains(1)", [1], var_c);
    return call786;
  };    // end of method isSpaceChar(_)
  this.methods["isSpaceChar(1)"] = func785;
  func785.methodName = "isSpaceChar(1)";
  func785.paramCounts = [1];
  func785.paramNames = ["c"];
  func785.definitionLine = 1223;
  func785.definitionModule = "lexer";
  var func787 = function(argcv, var_c) {    // method checkSeparatorString(_), line 1226
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("checkSeparatorString(_)", 0, numArgs - 1);
    }
    setLineNumber(1227);    // compilenode member
    // call case 6: other requests
    var call788 = request(var_c, "ord", [0]);
    var var_ordval = call788;
    var if789 = GraceDone;
    setLineNumber(1228);    // compilenode call
    // call case 2: outer request
    var call790 = selfRequest(importedModules["lexer"], "isBadSeparator(1)", [1], var_c);
    if (Grace_isTrue(call790)) {
      setLineNumber(1232);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call792 = request(var_errormessages, "suggestion", [0]);
      var call791 = request(call792, "new", [0]);
      var var_suggestion = call791;
      setLineNumber(1233);    // compilenode call
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      var string794 = new GraceString(" ");
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call793 = request(var_suggestion, "replaceChar(1)with(1)onLine(1)", [1, 1, 1], var_linePosition, string794, var_lineNumber);
      var if795 = GraceDone;
      setLineNumber(1234);    // compilenode op
      if (var_ordval === undefined) raiseUninitializedVariable("ordval");
      var opresult796 = request(var_ordval, "==(1)", [1], new GraceNum(9));
      if (Grace_isTrue(opresult796)) {
        setLineNumber(1235);    // compilenode call
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        setLineNumber(1236);    // compilenode string
        var string798 = new GraceString("\\t");
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        // call case 6: other requests
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        var call797 = request(var_suggestion, "replaceRange(2)with(1)onLine(1)", [2, 1, 1], var_linePosition, var_linePosition, string798, var_lineNumber);
        setLineNumber(1238);    // compilenode string
        var string800 = new GraceString("tabs cannot be ");
        setLineNumber(1239);    // compilenode string
        var string801 = new GraceString("written in the source code; ");
        var opresult802 = request(string800, "++(1)", [1], string801);
        setLineNumber(1240);    // compilenode string
        var string803 = new GraceString("use the string escape \\t instead");
        var opresult804 = request(opresult802, "++(1)", [1], string803);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call799 = request(var_errormessages, "syntaxError(1)atRange(3)withSuggestion(1)", [1, 3, 1], opresult804, var_lineNumber, var_linePosition, var_linePosition, var_suggestion);
        setLineNumber(1243);    // compilenode string
        var string806 = new GraceString("tabs are not allowed; use spaces instead.");
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call805 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestion(1)", [1, 2, 1], string806, var_lineNumber, var_linePosition, var_suggestion);
        if795 = call805;
      } else {
        setLineNumber(1246);    // compilenode call
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        setLineNumber(1247);    // compilenode string
        var string808 = new GraceString("\\u");
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call809 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult810 = request(string808, "++(1)", [1], call809);
        var string811 = new GraceString("");
        var opresult812 = request(opresult810, "++(1)", [1], string811);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        // call case 6: other requests
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        var call807 = request(var_suggestion, "replaceRange(2)with(1)onLine(1)", [2, 1, 1], var_linePosition, var_linePosition, opresult812, var_lineNumber);
        setLineNumber(1249);    // compilenode string
        var string814 = new GraceString("");
        // call case 6: other requests
        if (var_unicode === undefined) raiseUninitializedVariable("unicode");
        var call815 = request(var_unicode, "name(1)", [1], var_c);
        var opresult816 = request(string814, "++(1)", [1], call815);
        var string817 = new GraceString(" (U+");
        var opresult818 = request(opresult816, "++(1)", [1], string817);
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call819 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult820 = request(opresult818, "++(1)", [1], call819);
        var string821 = new GraceString(")");
        var opresult822 = request(opresult820, "++(1)", [1], string821);
        setLineNumber(1250);    // compilenode string
        var string823 = new GraceString(" is not a valid whitespace character");
        var opresult824 = request(opresult822, "++(1)", [1], string823);
        setLineNumber(1251);    // compilenode string
        var string825 = new GraceString(" and cannot be written in the source code;");
        var opresult826 = request(opresult824, "++(1)", [1], string825);
        setLineNumber(1252);    // compilenode string
        var string827 = new GraceString(" use the Unicode escape \\u");
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call828 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult829 = request(string827, "++(1)", [1], call828);
        var string830 = new GraceString(" instead");
        var opresult831 = request(opresult829, "++(1)", [1], string830);
        var opresult832 = request(opresult826, "++(1)", [1], opresult831);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call813 = request(var_errormessages, "syntaxError(1)atRange(3)withSuggestion(1)", [1, 3, 1], opresult832, var_lineNumber, var_linePosition, var_linePosition, var_suggestion);
        setLineNumber(1255);    // compilenode string
        var string834 = new GraceString("");
        // call case 6: other requests
        if (var_unicode === undefined) raiseUninitializedVariable("unicode");
        var call835 = request(var_unicode, "name(1)", [1], var_c);
        var opresult836 = request(string834, "++(1)", [1], call835);
        var string837 = new GraceString(" (U+");
        var opresult838 = request(opresult836, "++(1)", [1], string837);
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call839 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult840 = request(opresult838, "++(1)", [1], call839);
        var string841 = new GraceString(") ");
        var opresult842 = request(opresult840, "++(1)", [1], string841);
        setLineNumber(1256);    // compilenode string
        var string843 = new GraceString("is not a valid whitespace character; use spaces instead.");
        var opresult844 = request(opresult842, "++(1)", [1], string843);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call833 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestion(1)", [1, 2, 1], opresult844, var_lineNumber, var_linePosition, var_suggestion);
        if795 = call833;
      }
      if789 = if795;
    } else {
      var if845 = GraceDone;
      setLineNumber(1259);    // compilenode call
      // call case 2: outer request
      var call846 = selfRequest(importedModules["lexer"], "isBadControl(1)", [1], var_c);
      if (Grace_isTrue(call846)) {
        setLineNumber(1262);    // compilenode member
        // call case 6: other requests
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call848 = request(var_errormessages, "suggestion", [0]);
        var call847 = request(call848, "new", [0]);
        var var_suggestion = call847;
        setLineNumber(1263);    // compilenode call
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        setLineNumber(1264);    // compilenode string
        var string850 = new GraceString("\\u");
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call851 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult852 = request(string850, "++(1)", [1], call851);
        var string853 = new GraceString("");
        var opresult854 = request(opresult852, "++(1)", [1], string853);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        // call case 6: other requests
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        var call849 = request(var_suggestion, "replaceRange(2)with(1)onLine(1)", [2, 1, 1], var_linePosition, var_linePosition, opresult854, var_lineNumber);
        setLineNumber(1266);    // compilenode string
        var string856 = new GraceString("");
        // call case 6: other requests
        if (var_unicode === undefined) raiseUninitializedVariable("unicode");
        var call857 = request(var_unicode, "name(1)", [1], var_c);
        var opresult858 = request(string856, "++(1)", [1], call857);
        var string859 = new GraceString(" (U+");
        var opresult860 = request(opresult858, "++(1)", [1], string859);
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call861 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult862 = request(opresult860, "++(1)", [1], call861);
        var string863 = new GraceString(") ");
        var opresult864 = request(opresult862, "++(1)", [1], string863);
        setLineNumber(1267);    // compilenode string
        var string865 = new GraceString("is a control character and cannot be written in the source code; ");
        var opresult866 = request(opresult864, "++(1)", [1], string865);
        setLineNumber(1268);    // compilenode string
        var string867 = new GraceString("use the Unicode escape \\u");
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call868 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult869 = request(string867, "++(1)", [1], call868);
        var string870 = new GraceString(" instead");
        var opresult871 = request(opresult869, "++(1)", [1], string870);
        var opresult872 = request(opresult866, "++(1)", [1], opresult871);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call855 = request(var_errormessages, "syntaxError(1)atRange(3)withSuggestion(1)", [1, 3, 1], opresult872, var_lineNumber, var_linePosition, var_linePosition, var_suggestion);
        setLineNumber(1271);    // compilenode call
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        // call case 6: other requests
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        var call873 = request(var_suggestion, "deleteChar(1)onLine(1)", [1, 1], var_linePosition, var_lineNumber);
        setLineNumber(1272);    // compilenode string
        var string875 = new GraceString("");
        // call case 6: other requests
        if (var_unicode === undefined) raiseUninitializedVariable("unicode");
        var call876 = request(var_unicode, "name(1)", [1], var_c);
        var opresult877 = request(string875, "++(1)", [1], call876);
        var string878 = new GraceString(" (U+");
        var opresult879 = request(opresult877, "++(1)", [1], string878);
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call880 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult881 = request(opresult879, "++(1)", [1], call880);
        var string882 = new GraceString(") ");
        var opresult883 = request(opresult881, "++(1)", [1], string882);
        setLineNumber(1273);    // compilenode string
        var string884 = new GraceString("is a control character and cannot be written in the source code; ");
        var opresult885 = request(opresult883, "++(1)", [1], string884);
        setLineNumber(1274);    // compilenode string
        var string886 = new GraceString("consider using spaces instead.");
        var opresult887 = request(opresult885, "++(1)", [1], string886);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call874 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestion(1)", [1, 2, 1], opresult887, var_lineNumber, var_linePosition, var_suggestion);
        if845 = call874;
      }
      if789 = if845;
    }
    return if789;
  };    // end of method checkSeparatorString(_)
  this.methods["checkSeparatorString(1)"] = func787;
  func787.methodName = "checkSeparatorString(1)";
  func787.paramCounts = [1];
  func787.paramNames = ["c"];
  func787.definitionLine = 1226;
  func787.definitionModule = "lexer";
  var func888 = function(argcv, var_c) {    // method checkSeparator(_), line 1279
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("checkSeparator(_)", 0, numArgs - 1);
    }
    setLineNumber(1280);    // compilenode member
    // call case 6: other requests
    var call889 = request(var_c, "ord", [0]);
    var var_ordval = call889;
    var if890 = GraceDone;
    setLineNumber(1281);    // compilenode call
    // call case 2: outer request
    var call891 = selfRequest(importedModules["lexer"], "isBadSeparator(1)", [1], var_c);
    if (Grace_isTrue(call891)) {
      setLineNumber(1285);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call893 = request(var_errormessages, "suggestion", [0]);
      var call892 = request(call893, "new", [0]);
      var var_suggestion = call892;
      setLineNumber(1286);    // compilenode call
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      var string895 = new GraceString(" ");
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      // call case 6: other requests
      if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
      var call894 = request(var_suggestion, "replaceChar(1)with(1)onLine(1)", [1, 1, 1], var_linePosition, string895, var_lineNumber);
      var if896 = GraceDone;
      setLineNumber(1287);    // compilenode op
      if (var_ordval === undefined) raiseUninitializedVariable("ordval");
      var opresult897 = request(var_ordval, "==(1)", [1], new GraceNum(9));
      if (Grace_isTrue(opresult897)) {
        setLineNumber(1288);    // compilenode string
        var string899 = new GraceString("tabs are not allowed; use spaces instead.");
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call898 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestion(1)", [1, 2, 1], string899, var_lineNumber, var_linePosition, var_suggestion);
        if896 = call898;
      } else {
        setLineNumber(1291);    // compilenode string
        var string901 = new GraceString("");
        // call case 6: other requests
        if (var_unicode === undefined) raiseUninitializedVariable("unicode");
        var call902 = request(var_unicode, "name(1)", [1], var_c);
        var opresult903 = request(string901, "++(1)", [1], call902);
        var string904 = new GraceString(" (U+");
        var opresult905 = request(opresult903, "++(1)", [1], string904);
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call906 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult907 = request(opresult905, "++(1)", [1], call906);
        var string908 = new GraceString(") ");
        var opresult909 = request(opresult907, "++(1)", [1], string908);
        setLineNumber(1292);    // compilenode string
        var string910 = new GraceString("is not a valid whitespace character; use spaces instead.");
        var opresult911 = request(opresult909, "++(1)", [1], string910);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call900 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestion(1)", [1, 2, 1], opresult911, var_lineNumber, var_linePosition, var_suggestion);
        if896 = call900;
      }
      if890 = if896;
    } else {
      var if912 = GraceDone;
      setLineNumber(1295);    // compilenode call
      // call case 2: outer request
      var call913 = selfRequest(importedModules["lexer"], "isBadControl(1)", [1], var_c);
      if (Grace_isTrue(call913)) {
        setLineNumber(1298);    // compilenode member
        // call case 6: other requests
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call915 = request(var_errormessages, "suggestion", [0]);
        var call914 = request(call915, "new", [0]);
        var var_suggestion = call914;
        setLineNumber(1299);    // compilenode call
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        // call case 6: other requests
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        var call916 = request(var_suggestion, "deleteChar(1)onLine(1)", [1, 1], var_linePosition, var_lineNumber);
        setLineNumber(1300);    // compilenode string
        var string918 = new GraceString("");
        // call case 6: other requests
        if (var_unicode === undefined) raiseUninitializedVariable("unicode");
        var call919 = request(var_unicode, "name(1)", [1], var_c);
        var opresult920 = request(string918, "++(1)", [1], call919);
        var string921 = new GraceString(" (U+");
        var opresult922 = request(opresult920, "++(1)", [1], string921);
        if (var_ordval === undefined) raiseUninitializedVariable("ordval");
        // call case 2: outer request
        var call923 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
        var opresult924 = request(opresult922, "++(1)", [1], call923);
        var string925 = new GraceString(") ");
        var opresult926 = request(opresult924, "++(1)", [1], string925);
        setLineNumber(1301);    // compilenode string
        var string927 = new GraceString("is a control character and cannot be written in the source ");
        var opresult928 = request(opresult926, "++(1)", [1], string927);
        setLineNumber(1302);    // compilenode string
        var string929 = new GraceString("code; consider using spaces instead.");
        var opresult930 = request(opresult928, "++(1)", [1], string929);
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call917 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestion(1)", [1, 2, 1], opresult930, var_lineNumber, var_linePosition, var_suggestion);
        if912 = call917;
      }
      if890 = if912;
    }
    return if890;
  };    // end of method checkSeparator(_)
  this.methods["checkSeparator(1)"] = func888;
  func888.methodName = "checkSeparator(1)";
  func888.paramCounts = [1];
  func888.paramNames = ["c"];
  func888.definitionLine = 1279;
  func888.definitionModule = "lexer";
  var func931 = function(argcv, var_str, var_base) {    // method fromBase(_,_), line 1307
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("fromBase(_,_)", 0, numArgs - 2);
    }
    setLineNumber(1308);    // compilenode num
    var var_val = new GraceNum(0);
    setLineNumber(1309);    // compilenode num
    var var_i = new GraceNum(0);
    setLineNumber(1310);    // compilenode block
    var block933 = new GraceBlock(this, 1310, 1);
    block933.guard = jsTrue;
    block933.real = function block933(var_c) {
      setLineNumber(1311);    // compilenode member
      // call case 6: other requests
      var call934 = request(var_c, "ord", [0]);
      var var_n = call934;
      setLineNumber(1312);    // compilenode op
      if (var_val === undefined) raiseUninitializedVariable("val");
      var prod935 = request(var_val, "*(1)", [1], var_base);
      var_val = prod935;
      setLineNumber(1313);    // compilenode num
      var var_inc = new GraceNum(0);
      var if936 = GraceDone;
      setLineNumber(1314);    // compilenode op
      if (var_n === undefined) raiseUninitializedVariable("n");
      var opresult937 = request(var_n, "\u2265(1)", [1], new GraceNum(48));
      if (var_n === undefined) raiseUninitializedVariable("n");
      var opresult938 = request(var_n, "\u2264(1)", [1], new GraceNum(57));
      var opresult939 = request(opresult937, "&&(1)", [1], opresult938);
      if (Grace_isTrue(opresult939)) {
        setLineNumber(1315);    // compilenode op
        if (var_n === undefined) raiseUninitializedVariable("n");
        var diff940 = request(var_n, "-(1)", [1], new GraceNum(48));
        var_inc = diff940;
        if936 = GraceDone;
      } else {
        var if941 = GraceDone;
        setLineNumber(1316);    // compilenode op
        if (var_n === undefined) raiseUninitializedVariable("n");
        var opresult942 = request(var_n, "\u2265(1)", [1], new GraceNum(65));
        if (var_n === undefined) raiseUninitializedVariable("n");
        var opresult943 = request(var_n, "\u2264(1)", [1], new GraceNum(90));
        var opresult944 = request(opresult942, "&&(1)", [1], opresult943);
        if (Grace_isTrue(opresult944)) {
          setLineNumber(1317);    // compilenode op
          if (var_n === undefined) raiseUninitializedVariable("n");
          var diff945 = request(var_n, "-(1)", [1], new GraceNum(55));
          var_inc = diff945;
          if941 = GraceDone;
        } else {
          setLineNumber(1319);    // compilenode op
          if (var_n === undefined) raiseUninitializedVariable("n");
          var diff946 = request(var_n, "-(1)", [1], new GraceNum(87));
          var_inc = diff946;
          if941 = GraceDone;
        }
        if936 = if941;
      }
      var if947 = GraceDone;
      setLineNumber(1321);    // compilenode op
      if (var_inc === undefined) raiseUninitializedVariable("inc");
      var opresult948 = request(var_inc, "\u2265(1)", [1], var_base);
      if (Grace_isTrue(opresult948)) {
        var if949 = GraceDone;
        setLineNumber(1322);    // compilenode member
        // call case 6: other requests
        var call950 = request(var_str, "first", [0]);
        var string951 = new GraceString("0");
        var opresult952 = request(call950, "==(1)", [1], string951);
        if (var_inc === undefined) raiseUninitializedVariable("inc");
        var opresult953 = request(var_inc, "<(1)", [1], new GraceNum(16));
        var opresult954 = request(opresult952, "&&(1)", [1], opresult953);
        if (Grace_isTrue(opresult954)) {
          setLineNumber(1323);    // compilenode member
          // call case 6: other requests
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call956 = request(var_errormessages, "suggestion", [0]);
          var call955 = request(call956, "new", [0]);
          var var_suggestion = call955;
          setLineNumber(1324);    // compilenode string
          var string958 = new GraceString("x");
          setLineNumber(1325);    // compilenode op
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          var call959 = request(var_str, "size", [0]);
          var diff960 = request(var_linePosition, "-(1)", [1], call959);
          var sum961 = request(diff960, "+(1)", [1], new GraceNum(1));
          if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
          // call case 6: other requests
          if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
          var call957 = request(var_suggestion, "insert(1)atPosition(1)onLine(1)", [1, 1, 1], string958, sum961, var_lineNumber);
          setLineNumber(1326);    // compilenode string
          var string963 = new GraceString("a number in base 16 must start with '0x'.");
          if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
          setLineNumber(1327);    // compilenode op
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          var call964 = request(var_str, "size", [0]);
          var diff965 = request(var_linePosition, "-(1)", [1], call964);
          var sum966 = request(diff965, "+(1)", [1], new GraceNum(1));
          if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call962 = request(var_errormessages, "syntaxError(1)atPosition(2)withSuggestion(1)", [1, 2, 1], string963, var_lineNumber, sum966, var_suggestion);
          if949 = call962;
        } else {
          setLineNumber(1330);    // compilenode member
          // call case 6: other requests
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call968 = request(var_errormessages, "suggestion", [0]);
          var call967 = request(call968, "new", [0]);
          var var_suggestion = call967;
          var if969 = GraceDone;
          setLineNumber(1331);    // compilenode member
          // call case 6: other requests
          var call970 = request(var_str, "size", [0]);
          var opresult971 = request(call970, "==(1)", [1], new GraceNum(1));
          if (Grace_isTrue(opresult971)) {
            setLineNumber(1332);    // compilenode op
            if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
            // call case 6: other requests
            var call973 = request(var_str, "size", [0]);
            var diff974 = request(var_linePosition, "-(1)", [1], call973);
            var diff975 = request(diff974, "-(1)", [1], new GraceNum(1));
            if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
            var diff976 = request(var_linePosition, "-(1)", [1], new GraceNum(1));
            if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
            // call case 6: other requests
            if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
            var call972 = request(var_suggestion, "deleteRange(2)onLine(1)", [2, 1], diff975, diff976, var_lineNumber);
            if969 = call972;
          } else {
            setLineNumber(1335);    // compilenode op
            if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
            // call case 6: other requests
            var call978 = request(var_str, "size", [0]);
            var diff979 = request(var_linePosition, "-(1)", [1], call978);
            if (var_i === undefined) raiseUninitializedVariable("i");
            var sum980 = request(diff979, "+(1)", [1], var_i);
            if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
            // call case 6: other requests
            if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
            var call977 = request(var_suggestion, "deleteChar(1)onLine(1)", [1, 1], sum980, var_lineNumber);
            if969 = call977;
          }
          var if981 = GraceDone;
          setLineNumber(1338);    // compilenode num
          var opresult982 = request(var_base, "\u2264(1)", [1], new GraceNum(10));
          if (Grace_isTrue(opresult982)) {
            setLineNumber(1339);    // compilenode string
            var string983 = new GraceString("'0' to '");
            var diff984 = request(var_base, "-(1)", [1], new GraceNum(1));
            var opresult985 = request(string983, "++(1)", [1], diff984);
            var string986 = new GraceString("'");
            var opresult987 = request(opresult985, "++(1)", [1], string986);
            if981 = opresult987;
          } else {
            setLineNumber(1341);    // compilenode string
            var string988 = new GraceString("'0' to '9', 'a' to '");
            var sum990 = request(new GraceNum(87), "+(1)", [1], var_base);
            // call case 6: other requests
            if (var_unicode === undefined) raiseUninitializedVariable("unicode");
            var call989 = request(var_unicode, "create(1)", [1], sum990);
            var opresult991 = request(string988, "++(1)", [1], call989);
            var string992 = new GraceString("'");
            var opresult993 = request(opresult991, "++(1)", [1], string992);
            if981 = opresult993;
          }
          var var_validDigits = if981;
          setLineNumber(1343);    // compilenode string
          var string995 = new GraceString("'");
          var opresult996 = request(string995, "++(1)", [1], var_c);
          var string997 = new GraceString("' is not a valid digit in base ");
          var opresult998 = request(opresult996, "++(1)", [1], string997);
          var opresult999 = request(opresult998, "++(1)", [1], var_base);
          var string1000 = new GraceString(". ");
          var opresult1001 = request(opresult999, "++(1)", [1], string1000);
          setLineNumber(1344);    // compilenode string
          var string1002 = new GraceString("Valid digits are ");
          if (var_validDigits === undefined) raiseUninitializedVariable("validDigits");
          var opresult1003 = request(string1002, "++(1)", [1], var_validDigits);
          var string1004 = new GraceString(".");
          var opresult1005 = request(opresult1003, "++(1)", [1], string1004);
          var opresult1006 = request(opresult1001, "++(1)", [1], opresult1005);
          if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
          setLineNumber(1345);    // compilenode op
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          var call1007 = request(var_str, "size", [0]);
          var diff1008 = request(var_linePosition, "-(1)", [1], call1007);
          if (var_i === undefined) raiseUninitializedVariable("i");
          var sum1009 = request(diff1008, "+(1)", [1], var_i);
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          var call1010 = request(var_str, "size", [0]);
          var diff1011 = request(var_linePosition, "-(1)", [1], call1010);
          if (var_i === undefined) raiseUninitializedVariable("i");
          var sum1012 = request(diff1011, "+(1)", [1], var_i);
          if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call994 = request(var_errormessages, "syntaxError(1)atRange(3)withSuggestion(1)", [1, 3, 1], opresult1006, var_lineNumber, sum1009, sum1012, var_suggestion);
          if949 = call994;
        }
        if947 = if949;
      }
      setLineNumber(1349);    // compilenode op
      if (var_val === undefined) raiseUninitializedVariable("val");
      if (var_inc === undefined) raiseUninitializedVariable("inc");
      var sum1013 = request(var_val, "+(1)", [1], var_inc);
      var_val = sum1013;
      setLineNumber(1350);    // compilenode op
      if (var_i === undefined) raiseUninitializedVariable("i");
      var sum1014 = request(var_i, "+(1)", [1], new GraceNum(1));
      var_i = sum1014;
      return GraceDone;
    };
    let applyMeth933 = function apply_1 (argcv, ...args) {
        if (this.guard.apply(this.receiver, args))
            return this.real.apply(this.receiver, args);
        badBlockArgs.apply(this, args);
    };
    applyMeth933.methodName = "apply(1)";
    applyMeth933.paramCounts = [1];
    applyMeth933.paramNames = ["c"];
    applyMeth933.definitionLine = 1310;
    applyMeth933.definitionModule = "lexer";
    block933.methods["apply(1)"] = applyMeth933;
    let matchesMeth933 = function matches_1 (argcv, ...args) {
        return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
    };
    matchesMeth933.methodName = "matches(1)";
    matchesMeth933.paramCounts = [1];
    matchesMeth933.paramNames = ["c"];
    matchesMeth933.definitionLine = 1310;
    matchesMeth933.definitionModule = "lexer";
    block933.methods["matches(1)"] = matchesMeth933;
    // call case 2: outer request
    var call932 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], var_str, block933);
    return var_val;
  };    // end of method fromBase(_,_)
  this.methods["fromBase(2)"] = func931;
  func931.methodName = "fromBase(2)";
  func931.paramCounts = [2];
  func931.paramNames = ["str", "base"];
  func931.definitionLine = 1307;
  func931.definitionModule = "lexer";
  var func1015 = function(argcv, var_c) {    // method hexdecchar(_), line 1355
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("hexdecchar(_)", 0, numArgs - 1);
    }
    setLineNumber(1357);    // compilenode member
    // call case 6: other requests
    var string1017 = new GraceString("A");
    var call1016 = request(string1017, "ord", [0]);
    var var_AOrd = call1016;
    setLineNumber(1358);    // compilenode member
    // call case 6: other requests
    var string1019 = new GraceString("a");
    var call1018 = request(string1019, "ord", [0]);
    var var_aOrd = call1018;
    setLineNumber(1359);    // compilenode member
    // call case 6: other requests
    var string1021 = new GraceString("0");
    var call1020 = request(string1021, "ord", [0]);
    var var_zeroOrd = call1020;
    setLineNumber(1360);    // compilenode member
    // call case 6: other requests
    var call1022 = request(var_c, "ord", [0]);
    var var_cOrd = call1022;
    var if1023 = GraceDone;
    setLineNumber(1361);    // compilenode op
    var opresult1024 = request(var_cOrd, "\u2265(1)", [1], var_AOrd);
    // call case 6: other requests
    var string1026 = new GraceString("F");
    var call1025 = request(string1026, "ord", [0]);
    var opresult1027 = request(var_cOrd, "\u2264(1)", [1], call1025);
    var opresult1028 = request(opresult1024, "&&(1)", [1], opresult1027);
    if (Grace_isTrue(opresult1028)) {
      setLineNumber(1362);    // compilenode op
      if (var_cOrd === undefined) raiseUninitializedVariable("cOrd");
      if (var_AOrd === undefined) raiseUninitializedVariable("AOrd");
      var diff1029 = request(var_cOrd, "-(1)", [1], var_AOrd);
      var sum1030 = request(diff1029, "+(1)", [1], new GraceNum(10));
      if1023 = sum1030;
    } else {
      var if1031 = GraceDone;
      setLineNumber(1363);    // compilenode op
      if (var_cOrd === undefined) raiseUninitializedVariable("cOrd");
      if (var_aOrd === undefined) raiseUninitializedVariable("aOrd");
      var opresult1032 = request(var_cOrd, "\u2265(1)", [1], var_aOrd);
      if (var_cOrd === undefined) raiseUninitializedVariable("cOrd");
      // call case 6: other requests
      var string1034 = new GraceString("f");
      var call1033 = request(string1034, "ord", [0]);
      var opresult1035 = request(var_cOrd, "\u2264(1)", [1], call1033);
      var opresult1036 = request(opresult1032, "&&(1)", [1], opresult1035);
      if (Grace_isTrue(opresult1036)) {
        setLineNumber(1364);    // compilenode op
        if (var_cOrd === undefined) raiseUninitializedVariable("cOrd");
        if (var_aOrd === undefined) raiseUninitializedVariable("aOrd");
        var diff1037 = request(var_cOrd, "-(1)", [1], var_aOrd);
        var sum1038 = request(diff1037, "+(1)", [1], new GraceNum(10));
        if1031 = sum1038;
      } else {
        var if1039 = GraceDone;
        setLineNumber(1365);    // compilenode op
        if (var_cOrd === undefined) raiseUninitializedVariable("cOrd");
        if (var_zeroOrd === undefined) raiseUninitializedVariable("zeroOrd");
        var opresult1040 = request(var_cOrd, "\u2265(1)", [1], var_zeroOrd);
        if (var_cOrd === undefined) raiseUninitializedVariable("cOrd");
        // call case 6: other requests
        var string1042 = new GraceString("9");
        var call1041 = request(string1042, "ord", [0]);
        var opresult1043 = request(var_cOrd, "\u2264(1)", [1], call1041);
        var opresult1044 = request(opresult1040, "&&(1)", [1], opresult1043);
        if (Grace_isTrue(opresult1044)) {
          setLineNumber(1366);    // compilenode op
          if (var_cOrd === undefined) raiseUninitializedVariable("cOrd");
          if (var_zeroOrd === undefined) raiseUninitializedVariable("zeroOrd");
          var diff1045 = request(var_cOrd, "-(1)", [1], var_zeroOrd);
          if1039 = diff1045;
        } else {
          setLineNumber(1368);    // compilenode string
          var string1047 = new GraceString("the character '");
          var opresult1048 = request(string1047, "++(1)", [1], var_c);
          var string1049 = new GraceString("' must be a hexadecimal digit");
          var opresult1050 = request(opresult1048, "++(1)", [1], string1049);
          if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call1046 = request(var_errormessages, "syntaxError(1)atPosition(2)", [1, 2], opresult1050, var_lineNumber, var_linePosition);
          if1039 = call1046;
        }
        if1031 = if1039;
      }
      if1023 = if1031;
    }
    return if1023;
  };    // end of method hexdecchar(_)
  this.methods["hexdecchar(1)"] = func1015;
  func1015.methodName = "hexdecchar(1)";
  func1015.paramCounts = [1];
  func1015.paramNames = ["c"];
  func1015.definitionLine = 1355;
  func1015.definitionModule = "lexer";
  var func1051 = function(argcv, var_file) {    // method lexfile(_), line 1373
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("lexfile(_)", 0, numArgs - 1);
    }
    setLineNumber(1376);    // compilenode member
    // call case 6: other requests
    if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
    var call1052 = request(var_inputLines, "clear", [0]);
    setLineNumber(1377);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    if (var_util === undefined) raiseUninitializedVariable("util");
    var call1054 = request(var_util, "cLines", [0]);
    var call1053 = request(call1054, "clear", [0]);
    var var_cLines = call1053;
    setLineNumber(1381);    // compilenode string
    var string1055 = new GraceString("");
    var var_line = string1055;
    setLineNumber(1382);    // compilenode string
    var string1056 = new GraceString("");
    var var_cLine = string1056;
    setLineNumber(1383);    // compilenode vardec
    var var_wasCR = GraceFalse;
    setLineNumber(1384);    // compilenode member
    // call case 6: other requests
    var call1058 = request(var_file, "read", [0]);
    var block1059 = new GraceBlock(this, 1384, 1);
    block1059.guard = jsTrue;
    block1059.real = function block1059(var_c) {
      var if1060 = GraceDone;
      setLineNumber(1385);    // compilenode string
      var string1061 = new GraceString("\u000d");
      var opresult1062 = request(var_c, "==(1)", [1], string1061);
      if (Grace_isTrue(opresult1062)) {
        setLineNumber(1386);    // compilenode call
        if (var_line === undefined) raiseUninitializedVariable("line");
        // call case 6: other requests
        if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
        var call1063 = request(var_inputLines, "push(1)", [1], var_line);
        setLineNumber(1387);    // compilenode call
        if (var_cLine === undefined) raiseUninitializedVariable("cLine");
        // call case 6: other requests
        if (var_cLines === undefined) raiseUninitializedVariable("cLines");
        var call1064 = request(var_cLines, "push(1)", [1], var_cLine);
        setLineNumber(1388);    // compilenode string
        var string1065 = new GraceString("");
        var_line = string1065;
        setLineNumber(1389);    // compilenode string
        var string1066 = new GraceString("");
        var_cLine = string1066;
        setLineNumber(1390);    // compilenode bind
        var_wasCR = GraceTrue;
        if1060 = GraceDone;
      } else {
        var if1067 = GraceDone;
        setLineNumber(1391);    // compilenode string
        var string1068 = new GraceString("\n");
        var opresult1069 = request(var_c, "==(1)", [1], string1068);
        if (Grace_isTrue(opresult1069)) {
          var if1070 = GraceDone;
          setLineNumber(1392);    // compilenode if
          if (var_wasCR === undefined) raiseUninitializedVariable("wasCR");
          if (Grace_isTrue(var_wasCR)) {
            if1070 = GraceDone;
          } else {
            setLineNumber(1396);    // compilenode call
            if (var_line === undefined) raiseUninitializedVariable("line");
            // call case 6: other requests
            if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
            var call1071 = request(var_inputLines, "push(1)", [1], var_line);
            setLineNumber(1397);    // compilenode call
            if (var_cLine === undefined) raiseUninitializedVariable("cLine");
            // call case 6: other requests
            if (var_cLines === undefined) raiseUninitializedVariable("cLines");
            var call1072 = request(var_cLines, "push(1)", [1], var_cLine);
            setLineNumber(1398);    // compilenode string
            var string1073 = new GraceString("");
            var_line = string1073;
            setLineNumber(1399);    // compilenode string
            var string1074 = new GraceString("");
            var_cLine = string1074;
            if1070 = GraceDone;
          }
          setLineNumber(1401);    // compilenode bind
          var_wasCR = GraceFalse;
          if1067 = GraceDone;
        } else {
          var if1075 = GraceDone;
          setLineNumber(1402);    // compilenode string
          var string1076 = new GraceString("\u2028");
          var opresult1077 = request(var_c, "==(1)", [1], string1076);
          if (Grace_isTrue(opresult1077)) {
            setLineNumber(1403);    // compilenode call
            if (var_line === undefined) raiseUninitializedVariable("line");
            // call case 6: other requests
            if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
            var call1078 = request(var_inputLines, "push(1)", [1], var_line);
            setLineNumber(1404);    // compilenode call
            if (var_cLine === undefined) raiseUninitializedVariable("cLine");
            // call case 6: other requests
            if (var_cLines === undefined) raiseUninitializedVariable("cLines");
            var call1079 = request(var_cLines, "push(1)", [1], var_cLine);
            setLineNumber(1405);    // compilenode string
            var string1080 = new GraceString("");
            var_line = string1080;
            setLineNumber(1406);    // compilenode string
            var string1081 = new GraceString("");
            var_cLine = string1081;
            setLineNumber(1407);    // compilenode bind
            var_wasCR = GraceFalse;
            if1075 = GraceDone;
          } else {
            var if1082 = GraceDone;
            setLineNumber(1408);    // compilenode string
            var string1083 = new GraceString("\\");
            var opresult1084 = request(var_c, "==(1)", [1], string1083);
            if (Grace_isTrue(opresult1084)) {
              setLineNumber(1409);    // compilenode op
              if (var_line === undefined) raiseUninitializedVariable("line");
              var opresult1085 = request(var_line, "++(1)", [1], var_c);
              var_line = opresult1085;
              setLineNumber(1410);    // compilenode op
              if (var_cLine === undefined) raiseUninitializedVariable("cLine");
              var string1086 = new GraceString("\\\\");
              var opresult1087 = request(var_cLine, "++(1)", [1], string1086);
              var_cLine = opresult1087;
              setLineNumber(1411);    // compilenode bind
              var_wasCR = GraceFalse;
              if1082 = GraceDone;
            } else {
              var if1088 = GraceDone;
              setLineNumber(1412);    // compilenode string
              var string1089 = new GraceString("\"");
              var opresult1090 = request(var_c, "==(1)", [1], string1089);
              if (Grace_isTrue(opresult1090)) {
                setLineNumber(1413);    // compilenode op
                if (var_line === undefined) raiseUninitializedVariable("line");
                var opresult1091 = request(var_line, "++(1)", [1], var_c);
                var_line = opresult1091;
                setLineNumber(1414);    // compilenode op
                if (var_cLine === undefined) raiseUninitializedVariable("cLine");
                var string1092 = new GraceString("\\\"");
                var opresult1093 = request(var_cLine, "++(1)", [1], string1092);
                var_cLine = opresult1093;
                setLineNumber(1415);    // compilenode bind
                var_wasCR = GraceFalse;
                if1088 = GraceDone;
              } else {
                setLineNumber(1417);    // compilenode op
                if (var_line === undefined) raiseUninitializedVariable("line");
                var opresult1094 = request(var_line, "++(1)", [1], var_c);
                var_line = opresult1094;
                setLineNumber(1418);    // compilenode op
                if (var_cLine === undefined) raiseUninitializedVariable("cLine");
                var opresult1095 = request(var_cLine, "++(1)", [1], var_c);
                var_cLine = opresult1095;
                setLineNumber(1419);    // compilenode bind
                var_wasCR = GraceFalse;
                if1088 = GraceDone;
              }
              if1082 = if1088;
            }
            if1075 = if1082;
          }
          if1067 = if1075;
        }
        if1060 = if1067;
      }
      return if1060;
    };
    let applyMeth1059 = function apply_1 (argcv, ...args) {
        if (this.guard.apply(this.receiver, args))
            return this.real.apply(this.receiver, args);
        badBlockArgs.apply(this, args);
    };
    applyMeth1059.methodName = "apply(1)";
    applyMeth1059.paramCounts = [1];
    applyMeth1059.paramNames = ["c"];
    applyMeth1059.definitionLine = 1384;
    applyMeth1059.definitionModule = "lexer";
    block1059.methods["apply(1)"] = applyMeth1059;
    let matchesMeth1059 = function matches_1 (argcv, ...args) {
        return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
    };
    matchesMeth1059.methodName = "matches(1)";
    matchesMeth1059.paramCounts = [1];
    matchesMeth1059.paramNames = ["c"];
    matchesMeth1059.definitionLine = 1384;
    matchesMeth1059.definitionModule = "lexer";
    block1059.methods["matches(1)"] = matchesMeth1059;
    // call case 2: outer request
    var call1057 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call1058, block1059);
    var if1096 = GraceDone;
    setLineNumber(1422);    // compilenode string
    var string1097 = new GraceString("");
    var opresult1098 = request(var_line, "\u2260(1)", [1], string1097);
    if (Grace_isTrue(opresult1098)) {
      setLineNumber(1424);    // compilenode call
      if (var_line === undefined) raiseUninitializedVariable("line");
      // call case 6: other requests
      if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
      var call1099 = request(var_inputLines, "push(1)", [1], var_line);
      setLineNumber(1425);    // compilenode call
      if (var_cLine === undefined) raiseUninitializedVariable("cLine");
      // call case 6: other requests
      if (var_cLines === undefined) raiseUninitializedVariable("cLines");
      var call1100 = request(var_cLines, "push(1)", [1], var_cLine);
      if1096 = call1100;
    }
    setLineNumber(1427);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call1101 = selfRequest(this, "lexInputLines", [0]);
    return call1101;
  };    // end of method lexfile(_)
  this.methods["lexfile(1)"] = func1051;
  func1051.methodName = "lexfile(1)";
  func1051.paramCounts = [1];
  func1051.paramNames = ["file"];
  func1051.definitionLine = 1373;
  func1051.definitionModule = "lexer";
  var func1102 = function(argcv, var_inputString) {    // method lexString(_), line 1430
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("lexString(_)", 0, numArgs - 1);
    }
    setLineNumber(1432);    // compilenode string
    var string1105 = new GraceString("\n");
    // call case 6: other requests
    var call1104 = request(var_inputString, "split(1)", [1], string1105);
    // call case 2: outer request
    var call1103 = selfRequest(importedModules["lexer"], "lexLines(1)", [1], call1104);
    return call1103;
  };    // end of method lexString(_)
  this.methods["lexString(1)"] = func1102;
  func1102.methodName = "lexString(1)";
  func1102.paramCounts = [1];
  func1102.paramNames = ["inputString"];
  func1102.definitionLine = 1430;
  func1102.definitionModule = "lexer";
  var func1106 = function(argcv, var_input) {    // method lexLines(_), line 1435
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("lexLines(_)", 0, numArgs - 1);
    }
    setLineNumber(1436);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    if (var_util === undefined) raiseUninitializedVariable("util");
    var call1108 = request(var_util, "lines", [0]);
    var call1107 = request(call1108, "copy", [0]);
    var var_oldInput = call1107;
    setLineNumber(1437);    // compilenode block
    var block1110 = new GraceBlock(this, 1437, 0);
    block1110.guard = jsTrue;
    block1110.real = function block1110() {
      setLineNumber(1438);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_util === undefined) raiseUninitializedVariable("util");
      var call1112 = request(var_util, "lines", [0]);
      var call1111 = request(call1112, "clear", [0]);
      setLineNumber(1439);    // compilenode call
      // call case 6: other requests
      // call case 6: other requests
      if (var_util === undefined) raiseUninitializedVariable("util");
      var call1114 = request(var_util, "lines", [0]);
      var call1113 = request(call1114, "addAll(1)", [1], var_input);
      setLineNumber(1440);    // compilenode member
      // call case 4: self request with 0 args and 0 typeArgs 
      var call1115 = selfRequest(this, "initialize", [0]);
      setLineNumber(1441);    // compilenode member
      // call case 4: self request with 0 args and 0 typeArgs 
      var call1116 = selfRequest(this, "lexInputLines", [0]);
      return call1116;
    };
    let applyMeth1110 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth1110.methodName = "apply";
    applyMeth1110.paramCounts = [0];
    applyMeth1110.paramNames = [];
    applyMeth1110.definitionLine = 1437;
    applyMeth1110.definitionModule = "lexer";
    block1110.methods["apply"] = applyMeth1110;
    var cases1109 = [];
    setLineNumber(1442);    // compilenode block
    var block1117 = new GraceBlock(this, 1442, 0);
    block1117.guard = jsTrue;
    block1117.real = function block1117() {
      setLineNumber(1443);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      if (var_util === undefined) raiseUninitializedVariable("util");
      var call1119 = request(var_util, "lines", [0]);
      var call1118 = request(call1119, "clear", [0]);
      setLineNumber(1444);    // compilenode call
      if (var_oldInput === undefined) raiseUninitializedVariable("oldInput");
      // call case 6: other requests
      // call case 6: other requests
      if (var_util === undefined) raiseUninitializedVariable("util");
      var call1121 = request(var_util, "lines", [0]);
      var call1120 = request(call1121, "addAll(1)", [1], var_oldInput);
      return call1120;
    };
    let applyMeth1117 = function apply (argcv) {
        return this.real.apply(this.receiver);
    };
    applyMeth1117.methodName = "apply";
    applyMeth1117.paramCounts = [0];
    applyMeth1117.paramNames = [];
    applyMeth1117.definitionLine = 1442;
    applyMeth1117.definitionModule = "lexer";
    block1117.methods["apply"] = applyMeth1117;
    setLineNumber(1437);    // compiletrycatch
    var catchres1109 = tryCatch(block1110,cases1109,block1117);
    return catchres1109;
  };    // end of method lexLines(_)
  this.methods["lexLines(1)"] = func1106;
  func1106.methodName = "lexLines(1)";
  func1106.paramCounts = [1];
  func1106.paramNames = ["input"];
  func1106.definitionLine = 1435;
  func1106.definitionModule = "lexer";
  var func1122 = function(argcv) {    // method lexInputLines, line 1448
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("lexInputLines", 0, numArgs - 0);
    }
    setLineNumber(1450);    // compilenode object
    var obj1123_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_lexer_1450");
      this.outer_lexer_1450 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      this.data.header = undefined;
      var reader1124_header = function() {  // reader method header
          if (this.data.header === undefined) raiseUninitializedVariable("header");
          return this.data.header;
      };
      reader1124_header.isDef = true;
      reader1124_header.confidential = true;
      this.methods["header"] = reader1124_header;
      this.data.last = undefined;
      var reader1125_last = function() {  // reader method last
          if (this.data.last === undefined) raiseUninitializedVariable("last");
          return this.data.last;
      };
      reader1125_last.isVar = true;
      this.methods["last"] = reader1125_last;
      var writer1126_last = function(argcv, n) {   // writer method last:=(_)
        this.data.last = n;
        return GraceDone;
      };
      writer1126_last.confidential = true;
      this.methods["last:=(1)"] = writer1126_last;
      this.data.size = undefined;
      var reader1127_size = function() {  // reader method size
          if (this.data.size === undefined) raiseUninitializedVariable("size");
          return this.data.size;
      };
      reader1127_size.isVar = true;
      this.methods["size"] = reader1127_size;
      var writer1128_size = function(argcv, n) {   // writer method size:=(_)
        this.data.size = n;
        return GraceDone;
      };
      writer1128_size.confidential = true;
      this.methods["size:=(1)"] = writer1128_size;
      var func1129 = function(argcv) {    // method first, line 1456
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("first", 0, numArgs - 0);
        }
        setLineNumber(1456);    // compilenode member
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1131 = selfRequest(this, "header", [0]);
        var call1130 = request(call1131, "next", [0]);
        return call1130;
      };    // end of method first
      this.methods["first"] = func1129;
      func1129.methodName = "first";
      func1129.paramCounts = [0];
      func1129.paramNames = [];
      func1129.definitionLine = 1456;
      func1129.definitionModule = "lexer";
      var func1132 = function(argcv, var_nu) {    // method first:=(_), line 1457
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("first:=(_)", 0, numArgs - 1);
        }
        setLineNumber(1457);    // compilenode call
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1134 = selfRequest(this, "header", [0]);
        var call1133 = selfRequest(call1134, "next:=(1)", [1], var_nu);
        return call1133;
      };    // end of method first:=(_)
      func1132.confidential = true;
      this.methods["first:=(1)"] = func1132;
      func1132.methodName = "first:=(1)";
      func1132.paramCounts = [1];
      func1132.paramNames = ["nu"];
      func1132.definitionLine = 1457;
      func1132.definitionModule = "lexer";
      var func1135 = function(argcv) {    // method isEmpty, line 1458
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("isEmpty", 0, numArgs - 0);
        }
        setLineNumber(1458);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1136 = selfRequest(this, "header", [0]);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1137 = selfRequest(this, "last", [0]);
        var opresult1138 = request(call1136, "==(1)", [1], call1137);
        return opresult1138;
      };    // end of method isEmpty
      this.methods["isEmpty"] = func1135;
      func1135.methodName = "isEmpty";
      func1135.paramCounts = [0];
      func1135.paramNames = [];
      func1135.definitionLine = 1458;
      func1135.definitionModule = "lexer";
      var func1139 = function(argcv) {    // method savePosition, line 1460
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("savePosition", 0, numArgs - 0);
        }
        setLineNumber(1461);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1141 = selfRequest(this, "first", [0]);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1142 = selfRequest(this, "last", [0]);
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1143 = selfRequest(this, "size", [0]);
        var array1140 = new GraceSequence([call1141, call1142, call1143]);
        return array1140;
      };    // end of method savePosition
      this.methods["savePosition"] = func1139;
      func1139.methodName = "savePosition";
      func1139.paramCounts = [0];
      func1139.paramNames = [];
      func1139.definitionLine = 1460;
      func1139.definitionModule = "lexer";
      var func1144 = function(argcv, var_saved) {    // method restorePosition(_), line 1464
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("restorePosition(_)", 0, numArgs - 1);
        }
        setLineNumber(1465);    // compilenode member
        // call case 6: other requests
        var call1146 = request(var_saved, "first", [0]);
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1145 = selfRequest(this, "first:=(1)", [1], call1146);
        setLineNumber(1466);    // compilenode member
        // call case 6: other requests
        var call1148 = request(var_saved, "second", [0]);
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1147 = selfRequest(this, "last:=(1)", [1], call1148);
        setLineNumber(1467);    // compilenode member
        // call case 6: other requests
        var call1150 = request(var_saved, "third", [0]);
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1149 = selfRequest(this, "size:=(1)", [1], call1150);
        return call1149;
      };    // end of method restorePosition(_)
      this.methods["restorePosition(1)"] = func1144;
      func1144.methodName = "restorePosition(1)";
      func1144.paramCounts = [1];
      func1144.paramNames = ["saved"];
      func1144.definitionLine = 1464;
      func1144.definitionModule = "lexer";
      var func1151 = function(argcv, var_t) {    // method push(_), line 1470
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("push(_)", 0, numArgs - 1);
        }
        setLineNumber(1473);    // compilenode call
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1153 = selfRequest(this, "last", [0]);
        var call1152 = selfRequest(call1153, "next:=(1)", [1], var_t);
        setLineNumber(1474);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1155 = selfRequest(this, "last", [0]);
        // call case 6: other requests
        var call1154 = request(var_t, "prev:=(1)", [1], call1155);
        var if1156 = GraceDone;
        setLineNumber(1475);    // compilenode member
        // call case 6: other requests
        // call case 6: other requests
        var call1158 = request(var_t, "indent", [0]);
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1160 = selfRequest(this, "last", [0]);
        var call1159 = request(call1160, "indent", [0]);
        var diff1161 = request(call1158, "-(1)", [1], call1159);
        var call1157 = request(diff1161, "abs", [0]);
        var opresult1162 = request(call1157, "==(1)", [1], new GraceNum(1));
        if (Grace_isTrue(opresult1162)) {
          setLineNumber(1476);    // compilenode string
          var string1163 = new GraceString("the indentation for this line can't differ ");
          var var_m1 = string1163;
          setLineNumber(1477);    // compilenode string
          var string1164 = new GraceString("from that of the previous line by 1.\n  To start a block, or ");
          var var_m2 = string1164;
          setLineNumber(1478);    // compilenode string
          var string1165 = new GraceString("continue a statement on the next line line, indent by 2 or more. ");
          var var_m3 = string1165;
          setLineNumber(1479);    // compilenode string
          var string1166 = new GraceString("To end a block, or end the continuation, decrease the indent ");
          var var_m4 = string1166;
          setLineNumber(1480);    // compilenode string
          var string1167 = new GraceString("to the prior level. ");
          var var_m5 = string1167;
          setLineNumber(1481);    // compilenode string
          var string1168 = new GraceString("Otherwise, use the same indentation as the previous line");
          var var_m6 = string1168;
          setLineNumber(1482);    // compilenode op
          if (var_m1 === undefined) raiseUninitializedVariable("m1");
          if (var_m2 === undefined) raiseUninitializedVariable("m2");
          var opresult1169 = request(var_m1, "++(1)", [1], var_m2);
          if (var_m3 === undefined) raiseUninitializedVariable("m3");
          var opresult1170 = request(opresult1169, "++(1)", [1], var_m3);
          if (var_m4 === undefined) raiseUninitializedVariable("m4");
          var opresult1171 = request(opresult1170, "++(1)", [1], var_m4);
          if (var_m5 === undefined) raiseUninitializedVariable("m5");
          var opresult1172 = request(opresult1171, "++(1)", [1], var_m5);
          if (var_m6 === undefined) raiseUninitializedVariable("m6");
          var opresult1173 = request(opresult1172, "++(1)", [1], var_m6);
          var var_msg = opresult1173;
          setLineNumber(1483);    // compilenode call
          if (var_msg === undefined) raiseUninitializedVariable("msg");
          // call case 6: other requests
          var call1175 = request(var_t, "line", [0]);
          // call case 6: other requests
          var call1176 = request(var_t, "indent", [0]);
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call1174 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], var_msg, call1175, new GraceNum(1), call1176);
          if1156 = call1174;
        }
        setLineNumber(1485);    // compilenode call
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1177 = selfRequest(this, "last:=(1)", [1], var_t);
        setLineNumber(1486);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1179 = selfRequest(this, "size", [0]);
        var sum1180 = request(call1179, "+(1)", [1], new GraceNum(1));
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1178 = selfRequest(this, "size:=(1)", [1], sum1180);
        return call1178;
      };    // end of method push(_)
      this.methods["push(1)"] = func1151;
      func1151.methodName = "push(1)";
      func1151.paramCounts = [1];
      func1151.paramNames = ["t"];
      func1151.definitionLine = 1470;
      func1151.definitionModule = "lexer";
      var func1181 = function(argcv) {    // method pop, line 1489
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("pop", 0, numArgs - 0);
        }
        var if1182 = GraceDone;
        setLineNumber(1491);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1183 = selfRequest(this, "isEmpty", [0]);
        if (Grace_isTrue(call1183)) {
          setLineNumber(1492);    // compilenode string
          var string1185 = new GraceString("attempt to pop from an empty token list");
          // call case 6: other requests
          // call case 2: outer request
          var call1186 = selfRequest(var_prelude, "ProgrammingError", [0]);
          var call1184 = request(call1186, "raise(1)", [1], string1185);
          if1182 = call1184;
        }
        setLineNumber(1494);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1187 = selfRequest(this, "last", [0]);
        var var_popped = call1187;
        setLineNumber(1495);    // compilenode member
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1190 = selfRequest(this, "last", [0]);
        var call1189 = request(call1190, "prev", [0]);
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1188 = selfRequest(this, "last:=(1)", [1], call1189);
        setLineNumber(1496);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1192 = selfRequest(this, "size", [0]);
        var diff1193 = request(call1192, "-(1)", [1], new GraceNum(1));
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1191 = selfRequest(this, "size:=(1)", [1], diff1193);
        return var_popped;
      };    // end of method pop
      this.methods["pop"] = func1181;
      func1181.methodName = "pop";
      func1181.paramCounts = [0];
      func1181.paramNames = [];
      func1181.definitionLine = 1489;
      func1181.definitionModule = "lexer";
      var func1194 = function(argcv) {    // method poll, line 1500
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("poll", 0, numArgs - 0);
        }
        var if1195 = GraceDone;
        setLineNumber(1502);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1196 = selfRequest(this, "isEmpty", [0]);
        if (Grace_isTrue(call1196)) {
          setLineNumber(1503);    // compilenode string
          var string1198 = new GraceString("attempt to poll from an empty token list");
          // call case 6: other requests
          // call case 2: outer request
          var call1199 = selfRequest(var_prelude, "ProgrammingError", [0]);
          var call1197 = request(call1199, "raise(1)", [1], string1198);
          if1195 = call1197;
        }
        setLineNumber(1505);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1200 = selfRequest(this, "first", [0]);
        var var_polled = call1200;
        setLineNumber(1506);    // compilenode member
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1203 = selfRequest(this, "first", [0]);
        var call1202 = request(call1203, "next", [0]);
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1201 = selfRequest(this, "first:=(1)", [1], call1202);
        setLineNumber(1507);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1205 = selfRequest(this, "size", [0]);
        var diff1206 = request(call1205, "-(1)", [1], new GraceNum(1));
        // call case 4: self request with 1 args and 0 typeArgs 
        var call1204 = selfRequest(this, "size:=(1)", [1], diff1206);
        return var_polled;
      };    // end of method poll
      this.methods["poll"] = func1194;
      func1194.methodName = "poll";
      func1194.paramCounts = [0];
      func1194.paramNames = [];
      func1194.definitionLine = 1500;
      func1194.definitionModule = "lexer";
      var func1207 = function(argcv) {    // method iterator, line 1511
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("iterator", 0, numArgs - 0);
        }
        var ouc = emptyGraceObject("iterator", "lexer", 1511);
        var ouc_init = this.methods["iterator$build(3)"].call(this, null, ouc, [], []);
        ouc_init.call(ouc);
        return ouc;
      };    // end of method iterator
      this.methods["iterator"] = func1207;
      func1207.methodName = "iterator";
      func1207.paramCounts = [0];
      func1207.paramNames = [];
      func1207.definitionLine = 1511;
      func1207.definitionModule = "lexer";
      var func1208 = function(argcv, inheritingObject, aliases, exclusions) {    // method iterator$build(_,_,_), line 1511
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 4;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("iterator", 0, numArgs - 0);
        }
        var obj1209_build = function(ignore, outerObj, aliases, exclusions) {
          this.closureKeys = this.closureKeys || [];
          this.closureKeys.push("outer_lexer_1511");
          this.outer_lexer_1511 = outerObj;
          const inheritedExclusions = { };
          for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
              const exMeth = exclusions[eix];
              inheritedExclusions[exMeth] = this.methods[exMeth]; };
          this.data.n = undefined;
          var reader1210_n = function() {  // reader method n
              if (this.data.n === undefined) raiseUninitializedVariable("n");
              return this.data.n;
          };
          reader1210_n.isVar = true;
          reader1210_n.confidential = true;
          this.methods["n"] = reader1210_n;
          var writer1211_n = function(argcv, n) {   // writer method n:=(_)
            this.data.n = n;
            return GraceDone;
          };
          writer1211_n.confidential = true;
          this.methods["n:=(1)"] = writer1211_n;
          var func1212 = function(argcv) {    // method hasNext, line 1513
            var returnTarget = invocationCount;
            invocationCount++;
            const numArgs = arguments.length - 1;
            if ((numArgs > 0) && (numArgs !== 0)) {
                raiseTypeArgError("hasNext", 0, numArgs - 0);
            }
            setLineNumber(1514);    // compilenode member
            // call case 2: outer request
            var call1213 = selfRequest(this.outer_lexer_1511, "header", [0]);
            // call case 4: self request with 0 args and 0 typeArgs 
            var call1214 = selfRequest(this, "n", [0]);
            var opresult1215 = request(call1213, "\u2260(1)", [1], call1214);
            return opresult1215;
          };    // end of method hasNext
          this.methods["hasNext"] = func1212;
          func1212.methodName = "hasNext";
          func1212.paramCounts = [0];
          func1212.paramNames = [];
          func1212.definitionLine = 1513;
          func1212.definitionModule = "lexer";
          var func1216 = function(argcv) {    // method next, line 1516
            var returnTarget = invocationCount;
            invocationCount++;
            const numArgs = arguments.length - 1;
            if ((numArgs > 0) && (numArgs !== 0)) {
                raiseTypeArgError("next", 0, numArgs - 0);
            }
            setLineNumber(1517);    // compilenode member
            // call case 4: self request with 0 args and 0 typeArgs 
            var call1217 = selfRequest(this, "n", [0]);
            var var_ret = call1217;
            setLineNumber(1518);    // compilenode member
            // call case 6: other requests
            // call case 4: self request with 0 args and 0 typeArgs 
            var call1220 = selfRequest(this, "n", [0]);
            var call1219 = request(call1220, "next", [0]);
            // call case 4: self request with 1 args and 0 typeArgs 
            var call1218 = selfRequest(this, "n:=(1)", [1], call1219);
            return var_ret;
          };    // end of method next
          this.methods["next"] = func1216;
          func1216.methodName = "next";
          func1216.paramCounts = [0];
          func1216.paramNames = [];
          func1216.definitionLine = 1516;
          func1216.definitionModule = "lexer";
          this.mutable = true;
          const overridenByAliases = { };
          for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
              const a = aliases[aix];
              const newNm = a.newName;
              const oldNm = a.oldName;
              overridenByAliases[newNm] = this.methods[newNm];
              const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
              m.definitionLine = 1511;
              m.definitionModule = "lexer";
              this.methods[newNm] = m;
          }
          for (let exName in inheritedExclusions) {
              if (inheritedExclusions.hasOwnProperty(exName)) {
                  if (inheritedExclusions[exName]) {
                      this.methods[exName] = inheritedExclusions[exName];
                  } else {
                      delete this.methods[exName];
                  }
              }
          }
          var obj1209_init = function() {    // init of object on line 1511
            setLineNumber(1512);    // compilenode member
            // call case 2: outer request
            var call1221 = selfRequest(this.outer_lexer_1511, "first", [0]);
            this.data.n = call1221;
          };
          return obj1209_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
        };
        var obj1209_init = obj1209_build.call(inheritingObject, null, this, aliases, exclusions);
        return obj1209_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
      };    // end of method iterator$build(_,_,_)
      this.methods["iterator$build(3)"] = func1208;
      func1208.methodName = "iterator$build(3)";
      func1208.paramCounts = [0];
      func1208.paramNames = [];
      func1208.definitionLine = 1511;
      func1208.definitionModule = "lexer";
      var func1222 = function(argcv, var_action) {    // method do(_), line 1523
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("do(_)", 0, numArgs - 1);
        }
        setLineNumber(1524);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1223 = selfRequest(this, "first", [0]);
        var var_n = call1223;
        setLineNumber(1525);    // compilenode block
        var block1225 = new GraceBlock(this, 1525, 0);
        block1225.guard = jsTrue;
        block1225.real = function block1225() {
          setLineNumber(1525);    // compilenode member
          // call case 6: other requests
          // call case 6: other requests
          if (var_n === undefined) raiseUninitializedVariable("n");
          var call1227 = request(var_n, "isHeader", [0]);
          var call1226 = request(call1227, "not", [0]);
          return call1226;
        };
        let applyMeth1225 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth1225.methodName = "apply";
        applyMeth1225.paramCounts = [0];
        applyMeth1225.paramNames = [];
        applyMeth1225.definitionLine = 1525;
        applyMeth1225.definitionModule = "lexer";
        block1225.methods["apply"] = applyMeth1225;
        var block1228 = new GraceBlock(this, 1525, 0);
        block1228.guard = jsTrue;
        block1228.real = function block1228() {
          setLineNumber(1526);    // compilenode call
          if (var_n === undefined) raiseUninitializedVariable("n");
          // call case 6: other requests
          var call1229 = request(var_action, "apply(1)", [1], var_n);
          setLineNumber(1527);    // compilenode member
          // call case 6: other requests
          if (var_n === undefined) raiseUninitializedVariable("n");
          var call1230 = request(var_n, "next", [0]);
          var_n = call1230;
          return GraceDone;
        };
        let applyMeth1228 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth1228.methodName = "apply";
        applyMeth1228.paramCounts = [0];
        applyMeth1228.paramNames = [];
        applyMeth1228.definitionLine = 1525;
        applyMeth1228.definitionModule = "lexer";
        block1228.methods["apply"] = applyMeth1228;
        // call case 2: outer request
        var call1224 = selfRequest(var_prelude, "while(1)do(1)", [1, 1], block1225, block1228);
        return call1224;
      };    // end of method do(_)
      this.methods["do(1)"] = func1222;
      func1222.methodName = "do(1)";
      func1222.paramCounts = [1];
      func1222.paramNames = ["action"];
      func1222.definitionLine = 1523;
      func1222.definitionModule = "lexer";
      this.mutable = true;
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 1450;
          m.definitionModule = "lexer";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj1123_init = function() {    // init of object on line 1450
        setLineNumber(1451);    // compilenode member
        // call case 2: outer request
        var call1231 = selfRequest(importedModules["lexer"], "sofToken", [0]);
        this.data.header = call1231;
        setLineNumber(1452);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1233 = selfRequest(this, "header", [0]);
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1234 = selfRequest(this, "header", [0]);
        var call1232 = selfRequest(call1234, "next:=(1)", [1], call1233);
        setLineNumber(1453);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call1235 = selfRequest(this, "header", [0]);
        this.data.last = call1235;
        setLineNumber(1454);    // compilenode num
        this.data.size = new GraceNum(0);
      };
      return obj1123_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj1123 = emptyGraceObject("object", "lexer", 1450);
    var obj1123_init = obj1123_build.call(obj1123, null, this, [], []);
    obj1123_init.call(obj1123);  // end of compileobject
    var_tokens = obj1123;
    setLineNumber(1533);    // compilenode num
    var_linePosition = new GraceNum(0);
    setLineNumber(1534);    // compilenode string
    var string1237 = new GraceString("lexing.");
    // call case 6: other requests
    if (var_util === undefined) raiseUninitializedVariable("util");
    var call1236 = request(var_util, "log_verbose(1)", [1], string1237);
    setLineNumber(1535);    // compilenode bind
    if (var_startState === undefined) raiseUninitializedVariable("startState");
    var_state = var_startState;
    setLineNumber(1536);    // compilenode call
    if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
    var block1239 = new GraceBlock(this, 1536, 1);
    block1239.guard = jsTrue;
    block1239.real = function block1239(var_eachLine) {
      setLineNumber(1537);    // compilenode member
      // call case 6: other requests
      var call1240 = request(var_eachLine, "iterator", [0]);
      var var_charS = call1240;
      setLineNumber(1538);    // compilenode block
      var block1242 = new GraceBlock(this, 1538, 0);
      block1242.guard = jsTrue;
      block1242.real = function block1242() {
        setLineNumber(1538);    // compilenode member
        // call case 6: other requests
        if (var_charS === undefined) raiseUninitializedVariable("charS");
        var call1243 = request(var_charS, "hasNext", [0]);
        return call1243;
      };
      let applyMeth1242 = function apply (argcv) {
          return this.real.apply(this.receiver);
      };
      applyMeth1242.methodName = "apply";
      applyMeth1242.paramCounts = [0];
      applyMeth1242.paramNames = [];
      applyMeth1242.definitionLine = 1538;
      applyMeth1242.definitionModule = "lexer";
      block1242.methods["apply"] = applyMeth1242;
      var block1244 = new GraceBlock(this, 1538, 0);
      block1244.guard = jsTrue;
      block1244.real = function block1244() {
        setLineNumber(1539);    // compilenode member
        // call case 6: other requests
        if (var_charS === undefined) raiseUninitializedVariable("charS");
        var call1245 = request(var_charS, "next", [0]);
        var var_ch = call1245;
        setLineNumber(1540);    // compilenode op
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        var sum1246 = request(var_linePosition, "+(1)", [1], new GraceNum(1));
        var_linePosition = sum1246;
        var if1247 = GraceDone;
        setLineNumber(1541);    // compilenode member
        // call case 6: other requests
        if (var_inStr === undefined) raiseUninitializedVariable("inStr");
        var call1248 = request(var_inStr, "not", [0]);
        var block1249 = new GraceBlock(this, 1541, 0);
        block1249.guard = jsTrue;
        block1249.real = function block1249() {
          setLineNumber(1541);    // compilenode call
          if (var_ch === undefined) raiseUninitializedVariable("ch");
          // call case 6: other requests
          var string1251 = new GraceString("><![]-");
          var call1250 = request(string1251, "contains(1)", [1], var_ch);
          return call1250;
        };
        let applyMeth1249 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth1249.methodName = "apply";
        applyMeth1249.paramCounts = [0];
        applyMeth1249.paramNames = [];
        applyMeth1249.definitionLine = 1541;
        applyMeth1249.definitionModule = "lexer";
        block1249.methods["apply"] = applyMeth1249;
        var opresult1252 = request(call1248, "&&(1)", [1], block1249);
        // call case 6: other requests
        if (var_charS === undefined) raiseUninitializedVariable("charS");
        var call1253 = request(var_charS, "hasNext", [0]);
        var opresult1254 = request(opresult1252, "&&(1)", [1], call1253);
        if (Grace_isTrue(opresult1254)) {
          setLineNumber(1543);    // compilenode member
          // call case 6: other requests
          if (var_charS === undefined) raiseUninitializedVariable("charS");
          var call1255 = request(var_charS, "next", [0]);
          var var_nextCh = call1255;
          setLineNumber(1544);    // compilenode op
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          var sum1256 = request(var_linePosition, "+(1)", [1], new GraceNum(1));
          var_linePosition = sum1256;
          setLineNumber(1545);    // compilenode op
          if (var_ch === undefined) raiseUninitializedVariable("ch");
          if (var_nextCh === undefined) raiseUninitializedVariable("nextCh");
          var opresult1257 = request(var_ch, "++(1)", [1], var_nextCh);
          var var_bigraph = opresult1257;
          var if1258 = GraceDone;
          setLineNumber(1546);    // compilenode op
          if (var_bigraph === undefined) raiseUninitializedVariable("bigraph");
          var string1259 = new GraceString(">=");
          var opresult1260 = request(var_bigraph, "==(1)", [1], string1259);
          if (Grace_isTrue(opresult1260)) {
            var string1262 = new GraceString("\u2265");
            // call case 6: other requests
            if (var_state === undefined) raiseUninitializedVariable("state");
            var call1261 = request(var_state, "consume(1)", [1], string1262);
            if1258 = call1261;
          } else {
            var if1263 = GraceDone;
            setLineNumber(1547);    // compilenode op
            if (var_bigraph === undefined) raiseUninitializedVariable("bigraph");
            var string1264 = new GraceString("<=");
            var opresult1265 = request(var_bigraph, "==(1)", [1], string1264);
            if (Grace_isTrue(opresult1265)) {
              var string1267 = new GraceString("\u2264");
              // call case 6: other requests
              if (var_state === undefined) raiseUninitializedVariable("state");
              var call1266 = request(var_state, "consume(1)", [1], string1267);
              if1263 = call1266;
            } else {
              var if1268 = GraceDone;
              setLineNumber(1548);    // compilenode op
              if (var_bigraph === undefined) raiseUninitializedVariable("bigraph");
              var string1269 = new GraceString("!=");
              var opresult1270 = request(var_bigraph, "==(1)", [1], string1269);
              if (Grace_isTrue(opresult1270)) {
                var string1272 = new GraceString("\u2260");
                // call case 6: other requests
                if (var_state === undefined) raiseUninitializedVariable("state");
                var call1271 = request(var_state, "consume(1)", [1], string1272);
                if1268 = call1271;
              } else {
                var if1273 = GraceDone;
                setLineNumber(1549);    // compilenode op
                if (var_bigraph === undefined) raiseUninitializedVariable("bigraph");
                var string1274 = new GraceString("[[");
                var opresult1275 = request(var_bigraph, "==(1)", [1], string1274);
                if (Grace_isTrue(opresult1275)) {
                  var string1277 = new GraceString("\u27e6");
                  // call case 6: other requests
                  if (var_state === undefined) raiseUninitializedVariable("state");
                  var call1276 = request(var_state, "consume(1)", [1], string1277);
                  if1273 = call1276;
                } else {
                  var if1278 = GraceDone;
                  setLineNumber(1550);    // compilenode op
                  if (var_bigraph === undefined) raiseUninitializedVariable("bigraph");
                  var string1279 = new GraceString("]]");
                  var opresult1280 = request(var_bigraph, "==(1)", [1], string1279);
                  if (Grace_isTrue(opresult1280)) {
                    var string1282 = new GraceString("\u27e7");
                    // call case 6: other requests
                    if (var_state === undefined) raiseUninitializedVariable("state");
                    var call1281 = request(var_state, "consume(1)", [1], string1282);
                    if1278 = call1281;
                  } else {
                    var if1283 = GraceDone;
                    setLineNumber(1551);    // compilenode op
                    if (var_bigraph === undefined) raiseUninitializedVariable("bigraph");
                    var string1284 = new GraceString("->");
                    var opresult1285 = request(var_bigraph, "==(1)", [1], string1284);
                    if (Grace_isTrue(opresult1285)) {
                      var string1287 = new GraceString("\u2192");
                      // call case 6: other requests
                      if (var_state === undefined) raiseUninitializedVariable("state");
                      var call1286 = request(var_state, "consume(1)", [1], string1287);
                      if1283 = call1286;
                    } else {
                      setLineNumber(1553);    // compilenode op
                      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
                      var diff1288 = request(var_linePosition, "-(1)", [1], new GraceNum(1));
                      var_linePosition = diff1288;
                      setLineNumber(1554);    // compilenode call
                      if (var_ch === undefined) raiseUninitializedVariable("ch");
                      // call case 6: other requests
                      if (var_state === undefined) raiseUninitializedVariable("state");
                      var call1289 = request(var_state, "consume(1)", [1], var_ch);
                      setLineNumber(1555);    // compilenode op
                      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
                      var sum1290 = request(var_linePosition, "+(1)", [1], new GraceNum(1));
                      var_linePosition = sum1290;
                      setLineNumber(1556);    // compilenode call
                      if (var_nextCh === undefined) raiseUninitializedVariable("nextCh");
                      // call case 6: other requests
                      if (var_state === undefined) raiseUninitializedVariable("state");
                      var call1291 = request(var_state, "consume(1)", [1], var_nextCh);
                      if1283 = call1291;
                    }
                    if1278 = if1283;
                  }
                  if1273 = if1278;
                }
                if1268 = if1273;
              }
              if1263 = if1268;
            }
            if1258 = if1263;
          }
          if1247 = if1258;
        } else {
          setLineNumber(1559);    // compilenode call
          if (var_ch === undefined) raiseUninitializedVariable("ch");
          // call case 6: other requests
          if (var_state === undefined) raiseUninitializedVariable("state");
          var call1292 = request(var_state, "consume(1)", [1], var_ch);
          if1247 = call1292;
        }
        return if1247;
      };
      let applyMeth1244 = function apply (argcv) {
          return this.real.apply(this.receiver);
      };
      applyMeth1244.methodName = "apply";
      applyMeth1244.paramCounts = [0];
      applyMeth1244.paramNames = [];
      applyMeth1244.definitionLine = 1538;
      applyMeth1244.definitionModule = "lexer";
      block1244.methods["apply"] = applyMeth1244;
      // call case 2: outer request
      var call1241 = selfRequest(var_prelude, "while(1)do(1)", [1, 1], block1242, block1244);
      setLineNumber(1562);    // compilenode string
      var string1294 = new GraceString("\n");
      // call case 6: other requests
      if (var_state === undefined) raiseUninitializedVariable("state");
      var call1293 = request(var_state, "consume(1)", [1], string1294);
      setLineNumber(1563);    // compilenode num
      var_linePosition = new GraceNum(0);
      setLineNumber(1564);    // compilenode op
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      var sum1295 = request(var_lineNumber, "+(1)", [1], new GraceNum(1));
      var_lineNumber = sum1295;
      return GraceDone;
    };
    let applyMeth1239 = function apply_1 (argcv, ...args) {
        if (this.guard.apply(this.receiver, args))
            return this.real.apply(this.receiver, args);
        badBlockArgs.apply(this, args);
    };
    applyMeth1239.methodName = "apply(1)";
    applyMeth1239.paramCounts = [1];
    applyMeth1239.paramNames = ["eachLine"];
    applyMeth1239.definitionLine = 1536;
    applyMeth1239.definitionModule = "lexer";
    block1239.methods["apply(1)"] = applyMeth1239;
    let matchesMeth1239 = function matches_1 (argcv, ...args) {
        return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
    };
    matchesMeth1239.methodName = "matches(1)";
    matchesMeth1239.paramCounts = [1];
    matchesMeth1239.paramNames = ["eachLine"];
    matchesMeth1239.definitionLine = 1536;
    matchesMeth1239.definitionModule = "lexer";
    block1239.methods["matches(1)"] = matchesMeth1239;
    // call case 2: outer request
    var call1238 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], var_inputLines, block1239);
    setLineNumber(1566);    // compilenode op
    if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
    var sum1296 = request(var_linePosition, "+(1)", [1], new GraceNum(1));
    var_linePosition = sum1296;
    var if1297 = GraceDone;
    setLineNumber(1567);    // compilenode if
    if (var_inStr === undefined) raiseUninitializedVariable("inStr");
    if (Grace_isTrue(var_inStr)) {
      setLineNumber(1568);    // compilenode string
      var string1299 = new GraceString("a multi-line string must end with a '\u203a'.\n");
      setLineNumber(1569);    // compilenode string
      var string1300 = new GraceString("String opened on line ");
      if (var_startLine === undefined) raiseUninitializedVariable("startLine");
      var opresult1301 = request(string1300, "++(1)", [1], var_startLine);
      var string1302 = new GraceString(" and unclosed at end of input.");
      var opresult1303 = request(opresult1301, "++(1)", [1], string1302);
      var opresult1304 = request(string1299, "++(1)", [1], opresult1303);
      if (var_startLine === undefined) raiseUninitializedVariable("startLine");
      if (var_stringStart === undefined) raiseUninitializedVariable("stringStart");
      setLineNumber(1570);    // compilenode member
      // call case 6: other requests
      if (var_startLine === undefined) raiseUninitializedVariable("startLine");
      // call case 6: other requests
      if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
      var call1306 = request(var_inputLines, "at(1)", [1], var_startLine);
      var call1305 = request(call1306, "size", [0]);
      // call case 6: other requests
      if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
      var call1298 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], opresult1304, var_startLine, var_stringStart, call1305);
      if1297 = call1298;
    }
    setLineNumber(1572);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call1308 = selfRequest(this, "eofToken", [0]);
    // call case 6: other requests
    if (var_tokens === undefined) raiseUninitializedVariable("tokens");
    var call1307 = request(var_tokens, "push(1)", [1], call1308);
    if (var_tokens === undefined) raiseUninitializedVariable("tokens");
    return var_tokens;
  };    // end of method lexInputLines
  this.methods["lexInputLines"] = func1122;
  func1122.methodName = "lexInputLines";
  func1122.paramCounts = [0];
  func1122.paramNames = [];
  func1122.definitionLine = 1448;
  func1122.definitionModule = "lexer";
  setLineNumber(6);    // compilenode member
  // call case 6: other requests
  // call case 6: other requests
  var call1310 = request(var_map, "dictionary", [0]);
  var call1309 = request(call1310, "empty", [0]);
  var var_keywords = call1309;
  var reader1311_keywords = function() {  // reader method keywords
      if (var_keywords === undefined) raiseUninitializedVariable("keywords");
      return var_keywords;
  };
  reader1311_keywords.isDef = true;
  reader1311_keywords.confidential = true;
  this.methods["keywords"] = reader1311_keywords;
  setLineNumber(7);    // compilenode string
  var string1313 = new GraceString("alias");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1312 = request(var_keywords, "at(1)put(1)", [1, 1], string1313, GraceTrue);
  setLineNumber(8);    // compilenode string
  var string1315 = new GraceString("as");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1314 = request(var_keywords, "at(1)put(1)", [1, 1], string1315, GraceTrue);
  setLineNumber(9);    // compilenode string
  var string1317 = new GraceString("class");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1316 = request(var_keywords, "at(1)put(1)", [1, 1], string1317, GraceTrue);
  setLineNumber(10);    // compilenode string
  var string1319 = new GraceString("def");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1318 = request(var_keywords, "at(1)put(1)", [1, 1], string1319, GraceTrue);
  setLineNumber(11);    // compilenode string
  var string1321 = new GraceString("dialect");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1320 = request(var_keywords, "at(1)put(1)", [1, 1], string1321, GraceTrue);
  setLineNumber(12);    // compilenode string
  var string1323 = new GraceString("exclude");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1322 = request(var_keywords, "at(1)put(1)", [1, 1], string1323, GraceTrue);
  setLineNumber(13);    // compilenode string
  var string1325 = new GraceString("import");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1324 = request(var_keywords, "at(1)put(1)", [1, 1], string1325, GraceTrue);
  setLineNumber(14);    // compilenode string
  var string1327 = new GraceString("inherit");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1326 = request(var_keywords, "at(1)put(1)", [1, 1], string1327, GraceTrue);
  setLineNumber(15);    // compilenode string
  var string1329 = new GraceString("interface");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1328 = request(var_keywords, "at(1)put(1)", [1, 1], string1329, GraceTrue);
  setLineNumber(16);    // compilenode string
  var string1331 = new GraceString("is");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1330 = request(var_keywords, "at(1)put(1)", [1, 1], string1331, GraceTrue);
  setLineNumber(17);    // compilenode string
  var string1333 = new GraceString("method");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1332 = request(var_keywords, "at(1)put(1)", [1, 1], string1333, GraceTrue);
  setLineNumber(18);    // compilenode string
  var string1335 = new GraceString("object");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1334 = request(var_keywords, "at(1)put(1)", [1, 1], string1335, GraceTrue);
  setLineNumber(19);    // compilenode string
  var string1337 = new GraceString("once");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1336 = request(var_keywords, "at(1)put(1)", [1, 1], string1337, GraceTrue);
  setLineNumber(20);    // compilenode string
  var string1339 = new GraceString("prefix");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1338 = request(var_keywords, "at(1)put(1)", [1, 1], string1339, GraceTrue);
  setLineNumber(21);    // compilenode string
  var string1341 = new GraceString("return");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1340 = request(var_keywords, "at(1)put(1)", [1, 1], string1341, GraceTrue);
  setLineNumber(22);    // compilenode string
  var string1343 = new GraceString("trait");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1342 = request(var_keywords, "at(1)put(1)", [1, 1], string1343, GraceTrue);
  setLineNumber(23);    // compilenode string
  var string1345 = new GraceString("type");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1344 = request(var_keywords, "at(1)put(1)", [1, 1], string1345, GraceTrue);
  setLineNumber(24);    // compilenode string
  var string1347 = new GraceString("use");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1346 = request(var_keywords, "at(1)put(1)", [1, 1], string1347, GraceTrue);
  setLineNumber(25);    // compilenode string
  var string1349 = new GraceString("var");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1348 = request(var_keywords, "at(1)put(1)", [1, 1], string1349, GraceTrue);
  setLineNumber(26);    // compilenode string
  var string1351 = new GraceString("where");
  // call case 6: other requests
  if (var_keywords === undefined) raiseUninitializedVariable("keywords");
  var call1350 = request(var_keywords, "at(1)put(1)", [1, 1], string1351, GraceTrue);
  setLineNumber(28);    // compilenode string
  var string1352 = new GraceString("-&|:$#\\%^@?*/+!~");
  var var_operatorChars = string1352;
  var reader1353_operatorChars = function() {  // reader method operatorChars
      if (var_operatorChars === undefined) raiseUninitializedVariable("operatorChars");
      return var_operatorChars;
  };
  reader1353_operatorChars.isDef = true;
  reader1353_operatorChars.confidential = true;
  this.methods["operatorChars"] = reader1353_operatorChars;
  setLineNumber(35);    // compilenode string
  var string1354 = new GraceString(" \u00a0");
  var var_spaceChars = string1354;
  var reader1355_spaceChars = function() {  // reader method spaceChars
      if (var_spaceChars === undefined) raiseUninitializedVariable("spaceChars");
      return var_spaceChars;
  };
  reader1355_spaceChars.isDef = true;
  reader1355_spaceChars.confidential = true;
  this.methods["spaceChars"] = reader1355_spaceChars;
  setLineNumber(36);    // compilenode string
  var string1356 = new GraceString("\u0009");
  var var_tab = string1356;
  var reader1357_tab = function() {  // reader method tab
      if (var_tab === undefined) raiseUninitializedVariable("tab");
      return var_tab;
  };
  reader1357_tab.isDef = true;
  reader1357_tab.confidential = true;
  this.methods["tab"] = reader1357_tab;
  setLineNumber(43);    // compilenode string
  var string1358 = new GraceString("\n\u000d");
  var var_nl__95__cr = string1358;
  var reader1359_nl__95__cr = function() {  // reader method nl_cr
      if (var_nl__95__cr === undefined) raiseUninitializedVariable("nl_cr");
      return var_nl__95__cr;
  };
  reader1359_nl__95__cr.isDef = true;
  reader1359_nl__95__cr.confidential = true;
  this.methods["nl_cr"] = reader1359_nl__95__cr;
  setLineNumber(49);    // compilenode string
  var string1360 = new GraceString("(),{}[];\u27e6\u27e7");
  var var_selfModes = string1360;
  var reader1361_selfModes = function() {  // reader method selfModes
      if (var_selfModes === undefined) raiseUninitializedVariable("selfModes");
      return var_selfModes;
  };
  reader1361_selfModes.isDef = true;
  reader1361_selfModes.confidential = true;
  this.methods["selfModes"] = reader1361_selfModes;
  setLineNumber(52);    // compilenode string
  var string1362 = new GraceString("(){}[]\u27e6\u27e7");
  var var_brackets = string1362;
  var reader1363_brackets = function() {  // reader method brackets
      if (var_brackets === undefined) raiseUninitializedVariable("brackets");
      return var_brackets;
  };
  reader1363_brackets.isDef = true;
  reader1363_brackets.confidential = true;
  this.methods["brackets"] = reader1363_brackets;
  setLineNumber(55);    // compilenode string
  var string1364 = new GraceString("_'0123456789abcdefghijklmnopqrstuvwzyzABCDEFGHIJKLMNOPQRSTUVWXYZ");
  var var_alphaNums = string1364;
  var reader1365_alphaNums = function() {  // reader method alphaNums
      if (var_alphaNums === undefined) raiseUninitializedVariable("alphaNums");
      return var_alphaNums;
  };
  reader1365_alphaNums.isDef = true;
  reader1365_alphaNums.confidential = true;
  this.methods["alphaNums"] = reader1365_alphaNums;
  setLineNumber(62);    // compilenode string
  var string1366 = new GraceString("0123456789");
  var var_digits = string1366;
  var reader1367_digits = function() {  // reader method digits
      if (var_digits === undefined) raiseUninitializedVariable("digits");
      return var_digits;
  };
  reader1367_digits.isDef = true;
  reader1367_digits.confidential = true;
  this.methods["digits"] = reader1367_digits;
  setLineNumber(65);    // compilenode string
  var string1368 = new GraceString("abcdefghijklmnopqrstuvwzyzABCDEFGHIJKLMNOPQRSTUVWXYZ");
  var var_asciiLetters = string1368;
  var reader1369_asciiLetters = function() {  // reader method asciiLetters
      if (var_asciiLetters === undefined) raiseUninitializedVariable("asciiLetters");
      return var_asciiLetters;
  };
  reader1369_asciiLetters.isDef = true;
  reader1369_asciiLetters.confidential = true;
  this.methods["asciiLetters"] = reader1369_asciiLetters;
  setLineNumber(87);    // compilenode string
  var string1371 = new GraceString("noSuchLine");
  // call case 2: outer request
  var call1370 = selfRequest(var_prelude, "singleton(1)", [1], string1371);
  var var_noSuchLine = call1370;
  var reader1372_noSuchLine = function() {  // reader method noSuchLine
      if (var_noSuchLine === undefined) raiseUninitializedVariable("noSuchLine");
      return var_noSuchLine;
  };
  reader1372_noSuchLine.isDef = true;
  reader1372_noSuchLine.confidential = true;
  this.methods["noSuchLine"] = reader1372_noSuchLine;
  setLineNumber(109);    // compilenode vardec
  var var_tokens;
  var reader1373_tokens = function() {  // reader method tokens
      if (var_tokens === undefined) raiseUninitializedVariable("tokens");
      return var_tokens;
  };
  reader1373_tokens.isVar = true;
  reader1373_tokens.confidential = true;
  this.methods["tokens"] = reader1373_tokens;
  var writer1374_tokens = function(argcv, n) {   // writer method tokens:=(_)
    var_tokens = n;
    return GraceDone;
  };
  writer1374_tokens.confidential = true;
  this.methods["tokens:=(1)"] = writer1374_tokens;
  setLineNumber(110);    // compilenode vardec
  var var_lastNonCommentToken;
  var reader1375_lastNonCommentToken = function() {  // reader method lastNonCommentToken
      if (var_lastNonCommentToken === undefined) raiseUninitializedVariable("lastNonCommentToken");
      return var_lastNonCommentToken;
  };
  reader1375_lastNonCommentToken.isVar = true;
  reader1375_lastNonCommentToken.confidential = true;
  this.methods["lastNonCommentToken"] = reader1375_lastNonCommentToken;
  var writer1376_lastNonCommentToken = function(argcv, n) {   // writer method lastNonCommentToken:=(_)
    var_lastNonCommentToken = n;
    return GraceDone;
  };
  writer1376_lastNonCommentToken.confidential = true;
  this.methods["lastNonCommentToken:=(1)"] = writer1376_lastNonCommentToken;
  setLineNumber(111);    // compilenode vardec
  var var_lineNumber;
  var reader1377_lineNumber = function() {  // reader method lineNumber
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      return var_lineNumber;
  };
  reader1377_lineNumber.isVar = true;
  reader1377_lineNumber.confidential = true;
  this.methods["lineNumber"] = reader1377_lineNumber;
  var writer1378_lineNumber = function(argcv, n) {   // writer method lineNumber:=(_)
    var_lineNumber = n;
    return GraceDone;
  };
  writer1378_lineNumber.confidential = true;
  this.methods["lineNumber:=(1)"] = writer1378_lineNumber;
  setLineNumber(112);    // compilenode vardec
  var var_linePosition;
  var reader1379_linePosition = function() {  // reader method linePosition
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      return var_linePosition;
  };
  reader1379_linePosition.isVar = true;
  reader1379_linePosition.confidential = true;
  this.methods["linePosition"] = reader1379_linePosition;
  var writer1380_linePosition = function(argcv, n) {   // writer method linePosition:=(_)
    var_linePosition = n;
    return GraceDone;
  };
  writer1380_linePosition.confidential = true;
  this.methods["linePosition:=(1)"] = writer1380_linePosition;
  setLineNumber(113);    // compilenode vardec
  var var_startLine;
  var reader1381_startLine = function() {  // reader method startLine
      if (var_startLine === undefined) raiseUninitializedVariable("startLine");
      return var_startLine;
  };
  reader1381_startLine.isVar = true;
  reader1381_startLine.confidential = true;
  this.methods["startLine"] = reader1381_startLine;
  var writer1382_startLine = function(argcv, n) {   // writer method startLine:=(_)
    var_startLine = n;
    return GraceDone;
  };
  writer1382_startLine.confidential = true;
  this.methods["startLine:=(1)"] = writer1382_startLine;
  setLineNumber(114);    // compilenode vardec
  var var_startPosition;
  var reader1383_startPosition = function() {  // reader method startPosition
      if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
      return var_startPosition;
  };
  reader1383_startPosition.isVar = true;
  reader1383_startPosition.confidential = true;
  this.methods["startPosition"] = reader1383_startPosition;
  var writer1384_startPosition = function(argcv, n) {   // writer method startPosition:=(_)
    var_startPosition = n;
    return GraceDone;
  };
  writer1384_startPosition.confidential = true;
  this.methods["startPosition:=(1)"] = writer1384_startPosition;
  setLineNumber(115);    // compilenode vardec
  var var_currentLineIndent;
  var reader1385_currentLineIndent = function() {  // reader method currentLineIndent
      if (var_currentLineIndent === undefined) raiseUninitializedVariable("currentLineIndent");
      return var_currentLineIndent;
  };
  reader1385_currentLineIndent.isVar = true;
  reader1385_currentLineIndent.confidential = true;
  this.methods["currentLineIndent"] = reader1385_currentLineIndent;
  var writer1386_currentLineIndent = function(argcv, n) {   // writer method currentLineIndent:=(_)
    var_currentLineIndent = n;
    return GraceDone;
  };
  writer1386_currentLineIndent.confidential = true;
  this.methods["currentLineIndent:=(1)"] = writer1386_currentLineIndent;
  setLineNumber(116);    // compilenode vardec
  var var_stringStart;
  var reader1387_stringStart = function() {  // reader method stringStart
      if (var_stringStart === undefined) raiseUninitializedVariable("stringStart");
      return var_stringStart;
  };
  reader1387_stringStart.isVar = true;
  reader1387_stringStart.confidential = true;
  this.methods["stringStart"] = reader1387_stringStart;
  var writer1388_stringStart = function(argcv, n) {   // writer method stringStart:=(_)
    var_stringStart = n;
    return GraceDone;
  };
  writer1388_stringStart.confidential = true;
  this.methods["stringStart:=(1)"] = writer1388_stringStart;
  setLineNumber(117);    // compilenode vardec
  var var_unichars;
  var reader1389_unichars = function() {  // reader method unichars
      if (var_unichars === undefined) raiseUninitializedVariable("unichars");
      return var_unichars;
  };
  reader1389_unichars.isVar = true;
  reader1389_unichars.confidential = true;
  this.methods["unichars"] = reader1389_unichars;
  var writer1390_unichars = function(argcv, n) {   // writer method unichars:=(_)
    var_unichars = n;
    return GraceDone;
  };
  writer1390_unichars.confidential = true;
  this.methods["unichars:=(1)"] = writer1390_unichars;
  setLineNumber(118);    // compilenode vardec
  var var_braceChange;
  var reader1391_braceChange = function() {  // reader method braceChange
      if (var_braceChange === undefined) raiseUninitializedVariable("braceChange");
      return var_braceChange;
  };
  reader1391_braceChange.isVar = true;
  reader1391_braceChange.confidential = true;
  this.methods["braceChange"] = reader1391_braceChange;
  var writer1392_braceChange = function(argcv, n) {   // writer method braceChange:=(_)
    var_braceChange = n;
    return GraceDone;
  };
  writer1392_braceChange.confidential = true;
  this.methods["braceChange:=(1)"] = writer1392_braceChange;
  setLineNumber(119);    // compilenode vardec
  var var_currentLineBraceDepth;
  var reader1393_currentLineBraceDepth = function() {  // reader method currentLineBraceDepth
      if (var_currentLineBraceDepth === undefined) raiseUninitializedVariable("currentLineBraceDepth");
      return var_currentLineBraceDepth;
  };
  reader1393_currentLineBraceDepth.isVar = true;
  reader1393_currentLineBraceDepth.confidential = true;
  this.methods["currentLineBraceDepth"] = reader1393_currentLineBraceDepth;
  var writer1394_currentLineBraceDepth = function(argcv, n) {   // writer method currentLineBraceDepth:=(_)
    var_currentLineBraceDepth = n;
    return GraceDone;
  };
  writer1394_currentLineBraceDepth.confidential = true;
  this.methods["currentLineBraceDepth:=(1)"] = writer1394_currentLineBraceDepth;
  setLineNumber(120);    // compilenode vardec
  var var_unmatchedLeftBraces;
  var reader1395_unmatchedLeftBraces = function() {  // reader method unmatchedLeftBraces
      if (var_unmatchedLeftBraces === undefined) raiseUninitializedVariable("unmatchedLeftBraces");
      return var_unmatchedLeftBraces;
  };
  reader1395_unmatchedLeftBraces.isVar = true;
  reader1395_unmatchedLeftBraces.confidential = true;
  this.methods["unmatchedLeftBraces"] = reader1395_unmatchedLeftBraces;
  var writer1396_unmatchedLeftBraces = function(argcv, n) {   // writer method unmatchedLeftBraces:=(_)
    var_unmatchedLeftBraces = n;
    return GraceDone;
  };
  writer1396_unmatchedLeftBraces.confidential = true;
  this.methods["unmatchedLeftBraces:=(1)"] = writer1396_unmatchedLeftBraces;
  setLineNumber(121);    // compilenode vardec
  var var_indentOfLineBeingContinued;
  var reader1397_indentOfLineBeingContinued = function() {  // reader method indentOfLineBeingContinued
      if (var_indentOfLineBeingContinued === undefined) raiseUninitializedVariable("indentOfLineBeingContinued");
      return var_indentOfLineBeingContinued;
  };
  reader1397_indentOfLineBeingContinued.isVar = true;
  reader1397_indentOfLineBeingContinued.confidential = true;
  this.methods["indentOfLineBeingContinued"] = reader1397_indentOfLineBeingContinued;
  var writer1398_indentOfLineBeingContinued = function(argcv, n) {   // writer method indentOfLineBeingContinued:=(_)
    var_indentOfLineBeingContinued = n;
    return GraceDone;
  };
  writer1398_indentOfLineBeingContinued.confidential = true;
  this.methods["indentOfLineBeingContinued:=(1)"] = writer1398_indentOfLineBeingContinued;
  setLineNumber(122);    // compilenode vardec
  var var_indentStack;
  var reader1399_indentStack = function() {  // reader method indentStack
      if (var_indentStack === undefined) raiseUninitializedVariable("indentStack");
      return var_indentStack;
  };
  reader1399_indentStack.isVar = true;
  reader1399_indentStack.confidential = true;
  this.methods["indentStack"] = reader1399_indentStack;
  var writer1400_indentStack = function(argcv, n) {   // writer method indentStack:=(_)
    var_indentStack = n;
    return GraceDone;
  };
  writer1400_indentStack.confidential = true;
  this.methods["indentStack:=(1)"] = writer1400_indentStack;
  setLineNumber(125);    // compilenode vardec
  var var_maxIndentOfContinuation;
  var reader1401_maxIndentOfContinuation = function() {  // reader method maxIndentOfContinuation
      if (var_maxIndentOfContinuation === undefined) raiseUninitializedVariable("maxIndentOfContinuation");
      return var_maxIndentOfContinuation;
  };
  reader1401_maxIndentOfContinuation.isVar = true;
  reader1401_maxIndentOfContinuation.confidential = true;
  this.methods["maxIndentOfContinuation"] = reader1401_maxIndentOfContinuation;
  var writer1402_maxIndentOfContinuation = function(argcv, n) {   // writer method maxIndentOfContinuation:=(_)
    var_maxIndentOfContinuation = n;
    return GraceDone;
  };
  writer1402_maxIndentOfContinuation.confidential = true;
  this.methods["maxIndentOfContinuation:=(1)"] = writer1402_maxIndentOfContinuation;
  setLineNumber(127);    // compilenode vardec
  var var_priorLineBraceDepth;
  var reader1403_priorLineBraceDepth = function() {  // reader method priorLineBraceDepth
      if (var_priorLineBraceDepth === undefined) raiseUninitializedVariable("priorLineBraceDepth");
      return var_priorLineBraceDepth;
  };
  reader1403_priorLineBraceDepth.isVar = true;
  reader1403_priorLineBraceDepth.confidential = true;
  this.methods["priorLineBraceDepth"] = reader1403_priorLineBraceDepth;
  var writer1404_priorLineBraceDepth = function(argcv, n) {   // writer method priorLineBraceDepth:=(_)
    var_priorLineBraceDepth = n;
    return GraceDone;
  };
  writer1404_priorLineBraceDepth.confidential = true;
  this.methods["priorLineBraceDepth:=(1)"] = writer1404_priorLineBraceDepth;
  setLineNumber(128);    // compilenode vardec
  var var_priorLineIndent;
  var reader1405_priorLineIndent = function() {  // reader method priorLineIndent
      if (var_priorLineIndent === undefined) raiseUninitializedVariable("priorLineIndent");
      return var_priorLineIndent;
  };
  reader1405_priorLineIndent.isVar = true;
  reader1405_priorLineIndent.confidential = true;
  this.methods["priorLineIndent"] = reader1405_priorLineIndent;
  var writer1406_priorLineIndent = function(argcv, n) {   // writer method priorLineIndent:=(_)
    var_priorLineIndent = n;
    return GraceDone;
  };
  writer1406_priorLineIndent.confidential = true;
  this.methods["priorLineIndent:=(1)"] = writer1406_priorLineIndent;
  setLineNumber(129);    // compilenode vardec
  var var_state;
  var reader1407_state = function() {  // reader method state
      if (var_state === undefined) raiseUninitializedVariable("state");
      return var_state;
  };
  reader1407_state.isVar = true;
  reader1407_state.confidential = true;
  this.methods["state"] = reader1407_state;
  var writer1408_state = function(argcv, n) {   // writer method state:=(_)
    var_state = n;
    return GraceDone;
  };
  writer1408_state.confidential = true;
  this.methods["state:=(1)"] = writer1408_state;
  setLineNumber(130);    // compilenode vardec
  var var_accum;
  var reader1409_accum = function() {  // reader method accum
      if (var_accum === undefined) raiseUninitializedVariable("accum");
      return var_accum;
  };
  reader1409_accum.isVar = true;
  reader1409_accum.confidential = true;
  this.methods["accum"] = reader1409_accum;
  var writer1410_accum = function(argcv, n) {   // writer method accum:=(_)
    var_accum = n;
    return GraceDone;
  };
  writer1410_accum.confidential = true;
  this.methods["accum:=(1)"] = writer1410_accum;
  setLineNumber(131);    // compilenode vardec
  var var_inStr;
  var reader1411_inStr = function() {  // reader method inStr
      if (var_inStr === undefined) raiseUninitializedVariable("inStr");
      return var_inStr;
  };
  reader1411_inStr.isVar = true;
  reader1411_inStr.confidential = true;
  this.methods["inStr"] = reader1411_inStr;
  var writer1412_inStr = function(argcv, n) {   // writer method inStr:=(_)
    var_inStr = n;
    return GraceDone;
  };
  writer1412_inStr.confidential = true;
  this.methods["inStr:=(1)"] = writer1412_inStr;
  setLineNumber(132);    // compilenode vardec
  var var_codepoint;
  var reader1413_codepoint = function() {  // reader method codepoint
      if (var_codepoint === undefined) raiseUninitializedVariable("codepoint");
      return var_codepoint;
  };
  reader1413_codepoint.isVar = true;
  reader1413_codepoint.confidential = true;
  this.methods["codepoint"] = reader1413_codepoint;
  var writer1414_codepoint = function(argcv, n) {   // writer method codepoint:=(_)
    var_codepoint = n;
    return GraceDone;
  };
  writer1414_codepoint.confidential = true;
  this.methods["codepoint:=(1)"] = writer1414_codepoint;
  setLineNumber(133);    // compilenode vardec
  var var_interpDepth;
  var reader1415_interpDepth = function() {  // reader method interpDepth
      if (var_interpDepth === undefined) raiseUninitializedVariable("interpDepth");
      return var_interpDepth;
  };
  reader1415_interpDepth.isVar = true;
  reader1415_interpDepth.confidential = true;
  this.methods["interpDepth"] = reader1415_interpDepth;
  var writer1416_interpDepth = function(argcv, n) {   // writer method interpDepth:=(_)
    var_interpDepth = n;
    return GraceDone;
  };
  writer1416_interpDepth.confidential = true;
  this.methods["interpDepth:=(1)"] = writer1416_interpDepth;
  setLineNumber(134);    // compilenode vardec
  var var_stringContainsInterp;
  var reader1417_stringContainsInterp = function() {  // reader method stringContainsInterp
      if (var_stringContainsInterp === undefined) raiseUninitializedVariable("stringContainsInterp");
      return var_stringContainsInterp;
  };
  reader1417_stringContainsInterp.isVar = true;
  reader1417_stringContainsInterp.confidential = true;
  this.methods["stringContainsInterp"] = reader1417_stringContainsInterp;
  var writer1418_stringContainsInterp = function(argcv, n) {   // writer method stringContainsInterp:=(_)
    var_stringContainsInterp = n;
    return GraceDone;
  };
  writer1418_stringContainsInterp.confidential = true;
  this.methods["stringContainsInterp:=(1)"] = writer1418_stringContainsInterp;
  setLineNumber(136);    // compilenode vardec
  var var_braceDepthAtStartOfInterp;
  var reader1419_braceDepthAtStartOfInterp = function() {  // reader method braceDepthAtStartOfInterp
      if (var_braceDepthAtStartOfInterp === undefined) raiseUninitializedVariable("braceDepthAtStartOfInterp");
      return var_braceDepthAtStartOfInterp;
  };
  reader1419_braceDepthAtStartOfInterp.isVar = true;
  reader1419_braceDepthAtStartOfInterp.confidential = true;
  this.methods["braceDepthAtStartOfInterp"] = reader1419_braceDepthAtStartOfInterp;
  var writer1420_braceDepthAtStartOfInterp = function(argcv, n) {   // writer method braceDepthAtStartOfInterp:=(_)
    var_braceDepthAtStartOfInterp = n;
    return GraceDone;
  };
  writer1420_braceDepthAtStartOfInterp.confidential = true;
  this.methods["braceDepthAtStartOfInterp:=(1)"] = writer1420_braceDepthAtStartOfInterp;
  setLineNumber(139);    // compilenode member
  // call case 4: self request with 0 args and 0 typeArgs 
  var call1421 = selfRequest(this, "initialize", [0]);
  setLineNumber(141);    // compilenode member
  // call case 6: other requests
  var call1422 = request(var_util, "lines", [0]);
  var var_inputLines = call1422;
  var reader1423_inputLines = function() {  // reader method inputLines
      if (var_inputLines === undefined) raiseUninitializedVariable("inputLines");
      return var_inputLines;
  };
  reader1423_inputLines.isDef = true;
  reader1423_inputLines.confidential = true;
  this.methods["inputLines"] = reader1423_inputLines;
  setLineNumber(409);    // compilenode object
  var obj1424_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_409");
    this.outer_lexer_409 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1425 = function(argcv, var_c) {    // method consume(_), line 410
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      var if1426 = GraceDone;
      setLineNumber(411);    // compilenode string
      var string1427 = new GraceString("#");
      var opresult1428 = request(var_c, "==(1)", [1], string1427);
      if (Grace_isTrue(opresult1428)) {
        setLineNumber(412);    // compilenode call
        if (var_pragmaPrefixState === undefined) raiseUninitializedVariable("pragmaPrefixState");
        // call case 2: outer request
        var call1429 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_pragmaPrefixState);
        if1426 = call1429;
      } else {
        setLineNumber(414);    // compilenode call
        if (var_indentationState === undefined) raiseUninitializedVariable("indentationState");
        // call case 2: outer request
        var call1430 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_indentationState);
        setLineNumber(415);    // compilenode call
        // call case 6: other requests
        if (var_state === undefined) raiseUninitializedVariable("state");
        var call1431 = request(var_state, "consume(1)", [1], var_c);
        if1426 = call1431;
      }
      return if1426;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1425;
    func1425.methodName = "consume(1)";
    func1425.paramCounts = [1];
    func1425.paramNames = ["c"];
    func1425.definitionLine = 410;
    func1425.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 409;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1424_init = function() {    // init of object on line 409
    };
    return obj1424_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1424 = emptyGraceObject("startState", "lexer", 409);
  var obj1424_init = obj1424_build.call(obj1424, null, this, [], []);
  obj1424_init.call(obj1424);  // end of compileobject
  var var_startState = obj1424;
  var reader1432_startState = function() {  // reader method startState
      if (var_startState === undefined) raiseUninitializedVariable("startState");
      return var_startState;
  };
  reader1432_startState.isDef = true;
  reader1432_startState.confidential = true;
  this.methods["startState"] = reader1432_startState;
  setLineNumber(420);    // compilenode object
  var obj1433_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_420");
    this.outer_lexer_420 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1434 = function(argcv, var_c) {    // method consume(_), line 421
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(422);    // compilenode call
      // call case 2: outer request
      var call1435 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1436 = GraceDone;
      setLineNumber(423);    // compilenode string
      var string1437 = new GraceString("\n");
      var opresult1438 = request(var_c, "==(1)", [1], string1437);
      if (Grace_isTrue(opresult1438)) {
        setLineNumber(424);    // compilenode string
        var string1440 = new GraceString("incomplete pragma definition");
        if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
        if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        // call case 6: other requests
        if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
        var call1439 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], string1440, var_lineNumber, var_startPosition, var_linePosition);
        if1436 = call1439;
      } else {
        var if1441 = GraceDone;
        setLineNumber(426);    // compilenode call
        // call case 2: outer request
        var call1442 = selfRequest(importedModules["lexer"], "isSpaceChar(1)", [1], var_c);
        if (Grace_isTrue(call1442)) {
          var if1443 = GraceDone;
          setLineNumber(427);    // compilenode op
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          var string1444 = new GraceString("pragma");
          var opresult1445 = request(var_accum, "==(1)", [1], string1444);
          if (Grace_isTrue(opresult1445)) {
            setLineNumber(428);    // compilenode string
            var string1446 = new GraceString("");
            var_accum = string1446;
            setLineNumber(429);    // compilenode call
            if (var_pragmaBodyState === undefined) raiseUninitializedVariable("pragmaBodyState");
            // call case 2: outer request
            var call1447 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_pragmaBodyState);
            if1443 = call1447;
          } else {
            setLineNumber(431);    // compilenode string
            var string1449 = new GraceString("pragmas must start with #pragma");
            if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
            if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
            if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
            // call case 6: other requests
            if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
            var call1448 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], string1449, var_lineNumber, var_startPosition, var_linePosition);
            if1443 = call1448;
          }
          if1441 = if1443;
        } else {
          setLineNumber(435);    // compilenode call
          // call case 2: outer request
          var call1450 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
          if1441 = call1450;
        }
        if1436 = if1441;
      }
      return if1436;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1434;
    func1434.methodName = "consume(1)";
    func1434.paramCounts = [1];
    func1434.paramNames = ["c"];
    func1434.definitionLine = 421;
    func1434.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 420;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1433_init = function() {    // init of object on line 420
    };
    return obj1433_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1433 = emptyGraceObject("pragmaPrefixState", "lexer", 420);
  var obj1433_init = obj1433_build.call(obj1433, null, this, [], []);
  obj1433_init.call(obj1433);  // end of compileobject
  var var_pragmaPrefixState = obj1433;
  var reader1451_pragmaPrefixState = function() {  // reader method pragmaPrefixState
      if (var_pragmaPrefixState === undefined) raiseUninitializedVariable("pragmaPrefixState");
      return var_pragmaPrefixState;
  };
  reader1451_pragmaPrefixState.isDef = true;
  reader1451_pragmaPrefixState.confidential = true;
  this.methods["pragmaPrefixState"] = reader1451_pragmaPrefixState;
  setLineNumber(440);    // compilenode object
  var obj1452_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_440");
    this.outer_lexer_440 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1453 = function(argcv, var_c) {    // method consume(_), line 441
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(442);    // compilenode call
      // call case 2: outer request
      var call1454 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1455 = GraceDone;
      setLineNumber(443);    // compilenode string
      var string1456 = new GraceString("\n");
      var opresult1457 = request(var_c, "==(1)", [1], string1456);
      // call case 2: outer request
      var call1458 = selfRequest(importedModules["lexer"], "isSpaceChar(1)", [1], var_c);
      var opresult1459 = request(opresult1457, "||(1)", [1], call1458);
      if (Grace_isTrue(opresult1459)) {
        setLineNumber(445);    // compilenode call
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        // call case 6: other requests
        if (var_util === undefined) raiseUninitializedVariable("util");
        var call1460 = request(var_util, "processExtension(1)", [1], var_accum);
        setLineNumber(446);    // compilenode string
        var string1461 = new GraceString("");
        var_accum = string1461;
        setLineNumber(447);    // compilenode call
        if (var_pragmaTrailingWhitespaceState === undefined) raiseUninitializedVariable("pragmaTrailingWhitespaceState");
        // call case 2: outer request
        var call1462 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_pragmaTrailingWhitespaceState);
        setLineNumber(448);    // compilenode call
        // call case 6: other requests
        if (var_state === undefined) raiseUninitializedVariable("state");
        var call1463 = request(var_state, "consume(1)", [1], var_c);
        if1455 = call1463;
      } else {
        setLineNumber(450);    // compilenode call
        // call case 2: outer request
        var call1464 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if1455 = call1464;
      }
      return if1455;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1453;
    func1453.methodName = "consume(1)";
    func1453.paramCounts = [1];
    func1453.paramNames = ["c"];
    func1453.definitionLine = 441;
    func1453.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 440;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1452_init = function() {    // init of object on line 440
    };
    return obj1452_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1452 = emptyGraceObject("pragmaBodyState", "lexer", 440);
  var obj1452_init = obj1452_build.call(obj1452, null, this, [], []);
  obj1452_init.call(obj1452);  // end of compileobject
  var var_pragmaBodyState = obj1452;
  var reader1465_pragmaBodyState = function() {  // reader method pragmaBodyState
      if (var_pragmaBodyState === undefined) raiseUninitializedVariable("pragmaBodyState");
      return var_pragmaBodyState;
  };
  reader1465_pragmaBodyState.isDef = true;
  reader1465_pragmaBodyState.confidential = true;
  this.methods["pragmaBodyState"] = reader1465_pragmaBodyState;
  setLineNumber(455);    // compilenode object
  var obj1466_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_455");
    this.outer_lexer_455 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1467 = function(argcv, var_c) {    // method consume(_), line 456
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(457);    // compilenode call
      // call case 2: outer request
      var call1468 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1469 = GraceDone;
      setLineNumber(458);    // compilenode string
      var string1470 = new GraceString("\n");
      var opresult1471 = request(var_c, "==(1)", [1], string1470);
      if (Grace_isTrue(opresult1471)) {
        setLineNumber(459);    // compilenode call
        if (var_startState === undefined) raiseUninitializedVariable("startState");
        // call case 2: outer request
        var call1472 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_startState);
        if1469 = call1472;
      } else {
        var if1473 = GraceDone;
        setLineNumber(460);    // compilenode call
        // call case 6: other requests
        // call case 2: outer request
        var call1475 = selfRequest(importedModules["lexer"], "isSpaceChar(1)", [1], var_c);
        var call1474 = request(call1475, "prefix!", [0]);
        if (Grace_isTrue(call1474)) {
          setLineNumber(461);    // compilenode string
          var string1477 = new GraceString("pragmas must be a single word");
          if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
          if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call1476 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], string1477, var_lineNumber, var_startPosition, var_linePosition);
          if1473 = call1476;
        }
        if1469 = if1473;
      }
      return if1469;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1467;
    func1467.methodName = "consume(1)";
    func1467.paramCounts = [1];
    func1467.paramNames = ["c"];
    func1467.definitionLine = 456;
    func1467.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 455;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1466_init = function() {    // init of object on line 455
    };
    return obj1466_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1466 = emptyGraceObject("pragmaTrailingWhitespaceState", "lexer", 455);
  var obj1466_init = obj1466_build.call(obj1466, null, this, [], []);
  obj1466_init.call(obj1466);  // end of compileobject
  var var_pragmaTrailingWhitespaceState = obj1466;
  var reader1478_pragmaTrailingWhitespaceState = function() {  // reader method pragmaTrailingWhitespaceState
      if (var_pragmaTrailingWhitespaceState === undefined) raiseUninitializedVariable("pragmaTrailingWhitespaceState");
      return var_pragmaTrailingWhitespaceState;
  };
  reader1478_pragmaTrailingWhitespaceState.isDef = true;
  reader1478_pragmaTrailingWhitespaceState.confidential = true;
  this.methods["pragmaTrailingWhitespaceState"] = reader1478_pragmaTrailingWhitespaceState;
  setLineNumber(467);    // compilenode object
  var obj1479_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_467");
    this.outer_lexer_467 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1480 = function(argcv, var_c) {    // method consume(_), line 468
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(469);    // compilenode member
      // call case 6: other requests
      var call1481 = request(var_c, "ord", [0]);
      var var_ordval = call1481;
      setLineNumber(470);    // compilenode call
      // call case 2: outer request
      var call1482 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      setLineNumber(471);    // compilenode bind
      if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
      var_startPosition = var_linePosition;
      setLineNumber(472);    // compilenode bind
      if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
      var_startLine = var_lineNumber;
      var if1483 = GraceDone;
      setLineNumber(473);    // compilenode call
      // call case 6: other requests
      if (var_spaceChars === undefined) raiseUninitializedVariable("spaceChars");
      var call1484 = request(var_spaceChars, "contains(1)", [1], var_c);
      if (Grace_isTrue(call1484)) {
        if1483 = GraceDone;
      } else {
        var if1485 = GraceDone;
        setLineNumber(475);    // compilenode string
        var string1486 = new GraceString("\"");
        var opresult1487 = request(var_c, "==(1)", [1], string1486);
        if (Grace_isTrue(opresult1487)) {
          setLineNumber(476);    // compilenode bind
          var_inStr = GraceTrue;
          setLineNumber(477);    // compilenode bind
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          var_stringStart = var_linePosition;
          setLineNumber(478);    // compilenode call
          if (var_quotedStringState === undefined) raiseUninitializedVariable("quotedStringState");
          // call case 2: outer request
          var call1488 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_quotedStringState);
          if1485 = call1488;
        } else {
          var if1489 = GraceDone;
          setLineNumber(479);    // compilenode string
          var string1490 = new GraceString("\u2039");
          var opresult1491 = request(var_c, "==(1)", [1], string1490);
          if (Grace_isTrue(opresult1491)) {
            setLineNumber(480);    // compilenode bind
            var_inStr = GraceTrue;
            setLineNumber(481);    // compilenode bind
            if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
            var_stringStart = var_linePosition;
            setLineNumber(482);    // compilenode call
            if (var_extendedStringState === undefined) raiseUninitializedVariable("extendedStringState");
            // call case 2: outer request
            var call1492 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_extendedStringState);
            if1489 = call1492;
          } else {
            var if1493 = GraceDone;
            setLineNumber(483);    // compilenode call
            // call case 2: outer request
            var call1494 = selfRequest(importedModules["lexer"], "isDigit(1)", [1], var_c);
            if (Grace_isTrue(call1494)) {
              var if1495 = GraceDone;
              setLineNumber(484);    // compilenode member
              // call case 6: other requests
              if (var_tokens === undefined) raiseUninitializedVariable("tokens");
              var call1496 = request(var_tokens, "size", [0]);
              var opresult1497 = request(call1496, ">(1)", [1], new GraceNum(0));
              var block1498 = new GraceBlock(this, 484, 0);
              block1498.guard = jsTrue;
              block1498.real = function block1498() {
                setLineNumber(484);    // compilenode member
                // call case 6: other requests
                // call case 6: other requests
                if (var_tokens === undefined) raiseUninitializedVariable("tokens");
                var call1500 = request(var_tokens, "last", [0]);
                var call1499 = request(call1500, "kind", [0]);
                var string1501 = new GraceString("num");
                var opresult1502 = request(call1499, "==(1)", [1], string1501);
                return opresult1502;
              };
              let applyMeth1498 = function apply (argcv) {
                  return this.real.apply(this.receiver);
              };
              applyMeth1498.methodName = "apply";
              applyMeth1498.paramCounts = [0];
              applyMeth1498.paramNames = [];
              applyMeth1498.definitionLine = 484;
              applyMeth1498.definitionModule = "lexer";
              block1498.methods["apply"] = applyMeth1498;
              var opresult1503 = request(opresult1497, "&&(1)", [1], block1498);
              if (Grace_isTrue(opresult1503)) {
                setLineNumber(485);    // compilenode member
                // call case 6: other requests
                // call case 6: other requests
                if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
                var call1505 = request(var_errormessages, "suggestion", [0]);
                var call1504 = request(call1505, "new", [0]);
                var var_suggestion = call1504;
                setLineNumber(486);    // compilenode member
                // call case 6: other requests
                // call case 6: other requests
                if (var_tokens === undefined) raiseUninitializedVariable("tokens");
                var call1508 = request(var_tokens, "last", [0]);
                var call1507 = request(call1508, "linePos", [0]);
                // call case 6: other requests
                // call case 6: other requests
                if (var_tokens === undefined) raiseUninitializedVariable("tokens");
                var call1510 = request(var_tokens, "last", [0]);
                var call1509 = request(call1510, "size", [0]);
                var sum1511 = request(call1507, "+(1)", [1], call1509);
                if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
                var diff1512 = request(var_linePosition, "-(1)", [1], new GraceNum(1));
                if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
                // call case 6: other requests
                if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
                var call1506 = request(var_suggestion, "deleteRange(2)onLine(1)", [2, 1], sum1511, diff1512, var_lineNumber);
                setLineNumber(488);    // compilenode string
                var string1514 = new GraceString("consecutive numbers are not allowed");
                if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
                setLineNumber(489);    // compilenode member
                // call case 6: other requests
                // call case 6: other requests
                if (var_tokens === undefined) raiseUninitializedVariable("tokens");
                var call1516 = request(var_tokens, "last", [0]);
                var call1515 = request(call1516, "linePos", [0]);
                if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
                if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
                // call case 6: other requests
                if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
                var call1513 = request(var_errormessages, "syntaxError(1)atRange(3)withSuggestion(1)", [1, 3, 1], string1514, var_lineNumber, call1515, var_linePosition, var_suggestion);
                if1495 = call1513;
              }
              setLineNumber(492);    // compilenode bind
              if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
              var_startPosition = var_linePosition;
              setLineNumber(493);    // compilenode call
              if (var_numberStartState === undefined) raiseUninitializedVariable("numberStartState");
              // call case 2: outer request
              var call1517 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberStartState);
              setLineNumber(494);    // compilenode call
              // call case 6: other requests
              if (var_state === undefined) raiseUninitializedVariable("state");
              var call1518 = request(var_state, "consume(1)", [1], var_c);
              if1493 = call1518;
            } else {
              var if1519 = GraceDone;
              setLineNumber(495);    // compilenode call
              // call case 2: outer request
              var call1520 = selfRequest(importedModules["lexer"], "isIdentifierChar(1)", [1], var_c);
              if (Grace_isTrue(call1520)) {
                setLineNumber(496);    // compilenode call
                if (var_identifierState === undefined) raiseUninitializedVariable("identifierState");
                // call case 2: outer request
                var call1521 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_identifierState);
                setLineNumber(497);    // compilenode call
                // call case 6: other requests
                if (var_state === undefined) raiseUninitializedVariable("state");
                var call1522 = request(var_state, "consume(1)", [1], var_c);
                if1519 = call1522;
              } else {
                var if1523 = GraceDone;
                setLineNumber(498);    // compilenode string
                var string1524 = new GraceString("/");
                var opresult1525 = request(var_c, "==(1)", [1], string1524);
                if (Grace_isTrue(opresult1525)) {
                  setLineNumber(499);    // compilenode call
                  if (var_slashState === undefined) raiseUninitializedVariable("slashState");
                  // call case 2: outer request
                  var call1526 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_slashState);
                  setLineNumber(500);    // compilenode call
                  // call case 6: other requests
                  if (var_state === undefined) raiseUninitializedVariable("state");
                  var call1527 = request(var_state, "consume(1)", [1], var_c);
                  if1523 = call1527;
                } else {
                  var if1528 = GraceDone;
                  setLineNumber(501);    // compilenode string
                  var string1529 = new GraceString(".");
                  var opresult1530 = request(var_c, "==(1)", [1], string1529);
                  if (Grace_isTrue(opresult1530)) {
                    setLineNumber(502);    // compilenode call
                    if (var_dotState === undefined) raiseUninitializedVariable("dotState");
                    // call case 2: outer request
                    var call1531 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_dotState);
                    setLineNumber(503);    // compilenode call
                    // call case 6: other requests
                    if (var_state === undefined) raiseUninitializedVariable("state");
                    var call1532 = request(var_state, "consume(1)", [1], var_c);
                    if1528 = call1532;
                  } else {
                    var if1533 = GraceDone;
                    setLineNumber(504);    // compilenode call
                    if (var_ordval === undefined) raiseUninitializedVariable("ordval");
                    // call case 2: outer request
                    var call1534 = selfRequest(importedModules["lexer"], "isOperatorChar(2)", [2], var_c, var_ordval);
                    if (Grace_isTrue(call1534)) {
                      setLineNumber(505);    // compilenode call
                      if (var_operatorState === undefined) raiseUninitializedVariable("operatorState");
                      // call case 2: outer request
                      var call1535 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_operatorState);
                      setLineNumber(506);    // compilenode call
                      // call case 6: other requests
                      if (var_state === undefined) raiseUninitializedVariable("state");
                      var call1536 = request(var_state, "consume(1)", [1], var_c);
                      if1533 = call1536;
                    } else {
                      var if1537 = GraceDone;
                      setLineNumber(507);    // compilenode string
                      var string1538 = new GraceString(",");
                      var opresult1539 = request(var_c, "==(1)", [1], string1538);
                      if (Grace_isTrue(opresult1539)) {
                        setLineNumber(508);    // compilenode member
                        // call case 2: outer request
                        var call1541 = selfRequest(importedModules["lexer"], "commaToken", [0]);
                        // call case 2: outer request
                        var call1540 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1541);
                        if1537 = call1540;
                      } else {
                        var if1542 = GraceDone;
                        setLineNumber(509);    // compilenode string
                        var string1543 = new GraceString("{");
                        var opresult1544 = request(var_c, "==(1)", [1], string1543);
                        if (Grace_isTrue(opresult1544)) {
                          setLineNumber(510);    // compilenode member
                          // call case 2: outer request
                          var call1545 = selfRequest(importedModules["lexer"], "tallyLeftBrace", [0]);
                          setLineNumber(511);    // compilenode member
                          // call case 2: outer request
                          var call1547 = selfRequest(importedModules["lexer"], "lBraceToken", [0]);
                          // call case 2: outer request
                          var call1546 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1547);
                          if1542 = call1546;
                        } else {
                          var if1548 = GraceDone;
                          setLineNumber(512);    // compilenode string
                          var string1549 = new GraceString("}");
                          var opresult1550 = request(var_c, "==(1)", [1], string1549);
                          if (Grace_isTrue(opresult1550)) {
                            setLineNumber(513);    // compilenode call
                            if (var_rBraceState === undefined) raiseUninitializedVariable("rBraceState");
                            // call case 2: outer request
                            var call1551 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_rBraceState);
                            setLineNumber(514);    // compilenode call
                            // call case 6: other requests
                            if (var_state === undefined) raiseUninitializedVariable("state");
                            var call1552 = request(var_state, "consume(1)", [1], var_c);
                            if1548 = call1552;
                          } else {
                            var if1553 = GraceDone;
                            setLineNumber(515);    // compilenode string
                            var string1554 = new GraceString("(");
                            var opresult1555 = request(var_c, "==(1)", [1], string1554);
                            if (Grace_isTrue(opresult1555)) {
                              setLineNumber(516);    // compilenode member
                              // call case 2: outer request
                              var call1557 = selfRequest(importedModules["lexer"], "lParenToken", [0]);
                              // call case 2: outer request
                              var call1556 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1557);
                              if1553 = call1556;
                            } else {
                              var if1558 = GraceDone;
                              setLineNumber(517);    // compilenode string
                              var string1559 = new GraceString(")");
                              var opresult1560 = request(var_c, "==(1)", [1], string1559);
                              if (Grace_isTrue(opresult1560)) {
                                var if1561 = GraceDone;
                                setLineNumber(518);    // compilenode member
                                // call case 6: other requests
                                if (var_tokens === undefined) raiseUninitializedVariable("tokens");
                                var call1562 = request(var_tokens, "size", [0]);
                                var opresult1563 = request(call1562, ">(1)", [1], new GraceNum(0));
                                var block1564 = new GraceBlock(this, 518, 0);
                                block1564.guard = jsTrue;
                                block1564.real = function block1564() {
                                  setLineNumber(518);    // compilenode member
                                  // call case 6: other requests
                                  // call case 6: other requests
                                  if (var_tokens === undefined) raiseUninitializedVariable("tokens");
                                  var call1566 = request(var_tokens, "last", [0]);
                                  var call1565 = request(call1566, "kind", [0]);
                                  var string1567 = new GraceString("lparen");
                                  var opresult1568 = request(call1565, "==(1)", [1], string1567);
                                  return opresult1568;
                                };
                                let applyMeth1564 = function apply (argcv) {
                                    return this.real.apply(this.receiver);
                                };
                                applyMeth1564.methodName = "apply";
                                applyMeth1564.paramCounts = [0];
                                applyMeth1564.paramNames = [];
                                applyMeth1564.definitionLine = 518;
                                applyMeth1564.definitionModule = "lexer";
                                block1564.methods["apply"] = applyMeth1564;
                                var opresult1569 = request(opresult1563, "&&(1)", [1], block1564);
                                if (Grace_isTrue(opresult1569)) {
                                  setLineNumber(519);    // compilenode string
                                  var string1571 = new GraceString("empty parenthesis are not allowed; ");
                                  setLineNumber(520);    // compilenode string
                                  var string1572 = new GraceString("remove them, or put something between them");
                                  var opresult1573 = request(string1571, "++(1)", [1], string1572);
                                  if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
                                  setLineNumber(521);    // compilenode member
                                  // call case 6: other requests
                                  // call case 6: other requests
                                  if (var_tokens === undefined) raiseUninitializedVariable("tokens");
                                  var call1575 = request(var_tokens, "last", [0]);
                                  var call1574 = request(call1575, "linePos", [0]);
                                  if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
                                  // call case 6: other requests
                                  if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
                                  var call1570 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], opresult1573, var_lineNumber, call1574, var_linePosition);
                                  if1561 = call1570;
                                }
                                setLineNumber(523);    // compilenode member
                                // call case 2: outer request
                                var call1577 = selfRequest(importedModules["lexer"], "rParenToken", [0]);
                                // call case 2: outer request
                                var call1576 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1577);
                                if1558 = call1576;
                              } else {
                                var if1578 = GraceDone;
                                setLineNumber(524);    // compilenode string
                                var string1579 = new GraceString("[");
                                var opresult1580 = request(var_c, "==(1)", [1], string1579);
                                if (Grace_isTrue(opresult1580)) {
                                  setLineNumber(525);    // compilenode member
                                  // call case 2: outer request
                                  var call1582 = selfRequest(importedModules["lexer"], "lSquareToken", [0]);
                                  // call case 2: outer request
                                  var call1581 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1582);
                                  if1578 = call1581;
                                } else {
                                  var if1583 = GraceDone;
                                  setLineNumber(526);    // compilenode string
                                  var string1584 = new GraceString("]");
                                  var opresult1585 = request(var_c, "==(1)", [1], string1584);
                                  if (Grace_isTrue(opresult1585)) {
                                    setLineNumber(527);    // compilenode member
                                    // call case 2: outer request
                                    var call1587 = selfRequest(importedModules["lexer"], "rSquareToken", [0]);
                                    // call case 2: outer request
                                    var call1586 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1587);
                                    if1583 = call1586;
                                  } else {
                                    var if1588 = GraceDone;
                                    setLineNumber(528);    // compilenode string
                                    var string1589 = new GraceString("\u27e6");
                                    var opresult1590 = request(var_c, "==(1)", [1], string1589);
                                    if (Grace_isTrue(opresult1590)) {
                                      setLineNumber(529);    // compilenode member
                                      // call case 2: outer request
                                      var call1592 = selfRequest(importedModules["lexer"], "lGenericToken", [0]);
                                      // call case 2: outer request
                                      var call1591 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1592);
                                      if1588 = call1591;
                                    } else {
                                      var if1593 = GraceDone;
                                      setLineNumber(530);    // compilenode string
                                      var string1594 = new GraceString("\u27e7");
                                      var opresult1595 = request(var_c, "==(1)", [1], string1594);
                                      if (Grace_isTrue(opresult1595)) {
                                        setLineNumber(531);    // compilenode member
                                        // call case 2: outer request
                                        var call1597 = selfRequest(importedModules["lexer"], "rGenericToken", [0]);
                                        // call case 2: outer request
                                        var call1596 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1597);
                                        if1593 = call1596;
                                      } else {
                                        var if1598 = GraceDone;
                                        setLineNumber(532);    // compilenode string
                                        var string1599 = new GraceString(";");
                                        var opresult1600 = request(var_c, "==(1)", [1], string1599);
                                        if (Grace_isTrue(opresult1600)) {
                                          setLineNumber(533);    // compilenode member
                                          // call case 2: outer request
                                          var call1602 = selfRequest(importedModules["lexer"], "semicolonToken", [0]);
                                          // call case 2: outer request
                                          var call1601 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1602);
                                          if1598 = call1601;
                                        } else {
                                          var if1603 = GraceDone;
                                          setLineNumber(534);    // compilenode string
                                          var string1604 = new GraceString("\n");
                                          var opresult1605 = request(var_c, "==(1)", [1], string1604);
                                          if (Grace_isTrue(opresult1605)) {
                                            var if1606 = GraceDone;
                                            setLineNumber(535);    // compilenode op
                                            if (var_interpDepth === undefined) raiseUninitializedVariable("interpDepth");
                                            var opresult1607 = request(var_interpDepth, ">(1)", [1], new GraceNum(0));
                                            if (Grace_isTrue(opresult1607)) {
                                              // call case 2: outer request
                                              var call1608 = selfRequest(importedModules["lexer"], "newlineInInterpolationError", [0]);
                                              if1606 = call1608;
                                            }
                                            setLineNumber(536);    // compilenode num
                                            var_currentLineIndent = new GraceNum(0);
                                            setLineNumber(537);    // compilenode call
                                            if (var_indentationState === undefined) raiseUninitializedVariable("indentationState");
                                            // call case 2: outer request
                                            var call1609 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_indentationState);
                                            if1603 = call1609;
                                          } else {
                                            var if1610 = GraceDone;
                                            setLineNumber(539);    // compilenode member
                                            // call case 6: other requests
                                            if (var_ordval === undefined) raiseUninitializedVariable("ordval");
                                            // call case 6: other requests
                                            if (var_unicode === undefined) raiseUninitializedVariable("unicode");
                                            var call1612 = request(var_unicode, "isSeparator(1)", [1], var_ordval);
                                            var call1611 = request(call1612, "not", [0]);
                                            setLineNumber(540);    // compilenode member
                                            // call case 6: other requests
                                            if (var_ordval === undefined) raiseUninitializedVariable("ordval");
                                            // call case 6: other requests
                                            if (var_unicode === undefined) raiseUninitializedVariable("unicode");
                                            var call1614 = request(var_unicode, "isControl(1)", [1], var_ordval);
                                            var call1613 = request(call1614, "not", [0]);
                                            var opresult1615 = request(call1611, "&&(1)", [1], call1613);
                                            setLineNumber(541);    // compilenode op
                                            if (var_ordval === undefined) raiseUninitializedVariable("ordval");
                                            var opresult1616 = request(var_ordval, "\u2260(1)", [1], new GraceNum(10));
                                            var opresult1617 = request(opresult1615, "&&(1)", [1], opresult1616);
                                            setLineNumber(542);    // compilenode op
                                            if (var_ordval === undefined) raiseUninitializedVariable("ordval");
                                            var opresult1618 = request(var_ordval, "\u2260(1)", [1], new GraceNum(13));
                                            var opresult1619 = request(opresult1617, "&&(1)", [1], opresult1618);
                                            setLineNumber(543);    // compilenode op
                                            if (var_ordval === undefined) raiseUninitializedVariable("ordval");
                                            var opresult1620 = request(var_ordval, "\u2260(1)", [1], new GraceNum(32));
                                            var opresult1621 = request(opresult1619, "&&(1)", [1], opresult1620);
                                            if (Grace_isTrue(opresult1621)) {
                                              setLineNumber(544);    // compilenode string
                                              var string1623 = new GraceString("");
                                              // call case 6: other requests
                                              if (var_unicode === undefined) raiseUninitializedVariable("unicode");
                                              var call1624 = request(var_unicode, "name(1)", [1], var_c);
                                              var opresult1625 = request(string1623, "++(1)", [1], call1624);
                                              var string1626 = new GraceString(" (U+");
                                              var opresult1627 = request(opresult1625, "++(1)", [1], string1626);
                                              if (var_ordval === undefined) raiseUninitializedVariable("ordval");
                                              // call case 2: outer request
                                              var call1628 = selfRequest(importedModules["lexer"], "hex4(1)", [1], var_ordval);
                                              var opresult1629 = request(opresult1627, "++(1)", [1], call1628);
                                              var string1630 = new GraceString(") ");
                                              var opresult1631 = request(opresult1629, "++(1)", [1], string1630);
                                              setLineNumber(545);    // compilenode string
                                              var string1632 = new GraceString("is not a valid character");
                                              var opresult1633 = request(opresult1631, "++(1)", [1], string1632);
                                              if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
                                              if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
                                              // call case 6: other requests
                                              if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
                                              var call1622 = request(var_errormessages, "syntaxError(1)atPosition(2)", [1, 2], opresult1633, var_lineNumber, var_linePosition);
                                              if1610 = call1622;
                                            }
                                            if1603 = if1610;
                                          }
                                          if1598 = if1603;
                                        }
                                        if1593 = if1598;
                                      }
                                      if1588 = if1593;
                                    }
                                    if1583 = if1588;
                                  }
                                  if1578 = if1583;
                                }
                                if1558 = if1578;
                              }
                              if1553 = if1558;
                            }
                            if1548 = if1553;
                          }
                          if1542 = if1548;
                        }
                        if1537 = if1542;
                      }
                      if1533 = if1537;
                    }
                    if1528 = if1533;
                  }
                  if1523 = if1528;
                }
                if1519 = if1523;
              }
              if1493 = if1519;
            }
            if1489 = if1493;
          }
          if1485 = if1489;
        }
        if1483 = if1485;
      }
      return if1483;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1480;
    func1480.methodName = "consume(1)";
    func1480.paramCounts = [1];
    func1480.paramNames = ["c"];
    func1480.definitionLine = 468;
    func1480.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 467;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1479_init = function() {    // init of object on line 467
    };
    return obj1479_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1479 = emptyGraceObject("defaultState", "lexer", 467);
  var obj1479_init = obj1479_build.call(obj1479, null, this, [], []);
  obj1479_init.call(obj1479);  // end of compileobject
  var var_defaultState = obj1479;
  var reader1634_defaultState = function() {  // reader method defaultState
      if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
      return var_defaultState;
  };
  reader1634_defaultState.isDef = true;
  reader1634_defaultState.confidential = true;
  this.methods["defaultState"] = reader1634_defaultState;
  setLineNumber(552);    // compilenode object
  var obj1635_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_552");
    this.outer_lexer_552 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1636 = function(argcv, var_c) {    // method consume(_), line 553
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      var if1637 = GraceDone;
      setLineNumber(554);    // compilenode op
      if (var_interpDepth === undefined) raiseUninitializedVariable("interpDepth");
      var opresult1638 = request(var_interpDepth, ">(1)", [1], new GraceNum(0));
      if (Grace_isTrue(opresult1638)) {
        var if1639 = GraceDone;
        setLineNumber(555);    // compilenode member
        // call case 6: other requests
        // call case 6: other requests
        if (var_tokens === undefined) raiseUninitializedVariable("tokens");
        var call1641 = request(var_tokens, "last", [0]);
        var call1640 = request(call1641, "kind", [0]);
        var string1642 = new GraceString("lparen");
        var opresult1643 = request(call1640, "==(1)", [1], string1642);
        if (Grace_isTrue(opresult1643)) {
          setLineNumber(556);    // compilenode member
          // call case 2: outer request
          var call1644 = selfRequest(importedModules["lexer"], "reportEmptyStringInterpolation", [0]);
          if1639 = call1644;
        }
        var if1645 = GraceDone;
        setLineNumber(558);    // compilenode op
        if (var_currentLineBraceDepth === undefined) raiseUninitializedVariable("currentLineBraceDepth");
        if (var_braceDepthAtStartOfInterp === undefined) raiseUninitializedVariable("braceDepthAtStartOfInterp");
        var opresult1646 = request(var_currentLineBraceDepth, "==(1)", [1], var_braceDepthAtStartOfInterp);
        if (Grace_isTrue(opresult1646)) {
          setLineNumber(559);    // compilenode op
          if (var_interpDepth === undefined) raiseUninitializedVariable("interpDepth");
          var diff1647 = request(var_interpDepth, "-(1)", [1], new GraceNum(1));
          var_interpDepth = diff1647;
          if1645 = GraceDone;
        }
        var if1648 = GraceDone;
        setLineNumber(561);    // compilenode op
        if (var_interpDepth === undefined) raiseUninitializedVariable("interpDepth");
        var opresult1649 = request(var_interpDepth, "==(1)", [1], new GraceNum(0));
        if (Grace_isTrue(opresult1649)) {
          setLineNumber(562);    // compilenode member
          // call case 2: outer request
          var call1651 = selfRequest(importedModules["lexer"], "rParenToken", [0]);
          // call case 2: outer request
          var call1650 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1651);
          setLineNumber(563);    // compilenode string
          var string1654 = new GraceString("++");
          // call case 2: outer request
          var call1653 = selfRequest(importedModules["lexer"], "opToken(1)", [1], string1654);
          // call case 2: outer request
          var call1652 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1653);
          setLineNumber(564);    // compilenode bind
          var_inStr = GraceTrue;
          setLineNumber(565);    // compilenode call
          if (var_quotedStringState === undefined) raiseUninitializedVariable("quotedStringState");
          // call case 2: outer request
          var call1655 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_quotedStringState);
          setLineNumber(566);    // compilenode return
          return var_done;
        }
        if1637 = if1648;
      }
      setLineNumber(569);    // compilenode member
      // call case 2: outer request
      var call1656 = selfRequest(importedModules["lexer"], "rightBrace", [0]);
      setLineNumber(570);    // compilenode member
      // call case 2: outer request
      var call1658 = selfRequest(importedModules["lexer"], "rBraceToken", [0]);
      // call case 2: outer request
      var call1657 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1658);
      setLineNumber(571);    // compilenode call
      if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
      // call case 2: outer request
      var call1659 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
      return call1659;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1636;
    func1636.methodName = "consume(1)";
    func1636.paramCounts = [1];
    func1636.paramNames = ["c"];
    func1636.definitionLine = 553;
    func1636.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 552;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1635_init = function() {    // init of object on line 552
    };
    return obj1635_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1635 = emptyGraceObject("rBraceState", "lexer", 552);
  var obj1635_init = obj1635_build.call(obj1635, null, this, [], []);
  obj1635_init.call(obj1635);  // end of compileobject
  var var_rBraceState = obj1635;
  var reader1660_rBraceState = function() {  // reader method rBraceState
      if (var_rBraceState === undefined) raiseUninitializedVariable("rBraceState");
      return var_rBraceState;
  };
  reader1660_rBraceState.isDef = true;
  reader1660_rBraceState.confidential = true;
  this.methods["rBraceState"] = reader1660_rBraceState;
  setLineNumber(575);    // compilenode object
  var obj1661_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_575");
    this.outer_lexer_575 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1662 = function(argcv, var_c) {    // method consume(_), line 576
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(577);    // compilenode call
      // call case 2: outer request
      var call1663 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1664 = GraceDone;
      setLineNumber(578);    // compilenode string
      var string1665 = new GraceString("/");
      var opresult1666 = request(var_c, "==(1)", [1], string1665);
      if (Grace_isTrue(opresult1666)) {
        setLineNumber(579);    // compilenode call
        // call case 2: outer request
        var call1667 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        var if1668 = GraceDone;
        setLineNumber(580);    // compilenode op
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        var string1669 = new GraceString("//");
        var opresult1670 = request(var_accum, "==(1)", [1], string1669);
        if (Grace_isTrue(opresult1670)) {
          setLineNumber(581);    // compilenode call
          if (var_commentSpaceState === undefined) raiseUninitializedVariable("commentSpaceState");
          // call case 2: outer request
          var call1671 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_commentSpaceState);
          if1668 = call1671;
        }
        if1664 = if1668;
      } else {
        var if1672 = GraceDone;
        setLineNumber(583);    // compilenode op
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        var string1673 = new GraceString("/");
        var opresult1674 = request(var_accum, "==(1)", [1], string1673);
        if (Grace_isTrue(opresult1674)) {
          setLineNumber(584);    // compilenode call
          if (var_operatorState === undefined) raiseUninitializedVariable("operatorState");
          // call case 2: outer request
          var call1675 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_operatorState);
          setLineNumber(585);    // compilenode call
          // call case 6: other requests
          if (var_state === undefined) raiseUninitializedVariable("state");
          var call1676 = request(var_state, "consume(1)", [1], var_c);
          if1672 = call1676;
        } else {
          setLineNumber(587);    // compilenode call
          if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
          // call case 2: outer request
          var call1677 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
          setLineNumber(588);    // compilenode call
          // call case 6: other requests
          if (var_state === undefined) raiseUninitializedVariable("state");
          var call1678 = request(var_state, "consume(1)", [1], var_c);
          if1672 = call1678;
        }
        if1664 = if1672;
      }
      return if1664;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1662;
    func1662.methodName = "consume(1)";
    func1662.paramCounts = [1];
    func1662.paramNames = ["c"];
    func1662.definitionLine = 576;
    func1662.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 575;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1661_init = function() {    // init of object on line 575
    };
    return obj1661_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1661 = emptyGraceObject("slashState", "lexer", 575);
  var obj1661_init = obj1661_build.call(obj1661, null, this, [], []);
  obj1661_init.call(obj1661);  // end of compileobject
  var var_slashState = obj1661;
  var reader1679_slashState = function() {  // reader method slashState
      if (var_slashState === undefined) raiseUninitializedVariable("slashState");
      return var_slashState;
  };
  reader1679_slashState.isDef = true;
  reader1679_slashState.confidential = true;
  this.methods["slashState"] = reader1679_slashState;
  setLineNumber(593);    // compilenode object
  var obj1680_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_593");
    this.outer_lexer_593 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1681 = function(argcv, var_c) {    // method consume(_), line 594
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(595);    // compilenode call
      // call case 2: outer request
      var call1682 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1683 = GraceDone;
      setLineNumber(596);    // compilenode string
      var string1684 = new GraceString(".");
      var opresult1685 = request(var_c, "==(1)", [1], string1684);
      if (Grace_isTrue(opresult1685)) {
        setLineNumber(597);    // compilenode call
        // call case 2: outer request
        var call1686 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        var if1687 = GraceDone;
        setLineNumber(598);    // compilenode op
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        var string1688 = new GraceString("...");
        var opresult1689 = request(var_accum, "==(1)", [1], string1688);
        if (Grace_isTrue(opresult1689)) {
          setLineNumber(599);    // compilenode call
          if (var_identifierState === undefined) raiseUninitializedVariable("identifierState");
          // call case 2: outer request
          var call1690 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_identifierState);
          if1687 = call1690;
        }
        if1683 = if1687;
      } else {
        var if1691 = GraceDone;
        setLineNumber(601);    // compilenode op
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        var string1692 = new GraceString("..");
        var opresult1693 = request(var_accum, "==(1)", [1], string1692);
        if (Grace_isTrue(opresult1693)) {
          setLineNumber(602);    // compilenode call
          if (var_operatorState === undefined) raiseUninitializedVariable("operatorState");
          // call case 2: outer request
          var call1694 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_operatorState);
          setLineNumber(603);    // compilenode call
          // call case 6: other requests
          if (var_state === undefined) raiseUninitializedVariable("state");
          var call1695 = request(var_state, "consume(1)", [1], var_c);
          if1691 = call1695;
        } else {
          var if1696 = GraceDone;
          setLineNumber(604);    // compilenode call
          // call case 2: outer request
          var call1697 = selfRequest(importedModules["lexer"], "isDigit(1)", [1], var_c);
          if (Grace_isTrue(call1697)) {
            setLineNumber(605);    // compilenode string
            var string1698 = new GraceString("0.");
            var_accum = string1698;
            setLineNumber(607);    // compilenode call
            if (var_numberFractionState === undefined) raiseUninitializedVariable("numberFractionState");
            // call case 2: outer request
            var call1699 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberFractionState);
            setLineNumber(608);    // compilenode call
            // call case 6: other requests
            if (var_state === undefined) raiseUninitializedVariable("state");
            var call1700 = request(var_state, "consume(1)", [1], var_c);
            if1696 = call1700;
          } else {
            setLineNumber(610);    // compilenode member
            // call case 2: outer request
            var call1702 = selfRequest(importedModules["lexer"], "dotToken", [0]);
            // call case 2: outer request
            var call1701 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1702);
            setLineNumber(611);    // compilenode call
            if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
            // call case 2: outer request
            var call1703 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
            setLineNumber(612);    // compilenode call
            // call case 6: other requests
            if (var_state === undefined) raiseUninitializedVariable("state");
            var call1704 = request(var_state, "consume(1)", [1], var_c);
            if1696 = call1704;
          }
          if1691 = if1696;
        }
        if1683 = if1691;
      }
      return if1683;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1681;
    func1681.methodName = "consume(1)";
    func1681.paramCounts = [1];
    func1681.paramNames = ["c"];
    func1681.definitionLine = 594;
    func1681.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 593;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1680_init = function() {    // init of object on line 593
    };
    return obj1680_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1680 = emptyGraceObject("dotState", "lexer", 593);
  var obj1680_init = obj1680_build.call(obj1680, null, this, [], []);
  obj1680_init.call(obj1680);  // end of compileobject
  var var_dotState = obj1680;
  var reader1705_dotState = function() {  // reader method dotState
      if (var_dotState === undefined) raiseUninitializedVariable("dotState");
      return var_dotState;
  };
  reader1705_dotState.isDef = true;
  reader1705_dotState.confidential = true;
  this.methods["dotState"] = reader1705_dotState;
  setLineNumber(617);    // compilenode object
  var obj1706_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_617");
    this.outer_lexer_617 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1707 = function(argcv, var_c) {    // method consume(_), line 618
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(619);    // compilenode call
      // call case 2: outer request
      var call1708 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1709 = GraceDone;
      setLineNumber(620);    // compilenode call
      // call case 6: other requests
      // call case 2: outer request
      var call1711 = selfRequest(importedModules["lexer"], "isIdentifierChar(1)", [1], var_c);
      var call1710 = request(call1711, "prefix!", [0]);
      if (Grace_isTrue(call1710)) {
        var if1712 = GraceDone;
        setLineNumber(621);    // compilenode call
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        // call case 6: other requests
        if (var_keywords === undefined) raiseUninitializedVariable("keywords");
        var call1713 = request(var_keywords, "containsKey(1)", [1], var_accum);
        if (Grace_isTrue(call1713)) {
          setLineNumber(622);    // compilenode call
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          // call case 2: outer request
          var call1715 = selfRequest(importedModules["lexer"], "keywordToken(1)", [1], var_accum);
          // call case 2: outer request
          var call1714 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1715);
          if1712 = call1714;
        } else {
          setLineNumber(624);    // compilenode call
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          // call case 2: outer request
          var call1717 = selfRequest(importedModules["lexer"], "identifierToken(1)", [1], var_accum);
          // call case 2: outer request
          var call1716 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1717);
          if1712 = call1716;
        }
        setLineNumber(626);    // compilenode call
        if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
        // call case 2: outer request
        var call1718 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
        setLineNumber(627);    // compilenode call
        // call case 6: other requests
        if (var_state === undefined) raiseUninitializedVariable("state");
        var call1719 = request(var_state, "consume(1)", [1], var_c);
        if1709 = call1719;
      } else {
        setLineNumber(629);    // compilenode call
        // call case 2: outer request
        var call1720 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if1709 = call1720;
      }
      return if1709;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1707;
    func1707.methodName = "consume(1)";
    func1707.paramCounts = [1];
    func1707.paramNames = ["c"];
    func1707.definitionLine = 618;
    func1707.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 617;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1706_init = function() {    // init of object on line 617
    };
    return obj1706_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1706 = emptyGraceObject("identifierState", "lexer", 617);
  var obj1706_init = obj1706_build.call(obj1706, null, this, [], []);
  obj1706_init.call(obj1706);  // end of compileobject
  var var_identifierState = obj1706;
  var reader1721_identifierState = function() {  // reader method identifierState
      if (var_identifierState === undefined) raiseUninitializedVariable("identifierState");
      return var_identifierState;
  };
  reader1721_identifierState.isDef = true;
  reader1721_identifierState.confidential = true;
  this.methods["identifierState"] = reader1721_identifierState;
  setLineNumber(633);    // compilenode object
  var obj1722_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_633");
    this.outer_lexer_633 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1723 = function(argcv, var_c) {    // method consume(_), line 634
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(636);    // compilenode call
      // call case 2: outer request
      var call1724 = selfRequest(importedModules["lexer"], "checkSeparatorString(1)", [1], var_c);
      var if1725 = GraceDone;
      setLineNumber(637);    // compilenode string
      var string1726 = new GraceString("\"");
      var opresult1727 = request(var_c, "==(1)", [1], string1726);
      if (Grace_isTrue(opresult1727)) {
        setLineNumber(638);    // compilenode call
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        // call case 2: outer request
        var call1729 = selfRequest(importedModules["lexer"], "stringToken(1)", [1], var_accum);
        // call case 2: outer request
        var call1728 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1729);
        var if1730 = GraceDone;
        setLineNumber(639);    // compilenode op
        if (var_stringContainsInterp === undefined) raiseUninitializedVariable("stringContainsInterp");
        if (var_interpDepth === undefined) raiseUninitializedVariable("interpDepth");
        var opresult1731 = request(var_interpDepth, "==(1)", [1], new GraceNum(0));
        var opresult1732 = request(var_stringContainsInterp, "&&(1)", [1], opresult1731);
        if (Grace_isTrue(opresult1732)) {
          setLineNumber(640);    // compilenode member
          // call case 2: outer request
          var call1734 = selfRequest(importedModules["lexer"], "rParenToken", [0]);
          // call case 2: outer request
          var call1733 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1734);
          setLineNumber(641);    // compilenode bind
          var_stringContainsInterp = GraceFalse;
          if1730 = GraceDone;
        }
        setLineNumber(643);    // compilenode call
        if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
        // call case 2: outer request
        var call1735 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
        setLineNumber(644);    // compilenode bind
        var_inStr = GraceFalse;
        if1725 = GraceDone;
      } else {
        var if1736 = GraceDone;
        setLineNumber(645);    // compilenode string
        var string1737 = new GraceString("\n");
        var opresult1738 = request(var_c, "==(1)", [1], string1737);
        if (Grace_isTrue(opresult1738)) {
          setLineNumber(646);    // compilenode member
          // call case 2: outer request
          var call1739 = selfRequest(importedModules["lexer"], "unclosedStringError", [0]);
          if1736 = call1739;
        } else {
          var if1740 = GraceDone;
          setLineNumber(647);    // compilenode string
          var string1741 = new GraceString("{");
          var opresult1742 = request(var_c, "==(1)", [1], string1741);
          if (Grace_isTrue(opresult1742)) {
            setLineNumber(648);    // compilenode call
            if (var_accum === undefined) raiseUninitializedVariable("accum");
            // call case 2: outer request
            var call1743 = selfRequest(importedModules["lexer"], "stringToken(1)", [1], var_accum);
            var var_strToken = call1743;
            setLineNumber(649);    // compilenode bind
            if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
            var_startPosition = var_linePosition;
            var if1744 = GraceDone;
            setLineNumber(650);    // compilenode call
            // call case 6: other requests
            if (var_stringContainsInterp === undefined) raiseUninitializedVariable("stringContainsInterp");
            var call1745 = request(var_stringContainsInterp, "prefix!", [0]);
            if (Grace_isTrue(call1745)) {
              setLineNumber(651);    // compilenode member
              // call case 2: outer request
              var call1747 = selfRequest(importedModules["lexer"], "lParenToken", [0]);
              // call case 2: outer request
              var call1746 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1747);
              setLineNumber(652);    // compilenode bind
              var_stringContainsInterp = GraceTrue;
              if1744 = GraceDone;
            }
            setLineNumber(654);    // compilenode call
            if (var_strToken === undefined) raiseUninitializedVariable("strToken");
            // call case 2: outer request
            var call1748 = selfRequest(importedModules["lexer"], "emit(1)", [1], var_strToken);
            setLineNumber(655);    // compilenode string
            var string1751 = new GraceString("++");
            // call case 2: outer request
            var call1750 = selfRequest(importedModules["lexer"], "opToken(1)", [1], string1751);
            // call case 2: outer request
            var call1749 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1750);
            setLineNumber(656);    // compilenode member
            // call case 2: outer request
            var call1753 = selfRequest(importedModules["lexer"], "lParenToken", [0]);
            // call case 2: outer request
            var call1752 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1753);
            setLineNumber(657);    // compilenode call
            if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
            // call case 2: outer request
            var call1754 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
            setLineNumber(658);    // compilenode bind
            if (var_currentLineBraceDepth === undefined) raiseUninitializedVariable("currentLineBraceDepth");
            var_braceDepthAtStartOfInterp = var_currentLineBraceDepth;
            setLineNumber(659);    // compilenode bind
            var_inStr = GraceFalse;
            setLineNumber(660);    // compilenode op
            if (var_interpDepth === undefined) raiseUninitializedVariable("interpDepth");
            var sum1755 = request(var_interpDepth, "+(1)", [1], new GraceNum(1));
            var_interpDepth = sum1755;
            if1740 = GraceDone;
          } else {
            var if1756 = GraceDone;
            setLineNumber(661);    // compilenode string
            var string1757 = new GraceString("\\");
            var opresult1758 = request(var_c, "==(1)", [1], string1757);
            if (Grace_isTrue(opresult1758)) {
              setLineNumber(662);    // compilenode call
              if (var_quotedStringEscapedState === undefined) raiseUninitializedVariable("quotedStringEscapedState");
              // call case 2: outer request
              var call1759 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_quotedStringEscapedState);
              if1756 = call1759;
            } else {
              setLineNumber(664);    // compilenode call
              // call case 2: outer request
              var call1760 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
              if1756 = call1760;
            }
            if1740 = if1756;
          }
          if1736 = if1740;
        }
        if1725 = if1736;
      }
      return if1725;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1723;
    func1723.methodName = "consume(1)";
    func1723.paramCounts = [1];
    func1723.paramNames = ["c"];
    func1723.definitionLine = 634;
    func1723.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 633;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1722_init = function() {    // init of object on line 633
    };
    return obj1722_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1722 = emptyGraceObject("quotedStringState", "lexer", 633);
  var obj1722_init = obj1722_build.call(obj1722, null, this, [], []);
  obj1722_init.call(obj1722);  // end of compileobject
  var var_quotedStringState = obj1722;
  var reader1761_quotedStringState = function() {  // reader method quotedStringState
      if (var_quotedStringState === undefined) raiseUninitializedVariable("quotedStringState");
      return var_quotedStringState;
  };
  reader1761_quotedStringState.isDef = true;
  reader1761_quotedStringState.confidential = true;
  this.methods["quotedStringState"] = reader1761_quotedStringState;
  setLineNumber(668);    // compilenode object
  var obj1762_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_668");
    this.outer_lexer_668 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1763 = function(argcv, var_c) {    // method consume(_), line 669
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(670);    // compilenode op
      if (var_unichars === undefined) raiseUninitializedVariable("unichars");
      var diff1764 = request(var_unichars, "-(1)", [1], new GraceNum(1));
      var_unichars = diff1764;
      setLineNumber(671);    // compilenode op
      if (var_codepoint === undefined) raiseUninitializedVariable("codepoint");
      var prod1765 = request(var_codepoint, "*(1)", [1], new GraceNum(16));
      var_codepoint = prod1765;
      setLineNumber(672);    // compilenode op
      if (var_codepoint === undefined) raiseUninitializedVariable("codepoint");
      // call case 2: outer request
      var call1766 = selfRequest(importedModules["lexer"], "hexdecchar(1)", [1], var_c);
      var sum1767 = request(var_codepoint, "+(1)", [1], call1766);
      var_codepoint = sum1767;
      var if1768 = GraceDone;
      setLineNumber(674);    // compilenode op
      if (var_unichars === undefined) raiseUninitializedVariable("unichars");
      var opresult1769 = request(var_unichars, "==(1)", [1], new GraceNum(0));
      if (Grace_isTrue(opresult1769)) {
        setLineNumber(677);    // compilenode call
        if (var_codepoint === undefined) raiseUninitializedVariable("codepoint");
        // call case 6: other requests
        if (var_unicode === undefined) raiseUninitializedVariable("unicode");
        var call1771 = request(var_unicode, "create(1)", [1], var_codepoint);
        // call case 2: outer request
        var call1770 = selfRequest(importedModules["lexer"], "store(1)", [1], call1771);
        setLineNumber(678);    // compilenode call
        if (var_quotedStringState === undefined) raiseUninitializedVariable("quotedStringState");
        // call case 2: outer request
        var call1772 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_quotedStringState);
        if1768 = call1772;
      }
      return if1768;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1763;
    func1763.methodName = "consume(1)";
    func1763.paramCounts = [1];
    func1763.paramNames = ["c"];
    func1763.definitionLine = 669;
    func1763.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 668;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1762_init = function() {    // init of object on line 668
    };
    return obj1762_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1762 = emptyGraceObject("unicodeState", "lexer", 668);
  var obj1762_init = obj1762_build.call(obj1762, null, this, [], []);
  obj1762_init.call(obj1762);  // end of compileobject
  var var_unicodeState = obj1762;
  var reader1773_unicodeState = function() {  // reader method unicodeState
      if (var_unicodeState === undefined) raiseUninitializedVariable("unicodeState");
      return var_unicodeState;
  };
  reader1773_unicodeState.isDef = true;
  reader1773_unicodeState.confidential = true;
  this.methods["unicodeState"] = reader1773_unicodeState;
  setLineNumber(682);    // compilenode object
  var obj1774_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_682");
    this.outer_lexer_682 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1775 = function(argcv, var_c) {    // method consume(_), line 683
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(684);    // compilenode call
      // call case 2: outer request
      var call1776 = selfRequest(importedModules["lexer"], "checkSeparatorString(1)", [1], var_c);
      setLineNumber(685);    // compilenode call
      if (var_quotedStringState === undefined) raiseUninitializedVariable("quotedStringState");
      // call case 2: outer request
      var call1777 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_quotedStringState);
      var if1778 = GraceDone;
      setLineNumber(686);    // compilenode string
      var string1779 = new GraceString("\n");
      var opresult1780 = request(var_c, "==(1)", [1], string1779);
      if (Grace_isTrue(opresult1780)) {
        setLineNumber(687);    // compilenode member
        // call case 2: outer request
        var call1781 = selfRequest(importedModules["lexer"], "escapedNewlineError", [0]);
        if1778 = call1781;
      } else {
        var if1782 = GraceDone;
        setLineNumber(688);    // compilenode string
        var string1783 = new GraceString("n");
        var opresult1784 = request(var_c, "==(1)", [1], string1783);
        if (Grace_isTrue(opresult1784)) {
          setLineNumber(690);    // compilenode op
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          var string1785 = new GraceString("\n");
          var opresult1786 = request(var_accum, "++(1)", [1], string1785);
          var_accum = opresult1786;
          if1782 = GraceDone;
        } else {
          var if1787 = GraceDone;
          setLineNumber(691);    // compilenode string
          var string1788 = new GraceString("u");
          var opresult1789 = request(var_c, "==(1)", [1], string1788);
          if (Grace_isTrue(opresult1789)) {
            setLineNumber(694);    // compilenode num
            var_unichars = new GraceNum(4);
            setLineNumber(695);    // compilenode num
            var_codepoint = new GraceNum(0);
            setLineNumber(696);    // compilenode call
            if (var_unicodeState === undefined) raiseUninitializedVariable("unicodeState");
            // call case 2: outer request
            var call1790 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_unicodeState);
            if1787 = call1790;
          } else {
            var if1791 = GraceDone;
            setLineNumber(697);    // compilenode string
            var string1792 = new GraceString("U");
            var opresult1793 = request(var_c, "==(1)", [1], string1792);
            if (Grace_isTrue(opresult1793)) {
              setLineNumber(700);    // compilenode num
              var_unichars = new GraceNum(6);
              setLineNumber(701);    // compilenode num
              var_codepoint = new GraceNum(0);
              setLineNumber(702);    // compilenode call
              if (var_unicodeState === undefined) raiseUninitializedVariable("unicodeState");
              // call case 2: outer request
              var call1794 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_unicodeState);
              if1791 = call1794;
            } else {
              var if1795 = GraceDone;
              setLineNumber(703);    // compilenode string
              var string1796 = new GraceString("t");
              var opresult1797 = request(var_c, "==(1)", [1], string1796);
              if (Grace_isTrue(opresult1797)) {
                setLineNumber(705);    // compilenode op
                if (var_accum === undefined) raiseUninitializedVariable("accum");
                var string1798 = new GraceString("\u0009");
                var opresult1799 = request(var_accum, "++(1)", [1], string1798);
                var_accum = opresult1799;
                if1795 = GraceDone;
              } else {
                var if1800 = GraceDone;
                setLineNumber(706);    // compilenode string
                var string1801 = new GraceString("r");
                var opresult1802 = request(var_c, "==(1)", [1], string1801);
                if (Grace_isTrue(opresult1802)) {
                  setLineNumber(708);    // compilenode op
                  if (var_accum === undefined) raiseUninitializedVariable("accum");
                  var string1803 = new GraceString("\u000d");
                  var opresult1804 = request(var_accum, "++(1)", [1], string1803);
                  var_accum = opresult1804;
                  if1800 = GraceDone;
                } else {
                  var if1805 = GraceDone;
                  setLineNumber(709);    // compilenode string
                  var string1806 = new GraceString("b");
                  var opresult1807 = request(var_c, "==(1)", [1], string1806);
                  if (Grace_isTrue(opresult1807)) {
                    setLineNumber(711);    // compilenode op
                    if (var_accum === undefined) raiseUninitializedVariable("accum");
                    var string1808 = new GraceString("\u0008");
                    var opresult1809 = request(var_accum, "++(1)", [1], string1808);
                    var_accum = opresult1809;
                    if1805 = GraceDone;
                  } else {
                    var if1810 = GraceDone;
                    setLineNumber(712);    // compilenode string
                    var string1811 = new GraceString("l");
                    var opresult1812 = request(var_c, "==(1)", [1], string1811);
                    if (Grace_isTrue(opresult1812)) {
                      setLineNumber(714);    // compilenode op
                      if (var_accum === undefined) raiseUninitializedVariable("accum");
                      var string1813 = new GraceString("\u2028");
                      var opresult1814 = request(var_accum, "++(1)", [1], string1813);
                      var_accum = opresult1814;
                      if1810 = GraceDone;
                    } else {
                      var if1815 = GraceDone;
                      setLineNumber(715);    // compilenode string
                      var string1816 = new GraceString("f");
                      var opresult1817 = request(var_c, "==(1)", [1], string1816);
                      if (Grace_isTrue(opresult1817)) {
                        setLineNumber(717);    // compilenode op
                        if (var_accum === undefined) raiseUninitializedVariable("accum");
                        var string1818 = new GraceString("\u000c");
                        var opresult1819 = request(var_accum, "++(1)", [1], string1818);
                        var_accum = opresult1819;
                        if1815 = GraceDone;
                      } else {
                        var if1820 = GraceDone;
                        setLineNumber(718);    // compilenode string
                        var string1821 = new GraceString("e");
                        var opresult1822 = request(var_c, "==(1)", [1], string1821);
                        if (Grace_isTrue(opresult1822)) {
                          setLineNumber(720);    // compilenode op
                          if (var_accum === undefined) raiseUninitializedVariable("accum");
                          var string1823 = new GraceString("\u001b");
                          var opresult1824 = request(var_accum, "++(1)", [1], string1823);
                          var_accum = opresult1824;
                          if1820 = GraceDone;
                        } else {
                          var if1825 = GraceDone;
                          setLineNumber(721);    // compilenode string
                          var string1826 = new GraceString("_");
                          var opresult1827 = request(var_c, "==(1)", [1], string1826);
                          if (Grace_isTrue(opresult1827)) {
                            setLineNumber(723);    // compilenode op
                            if (var_accum === undefined) raiseUninitializedVariable("accum");
                            var string1828 = new GraceString("\u00a0");
                            var opresult1829 = request(var_accum, "++(1)", [1], string1828);
                            var_accum = opresult1829;
                            if1825 = GraceDone;
                          } else {
                            setLineNumber(727);    // compilenode call
                            // call case 2: outer request
                            var call1830 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
                            if1825 = call1830;
                          }
                          if1820 = if1825;
                        }
                        if1815 = if1820;
                      }
                      if1810 = if1815;
                    }
                    if1805 = if1810;
                  }
                  if1800 = if1805;
                }
                if1795 = if1800;
              }
              if1791 = if1795;
            }
            if1787 = if1791;
          }
          if1782 = if1787;
        }
        if1778 = if1782;
      }
      return if1778;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1775;
    func1775.methodName = "consume(1)";
    func1775.paramCounts = [1];
    func1775.paramNames = ["c"];
    func1775.definitionLine = 683;
    func1775.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 682;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1774_init = function() {    // init of object on line 682
    };
    return obj1774_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1774 = emptyGraceObject("quotedStringEscapedState", "lexer", 682);
  var obj1774_init = obj1774_build.call(obj1774, null, this, [], []);
  obj1774_init.call(obj1774);  // end of compileobject
  var var_quotedStringEscapedState = obj1774;
  var reader1831_quotedStringEscapedState = function() {  // reader method quotedStringEscapedState
      if (var_quotedStringEscapedState === undefined) raiseUninitializedVariable("quotedStringEscapedState");
      return var_quotedStringEscapedState;
  };
  reader1831_quotedStringEscapedState.isDef = true;
  reader1831_quotedStringEscapedState.confidential = true;
  this.methods["quotedStringEscapedState"] = reader1831_quotedStringEscapedState;
  setLineNumber(732);    // compilenode object
  var obj1832_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_732");
    this.outer_lexer_732 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1833 = function(argcv, var_c) {    // method consume(_), line 733
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      var if1834 = GraceDone;
      setLineNumber(735);    // compilenode string
      var string1835 = new GraceString("\u203a");
      var opresult1836 = request(var_c, "==(1)", [1], string1835);
      if (Grace_isTrue(opresult1836)) {
        setLineNumber(736);    // compilenode call
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        // call case 2: outer request
        var call1838 = selfRequest(importedModules["lexer"], "multiLineStringToken(1)", [1], var_accum);
        // call case 2: outer request
        var call1837 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1838);
        setLineNumber(737);    // compilenode bind
        var_inStr = GraceFalse;
        setLineNumber(738);    // compilenode call
        if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
        // call case 2: outer request
        var call1839 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
        if1834 = call1839;
      } else {
        setLineNumber(740);    // compilenode call
        // call case 2: outer request
        var call1840 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if1834 = call1840;
      }
      return if1834;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1833;
    func1833.methodName = "consume(1)";
    func1833.paramCounts = [1];
    func1833.paramNames = ["c"];
    func1833.definitionLine = 733;
    func1833.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 732;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1832_init = function() {    // init of object on line 732
    };
    return obj1832_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1832 = emptyGraceObject("extendedStringState", "lexer", 732);
  var obj1832_init = obj1832_build.call(obj1832, null, this, [], []);
  obj1832_init.call(obj1832);  // end of compileobject
  var var_extendedStringState = obj1832;
  var reader1841_extendedStringState = function() {  // reader method extendedStringState
      if (var_extendedStringState === undefined) raiseUninitializedVariable("extendedStringState");
      return var_extendedStringState;
  };
  reader1841_extendedStringState.isDef = true;
  reader1841_extendedStringState.confidential = true;
  this.methods["extendedStringState"] = reader1841_extendedStringState;
  setLineNumber(745);    // compilenode vardec
  var var_tokenBase;
  var reader1842_tokenBase = function() {  // reader method tokenBase
      if (var_tokenBase === undefined) raiseUninitializedVariable("tokenBase");
      return var_tokenBase;
  };
  reader1842_tokenBase.isVar = true;
  reader1842_tokenBase.confidential = true;
  this.methods["tokenBase"] = reader1842_tokenBase;
  var writer1843_tokenBase = function(argcv, n) {   // writer method tokenBase:=(_)
    var_tokenBase = n;
    return GraceDone;
  };
  this.methods["tokenBase:=(1)"] = writer1843_tokenBase;
  setLineNumber(746);    // compilenode vardec
  var var_mantissa;
  var reader1844_mantissa = function() {  // reader method mantissa
      if (var_mantissa === undefined) raiseUninitializedVariable("mantissa");
      return var_mantissa;
  };
  reader1844_mantissa.isVar = true;
  reader1844_mantissa.confidential = true;
  this.methods["mantissa"] = reader1844_mantissa;
  var writer1845_mantissa = function(argcv, n) {   // writer method mantissa:=(_)
    var_mantissa = n;
    return GraceDone;
  };
  this.methods["mantissa:=(1)"] = writer1845_mantissa;
  setLineNumber(747);    // compilenode object
  var obj1846_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_747");
    this.outer_lexer_747 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1847 = function(argcv, var_c) {    // method consume(_), line 748
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(749);    // compilenode call
      // call case 2: outer request
      var call1848 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1849 = GraceDone;
      setLineNumber(750);    // compilenode call
      // call case 2: outer request
      var call1850 = selfRequest(importedModules["lexer"], "isDigit(1)", [1], var_c);
      if (Grace_isTrue(call1850)) {
        setLineNumber(751);    // compilenode call
        // call case 2: outer request
        var call1851 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if1849 = call1851;
      } else {
        var if1852 = GraceDone;
        setLineNumber(752);    // compilenode string
        var string1853 = new GraceString("x");
        var opresult1854 = request(var_c, "==(1)", [1], string1853);
        if (Grace_isTrue(opresult1854)) {
          setLineNumber(753);    // compilenode member
          // call case 6: other requests
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          var call1855 = request(var_accum, "asNumber", [0]);
          var_tokenBase = call1855;
          var if1856 = GraceDone;
          setLineNumber(754);    // compilenode op
          if (var_tokenBase === undefined) raiseUninitializedVariable("tokenBase");
          var opresult1857 = request(var_tokenBase, "==(1)", [1], new GraceNum(0));
          if (Grace_isTrue(opresult1857)) {
            setLineNumber(755);    // compilenode num
            var_tokenBase = new GraceNum(16);
            if1856 = GraceDone;
          }
          setLineNumber(757);    // compilenode string
          var string1858 = new GraceString("");
          var_accum = string1858;
          setLineNumber(758);    // compilenode call
          if (var_numberBaseState === undefined) raiseUninitializedVariable("numberBaseState");
          // call case 2: outer request
          var call1859 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberBaseState);
          if1852 = call1859;
        } else {
          var if1860 = GraceDone;
          setLineNumber(759);    // compilenode string
          var string1861 = new GraceString("e");
          var opresult1862 = request(var_c, "==(1)", [1], string1861);
          var string1863 = new GraceString("E");
          var opresult1864 = request(var_c, "==(1)", [1], string1863);
          var opresult1865 = request(opresult1862, "||(1)", [1], opresult1864);
          if (Grace_isTrue(opresult1865)) {
            setLineNumber(760);    // compilenode member
            // call case 6: other requests
            if (var_accum === undefined) raiseUninitializedVariable("accum");
            var call1866 = request(var_accum, "asNumber", [0]);
            var_mantissa = call1866;
            setLineNumber(761);    // compilenode string
            var string1867 = new GraceString("");
            var_accum = string1867;
            setLineNumber(762);    // compilenode call
            if (var_numberExponentSignState === undefined) raiseUninitializedVariable("numberExponentSignState");
            // call case 2: outer request
            var call1868 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberExponentSignState);
            if1860 = call1868;
          } else {
            var if1869 = GraceDone;
            setLineNumber(763);    // compilenode call
            // call case 2: outer request
            var call1870 = selfRequest(importedModules["lexer"], "isLetter(1)", [1], var_c);
            if (Grace_isTrue(call1870)) {
              setLineNumber(764);    // compilenode member
              // call case 6: other requests
              // call case 6: other requests
              if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
              var call1872 = request(var_errormessages, "suggestion", [0]);
              var call1871 = request(call1872, "new", [0]);
              var var_suggestion = call1871;
              setLineNumber(765);    // compilenode string
              var string1874 = new GraceString(".");
              if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
              if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
              // call case 6: other requests
              if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
              var call1873 = request(var_suggestion, "insert(1)atPosition(1)onLine(1)", [1, 1, 1], string1874, var_linePosition, var_lineNumber);
              setLineNumber(766);    // compilenode string
              var string1876 = new GraceString("'");
              var opresult1877 = request(string1876, "++(1)", [1], var_c);
              var string1878 = new GraceString("' is not a valid digit in base 10. Valid digits are 0..9.");
              var opresult1879 = request(opresult1877, "++(1)", [1], string1878);
              if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
              if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
              if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
              if (var_suggestion === undefined) raiseUninitializedVariable("suggestion");
              // call case 6: other requests
              if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
              var call1875 = request(var_errormessages, "syntaxError(1)atRange(3)withSuggestion(1)", [1, 3, 1], opresult1879, var_lineNumber, var_startPosition, var_linePosition, var_suggestion);
              if1869 = call1875;
            } else {
              var if1880 = GraceDone;
              setLineNumber(768);    // compilenode string
              var string1881 = new GraceString(".");
              var opresult1882 = request(var_c, "==(1)", [1], string1881);
              if (Grace_isTrue(opresult1882)) {
                setLineNumber(769);    // compilenode call
                if (var_numberDotState === undefined) raiseUninitializedVariable("numberDotState");
                // call case 2: outer request
                var call1883 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberDotState);
                if1880 = call1883;
              } else {
                setLineNumber(771);    // compilenode call
                if (var_accum === undefined) raiseUninitializedVariable("accum");
                // call case 2: outer request
                var call1885 = selfRequest(importedModules["lexer"], "numToken(2)", [2], var_accum, new GraceNum(10));
                // call case 2: outer request
                var call1884 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1885);
                setLineNumber(772);    // compilenode call
                if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
                // call case 2: outer request
                var call1886 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
                setLineNumber(773);    // compilenode call
                // call case 6: other requests
                if (var_state === undefined) raiseUninitializedVariable("state");
                var call1887 = request(var_state, "consume(1)", [1], var_c);
                if1880 = call1887;
              }
              if1869 = if1880;
            }
            if1860 = if1869;
          }
          if1852 = if1860;
        }
        if1849 = if1852;
      }
      return if1849;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1847;
    func1847.methodName = "consume(1)";
    func1847.paramCounts = [1];
    func1847.paramNames = ["c"];
    func1847.definitionLine = 748;
    func1847.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 747;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1846_init = function() {    // init of object on line 747
    };
    return obj1846_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1846 = emptyGraceObject("numberStartState", "lexer", 747);
  var obj1846_init = obj1846_build.call(obj1846, null, this, [], []);
  obj1846_init.call(obj1846);  // end of compileobject
  var var_numberStartState = obj1846;
  var reader1888_numberStartState = function() {  // reader method numberStartState
      if (var_numberStartState === undefined) raiseUninitializedVariable("numberStartState");
      return var_numberStartState;
  };
  reader1888_numberStartState.isDef = true;
  reader1888_numberStartState.confidential = true;
  this.methods["numberStartState"] = reader1888_numberStartState;
  setLineNumber(777);    // compilenode object
  var obj1889_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_777");
    this.outer_lexer_777 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1890 = function(argcv, var_c) {    // method consume(_), line 778
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(779);    // compilenode call
      // call case 2: outer request
      var call1891 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1892 = GraceDone;
      setLineNumber(780);    // compilenode call
      // call case 2: outer request
      var call1893 = selfRequest(importedModules["lexer"], "isDigit(1)", [1], var_c);
      // call case 2: outer request
      var call1894 = selfRequest(importedModules["lexer"], "isLetter(1)", [1], var_c);
      var opresult1895 = request(call1893, "||(1)", [1], call1894);
      if (Grace_isTrue(opresult1895)) {
        setLineNumber(781);    // compilenode call
        // call case 2: outer request
        var call1896 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if1892 = call1896;
      } else {
        var if1897 = GraceDone;
        setLineNumber(782);    // compilenode string
        var string1898 = new GraceString(".");
        var opresult1899 = request(var_c, "==(1)", [1], string1898);
        if (var_tokenBase === undefined) raiseUninitializedVariable("tokenBase");
        var opresult1900 = request(var_tokenBase, "==(1)", [1], new GraceNum(10));
        var opresult1901 = request(opresult1899, "&&(1)", [1], opresult1900);
        if (Grace_isTrue(opresult1901)) {
          setLineNumber(783);    // compilenode call
          if (var_numberDotState === undefined) raiseUninitializedVariable("numberDotState");
          // call case 2: outer request
          var call1902 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberDotState);
          if1897 = call1902;
        } else {
          setLineNumber(785);    // compilenode member
          // call case 6: other requests
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          if (var_tokenBase === undefined) raiseUninitializedVariable("tokenBase");
          // call case 2: outer request
          var call1906 = selfRequest(importedModules["lexer"], "fromBase(2)", [2], var_accum, var_tokenBase);
          var call1905 = request(call1906, "asString", [0]);
          if (var_tokenBase === undefined) raiseUninitializedVariable("tokenBase");
          // call case 2: outer request
          var call1904 = selfRequest(importedModules["lexer"], "numToken(2)", [2], call1905, var_tokenBase);
          // call case 2: outer request
          var call1903 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1904);
          setLineNumber(786);    // compilenode call
          if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
          // call case 2: outer request
          var call1907 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
          setLineNumber(787);    // compilenode call
          // call case 6: other requests
          if (var_state === undefined) raiseUninitializedVariable("state");
          var call1908 = request(var_state, "consume(1)", [1], var_c);
          if1897 = call1908;
        }
        if1892 = if1897;
      }
      return if1892;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1890;
    func1890.methodName = "consume(1)";
    func1890.paramCounts = [1];
    func1890.paramNames = ["c"];
    func1890.definitionLine = 778;
    func1890.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 777;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1889_init = function() {    // init of object on line 777
    };
    return obj1889_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1889 = emptyGraceObject("numberBaseState", "lexer", 777);
  var obj1889_init = obj1889_build.call(obj1889, null, this, [], []);
  obj1889_init.call(obj1889);  // end of compileobject
  var var_numberBaseState = obj1889;
  var reader1909_numberBaseState = function() {  // reader method numberBaseState
      if (var_numberBaseState === undefined) raiseUninitializedVariable("numberBaseState");
      return var_numberBaseState;
  };
  reader1909_numberBaseState.isDef = true;
  reader1909_numberBaseState.confidential = true;
  this.methods["numberBaseState"] = reader1909_numberBaseState;
  setLineNumber(793);    // compilenode object
  var obj1910_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_793");
    this.outer_lexer_793 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1911 = function(argcv, var_c) {    // method consume(_), line 794
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      var if1912 = GraceDone;
      setLineNumber(795);    // compilenode call
      // call case 2: outer request
      var call1913 = selfRequest(importedModules["lexer"], "isDigit(1)", [1], var_c);
      if (Grace_isTrue(call1913)) {
        setLineNumber(796);    // compilenode string
        var string1915 = new GraceString(".");
        // call case 2: outer request
        var call1914 = selfRequest(importedModules["lexer"], "store(1)", [1], string1915);
        setLineNumber(797);    // compilenode call
        if (var_numberFractionState === undefined) raiseUninitializedVariable("numberFractionState");
        // call case 2: outer request
        var call1916 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberFractionState);
        setLineNumber(798);    // compilenode call
        // call case 6: other requests
        if (var_state === undefined) raiseUninitializedVariable("state");
        var call1917 = request(var_state, "consume(1)", [1], var_c);
        if1912 = call1917;
      } else {
        var if1918 = GraceDone;
        setLineNumber(799);    // compilenode call
        // call case 6: other requests
        if (var_spaceChars === undefined) raiseUninitializedVariable("spaceChars");
        var call1919 = request(var_spaceChars, "contains(1)", [1], var_c);
        if (Grace_isTrue(call1919)) {
          setLineNumber(801);    // compilenode call
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          // call case 2: outer request
          var call1921 = selfRequest(importedModules["lexer"], "numToken(2)", [2], var_accum, new GraceNum(10));
          // call case 2: outer request
          var call1920 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1921);
          setLineNumber(802);    // compilenode call
          if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
          // call case 2: outer request
          var call1922 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
          setLineNumber(803);    // compilenode call
          // call case 6: other requests
          if (var_state === undefined) raiseUninitializedVariable("state");
          var call1923 = request(var_state, "consume(1)", [1], var_c);
          if1918 = call1923;
        } else {
          setLineNumber(805);    // compilenode call
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          // call case 2: outer request
          var call1925 = selfRequest(importedModules["lexer"], "numToken(2)", [2], var_accum, new GraceNum(10));
          // call case 2: outer request
          var call1924 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1925);
          setLineNumber(806);    // compilenode string
          var string1926 = new GraceString(".");
          var_accum = string1926;
          setLineNumber(807);    // compilenode call
          if (var_dotState === undefined) raiseUninitializedVariable("dotState");
          // call case 2: outer request
          var call1927 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_dotState);
          setLineNumber(808);    // compilenode call
          // call case 6: other requests
          if (var_state === undefined) raiseUninitializedVariable("state");
          var call1928 = request(var_state, "consume(1)", [1], var_c);
          if1918 = call1928;
        }
        if1912 = if1918;
      }
      return if1912;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1911;
    func1911.methodName = "consume(1)";
    func1911.paramCounts = [1];
    func1911.paramNames = ["c"];
    func1911.definitionLine = 794;
    func1911.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 793;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1910_init = function() {    // init of object on line 793
    };
    return obj1910_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1910 = emptyGraceObject("numberDotState", "lexer", 793);
  var obj1910_init = obj1910_build.call(obj1910, null, this, [], []);
  obj1910_init.call(obj1910);  // end of compileobject
  var var_numberDotState = obj1910;
  var reader1929_numberDotState = function() {  // reader method numberDotState
      if (var_numberDotState === undefined) raiseUninitializedVariable("numberDotState");
      return var_numberDotState;
  };
  reader1929_numberDotState.isDef = true;
  reader1929_numberDotState.confidential = true;
  this.methods["numberDotState"] = reader1929_numberDotState;
  setLineNumber(812);    // compilenode object
  var obj1930_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_812");
    this.outer_lexer_812 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1931 = function(argcv, var_c) {    // method consume(_), line 813
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(814);    // compilenode call
      // call case 2: outer request
      var call1932 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1933 = GraceDone;
      setLineNumber(815);    // compilenode call
      // call case 2: outer request
      var call1934 = selfRequest(importedModules["lexer"], "isDigit(1)", [1], var_c);
      if (Grace_isTrue(call1934)) {
        setLineNumber(816);    // compilenode call
        // call case 2: outer request
        var call1935 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if1933 = call1935;
      } else {
        var if1936 = GraceDone;
        setLineNumber(817);    // compilenode string
        var string1937 = new GraceString("e");
        var opresult1938 = request(var_c, "==(1)", [1], string1937);
        var string1939 = new GraceString("E");
        var opresult1940 = request(var_c, "==(1)", [1], string1939);
        var opresult1941 = request(opresult1938, "||(1)", [1], opresult1940);
        if (Grace_isTrue(opresult1941)) {
          setLineNumber(818);    // compilenode member
          // call case 6: other requests
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          var call1942 = request(var_accum, "asNumber", [0]);
          var_mantissa = call1942;
          setLineNumber(819);    // compilenode string
          var string1943 = new GraceString("");
          var_accum = string1943;
          setLineNumber(820);    // compilenode call
          if (var_numberExponentSignState === undefined) raiseUninitializedVariable("numberExponentSignState");
          // call case 2: outer request
          var call1944 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberExponentSignState);
          if1936 = call1944;
        } else {
          var if1945 = GraceDone;
          setLineNumber(821);    // compilenode call
          // call case 2: outer request
          var call1946 = selfRequest(importedModules["lexer"], "isLetter(1)", [1], var_c);
          if (Grace_isTrue(call1946)) {
            setLineNumber(822);    // compilenode string
            var string1948 = new GraceString("the fractional part of a number must be in base 10.");
            if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
            if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
            if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
            // call case 6: other requests
            if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
            var call1947 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], string1948, var_lineNumber, var_startPosition, var_linePosition);
            if1945 = call1947;
          } else {
            setLineNumber(825);    // compilenode call
            if (var_accum === undefined) raiseUninitializedVariable("accum");
            // call case 2: outer request
            var call1950 = selfRequest(importedModules["lexer"], "numToken(2)", [2], var_accum, new GraceNum(10));
            // call case 2: outer request
            var call1949 = selfRequest(importedModules["lexer"], "emit(1)", [1], call1950);
            setLineNumber(826);    // compilenode call
            if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
            // call case 2: outer request
            var call1951 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
            setLineNumber(827);    // compilenode call
            // call case 6: other requests
            if (var_state === undefined) raiseUninitializedVariable("state");
            var call1952 = request(var_state, "consume(1)", [1], var_c);
            if1945 = call1952;
          }
          if1936 = if1945;
        }
        if1933 = if1936;
      }
      return if1933;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1931;
    func1931.methodName = "consume(1)";
    func1931.paramCounts = [1];
    func1931.paramNames = ["c"];
    func1931.definitionLine = 813;
    func1931.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 812;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1930_init = function() {    // init of object on line 812
    };
    return obj1930_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1930 = emptyGraceObject("numberFractionState", "lexer", 812);
  var obj1930_init = obj1930_build.call(obj1930, null, this, [], []);
  obj1930_init.call(obj1930);  // end of compileobject
  var var_numberFractionState = obj1930;
  var reader1953_numberFractionState = function() {  // reader method numberFractionState
      if (var_numberFractionState === undefined) raiseUninitializedVariable("numberFractionState");
      return var_numberFractionState;
  };
  reader1953_numberFractionState.isDef = true;
  reader1953_numberFractionState.confidential = true;
  this.methods["numberFractionState"] = reader1953_numberFractionState;
  setLineNumber(831);    // compilenode object
  var obj1954_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_831");
    this.outer_lexer_831 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1955 = function(argcv, var_c) {    // method consume(_), line 832
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(833);    // compilenode call
      // call case 2: outer request
      var call1956 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1957 = GraceDone;
      setLineNumber(834);    // compilenode call
      // call case 2: outer request
      var call1958 = selfRequest(importedModules["lexer"], "isDigit(1)", [1], var_c);
      var string1959 = new GraceString("-");
      var opresult1960 = request(var_c, "==(1)", [1], string1959);
      var opresult1961 = request(call1958, "||(1)", [1], opresult1960);
      if (Grace_isTrue(opresult1961)) {
        setLineNumber(835);    // compilenode call
        // call case 2: outer request
        var call1962 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        setLineNumber(836);    // compilenode call
        if (var_numberExponentState === undefined) raiseUninitializedVariable("numberExponentState");
        // call case 2: outer request
        var call1963 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_numberExponentState);
        if1957 = call1963;
      } else {
        var if1964 = GraceDone;
        setLineNumber(837);    // compilenode call
        // call case 2: outer request
        var call1965 = selfRequest(importedModules["lexer"], "isLetter(1)", [1], var_c);
        if (Grace_isTrue(call1965)) {
          setLineNumber(838);    // compilenode string
          var string1967 = new GraceString("");
          var opresult1968 = request(string1967, "++(1)", [1], var_c);
          var string1969 = new GraceString(" is not an allowed base-10 exponent character");
          var opresult1970 = request(opresult1968, "++(1)", [1], string1969);
          if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
          if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call1966 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], opresult1970, var_lineNumber, var_startPosition, var_linePosition);
          if1964 = call1966;
        } else {
          setLineNumber(841);    // compilenode string
          var string1972 = new GraceString("exponents must have at least one digit");
          if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
          if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call1971 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], string1972, var_lineNumber, var_startPosition, var_linePosition);
          if1964 = call1971;
        }
        if1957 = if1964;
      }
      return if1957;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1955;
    func1955.methodName = "consume(1)";
    func1955.paramCounts = [1];
    func1955.paramNames = ["c"];
    func1955.definitionLine = 832;
    func1955.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 831;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1954_init = function() {    // init of object on line 831
    };
    return obj1954_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1954 = emptyGraceObject("numberExponentSignState", "lexer", 831);
  var obj1954_init = obj1954_build.call(obj1954, null, this, [], []);
  obj1954_init.call(obj1954);  // end of compileobject
  var var_numberExponentSignState = obj1954;
  var reader1973_numberExponentSignState = function() {  // reader method numberExponentSignState
      if (var_numberExponentSignState === undefined) raiseUninitializedVariable("numberExponentSignState");
      return var_numberExponentSignState;
  };
  reader1973_numberExponentSignState.isDef = true;
  reader1973_numberExponentSignState.confidential = true;
  this.methods["numberExponentSignState"] = reader1973_numberExponentSignState;
  setLineNumber(846);    // compilenode object
  var obj1974_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_846");
    this.outer_lexer_846 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func1975 = function(argcv, var_c) {    // method consume(_), line 847
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(848);    // compilenode call
      // call case 2: outer request
      var call1976 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if1977 = GraceDone;
      setLineNumber(849);    // compilenode call
      // call case 2: outer request
      var call1978 = selfRequest(importedModules["lexer"], "isDigit(1)", [1], var_c);
      if (Grace_isTrue(call1978)) {
        setLineNumber(850);    // compilenode call
        // call case 2: outer request
        var call1979 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if1977 = call1979;
      } else {
        var if1980 = GraceDone;
        setLineNumber(851);    // compilenode call
        // call case 2: outer request
        var call1981 = selfRequest(importedModules["lexer"], "isLetter(1)", [1], var_c);
        if (Grace_isTrue(call1981)) {
          setLineNumber(852);    // compilenode string
          var string1983 = new GraceString("");
          var opresult1984 = request(string1983, "++(1)", [1], var_c);
          var string1985 = new GraceString(" is not an allowed base-10 exponent character");
          var opresult1986 = request(opresult1984, "++(1)", [1], string1985);
          if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
          if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
          if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
          // call case 6: other requests
          if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
          var call1982 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], opresult1986, var_lineNumber, var_startPosition, var_linePosition);
          if1980 = call1982;
        } else {
          var if1987 = GraceDone;
          setLineNumber(855);    // compilenode op
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          var string1988 = new GraceString("-");
          var opresult1989 = request(var_accum, "==(1)", [1], string1988);
          if (Grace_isTrue(opresult1989)) {
            setLineNumber(856);    // compilenode string
            var string1991 = new GraceString("exponents must have at least one digit");
            if (var_lineNumber === undefined) raiseUninitializedVariable("lineNumber");
            if (var_startPosition === undefined) raiseUninitializedVariable("startPosition");
            if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
            // call case 6: other requests
            if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
            var call1990 = request(var_errormessages, "syntaxError(1)atRange(3)", [1, 3], string1991, var_lineNumber, var_startPosition, var_linePosition);
            if1987 = call1990;
          }
          setLineNumber(859);    // compilenode member
          // call case 6: other requests
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          var call1992 = request(var_accum, "asNumber", [0]);
          var var_exponent = call1992;
          setLineNumber(860);    // compilenode vardec
          if (var_exponent === undefined) raiseUninitializedVariable("exponent");
          var var_absExp = var_exponent;
          var if1993 = GraceDone;
          setLineNumber(861);    // compilenode op
          if (var_exponent === undefined) raiseUninitializedVariable("exponent");
          var opresult1994 = request(var_exponent, "<(1)", [1], new GraceNum(0));
          if (Grace_isTrue(opresult1994)) {
            setLineNumber(863);    // compilenode call
            // call case 6: other requests
            if (var_exponent === undefined) raiseUninitializedVariable("exponent");
            var call1995 = request(var_exponent, "prefix-", [0]);
            var_absExp = call1995;
            if1993 = GraceDone;
          }
          setLineNumber(864);    // compilenode num
          var var_absMultiplier = new GraceNum(1);
          setLineNumber(865);    // compilenode call
          if (var_absExp === undefined) raiseUninitializedVariable("absExp");
          var block1997 = new GraceBlock(this, 865, 0);
          block1997.guard = jsTrue;
          block1997.real = function block1997() {
            setLineNumber(866);    // compilenode op
            if (var_absMultiplier === undefined) raiseUninitializedVariable("absMultiplier");
            var prod1998 = request(var_absMultiplier, "*(1)", [1], new GraceNum(10));
            var_absMultiplier = prod1998;
            return GraceDone;
          };
          let applyMeth1997 = function apply (argcv) {
              return this.real.apply(this.receiver);
          };
          applyMeth1997.methodName = "apply";
          applyMeth1997.paramCounts = [0];
          applyMeth1997.paramNames = [];
          applyMeth1997.definitionLine = 865;
          applyMeth1997.definitionModule = "lexer";
          block1997.methods["apply"] = applyMeth1997;
          // call case 2: outer request
          var call1996 = selfRequest(var_prelude, "repeat(1)times(1)", [1, 1], var_absExp, block1997);
          var if1999 = GraceDone;
          setLineNumber(868);    // compilenode op
          if (var_exponent === undefined) raiseUninitializedVariable("exponent");
          var opresult2000 = request(var_exponent, "\u2265(1)", [1], new GraceNum(0));
          if (Grace_isTrue(opresult2000)) {
            setLineNumber(869);    // compilenode member
            // call case 6: other requests
            if (var_mantissa === undefined) raiseUninitializedVariable("mantissa");
            if (var_absMultiplier === undefined) raiseUninitializedVariable("absMultiplier");
            var prod2004 = request(var_mantissa, "*(1)", [1], var_absMultiplier);
            var call2003 = request(prod2004, "asString", [0]);
            // call case 2: outer request
            var call2002 = selfRequest(importedModules["lexer"], "numToken(2)", [2], call2003, new GraceNum(10));
            // call case 2: outer request
            var call2001 = selfRequest(importedModules["lexer"], "emit(1)", [1], call2002);
            if1999 = call2001;
          } else {
            setLineNumber(871);    // compilenode member
            // call case 6: other requests
            if (var_mantissa === undefined) raiseUninitializedVariable("mantissa");
            if (var_absMultiplier === undefined) raiseUninitializedVariable("absMultiplier");
            var quotient2008 = request(var_mantissa, "/(1)", [1], var_absMultiplier);
            var call2007 = request(quotient2008, "asString", [0]);
            // call case 2: outer request
            var call2006 = selfRequest(importedModules["lexer"], "numToken(2)", [2], call2007, new GraceNum(10));
            // call case 2: outer request
            var call2005 = selfRequest(importedModules["lexer"], "emit(1)", [1], call2006);
            if1999 = call2005;
          }
          setLineNumber(873);    // compilenode call
          if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
          // call case 2: outer request
          var call2009 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
          setLineNumber(874);    // compilenode call
          // call case 6: other requests
          if (var_state === undefined) raiseUninitializedVariable("state");
          var call2010 = request(var_state, "consume(1)", [1], var_c);
          if1980 = call2010;
        }
        if1977 = if1980;
      }
      return if1977;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func1975;
    func1975.methodName = "consume(1)";
    func1975.paramCounts = [1];
    func1975.paramNames = ["c"];
    func1975.definitionLine = 847;
    func1975.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 846;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj1974_init = function() {    // init of object on line 846
    };
    return obj1974_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj1974 = emptyGraceObject("numberExponentState", "lexer", 846);
  var obj1974_init = obj1974_build.call(obj1974, null, this, [], []);
  obj1974_init.call(obj1974);  // end of compileobject
  var var_numberExponentState = obj1974;
  var reader2011_numberExponentState = function() {  // reader method numberExponentState
      if (var_numberExponentState === undefined) raiseUninitializedVariable("numberExponentState");
      return var_numberExponentState;
  };
  reader2011_numberExponentState.isDef = true;
  reader2011_numberExponentState.confidential = true;
  this.methods["numberExponentState"] = reader2011_numberExponentState;
  setLineNumber(879);    // compilenode object
  var obj2012_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_879");
    this.outer_lexer_879 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func2013 = function(argcv, var_c) {    // method consume(_), line 880
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      setLineNumber(881);    // compilenode call
      // call case 2: outer request
      var call2014 = selfRequest(importedModules["lexer"], "checkSeparator(1)", [1], var_c);
      var if2015 = GraceDone;
      setLineNumber(882);    // compilenode member
      // call case 6: other requests
      var call2017 = request(var_c, "ord", [0]);
      // call case 2: outer request
      var call2016 = selfRequest(importedModules["lexer"], "isOperatorChar(2)", [2], var_c, call2017);
      if (Grace_isTrue(call2016)) {
        setLineNumber(883);    // compilenode call
        // call case 2: outer request
        var call2018 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if2015 = call2018;
      } else {
        var if2019 = GraceDone;
        setLineNumber(885);    // compilenode op
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        var string2020 = new GraceString("\u2192");
        var opresult2021 = request(var_accum, "==(1)", [1], string2020);
        if (Grace_isTrue(opresult2021)) {
          setLineNumber(886);    // compilenode member
          // call case 2: outer request
          var call2023 = selfRequest(importedModules["lexer"], "arrowToken", [0]);
          // call case 2: outer request
          var call2022 = selfRequest(importedModules["lexer"], "emit(1)", [1], call2023);
          if2019 = call2022;
        } else {
          var if2024 = GraceDone;
          setLineNumber(887);    // compilenode op
          if (var_accum === undefined) raiseUninitializedVariable("accum");
          var string2025 = new GraceString(":=");
          var opresult2026 = request(var_accum, "==(1)", [1], string2025);
          if (Grace_isTrue(opresult2026)) {
            setLineNumber(888);    // compilenode member
            // call case 2: outer request
            var call2028 = selfRequest(importedModules["lexer"], "bindToken", [0]);
            // call case 2: outer request
            var call2027 = selfRequest(importedModules["lexer"], "emit(1)", [1], call2028);
            if2024 = call2027;
          } else {
            var if2029 = GraceDone;
            setLineNumber(889);    // compilenode op
            if (var_accum === undefined) raiseUninitializedVariable("accum");
            var string2030 = new GraceString(":");
            var opresult2031 = request(var_accum, "==(1)", [1], string2030);
            if (Grace_isTrue(opresult2031)) {
              setLineNumber(890);    // compilenode member
              // call case 2: outer request
              var call2033 = selfRequest(importedModules["lexer"], "colonToken", [0]);
              // call case 2: outer request
              var call2032 = selfRequest(importedModules["lexer"], "emit(1)", [1], call2033);
              if2029 = call2032;
            } else {
              setLineNumber(892);    // compilenode call
              if (var_accum === undefined) raiseUninitializedVariable("accum");
              // call case 2: outer request
              var call2035 = selfRequest(importedModules["lexer"], "opToken(1)", [1], var_accum);
              // call case 2: outer request
              var call2034 = selfRequest(importedModules["lexer"], "emit(1)", [1], call2035);
              if2029 = call2034;
            }
            if2024 = if2029;
          }
          if2019 = if2024;
        }
        setLineNumber(894);    // compilenode call
        if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
        // call case 2: outer request
        var call2036 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
        setLineNumber(895);    // compilenode call
        // call case 6: other requests
        if (var_state === undefined) raiseUninitializedVariable("state");
        var call2037 = request(var_state, "consume(1)", [1], var_c);
        if2015 = call2037;
      }
      return if2015;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func2013;
    func2013.methodName = "consume(1)";
    func2013.paramCounts = [1];
    func2013.paramNames = ["c"];
    func2013.definitionLine = 880;
    func2013.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 879;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj2012_init = function() {    // init of object on line 879
    };
    return obj2012_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj2012 = emptyGraceObject("operatorState", "lexer", 879);
  var obj2012_init = obj2012_build.call(obj2012, null, this, [], []);
  obj2012_init.call(obj2012);  // end of compileobject
  var var_operatorState = obj2012;
  var reader2038_operatorState = function() {  // reader method operatorState
      if (var_operatorState === undefined) raiseUninitializedVariable("operatorState");
      return var_operatorState;
  };
  reader2038_operatorState.isDef = true;
  reader2038_operatorState.confidential = true;
  this.methods["operatorState"] = reader2038_operatorState;
  setLineNumber(900);    // compilenode object
  var obj2039_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_900");
    this.outer_lexer_900 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func2040 = function(argcv, var_c) {    // method consume(_), line 901
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      var if2041 = GraceDone;
      setLineNumber(902);    // compilenode call
      // call case 6: other requests
      if (var_spaceChars === undefined) raiseUninitializedVariable("spaceChars");
      var call2042 = request(var_spaceChars, "contains(1)", [1], var_c);
      if (Grace_isTrue(call2042)) {
        setLineNumber(903);    // compilenode bind
        if (var_linePosition === undefined) raiseUninitializedVariable("linePosition");
        var_currentLineIndent = var_linePosition;
        if2041 = GraceDone;
      } else {
        setLineNumber(905);    // compilenode call
        // call case 2: outer request
        var call2043 = selfRequest(importedModules["lexer"], "newline(1)", [1], var_c);
        setLineNumber(906);    // compilenode call
        if (var_defaultState === undefined) raiseUninitializedVariable("defaultState");
        // call case 2: outer request
        var call2044 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_defaultState);
        setLineNumber(907);    // compilenode call
        // call case 6: other requests
        if (var_state === undefined) raiseUninitializedVariable("state");
        var call2045 = request(var_state, "consume(1)", [1], var_c);
        if2041 = call2045;
      }
      return if2041;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func2040;
    func2040.methodName = "consume(1)";
    func2040.paramCounts = [1];
    func2040.paramNames = ["c"];
    func2040.definitionLine = 901;
    func2040.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 900;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj2039_init = function() {    // init of object on line 900
    };
    return obj2039_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj2039 = emptyGraceObject("indentationState", "lexer", 900);
  var obj2039_init = obj2039_build.call(obj2039, null, this, [], []);
  obj2039_init.call(obj2039);  // end of compileobject
  var var_indentationState = obj2039;
  var reader2046_indentationState = function() {  // reader method indentationState
      if (var_indentationState === undefined) raiseUninitializedVariable("indentationState");
      return var_indentationState;
  };
  reader2046_indentationState.isDef = true;
  reader2046_indentationState.confidential = true;
  this.methods["indentationState"] = reader2046_indentationState;
  setLineNumber(911);    // compilenode object
  var obj2047_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_911");
    this.outer_lexer_911 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func2048 = function(argcv, var_c) {    // method consume(_), line 912
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      var if2049 = GraceDone;
      setLineNumber(913);    // compilenode call
      // call case 6: other requests
      // call case 6: other requests
      if (var_spaceChars === undefined) raiseUninitializedVariable("spaceChars");
      var call2051 = request(var_spaceChars, "contains(1)", [1], var_c);
      var call2050 = request(call2051, "prefix!", [0]);
      if (Grace_isTrue(call2050)) {
        setLineNumber(914);    // compilenode string
        var string2052 = new GraceString("");
        var_accum = string2052;
        setLineNumber(915);    // compilenode call
        if (var_commentState === undefined) raiseUninitializedVariable("commentState");
        // call case 2: outer request
        var call2053 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_commentState);
        setLineNumber(916);    // compilenode call
        // call case 6: other requests
        if (var_state === undefined) raiseUninitializedVariable("state");
        var call2054 = request(var_state, "consume(1)", [1], var_c);
        if2049 = call2054;
      }
      return if2049;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func2048;
    func2048.methodName = "consume(1)";
    func2048.paramCounts = [1];
    func2048.paramNames = ["c"];
    func2048.definitionLine = 912;
    func2048.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 911;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj2047_init = function() {    // init of object on line 911
    };
    return obj2047_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj2047 = emptyGraceObject("commentSpaceState", "lexer", 911);
  var obj2047_init = obj2047_build.call(obj2047, null, this, [], []);
  obj2047_init.call(obj2047);  // end of compileobject
  var var_commentSpaceState = obj2047;
  var reader2055_commentSpaceState = function() {  // reader method commentSpaceState
      if (var_commentSpaceState === undefined) raiseUninitializedVariable("commentSpaceState");
      return var_commentSpaceState;
  };
  reader2055_commentSpaceState.isDef = true;
  reader2055_commentSpaceState.confidential = true;
  this.methods["commentSpaceState"] = reader2055_commentSpaceState;
  setLineNumber(920);    // compilenode object
  var obj2056_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_lexer_920");
    this.outer_lexer_920 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func2057 = function(argcv, var_c) {    // method consume(_), line 921
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("consume(_)", 0, numArgs - 1);
      }
      var if2058 = GraceDone;
      setLineNumber(922);    // compilenode string
      var string2059 = new GraceString("\n");
      var opresult2060 = request(var_c, "==(1)", [1], string2059);
      if (Grace_isTrue(opresult2060)) {
        setLineNumber(923);    // compilenode call
        if (var_accum === undefined) raiseUninitializedVariable("accum");
        // call case 2: outer request
        var call2062 = selfRequest(importedModules["lexer"], "commentToken(1)", [1], var_accum);
        // call case 2: outer request
        var call2061 = selfRequest(importedModules["lexer"], "emit(1)", [1], call2062);
        setLineNumber(924);    // compilenode num
        var_currentLineIndent = new GraceNum(0);
        setLineNumber(925);    // compilenode call
        if (var_indentationState === undefined) raiseUninitializedVariable("indentationState");
        // call case 2: outer request
        var call2063 = selfRequest(importedModules["lexer"], "advanceTo(1)", [1], var_indentationState);
        if2058 = call2063;
      } else {
        setLineNumber(927);    // compilenode call
        // call case 2: outer request
        var call2064 = selfRequest(importedModules["lexer"], "store(1)", [1], var_c);
        if2058 = call2064;
      }
      return if2058;
    };    // end of method consume(_)
    this.methods["consume(1)"] = func2057;
    func2057.methodName = "consume(1)";
    func2057.paramCounts = [1];
    func2057.paramNames = ["c"];
    func2057.definitionLine = 921;
    func2057.definitionModule = "lexer";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 920;
        m.definitionModule = "lexer";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj2056_init = function() {    // init of object on line 920
    };
    return obj2056_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj2056 = emptyGraceObject("commentState", "lexer", 920);
  var obj2056_init = obj2056_build.call(obj2056, null, this, [], []);
  obj2056_init.call(obj2056);  // end of compileobject
  var var_commentState = obj2056;
  var reader2065_commentState = function() {  // reader method commentState
      if (var_commentState === undefined) raiseUninitializedVariable("commentState");
      return var_commentState;
  };
  reader2065_commentState.isDef = true;
  reader2065_commentState.confidential = true;
  this.methods["commentState"] = reader2065_commentState;
  return this;
}
if (typeof global !== "undefined")
  global.gracecode_lexer = gracecode_lexer;
if (typeof window !== "undefined")
  window.gracecode_lexer = gracecode_lexer;
gracecode_lexer.imports = ["errormessages", "fastDict", "standardGrace", "unicode", "util"];
gracecode_lexer.definitionModule = "lexer";
gracecode_lexer.definitionLine = 1;
