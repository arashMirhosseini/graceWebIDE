if (typeof gctCache !== "undefined")
  gctCache["dialect"] = "classes:\nconfidential:\n aParameter\n aTypeAnnotation\n astVisitor\n cache\n currentLine\n currentLine:=(currentLine': Unknown) \u2192 Done\n entryFrom(1)to(1)\n rules\n stackOfKind(1)\ndialect:\nfresh-methods:\n RequestOf(1)\n aMutableMap\n aPatternMatchingNode(1)\n entryFrom(1)to(1)\n methods\n stackOfKind(1)\nfresh:RequestOf(1):\n matches(1)\nfresh:aMutableMap:\n asString\n at(1)\n at(1)put(1)\n atKey(1)do(1)\n atKey(1)do(1)else(1)\n containsKey(1)\n entries\n isEmpty\n keys\n size\n values\nfresh:aPatternMatchingNode(1):\n matches(1)\nfresh:entryFrom(1)to(1):\n key\n value\n value:=(_)\nfresh:methods:\n AndPattern\n AndPattern(2)\n ArrayLiteral\n AstNode\n BaseType\n BasicPattern\n Bind\n Binding\n BlockLiteral\n BoundsError\n CheckerFailure\n Class\n Collection\n ConcurrentModification\n Def\n Dialect\n DialectError\n Dictionary\n Enumerable\n EqualityObject\n ExceptionKind\n Expandable\n Extractable\n ForRequest\n Function0\n Function1\n Function2\n Function3\n Generic\n Identifier\n If\n Import\n Inherit\n Iterable\n Iterator\n IteratorExhausted\n List\n MatchCase\n Member\n Method\n MethodSignature\n NoSuchObject\n NotPattern(1)\n NumberLiteral\n ObjectLiteral\n OctetsLiteral\n Operator\n OrPattern\n OrPattern(2)\n Outer\n Parameter\n Pattern\n Point\n Predicate0\n Predicate1\n Predicate2\n Predicate3\n Procedure0\n Procedure1\n Procedure2\n Procedure3\n Request\n RequestError\n RequestOf(1)\n Return\n Sequence\n Set\n Singleton\n StringLiteral\n SubobjectResponsibility\n TryCatch\n TypeAnnotation\n TypeDeclaration\n TypeIntersection\n TypeLiteral\n TypeSubtraction\n TypeUnion\n TypeVariant\n UninitializedVariable\n Var\n WhileRequest\n aMutableMap\n aPatternMatchingNode(1)\n abstract\n binding\n check(1)\n checkTypes(1)\n collection\n collections\n createSuggestion\n dictionary\n dictionary(1)\n do(1)while(1)\n emptyDictionary\n emptyList\n emptySequence\n emptySet\n enumerable\n equality\n fail(1)\n fail(1)at(1)\n fail(1)from(1)to(1)\n fail(1)from(1)to(1)suggest(1)\n fail(1)when(1)\n for(1)and(1)do(1)\n forBody(1)\n forCollection(1)\n hashCombine(2)\n identityEquality\n indexable\n list\n list(1)\n max(2)\n methods\n min(2)\n pi\n point2Dx(1)y(1)\n range\n repeat(1)times(1)\n required\n rule(1)\n runRules(1)\n scope\n sequence\n sequence(1)\n set\n set(1)\n singleton\n singleton(1)\n typeOf(1)\n valueOf(1)\n when(1)error(1)\n whileBody(1)\n whileCond(1)\nfresh:stackOfKind(1):\n at(1)put(1)\n find(1)butIfMissing(1)\n stack\nmethodtypes-of:AstNode:\n 1 kind \u2192 String\nmodules:\n ast\n collectionsPrelude\n errormessages\n fastDict\n identifierKinds\n io\n standardGrace\n sys\n unixFilePath\n util\npath:\n /Users/black/Development/mg/gracelang/minigrace/modules/dialect.grace\npublic:\n AndPattern\n AndPattern(2)\n ArrayLiteral\n AstNode\n BaseType\n BasicPattern\n Bind\n Binding\n BlockLiteral\n BoundsError\n CheckerFailure\n Class\n Collection\n ConcurrentModification\n Def\n Dialect\n DialectError\n Dictionary\n Enumerable\n EqualityObject\n ExceptionKind\n Expandable\n Extractable\n ForRequest\n Function0\n Function1\n Function2\n Function3\n Generic\n Identifier\n If\n Import\n Inherit\n Iterable\n Iterator\n IteratorExhausted\n List\n MatchCase\n Member\n Method\n MethodSignature\n NoSuchObject\n NotPattern(1)\n NumberLiteral\n ObjectLiteral\n OctetsLiteral\n Operator\n OrPattern\n OrPattern(2)\n Outer\n Parameter\n Pattern\n Point\n Predicate0\n Predicate1\n Predicate2\n Predicate3\n Procedure0\n Procedure1\n Procedure2\n Procedure3\n Request\n RequestError\n RequestOf(1)\n Return\n Sequence\n Set\n Singleton\n StringLiteral\n SubobjectResponsibility\n TryCatch\n TypeAnnotation\n TypeDeclaration\n TypeIntersection\n TypeLiteral\n TypeSubtraction\n TypeUnion\n TypeVariant\n UninitializedVariable\n Var\n WhileRequest\n aMutableMap\n aPatternMatchingNode(1)\n abstract\n binding\n check(1)\n checkTypes(1)\n collection\n collections\n createSuggestion\n dictionary\n dictionary(1)\n do(1)while(1)\n emptyDictionary\n emptyList\n emptySequence\n emptySet\n enumerable\n equality\n fail(1)\n fail(1)at(1)\n fail(1)from(1)to(1)\n fail(1)from(1)to(1)suggest(1)\n fail(1)when(1)\n for(1)and(1)do(1)\n forBody(1)\n forCollection(1)\n hashCombine(2)\n identityEquality\n indexable\n list\n list(1)\n max(2)\n methods\n min(2)\n pi\n point2Dx(1)y(1)\n range\n repeat(1)times(1)\n required\n rule(1)\n runRules(1)\n scope\n sequence\n sequence(1)\n set\n set(1)\n singleton\n singleton(1)\n typeOf(1)\n valueOf(1)\n when(1)error(1)\n whileBody(1)\n whileCond(1)\npublicMethodTypes:\n ArrayLiteral \u2192 Unknown\n Bind \u2192 Unknown\n BlockLiteral \u2192 Unknown\n CheckerFailure \u2192 Unknown\n Class \u2192 Unknown\n Def \u2192 Unknown\n Dialect \u2192 Unknown\n DialectError \u2192 Unknown\n ForRequest \u2192 Unknown\n Generic \u2192 Unknown\n Identifier \u2192 Unknown\n If \u2192 Unknown\n Import \u2192 Unknown\n Inherit \u2192 Unknown\n MatchCase \u2192 Unknown\n Member \u2192 Unknown\n Method \u2192 Unknown\n MethodSignature \u2192 Unknown\n NumberLiteral \u2192 Unknown\n ObjectLiteral \u2192 Unknown\n OctetsLiteral \u2192 Unknown\n Operator \u2192 Unknown\n Outer \u2192 Unknown\n Parameter \u2192 Unknown\n Request \u2192 Unknown\n RequestOf(methodName:String) \u2192 Pattern\n Return \u2192 Unknown\n StringLiteral \u2192 Unknown\n TryCatch \u2192 Unknown\n TypeAnnotation \u2192 Unknown\n TypeDeclaration \u2192 Unknown\n TypeLiteral \u2192 Unknown\n Var \u2192 Unknown\n WhileRequest \u2192 Unknown\n aMutableMap\n aPatternMatchingNode(kind:String) \u2192 Pattern\n check(module) \u2192 Done\n checkTypes(node)\n createSuggestion\n fail(message)\n fail(message)at(rng:ast.Range)\n fail(message)from(startCol)to(endCol)\n fail(message)from(startCol)to(endCol)suggest(sugg)\n fail(msg)when(pat) \u2192 Done\n forBody(node)\n forCollection(node)\n methods\n rule(block) \u2192 Done\n runRules(node)\n scope \u2192 Unknown\n typeOf(node)\n when(pat)error(msg)\n whileBody(node)\n whileCond(node)\ntypes:\n AstNode\n";
if (typeof originalSourceLines !== "undefined") {
  originalSourceLines["dialect"] = [
    "dialect \"none\"",
    "import \"standardGrace\" as sg",
    "import \"errormessages\" as errormessages",
    "import \"ast\" as ast",
    "",
    "inherit sg.methods",
    "",
    "method methods {",
    "    // This should be unnecessary, because it is inherited.",
    "    // But the compiler gets confised and does not find it statically.",
    "    prelude.clone(self)",
    "}",
    "",
    "// Checker error",
    "",
    "def CheckerFailure is public = Exception.refine \"CheckerFailure\"",
    "def DialectError is public = Exception.refine \"DialectError\"",
    "",
    "// Helper Map",
    "",
    "class entryFrom(key') to(value') is confidential {",
    "    def key is public = key'",
    "    var value is public := value'",
    "}",
    "",
    "class aMutableMap {",
    "",
    "    def entries = list []",
    "",
    "    method isEmpty -> Boolean { size == 0 }",
    "",
    "    method size -> Number { entries.size }",
    "",
    "    method at(key) {",
    "        atKey(key) do { value -> return value }",
    "",
    "        NoSuchObject.raise \"no key {key} in aMutableMap\"",
    "    }",
    "",
    "    method at(key) put(value) -> Done {",
    "        if (value.asString == \"done\") then {",
    "            ProgrammingError.raise \"mutableMap: attempting to put ‹done› at key {key}\"",
    "        }",
    "        for(entries) do { entry ->",
    "            if(entry.key == key) then {",
    "                entry.value := value",
    "                return done",
    "            }",
    "        }",
    "",
    "        entries.push(entryFrom(key) to(value))",
    "    }",
    "",
    "    method keys -> List {",
    "        def keys' = list []",
    "",
    "        for(entries) do { entry ->",
    "            keys'.push(entry.key)",
    "        }",
    "",
    "        return keys'",
    "    }",
    "",
    "    method values -> List {",
    "        def values' = list []",
    "",
    "        for(entries) do { entry ->",
    "            values'.push(entry.value)",
    "        }",
    "",
    "        return values'",
    "    }",
    "",
    "    method containsKey(key) -> Boolean {",
    "        atKey(key) do { _ -> return true }",
    "",
    "        return false",
    "    }",
    "",
    "    method atKey(key) do(block) -> Done {",
    "        atKey(key) do(block) else {}",
    "        return",
    "    }",
    "",
    "    method atKey(key) do(block) else(block') {",
    "        for(entries) do { entry ->",
    "            if(entry.key == key) then {",
    "                return block.apply(entry.value)",
    "            }",
    "        }",
    "",
    "        return block'.apply",
    "    }",
    "",
    "    method asString -> String is override {",
    "        var out := \"\\{\"",
    "        entries.do { entry ->",
    "            out := \"{out} {entry.key} => {entry.value}\"",
    "        } separatedBy {",
    "            out := \"{out},\"",
    "        }",
    "        return \"{out} \\}\"",
    "    }",
    "",
    "}",
    "",
    "",
    "",
    "// Rules",
    "",
    "// The defined type rules.",
    "def rules = list []",
    "",
    "// The cached type assignments.",
    "def cache = aMutableMap",
    "",
    "// Creates a new type rule.",
    "method rule(block) -> Done {",
    "    rules.push(block)",
    "}",
    "",
    "",
    "var currentLine := 0        // Will be updated with each node examined",
    "",
    "method fail (message) {",
    "    // short fail-with-message; gives source line only",
    "",
    "    DialectError.raise (message) with (ast.line(currentLine) column 0)",
    "}",
    "",
    "method fail (message) at (rng:ast.Range) {",
    "    // fail, using the source-file range rng for the error message",
    "    // Note that, since ast.AstNode conforms to ast.Range, the second",
    "    // argument can be an AstNode.",
    "",
    "    DialectError.raise (message) with (rng)",
    "}",
    "method fail (message) from (startCol) to (endCol) {",
    "    // fail, with a source range",
    "",
    "    def rng = ast.start (ast.line(currentLine) column(startCol))",
    "                  end (ast.line(currentLine) column(endCol))",
    "    DialectError.raise (message) with (rng)",
    "}",
    "",
    "method fail(message) from (startCol) to (endCol) suggest (sugg) {",
    "    // fail, with an object that contains a source range and a suggestion",
    "",
    "    def o = object {",
    "        inherit ast.start (ast.line(currentLine) column(startCol))",
    "                end (ast.line(currentLine) column(endCol))",
    "        def suggestions is public = [sugg]",
    "    }",
    "    DialectError.raise (message) with (o)",
    "}",
    "",
    "method fail (msg) when (pat) -> Done {",
    "    // adds a rule the the list of active rules.  The rule will fail when",
    "    // applied to a node x such that pat.match(x), and pat.apply(x) is true",
    "",
    "    rule { x ->",
    "        if (pat.matches(x) && {pat.apply(x)}) then {",
    "            fail(msg)",
    "        }",
    "    }",
    "}",
    "",
    "method createSuggestion {",
    "    errormessages.suggestion.new",
    "}",
    "",
    "method when(pat)error(msg) {",
    "    // alternate syntax for fail (msg) when (pat)",
    "",
    "    fail(msg)when(pat)",
    "}",
    "",
    "// Scope",
    "",
    "class stackOfKind(kind : String) is confidential {",
    "    def stack is public = list [aMutableMap]",
    "",
    "    method at(name : String) put(value) -> Done {",
    "        stack.last.at(name) put(value)",
    "    }",
    "",
    "    method find(name : String) butIfMissing(bl) {",
    "        var i := stack.size",
    "        while { i > 0 } do {",
    "            stack.at(i).atKey(name) do { value ->",
    "                return value",
    "            }",
    "",
    "            i := i - 1",
    "        }",
    "",
    "        return bl.apply",
    "    }",
    "",
    "}",
    "",
    "def scope is public = object {",
    "    def variables is public = stackOfKind(\"variable\")",
    "    def methods is public = stackOfKind(\"method\")",
    "    def types is public = stackOfKind(\"type\")",
    "",
    "    method size -> Number {",
    "        variables.stack.size",
    "    }",
    "",
    "    method enter(bl) {",
    "        variables.stack.push(aMutableMap)",
    "        methods.stack.push(aMutableMap)",
    "        types.stack.push(aMutableMap)",
    "",
    "        def result = bl.apply",
    "",
    "        variables.stack.pop",
    "        methods.stack.pop",
    "        types.stack.pop",
    "",
    "        return result",
    "    }",
    "",
    "    method asString -> String is override {",
    "        \"scope<{size}>\"",
    "    }",
    "}",
    "",
    "method checkTypes(node) {",
    "    node.accept(astVisitor)",
    "}",
    "",
    "method typeOf(node) {",
    "    checkTypes(node)",
    "    cache.atKey(node) do { value -> return value }",
    "    DialectError.raise \"cannot type non-expression {node}\" with (node)",
    "}",
    "",
    "method runRules(node) {",
    "    // apply all rules to node; returns the result of the last applicable rule.",
    "    // if there are no applicable rules, returns false",
    "    cache.atKey(node) do { value -> return value }",
    "    currentLine := node.line",
    "",
    "    var result := false",
    "    for (rules) do { each ->",
    "        if (each.matches(node)) then {",
    "            result := each.apply(node)",
    "            if (result.asString == \"done\") then {",
    "                ProgrammingError.raise",
    "                    \"rule.apply(node) has result 'done' when rule is {each} and node = {node}\"",
    "            }",
    "            cache.at(node) put(result)",
    "        }",
    "    }",
    "    return result",
    "}",
    "",
    "",
    "// Type checker",
    "",
    "// Checks the defined rules on the given AST.",
    "method check(module) -> Done {",
    "    // Runs the check on the module object.",
    "    module.accept(astVisitor)",
    "}",
    "",
    "type AstNode = { kind -> String }",
    "",
    "class aPatternMatchingNode(kind:String) -> Pattern {",
    "    inherit BasicPattern.new",
    "",
    "    method matches(obj:Object) -> Boolean {",
    "        match(obj)",
    "            case { node:AstNode -> kind == node.kind }",
    "            else { false }",
    "    }",
    "}",
    "",
    "def If is public = aPatternMatchingNode \"if\"",
    "def BlockLiteral is public = aPatternMatchingNode \"block\"",
    "def MatchCase is public = aPatternMatchingNode \"matchcase\"",
    "def TryCatch is public = aPatternMatchingNode \"trycatch\"",
    "def Outer is public = aPatternMatchingNode \"outer\"",
    "def MethodSignature is public = aPatternMatchingNode \"methodtype\"",
    "def TypeLiteral is public = aPatternMatchingNode \"typeliteral\"",
    "def TypeDeclaration is public = aPatternMatchingNode \"typedec\"",
    "def TypeAnnotation is public = aPatternMatchingNode \"dtype\"",
    "def Method is public = aPatternMatchingNode \"method\"",
    "def Parameter is public = aPatternMatchingNode \"parameter\"",
    "def Request is public = aPatternMatchingNode \"call\"",
    "def Class is public = aPatternMatchingNode \"class\"",
    "def ObjectLiteral is public = aPatternMatchingNode \"object\"",
    "def ArrayLiteral is public = aPatternMatchingNode \"array\"",
    "def Member is public = aPatternMatchingNode \"member\"",
    "def Generic is public = aPatternMatchingNode \"generic\"",
    "def Identifier is public = aPatternMatchingNode \"identifier\"",
    "def OctetsLiteral is public = aPatternMatchingNode \"octets\"",
    "def StringLiteral is public = aPatternMatchingNode \"string\"",
    "def NumberLiteral is public = aPatternMatchingNode \"num\"",
    "def Operator is public = aPatternMatchingNode \"op\"",
    "def Bind is public = aPatternMatchingNode \"bind\"",
    "def Def is public = aPatternMatchingNode \"defdec\"",
    "def Var is public = aPatternMatchingNode \"vardec\"",
    "def Import is public = aPatternMatchingNode \"import\"",
    "def Dialect is public = aPatternMatchingNode \"dialect\"",
    "def Return is public = aPatternMatchingNode \"return\"",
    "def Inherit is public = aPatternMatchingNode \"inherit\"",
    "",
    "// Special requests patterns.",
    "",
    "class RequestOf(methodName:String) -> Pattern {",
    "",
    "    inherit BasicPattern.new",
    "",
    "    method matches(obj:Object) -> Boolean {",
    "        match(obj)",
    "          case { node:AstNode ->",
    "            node.isCall && {node.canonicalName == methodName}",
    "        } else {",
    "            false",
    "        }",
    "    }",
    "}",
    "",
    "def WhileRequest is public = RequestOf \"while(_)do(_)\"",
    "def ForRequest is public = RequestOf \"for(_)do(_)\"",
    "",
    "method whileCond(node) {",
    "    // answers the condition expression from node, which must be a",
    "    // a callNode calling \"while(_)do(_)\"",
    "    def sig = node.parts",
    "    sig.first.args.first",
    "}",
    "",
    "method whileBody(node) {",
    "    // answers the body expression from node, which must be a",
    "    // a callNode calling \"while(_)do(_)\"",
    "    def sig = node.parts",
    "    sig.second.args.first",
    "}",
    "",
    "method forCollection(node) {",
    "    // answers the collection expression from node, which must be a",
    "    // a callNode calling \"for(_)do(_)\"",
    "    def sig = node.parts",
    "    sig.first.args.first",
    "}",
    "",
    "method forBody(node) {",
    "    // answers the body expression from node, which must be a",
    "    // a callNode calling \"for(_)do(_)\"",
    "    def sig = node.parts",
    "    sig.second.args.first",
    "}",
    "",
    "def astVisitor = object {",
    "    inherit ast.baseVisitor",
    "",
    "    method checkMatch(node) -> Boolean {",
    "        runRules(node)",
    "        return true",
    "    }",
    "",
    "    method visitIf(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitBlock(node) -> Boolean {",
    "        runRules(node)",
    "",
    "        for(node.params) do { param ->",
    "            runRules(aParameter.fromNode(param))",
    "        }",
    "",
    "        for(node.body) do { stmt ->",
    "            stmt.accept(self)",
    "        }",
    "",
    "        return false",
    "    }",
    "",
    "    method visitMatchCase(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitTryCatch(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitMethodType(node) -> Boolean {",
    "        runRules(node)",
    "",
    "        for(node.signature) do { part ->",
    "            for(part.params) do { param ->",
    "                runRules(aParameter.fromNode(param))",
    "            }",
    "        }",
    "",
    "        return false",
    "    }",
    "",
    "    method visitType(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitMethod(node) -> Boolean {",
    "        runRules(node)",
    "",
    "        for(node.signature) do { part ->",
    "            for(part.params) do { param ->",
    "                runRules(aParameter.fromNode(param))",
    "            }",
    "        }",
    "",
    "        for(node.body) do { stmt ->",
    "            stmt.accept(self)",
    "        }",
    "",
    "        return false",
    "    }",
    "",
    "    method visitCall(node) -> Boolean {",
    "        checkMatch(node)",
    "",
    "        match(node.receiver)",
    "          case { memb : Member ->",
    "            memb.receiver.accept(self)",
    "        } else { }",
    "",
    "        for(node.parts) do { part ->",
    "            for(part.args) do { arg ->",
    "                arg.accept(self)",
    "            }",
    "        }",
    "",
    "        return false",
    "    }",
    "",
    "    method visitObject(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitArray(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitMember(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitGeneric(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitIdentifier(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitOctets(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitString(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitNum(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitOp(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitBind(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitDefDec(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitVarDec(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitImport(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitReturn(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitInherits(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "    method visitDialect(node) -> Boolean {",
    "        checkMatch(node)",
    "    }",
    "",
    "}",
    "",
    "def aTypeAnnotation is confidential = object {",
    "    class fromNode(node) -> TypeAnnotation {",
    "        use identityEquality",
    "        def kind is public = \"dtype\"",
    "        def value is public = node",
    "        def line is public = node.line",
    "        def linePos is public = node.linePos",
    "    }",
    "}",
    "",
    "def aParameter is confidential = object {",
    "    class fromNode(node) -> Parameter {",
    "        use identityEquality",
    "        def kind is public = \"parameter\"",
    "        def value is public = node.value",
    "        def dtype is public = node.dtype",
    "        def line is public = node.line",
    "        def linePos is public = node.linePos",
    "    }",
    "}",
    "" ];
}
function gracecode_dialect() {
  importedModules["dialect"] = this;
  var module$dialect = this;
  this.definitionModule = "dialect";
  this.definitionLine = 1;
  var var_prelude = var___95__prelude;
  this.closureKeys = this.closureKeys || [];
  this.closureKeys.push("outer_dialect_1");
  this.outer_dialect_1 = var_prelude;
  setLineNumber(2);    // compilenode import
  // Import of "standardGrace" as sg
  if (typeof gracecode_standardGrace == "undefined")
    throw new GraceExceptionPacket(EnvironmentExceptionObject, 
      new GraceString("could not find module standardGrace"));
  var var_sg = do_import("standardGrace", gracecode_standardGrace);
  var func0 = function(argcv) {     // accessor method sg
    const numArgs = arguments.length - 1;
    if (numArgs > 0) raiseTypeArgError("sg", 0, numArgs - 0);
    if (var_sg === undefined) raiseUninitializedVariable("sg");
    return var_sg;
  };    // end of method sg
  this.methods["sg"] = func0;
  func0.methodName = "sg";
  func0.paramCounts = [0];
  func0.paramNames = [];
  func0.definitionLine = 2;
  func0.definitionModule = "dialect";
  func0.debug = "import";
  func0.confidential = true;
  setLineNumber(3);    // compilenode import
  // Import of "errormessages" as errormessages
  if (typeof gracecode_errormessages == "undefined")
    throw new GraceExceptionPacket(EnvironmentExceptionObject, 
      new GraceString("could not find module errormessages"));
  var var_errormessages = do_import("errormessages", gracecode_errormessages);
  var func1 = function(argcv) {     // accessor method errormessages
    const numArgs = arguments.length - 1;
    if (numArgs > 0) raiseTypeArgError("errormessages", 0, numArgs - 0);
    if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
    return var_errormessages;
  };    // end of method errormessages
  this.methods["errormessages"] = func1;
  func1.methodName = "errormessages";
  func1.paramCounts = [0];
  func1.paramNames = [];
  func1.definitionLine = 3;
  func1.definitionModule = "dialect";
  func1.debug = "import";
  func1.confidential = true;
  setLineNumber(4);    // compilenode import
  // Import of "ast" as ast
  if (typeof gracecode_ast == "undefined")
    throw new GraceExceptionPacket(EnvironmentExceptionObject, 
      new GraceString("could not find module ast"));
  var var_ast = do_import("ast", gracecode_ast);
  var func2 = function(argcv) {     // accessor method ast
    const numArgs = arguments.length - 1;
    if (numArgs > 0) raiseTypeArgError("ast", 0, numArgs - 0);
    if (var_ast === undefined) raiseUninitializedVariable("ast");
    return var_ast;
  };    // end of method ast
  this.methods["ast"] = func2;
  func2.methodName = "ast";
  func2.paramCounts = [0];
  func2.paramNames = [];
  func2.definitionLine = 4;
  func2.definitionModule = "dialect";
  func2.debug = "import";
  func2.confidential = true;
  setLineNumber(6);    // reuse call
  var initFun3 = request(var_sg, "methods$build(3)", [null], this, [], []);  // compileReuseCall
  var func4 = function(argcv) {    // method methods, line 8
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("methods", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("methods", "dialect", 8);
    var ouc_init = this.methods["methods$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method methods
  this.methods["methods"] = func4;
  func4.methodName = "methods";
  func4.paramCounts = [0];
  func4.paramNames = [];
  func4.definitionLine = 8;
  func4.definitionModule = "dialect";
  var func5 = function(argcv, ouc, aliases, exclusions) {    // method methods$build(_,_,_), line 8
    var returnTarget = invocationCount;
    invocationCount++;
    // call case 2: outer request
    var call6 = selfRequest(var_prelude, "clone(1)$build(3)", [1, 3], this, ouc, aliases, exclusions);
    return call6;      // from compileBuildMethodFor(_)withFreshCall(_)inside(_)
  };    // end of method methods$build(_,_,_)
  this.methods["methods$build(3)"] = func5;
  func5.methodName = "methods$build(3)";
  func5.paramCounts = [0];
  func5.paramNames = [];
  func5.definitionLine = 8;
  func5.definitionModule = "dialect";
  var func7 = function(argcv, var_key__39__, var_value__39__) {    // method entryFrom(_)to(_), line 21
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("entryFrom(_)to(_)", 0, numArgs - 2);
    }
    var ouc = emptyGraceObject("entryFrom(_)to(_)", "dialect", 21);
    var ouc_init = this.methods["entryFrom(1)to(1)$build(3)"].call(this, null, var_key__39__, var_value__39__, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method entryFrom(_)to(_)
  func7.confidential = true;
  this.methods["entryFrom(1)to(1)"] = func7;
  func7.methodName = "entryFrom(1)to(1)";
  func7.paramCounts = [1, 1];
  func7.paramNames = ["key'", "value'"];
  func7.definitionLine = 21;
  func7.definitionModule = "dialect";
  var func8 = function(argcv, var_key__39__, var_value__39__, inheritingObject, aliases, exclusions) {    // method entryFrom(_)to(_)$build(_,_,_), line 21
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("entryFrom(_)to(_)", 0, numArgs - 2);
    }
    var obj9_build = function(ignore, var_key__39__, var_value__39__, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_dialect_21");
      this.outer_dialect_21 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      this.data.key = undefined;
      var reader10_key = function() {  // reader method key
          if (this.data.key === undefined) raiseUninitializedVariable("key");
          return this.data.key;
      };
      reader10_key.isDef = true;
      this.methods["key"] = reader10_key;
      this.data.value = undefined;
      var reader11_value = function() {  // reader method value
          if (this.data.value === undefined) raiseUninitializedVariable("value");
          return this.data.value;
      };
      reader11_value.isVar = true;
      this.methods["value"] = reader11_value;
      var writer12_value = function(argcv, n) {   // writer method value:=(_)
        this.data.value = n;
        return GraceDone;
      };
      this.methods["value:=(1)"] = writer12_value;
      this.mutable = true;
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 21;
          m.definitionModule = "dialect";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj9_init = function() {    // init of object on line 21
        this.data.key = var_key__39__;
        this.data.value = var_value__39__;
      };
      return obj9_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj9_init = obj9_build.call(inheritingObject, null, var_key__39__, var_value__39__, this, aliases, exclusions);
    return obj9_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method entryFrom(_)to(_)$build(_,_,_)
  func8.confidential = true;
  this.methods["entryFrom(1)to(1)$build(3)"] = func8;
  func8.methodName = "entryFrom(1)to(1)$build(3)";
  func8.paramCounts = [1, 1];
  func8.paramNames = ["key'", "value'"];
  func8.definitionLine = 21;
  func8.definitionModule = "dialect";
  var func13 = function(argcv) {    // method aMutableMap, line 26
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("aMutableMap", 0, numArgs - 0);
    }
    var ouc = emptyGraceObject("aMutableMap", "dialect", 26);
    var ouc_init = this.methods["aMutableMap$build(3)"].call(this, null, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method aMutableMap
  this.methods["aMutableMap"] = func13;
  func13.methodName = "aMutableMap";
  func13.paramCounts = [0];
  func13.paramNames = [];
  func13.definitionLine = 26;
  func13.definitionModule = "dialect";
  var func14 = function(argcv, inheritingObject, aliases, exclusions) {    // method aMutableMap$build(_,_,_), line 26
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("aMutableMap", 0, numArgs - 0);
    }
    var obj15_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_dialect_26");
      this.outer_dialect_26 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      this.data.entries = undefined;
      var reader16_entries = function() {  // reader method entries
          if (this.data.entries === undefined) raiseUninitializedVariable("entries");
          return this.data.entries;
      };
      reader16_entries.isDef = true;
      reader16_entries.confidential = true;
      this.methods["entries"] = reader16_entries;
      var func17 = function(argcv) {    // method isEmpty, line 30
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("isEmpty", 0, numArgs - 0);
        }
        setLineNumber(30);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call18 = selfRequest(this, "size", [0]);
        var opresult19 = request(call18, "==(1)", [1], new GraceNum(0));
        assertTypeOrMsg(opresult19, var_Boolean, "result of method isEmpty", "Boolean");
        return opresult19;
      };    // end of method isEmpty
      this.methods["isEmpty"] = func17;
      func17.methodName = "isEmpty";
      func17.paramCounts = [0];
      func17.paramNames = [];
      func17.definitionLine = 30;
      func17.definitionModule = "dialect";
      var func20 = function(argcv) {    // method size, line 32
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("size", 0, numArgs - 0);
        }
        setLineNumber(32);    // compilenode member
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call22 = selfRequest(this, "entries", [0]);
        var call21 = request(call22, "size", [0]);
        assertTypeOrMsg(call21, var_Number, "result of method size", "Number");
        return call21;
      };    // end of method size
      this.methods["size"] = func20;
      func20.methodName = "size";
      func20.paramCounts = [0];
      func20.paramNames = [];
      func20.definitionLine = 32;
      func20.definitionModule = "dialect";
      var func23 = function(argcv, var_key) {    // method at(_), line 34
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("at(_)", 0, numArgs - 1);
        }
        setLineNumber(35);    // compilenode block
        var block25 = new GraceBlock(this, 35, 1);
        block25.guard = jsTrue;
        block25.real = function block25(var_value) {
          setLineNumber(35);    // compilenode return
          throw new ReturnException(var_value, returnTarget);
        };
        let applyMeth25 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth25.methodName = "apply(1)";
        applyMeth25.paramCounts = [1];
        applyMeth25.paramNames = ["value"];
        applyMeth25.definitionLine = 35;
        applyMeth25.definitionModule = "dialect";
        block25.methods["apply(1)"] = applyMeth25;
        let matchesMeth25 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth25.methodName = "matches(1)";
        matchesMeth25.paramCounts = [1];
        matchesMeth25.paramNames = ["value"];
        matchesMeth25.definitionLine = 35;
        matchesMeth25.definitionModule = "dialect";
        block25.methods["matches(1)"] = matchesMeth25;
        // call case 4: self request with 2 args and 0 typeArgs 
        var call24 = selfRequest(this, "atKey(1)do(1)", [1, 1], var_key, block25);
        setLineNumber(37);    // compilenode string
        var string27 = new GraceString("no key ");
        var opresult28 = request(string27, "++(1)", [1], var_key);
        var string29 = new GraceString(" in aMutableMap");
        var opresult30 = request(opresult28, "++(1)", [1], string29);
        // call case 6: other requests
        // call case 2: outer request
        var call31 = selfRequest(importedModules["dialect"], "NoSuchObject", [0]);
        var call26 = request(call31, "raise(1)", [1], opresult30);
        return call26;
      };    // end of method at(_)
      this.methods["at(1)"] = func23;
      func23.methodName = "at(1)";
      func23.paramCounts = [1];
      func23.paramNames = ["key"];
      func23.definitionLine = 34;
      func23.definitionModule = "dialect";
      var func32 = function(argcv, var_key, var_value) {    // method at(_)put(_), line 40
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 2) && (numArgs !== 2)) {
            raiseTypeArgError("at(_)put(_)", 0, numArgs - 2);
        }
        var if33 = GraceDone;
        setLineNumber(41);    // compilenode member
        // call case 6: other requests
        var call34 = request(var_value, "asString", [0]);
        var string35 = new GraceString("done");
        var opresult36 = request(call34, "==(1)", [1], string35);
        if (Grace_isTrue(opresult36)) {
          setLineNumber(42);    // compilenode string
          var string38 = new GraceString("mutableMap: attempting to put \u2039done\u203a at key ");
          var opresult39 = request(string38, "++(1)", [1], var_key);
          var string40 = new GraceString("");
          var opresult41 = request(opresult39, "++(1)", [1], string40);
          // call case 6: other requests
          // call case 2: outer request
          var call42 = selfRequest(var_prelude, "ProgrammingError", [0]);
          var call37 = request(call42, "raise(1)", [1], opresult41);
          if33 = call37;
        }
        setLineNumber(44);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call44 = selfRequest(this, "entries", [0]);
        var block45 = new GraceBlock(this, 44, 1);
        block45.guard = jsTrue;
        block45.real = function block45(var_entry) {
          var if46 = GraceDone;
          setLineNumber(45);    // compilenode member
          // call case 6: other requests
          var call47 = request(var_entry, "key", [0]);
          var opresult48 = request(call47, "==(1)", [1], var_key);
          if (Grace_isTrue(opresult48)) {
            setLineNumber(46);    // compilenode call
            // call case 6: other requests
            var call49 = request(var_entry, "value:=(1)", [1], var_value);
            setLineNumber(47);    // compilenode return
            throw new ReturnException(var_done, returnTarget);
          }
          return if46;
        };
        let applyMeth45 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth45.methodName = "apply(1)";
        applyMeth45.paramCounts = [1];
        applyMeth45.paramNames = ["entry"];
        applyMeth45.definitionLine = 44;
        applyMeth45.definitionModule = "dialect";
        block45.methods["apply(1)"] = applyMeth45;
        let matchesMeth45 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth45.methodName = "matches(1)";
        matchesMeth45.paramCounts = [1];
        matchesMeth45.paramNames = ["entry"];
        matchesMeth45.definitionLine = 44;
        matchesMeth45.definitionModule = "dialect";
        block45.methods["matches(1)"] = matchesMeth45;
        // call case 2: outer request
        var call43 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call44, block45);
        setLineNumber(51);    // compilenode call
        // call case 2: outer request
        var call51 = selfRequest(importedModules["dialect"], "entryFrom(1)to(1)", [1, 1], var_key, var_value);
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call52 = selfRequest(this, "entries", [0]);
        var call50 = request(call52, "push(1)", [1], call51);
        return call50;
      };    // end of method at(_)put(_)
      this.methods["at(1)put(1)"] = func32;
      func32.methodName = "at(1)put(1)";
      func32.paramCounts = [1, 1];
      func32.paramNames = ["key", "value"];
      func32.definitionLine = 40;
      func32.definitionModule = "dialect";
      var func53 = function(argcv) {    // method keys, line 54
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("keys", 0, numArgs - 0);
        }
        setLineNumber(55);    // compilenode array
        var array55 = new GraceSequence([]);
        // call case 2: outer request
        var call54 = selfRequest(importedModules["dialect"], "list(1)", [1], array55);
        var var_keys__39__ = call54;
        setLineNumber(57);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call57 = selfRequest(this, "entries", [0]);
        var block58 = new GraceBlock(this, 57, 1);
        block58.guard = jsTrue;
        block58.real = function block58(var_entry) {
          setLineNumber(58);    // compilenode member
          // call case 6: other requests
          var call60 = request(var_entry, "key", [0]);
          // call case 6: other requests
          if (var_keys__39__ === undefined) raiseUninitializedVariable("keys'");
          var call59 = request(var_keys__39__, "push(1)", [1], call60);
          return call59;
        };
        let applyMeth58 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth58.methodName = "apply(1)";
        applyMeth58.paramCounts = [1];
        applyMeth58.paramNames = ["entry"];
        applyMeth58.definitionLine = 57;
        applyMeth58.definitionModule = "dialect";
        block58.methods["apply(1)"] = applyMeth58;
        let matchesMeth58 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth58.methodName = "matches(1)";
        matchesMeth58.paramCounts = [1];
        matchesMeth58.paramNames = ["entry"];
        matchesMeth58.definitionLine = 57;
        matchesMeth58.definitionModule = "dialect";
        block58.methods["matches(1)"] = matchesMeth58;
        // call case 2: outer request
        var call56 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call57, block58);
        setLineNumber(54);    // compilenode member
        // call case 2: outer request
        var call61 = selfRequest(importedModules["dialect"], "List", [0]);
        setLineNumber(61);    // typecheck
        assertTypeOrMsg(var_keys__39__, call61, "return value", "List");
        return var_keys__39__;
      };    // end of method keys
      this.methods["keys"] = func53;
      func53.methodName = "keys";
      func53.paramCounts = [0];
      func53.paramNames = [];
      func53.definitionLine = 54;
      func53.definitionModule = "dialect";
      var func62 = function(argcv) {    // method values, line 64
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("values", 0, numArgs - 0);
        }
        setLineNumber(65);    // compilenode array
        var array64 = new GraceSequence([]);
        // call case 2: outer request
        var call63 = selfRequest(importedModules["dialect"], "list(1)", [1], array64);
        var var_values__39__ = call63;
        setLineNumber(67);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call66 = selfRequest(this, "entries", [0]);
        var block67 = new GraceBlock(this, 67, 1);
        block67.guard = jsTrue;
        block67.real = function block67(var_entry) {
          setLineNumber(68);    // compilenode member
          // call case 6: other requests
          var call69 = request(var_entry, "value", [0]);
          // call case 6: other requests
          if (var_values__39__ === undefined) raiseUninitializedVariable("values'");
          var call68 = request(var_values__39__, "push(1)", [1], call69);
          return call68;
        };
        let applyMeth67 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth67.methodName = "apply(1)";
        applyMeth67.paramCounts = [1];
        applyMeth67.paramNames = ["entry"];
        applyMeth67.definitionLine = 67;
        applyMeth67.definitionModule = "dialect";
        block67.methods["apply(1)"] = applyMeth67;
        let matchesMeth67 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth67.methodName = "matches(1)";
        matchesMeth67.paramCounts = [1];
        matchesMeth67.paramNames = ["entry"];
        matchesMeth67.definitionLine = 67;
        matchesMeth67.definitionModule = "dialect";
        block67.methods["matches(1)"] = matchesMeth67;
        // call case 2: outer request
        var call65 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call66, block67);
        setLineNumber(64);    // compilenode member
        // call case 2: outer request
        var call70 = selfRequest(importedModules["dialect"], "List", [0]);
        setLineNumber(71);    // typecheck
        assertTypeOrMsg(var_values__39__, call70, "return value", "List");
        return var_values__39__;
      };    // end of method values
      this.methods["values"] = func62;
      func62.methodName = "values";
      func62.paramCounts = [0];
      func62.paramNames = [];
      func62.definitionLine = 64;
      func62.definitionModule = "dialect";
      var func71 = function(argcv, var_key) {    // method containsKey(_), line 74
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("containsKey(_)", 0, numArgs - 1);
        }
        setLineNumber(75);    // compilenode block
        var block73 = new GraceBlock(this, 75, 1);
        block73.guard = jsTrue;
        block73.real = function block73(var___95____95__1) {
          setLineNumber(75);    // typecheck
          assertTypeOrMsg(GraceTrue, var_Boolean, "return value", "Boolean");
          throw new ReturnException(GraceTrue, returnTarget);
        };
        let applyMeth73 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth73.methodName = "apply(1)";
        applyMeth73.paramCounts = [1];
        applyMeth73.paramNames = ["__1"];
        applyMeth73.definitionLine = 75;
        applyMeth73.definitionModule = "dialect";
        block73.methods["apply(1)"] = applyMeth73;
        let matchesMeth73 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth73.methodName = "matches(1)";
        matchesMeth73.paramCounts = [1];
        matchesMeth73.paramNames = ["__1"];
        matchesMeth73.definitionLine = 75;
        matchesMeth73.definitionModule = "dialect";
        block73.methods["matches(1)"] = matchesMeth73;
        // call case 4: self request with 2 args and 0 typeArgs 
        var call72 = selfRequest(this, "atKey(1)do(1)", [1, 1], var_key, block73);
        setLineNumber(77);    // typecheck
        assertTypeOrMsg(GraceFalse, var_Boolean, "return value", "Boolean");
        return GraceFalse;
      };    // end of method containsKey(_)
      this.methods["containsKey(1)"] = func71;
      func71.methodName = "containsKey(1)";
      func71.paramCounts = [1];
      func71.paramNames = ["key"];
      func71.definitionLine = 74;
      func71.definitionModule = "dialect";
      var func74 = function(argcv, var_key, var_block) {    // method atKey(_)do(_), line 80
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 2) && (numArgs !== 2)) {
            raiseTypeArgError("atKey(_)do(_)", 0, numArgs - 2);
        }
        setLineNumber(81);    // compilenode block
        var block76 = new GraceBlock(this, 81, 0);
        block76.guard = jsTrue;
        block76.real = function block76() {
          setLineNumber(81);    // compileBlock
          return GraceDone;
        };
        let applyMeth76 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth76.methodName = "apply";
        applyMeth76.paramCounts = [0];
        applyMeth76.paramNames = [];
        applyMeth76.definitionLine = 81;
        applyMeth76.definitionModule = "dialect";
        block76.methods["apply"] = applyMeth76;
        // call case 4: self request with 3 args and 0 typeArgs 
        var call75 = selfRequest(this, "atKey(1)do(1)else(1)", [1, 1, 1], var_key, var_block, block76);
        setLineNumber(82);    // compilenode return
        return var_done;
      };    // end of method atKey(_)do(_)
      this.methods["atKey(1)do(1)"] = func74;
      func74.methodName = "atKey(1)do(1)";
      func74.paramCounts = [1, 1];
      func74.paramNames = ["key", "block"];
      func74.definitionLine = 80;
      func74.definitionModule = "dialect";
      var func77 = function(argcv, var_key, var_block, var_block__39__) {    // method atKey(_)do(_)else(_), line 85
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 3) && (numArgs !== 3)) {
            raiseTypeArgError("atKey(_)do(_)else(_)", 0, numArgs - 3);
        }
        setLineNumber(86);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call79 = selfRequest(this, "entries", [0]);
        var block80 = new GraceBlock(this, 86, 1);
        block80.guard = jsTrue;
        block80.real = function block80(var_entry) {
          var if81 = GraceDone;
          setLineNumber(87);    // compilenode member
          // call case 6: other requests
          var call82 = request(var_entry, "key", [0]);
          var opresult83 = request(call82, "==(1)", [1], var_key);
          if (Grace_isTrue(opresult83)) {
            setLineNumber(88);    // compilenode member
            // call case 6: other requests
            var call85 = request(var_entry, "value", [0]);
            // call case 6: other requests
            var call84 = request(var_block, "apply(1)", [1], call85);
            throw new ReturnException(call84, returnTarget);
          }
          return if81;
        };
        let applyMeth80 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth80.methodName = "apply(1)";
        applyMeth80.paramCounts = [1];
        applyMeth80.paramNames = ["entry"];
        applyMeth80.definitionLine = 86;
        applyMeth80.definitionModule = "dialect";
        block80.methods["apply(1)"] = applyMeth80;
        let matchesMeth80 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth80.methodName = "matches(1)";
        matchesMeth80.paramCounts = [1];
        matchesMeth80.paramNames = ["entry"];
        matchesMeth80.definitionLine = 86;
        matchesMeth80.definitionModule = "dialect";
        block80.methods["matches(1)"] = matchesMeth80;
        // call case 2: outer request
        var call78 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call79, block80);
        setLineNumber(92);    // compilenode member
        // call case 6: other requests
        var call86 = request(var_block__39__, "apply", [0]);
        return call86;
      };    // end of method atKey(_)do(_)else(_)
      this.methods["atKey(1)do(1)else(1)"] = func77;
      func77.methodName = "atKey(1)do(1)else(1)";
      func77.paramCounts = [1, 1, 1];
      func77.paramNames = ["key", "block", "block'"];
      func77.definitionLine = 85;
      func77.definitionModule = "dialect";
      var func87 = function(argcv) {    // method asString, line 95
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 0) && (numArgs !== 0)) {
            raiseTypeArgError("asString", 0, numArgs - 0);
        }
        setLineNumber(96);    // compilenode string
        var string88 = new GraceString("{");
        var var_out = string88;
        setLineNumber(97);    // compilenode block
        var block90 = new GraceBlock(this, 97, 1);
        block90.guard = jsTrue;
        block90.real = function block90(var_entry) {
          setLineNumber(98);    // compilenode string
          var string91 = new GraceString("");
          if (var_out === undefined) raiseUninitializedVariable("out");
          var opresult92 = request(string91, "++(1)", [1], var_out);
          var string93 = new GraceString(" ");
          var opresult94 = request(opresult92, "++(1)", [1], string93);
          // call case 6: other requests
          var call95 = request(var_entry, "key", [0]);
          var opresult96 = request(opresult94, "++(1)", [1], call95);
          var string97 = new GraceString(" => ");
          var opresult98 = request(opresult96, "++(1)", [1], string97);
          // call case 6: other requests
          var call99 = request(var_entry, "value", [0]);
          var opresult100 = request(opresult98, "++(1)", [1], call99);
          var string101 = new GraceString("");
          var opresult102 = request(opresult100, "++(1)", [1], string101);
          var_out = opresult102;
          return GraceDone;
        };
        let applyMeth90 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth90.methodName = "apply(1)";
        applyMeth90.paramCounts = [1];
        applyMeth90.paramNames = ["entry"];
        applyMeth90.definitionLine = 97;
        applyMeth90.definitionModule = "dialect";
        block90.methods["apply(1)"] = applyMeth90;
        let matchesMeth90 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth90.methodName = "matches(1)";
        matchesMeth90.paramCounts = [1];
        matchesMeth90.paramNames = ["entry"];
        matchesMeth90.definitionLine = 97;
        matchesMeth90.definitionModule = "dialect";
        block90.methods["matches(1)"] = matchesMeth90;
        setLineNumber(99);    // compilenode block
        var block103 = new GraceBlock(this, 99, 0);
        block103.guard = jsTrue;
        block103.real = function block103() {
          setLineNumber(100);    // compilenode string
          var string104 = new GraceString("");
          if (var_out === undefined) raiseUninitializedVariable("out");
          var opresult105 = request(string104, "++(1)", [1], var_out);
          var string106 = new GraceString(",");
          var opresult107 = request(opresult105, "++(1)", [1], string106);
          var_out = opresult107;
          return GraceDone;
        };
        let applyMeth103 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth103.methodName = "apply";
        applyMeth103.paramCounts = [0];
        applyMeth103.paramNames = [];
        applyMeth103.definitionLine = 99;
        applyMeth103.definitionModule = "dialect";
        block103.methods["apply"] = applyMeth103;
        // call case 6: other requests
        setLineNumber(97);    // compilenode member
        // call case 4: self request with 0 args and 0 typeArgs 
        var call108 = selfRequest(this, "entries", [0]);
        var call89 = request(call108, "do(1)separatedBy(1)", [1, 1], block90, block103);
        setLineNumber(102);    // compilenode string
        var string109 = new GraceString("");
        var opresult110 = request(string109, "++(1)", [1], var_out);
        var string111 = new GraceString(" }");
        var opresult112 = request(opresult110, "++(1)", [1], string111);
        assertTypeOrMsg(opresult112, var_String, "return value", "String");
        return opresult112;
      };    // end of method asString
      this.methods["asString"] = func87;
      func87.methodName = "asString";
      func87.paramCounts = [0];
      func87.paramNames = [];
      func87.definitionLine = 95;
      func87.definitionModule = "dialect";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 26;
          m.definitionModule = "dialect";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj15_init = function() {    // init of object on line 26
        setLineNumber(28);    // compilenode array
        var array114 = new GraceSequence([]);
        // call case 2: outer request
        var call113 = selfRequest(importedModules["dialect"], "list(1)", [1], array114);
        this.data.entries = call113;
      };
      return obj15_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj15_init = obj15_build.call(inheritingObject, null, this, aliases, exclusions);
    return obj15_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method aMutableMap$build(_,_,_)
  this.methods["aMutableMap$build(3)"] = func14;
  func14.methodName = "aMutableMap$build(3)";
  func14.paramCounts = [0];
  func14.paramNames = [];
  func14.definitionLine = 26;
  func14.definitionModule = "dialect";
  var func115 = function(argcv, var_block) {    // method rule(_), line 118
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("rule(_)", 0, numArgs - 1);
    }
    setLineNumber(119);    // compilenode call
    // call case 6: other requests
    if (var_rules === undefined) raiseUninitializedVariable("rules");
    var call116 = request(var_rules, "push(1)", [1], var_block);
    return call116;
  };    // end of method rule(_)
  this.methods["rule(1)"] = func115;
  func115.methodName = "rule(1)";
  func115.paramCounts = [1];
  func115.paramNames = ["block"];
  func115.definitionLine = 118;
  func115.definitionModule = "dialect";
  var func117 = function(argcv, var_message) {    // method fail(_), line 125
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("fail(_)", 0, numArgs - 1);
    }
    setLineNumber(128);    // compilenode call
    if (var_currentLine === undefined) raiseUninitializedVariable("currentLine");
    // call case 6: other requests
    if (var_ast === undefined) raiseUninitializedVariable("ast");
    var call119 = request(var_ast, "line(1)column(1)", [1, 1], var_currentLine, new GraceNum(0));
    // call case 6: other requests
    if (var_DialectError === undefined) raiseUninitializedVariable("DialectError");
    var call118 = request(var_DialectError, "raise(1)with(1)", [1, 1], var_message, call119);
    return call118;
  };    // end of method fail(_)
  this.methods["fail(1)"] = func117;
  func117.methodName = "fail(1)";
  func117.paramCounts = [1];
  func117.paramNames = ["message"];
  func117.definitionLine = 125;
  func117.definitionModule = "dialect";
  var func120 = function(argcv, var_message, var_rng) {    // method fail(_)at(_), line 131
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("fail(_)at(_)", 0, numArgs - 2);
    }
    setLineNumber(131);    // compilenode member
    // call case 6: other requests
    if (var_ast === undefined) raiseUninitializedVariable("ast");
    var call121 = request(var_ast, "Range", [0]);
    assertTypeOrMsg(var_rng, call121, "argument 2 in request of `fail(_)at(_)`", "ast.Range");
    setLineNumber(136);    // compilenode call
    // call case 6: other requests
    if (var_DialectError === undefined) raiseUninitializedVariable("DialectError");
    var call122 = request(var_DialectError, "raise(1)with(1)", [1, 1], var_message, var_rng);
    return call122;
  };    // end of method fail(_)at(_)
  this.methods["fail(1)at(1)"] = func120;
  func120.methodName = "fail(1)at(1)";
  func120.paramCounts = [1, 1];
  func120.paramNames = ["message", "rng"];
  func120.definitionLine = 131;
  func120.definitionModule = "dialect";
  var func123 = function(argcv, var_message, var_startCol, var_endCol) {    // method fail(_)from(_)to(_), line 138
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 3) && (numArgs !== 3)) {
        raiseTypeArgError("fail(_)from(_)to(_)", 0, numArgs - 3);
    }
    setLineNumber(141);    // compilenode call
    if (var_currentLine === undefined) raiseUninitializedVariable("currentLine");
    // call case 6: other requests
    if (var_ast === undefined) raiseUninitializedVariable("ast");
    var call125 = request(var_ast, "line(1)column(1)", [1, 1], var_currentLine, var_startCol);
    setLineNumber(142);    // compilenode call
    if (var_currentLine === undefined) raiseUninitializedVariable("currentLine");
    // call case 6: other requests
    if (var_ast === undefined) raiseUninitializedVariable("ast");
    var call126 = request(var_ast, "line(1)column(1)", [1, 1], var_currentLine, var_endCol);
    // call case 6: other requests
    if (var_ast === undefined) raiseUninitializedVariable("ast");
    var call124 = request(var_ast, "start(1)end(1)", [1, 1], call125, call126);
    var var_rng = call124;
    setLineNumber(143);    // compilenode call
    // call case 6: other requests
    if (var_DialectError === undefined) raiseUninitializedVariable("DialectError");
    var call127 = request(var_DialectError, "raise(1)with(1)", [1, 1], var_message, var_rng);
    return call127;
  };    // end of method fail(_)from(_)to(_)
  this.methods["fail(1)from(1)to(1)"] = func123;
  func123.methodName = "fail(1)from(1)to(1)";
  func123.paramCounts = [1, 1, 1];
  func123.paramNames = ["message", "startCol", "endCol"];
  func123.definitionLine = 138;
  func123.definitionModule = "dialect";
  var func128 = function(argcv, var_message, var_startCol, var_endCol, var_sugg) {    // method fail(_)from(_)to(_)suggest(_), line 146
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 4) && (numArgs !== 4)) {
        raiseTypeArgError("fail(_)from(_)to(_)suggest(_)", 0, numArgs - 4);
    }
    setLineNumber(149);    // compilenode object
    var obj129_build = function(ignore, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_dialect_149");
      this.outer_dialect_149 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(150);    // reuse call
      if (var_ast === undefined) raiseUninitializedVariable("ast");
      if (var_currentLine === undefined) raiseUninitializedVariable("currentLine");
      // call case 6: other requests
      if (var_ast === undefined) raiseUninitializedVariable("ast");
      var call130 = request(var_ast, "line(1)column(1)", [1, 1], var_currentLine, var_startCol);
      setLineNumber(151);    // compilenode call
      if (var_currentLine === undefined) raiseUninitializedVariable("currentLine");
      // call case 6: other requests
      if (var_ast === undefined) raiseUninitializedVariable("ast");
      var call131 = request(var_ast, "line(1)column(1)", [1, 1], var_currentLine, var_endCol);
      var initFun132 = request(var_ast, "start(1)end(1)$build(3)", [null], call130, call131, this, [], []);  // compileReuseCall
      this.data.suggestions = undefined;
      var reader133_suggestions = function() {  // reader method suggestions
          if (this.data.suggestions === undefined) raiseUninitializedVariable("suggestions");
          return this.data.suggestions;
      };
      reader133_suggestions.isDef = true;
      this.methods["suggestions"] = reader133_suggestions;
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 149;
          m.definitionModule = "dialect";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj129_init = function() {    // init of object on line 149
        initFun132.call(this);
        setLineNumber(152);    // compilenode array
        var array134 = new GraceSequence([var_sugg]);
        this.data.suggestions = array134;
      };
      return obj129_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj129 = emptyGraceObject("o", "dialect", 149);
    var obj129_init = obj129_build.call(obj129, null, this, [], []);
    obj129_init.call(obj129);  // end of compileobject
    var var_o = obj129;
    setLineNumber(154);    // compilenode call
    // call case 6: other requests
    if (var_DialectError === undefined) raiseUninitializedVariable("DialectError");
    var call135 = request(var_DialectError, "raise(1)with(1)", [1, 1], var_message, var_o);
    return call135;
  };    // end of method fail(_)from(_)to(_)suggest(_)
  this.methods["fail(1)from(1)to(1)suggest(1)"] = func128;
  func128.methodName = "fail(1)from(1)to(1)suggest(1)";
  func128.paramCounts = [1, 1, 1, 1];
  func128.paramNames = ["message", "startCol", "endCol", "sugg"];
  func128.definitionLine = 146;
  func128.definitionModule = "dialect";
  var func136 = function(argcv, var_msg, var_pat) {    // method fail(_)when(_), line 157
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("fail(_)when(_)", 0, numArgs - 2);
    }
    setLineNumber(161);    // compilenode block
    var block138 = new GraceBlock(this, 161, 1);
    block138.guard = jsTrue;
    block138.real = function block138(var_x) {
      var if139 = GraceDone;
      setLineNumber(162);    // compilenode call
      // call case 6: other requests
      var call140 = request(var_pat, "matches(1)", [1], var_x);
      var block141 = new GraceBlock(this, 162, 0);
      block141.guard = jsTrue;
      block141.real = function block141() {
        setLineNumber(162);    // compilenode call
        // call case 6: other requests
        var call142 = request(var_pat, "apply(1)", [1], var_x);
        return call142;
      };
      let applyMeth141 = function apply (argcv) {
          return this.real.apply(this.receiver);
      };
      applyMeth141.methodName = "apply";
      applyMeth141.paramCounts = [0];
      applyMeth141.paramNames = [];
      applyMeth141.definitionLine = 162;
      applyMeth141.definitionModule = "dialect";
      block141.methods["apply"] = applyMeth141;
      var opresult143 = request(call140, "&&(1)", [1], block141);
      if (Grace_isTrue(opresult143)) {
        setLineNumber(163);    // compilenode call
        // call case 2: outer request
        var call144 = selfRequest(importedModules["dialect"], "fail(1)", [1], var_msg);
        if139 = call144;
      }
      return if139;
    };
    let applyMeth138 = function apply_1 (argcv, ...args) {
        if (this.guard.apply(this.receiver, args))
            return this.real.apply(this.receiver, args);
        badBlockArgs.apply(this, args);
    };
    applyMeth138.methodName = "apply(1)";
    applyMeth138.paramCounts = [1];
    applyMeth138.paramNames = ["x"];
    applyMeth138.definitionLine = 161;
    applyMeth138.definitionModule = "dialect";
    block138.methods["apply(1)"] = applyMeth138;
    let matchesMeth138 = function matches_1 (argcv, ...args) {
        return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
    };
    matchesMeth138.methodName = "matches(1)";
    matchesMeth138.paramCounts = [1];
    matchesMeth138.paramNames = ["x"];
    matchesMeth138.definitionLine = 161;
    matchesMeth138.definitionModule = "dialect";
    block138.methods["matches(1)"] = matchesMeth138;
    // call case 2: outer request
    var call137 = selfRequest(importedModules["dialect"], "rule(1)", [1], block138);
    return call137;
  };    // end of method fail(_)when(_)
  this.methods["fail(1)when(1)"] = func136;
  func136.methodName = "fail(1)when(1)";
  func136.paramCounts = [1, 1];
  func136.paramNames = ["msg", "pat"];
  func136.definitionLine = 157;
  func136.definitionModule = "dialect";
  var func145 = function(argcv) {    // method createSuggestion, line 168
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 0) && (numArgs !== 0)) {
        raiseTypeArgError("createSuggestion", 0, numArgs - 0);
    }
    setLineNumber(169);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    if (var_errormessages === undefined) raiseUninitializedVariable("errormessages");
    var call147 = request(var_errormessages, "suggestion", [0]);
    var call146 = request(call147, "new", [0]);
    return call146;
  };    // end of method createSuggestion
  this.methods["createSuggestion"] = func145;
  func145.methodName = "createSuggestion";
  func145.paramCounts = [0];
  func145.paramNames = [];
  func145.definitionLine = 168;
  func145.definitionModule = "dialect";
  var func148 = function(argcv, var_pat, var_msg) {    // method when(_)error(_), line 172
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 2) && (numArgs !== 2)) {
        raiseTypeArgError("when(_)error(_)", 0, numArgs - 2);
    }
    setLineNumber(175);    // compilenode call
    // call case 2: outer request
    var call149 = selfRequest(importedModules["dialect"], "fail(1)when(1)", [1, 1], var_msg, var_pat);
    return call149;
  };    // end of method when(_)error(_)
  this.methods["when(1)error(1)"] = func148;
  func148.methodName = "when(1)error(1)";
  func148.paramCounts = [1, 1];
  func148.paramNames = ["pat", "msg"];
  func148.definitionLine = 172;
  func148.definitionModule = "dialect";
  var func150 = function(argcv, var_kind) {    // method stackOfKind(_), line 180
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("stackOfKind(_)", 0, numArgs - 1);
    }
    assertTypeOrMsg(var_kind, var_String, "argument to request of `stackOfKind(_)`", "String");
    var ouc = emptyGraceObject("stackOfKind(_)", "dialect", 180);
    var ouc_init = this.methods["stackOfKind(1)$build(3)"].call(this, null, var_kind, ouc, [], []);
    ouc_init.call(ouc);
    return ouc;
  };    // end of method stackOfKind(_)
  func150.paramTypes = [];
  func150.paramTypes.push([type_String, "kind"]);
  func150.confidential = true;
  this.methods["stackOfKind(1)"] = func150;
  func150.methodName = "stackOfKind(1)";
  func150.paramCounts = [1];
  func150.paramNames = ["kind"];
  func150.definitionLine = 180;
  func150.definitionModule = "dialect";
  var func151 = function(argcv, var_kind, inheritingObject, aliases, exclusions) {    // method stackOfKind(_)$build(_,_,_), line 180
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("stackOfKind(_)", 0, numArgs - 1);
    }
    assertTypeOrMsg(var_kind, var_String, "argument to request of `stackOfKind(_)`", "String");
    var obj152_build = function(ignore, var_kind, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_dialect_180");
      this.outer_dialect_180 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      this.data.stack = undefined;
      var reader153_stack = function() {  // reader method stack
          if (this.data.stack === undefined) raiseUninitializedVariable("stack");
          return this.data.stack;
      };
      reader153_stack.isDef = true;
      this.methods["stack"] = reader153_stack;
      var func154 = function(argcv, var_name, var_value) {    // method at(_)put(_), line 183
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 2) && (numArgs !== 2)) {
            raiseTypeArgError("at(_)put(_)", 0, numArgs - 2);
        }
        assertTypeOrMsg(var_name, var_String, "argument 1 in request of `at(_)put(_)`", "String");
        setLineNumber(184);    // compilenode call
        // call case 6: other requests
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call157 = selfRequest(this, "stack", [0]);
        var call156 = request(call157, "last", [0]);
        var call155 = request(call156, "at(1)put(1)", [1, 1], var_name, var_value);
        return call155;
      };    // end of method at(_)put(_)
      func154.paramTypes = [];
      func154.paramTypes.push([type_String, "name"]);
      func154.paramTypes.push([]);
      this.methods["at(1)put(1)"] = func154;
      func154.methodName = "at(1)put(1)";
      func154.paramCounts = [1, 1];
      func154.paramNames = ["name", "value"];
      func154.definitionLine = 183;
      func154.definitionModule = "dialect";
      var func158 = function(argcv, var_name, var_bl) {    // method find(_)butIfMissing(_), line 187
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 2) && (numArgs !== 2)) {
            raiseTypeArgError("find(_)butIfMissing(_)", 0, numArgs - 2);
        }
        assertTypeOrMsg(var_name, var_String, "argument 1 in request of `find(_)butIfMissing(_)`", "String");
        setLineNumber(188);    // compilenode member
        // call case 6: other requests
        // call case 4: self request with 0 args and 0 typeArgs 
        var call160 = selfRequest(this, "stack", [0]);
        var call159 = request(call160, "size", [0]);
        var var_i = call159;
        setLineNumber(189);    // compilenode block
        var block162 = new GraceBlock(this, 189, 0);
        block162.guard = jsTrue;
        block162.real = function block162() {
          setLineNumber(189);    // compilenode op
          if (var_i === undefined) raiseUninitializedVariable("i");
          var opresult163 = request(var_i, ">(1)", [1], new GraceNum(0));
          return opresult163;
        };
        let applyMeth162 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth162.methodName = "apply";
        applyMeth162.paramCounts = [0];
        applyMeth162.paramNames = [];
        applyMeth162.definitionLine = 189;
        applyMeth162.definitionModule = "dialect";
        block162.methods["apply"] = applyMeth162;
        var block164 = new GraceBlock(this, 189, 0);
        block164.guard = jsTrue;
        block164.real = function block164() {
          setLineNumber(190);    // compilenode block
          var block166 = new GraceBlock(this, 190, 1);
          block166.guard = jsTrue;
          block166.real = function block166(var_value) {
            setLineNumber(191);    // compilenode return
            throw new ReturnException(var_value, returnTarget);
          };
          let applyMeth166 = function apply_1 (argcv, ...args) {
              if (this.guard.apply(this.receiver, args))
                  return this.real.apply(this.receiver, args);
              badBlockArgs.apply(this, args);
          };
          applyMeth166.methodName = "apply(1)";
          applyMeth166.paramCounts = [1];
          applyMeth166.paramNames = ["value"];
          applyMeth166.definitionLine = 190;
          applyMeth166.definitionModule = "dialect";
          block166.methods["apply(1)"] = applyMeth166;
          let matchesMeth166 = function matches_1 (argcv, ...args) {
              return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
          };
          matchesMeth166.methodName = "matches(1)";
          matchesMeth166.paramCounts = [1];
          matchesMeth166.paramNames = ["value"];
          matchesMeth166.definitionLine = 190;
          matchesMeth166.definitionModule = "dialect";
          block166.methods["matches(1)"] = matchesMeth166;
          // call case 6: other requests
          setLineNumber(190);    // compilenode call
          if (var_i === undefined) raiseUninitializedVariable("i");
          // call case 6: other requests
          // call case 4: self request with 0 args and 0 typeArgs 
          var call168 = selfRequest(this, "stack", [0]);
          var call167 = request(call168, "at(1)", [1], var_i);
          var call165 = request(call167, "atKey(1)do(1)", [1, 1], var_name, block166);
          setLineNumber(194);    // compilenode op
          if (var_i === undefined) raiseUninitializedVariable("i");
          var diff169 = request(var_i, "-(1)", [1], new GraceNum(1));
          var_i = diff169;
          return GraceDone;
        };
        let applyMeth164 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth164.methodName = "apply";
        applyMeth164.paramCounts = [0];
        applyMeth164.paramNames = [];
        applyMeth164.definitionLine = 189;
        applyMeth164.definitionModule = "dialect";
        block164.methods["apply"] = applyMeth164;
        // call case 2: outer request
        var call161 = selfRequest(var_prelude, "while(1)do(1)", [1, 1], block162, block164);
        setLineNumber(197);    // compilenode member
        // call case 6: other requests
        var call170 = request(var_bl, "apply", [0]);
        return call170;
      };    // end of method find(_)butIfMissing(_)
      func158.paramTypes = [];
      func158.paramTypes.push([type_String, "name"]);
      func158.paramTypes.push([]);
      this.methods["find(1)butIfMissing(1)"] = func158;
      func158.methodName = "find(1)butIfMissing(1)";
      func158.paramCounts = [1, 1];
      func158.paramNames = ["name", "bl"];
      func158.definitionLine = 187;
      func158.definitionModule = "dialect";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 180;
          m.definitionModule = "dialect";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj152_init = function() {    // init of object on line 180
        setLineNumber(181);    // compilenode member
        // call case 2: outer request
        var call173 = selfRequest(importedModules["dialect"], "aMutableMap", [0]);
        var array172 = new GraceSequence([call173]);
        // call case 2: outer request
        var call171 = selfRequest(importedModules["dialect"], "list(1)", [1], array172);
        this.data.stack = call171;
      };
      return obj152_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj152_init = obj152_build.call(inheritingObject, null, var_kind, this, aliases, exclusions);
    return obj152_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method stackOfKind(_)$build(_,_,_)
  func151.paramTypes = [];
  func151.paramTypes.push([type_String, "kind"]);
  func151.confidential = true;
  this.methods["stackOfKind(1)$build(3)"] = func151;
  func151.methodName = "stackOfKind(1)$build(3)";
  func151.paramCounts = [1];
  func151.paramNames = ["kind"];
  func151.definitionLine = 180;
  func151.definitionModule = "dialect";
  var func174 = function(argcv, var_node) {    // method checkTypes(_), line 230
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("checkTypes(_)", 0, numArgs - 1);
    }
    setLineNumber(231);    // compilenode call
    if (var_astVisitor === undefined) raiseUninitializedVariable("astVisitor");
    // call case 6: other requests
    var call175 = request(var_node, "accept(1)", [1], var_astVisitor);
    return call175;
  };    // end of method checkTypes(_)
  this.methods["checkTypes(1)"] = func174;
  func174.methodName = "checkTypes(1)";
  func174.paramCounts = [1];
  func174.paramNames = ["node"];
  func174.definitionLine = 230;
  func174.definitionModule = "dialect";
  var func176 = function(argcv, var_node) {    // method typeOf(_), line 234
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("typeOf(_)", 0, numArgs - 1);
    }
    setLineNumber(235);    // compilenode call
    // call case 2: outer request
    var call177 = selfRequest(importedModules["dialect"], "checkTypes(1)", [1], var_node);
    setLineNumber(236);    // compilenode block
    var block179 = new GraceBlock(this, 236, 1);
    block179.guard = jsTrue;
    block179.real = function block179(var_value) {
      setLineNumber(236);    // compilenode return
      throw new ReturnException(var_value, returnTarget);
    };
    let applyMeth179 = function apply_1 (argcv, ...args) {
        if (this.guard.apply(this.receiver, args))
            return this.real.apply(this.receiver, args);
        badBlockArgs.apply(this, args);
    };
    applyMeth179.methodName = "apply(1)";
    applyMeth179.paramCounts = [1];
    applyMeth179.paramNames = ["value"];
    applyMeth179.definitionLine = 236;
    applyMeth179.definitionModule = "dialect";
    block179.methods["apply(1)"] = applyMeth179;
    let matchesMeth179 = function matches_1 (argcv, ...args) {
        return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
    };
    matchesMeth179.methodName = "matches(1)";
    matchesMeth179.paramCounts = [1];
    matchesMeth179.paramNames = ["value"];
    matchesMeth179.definitionLine = 236;
    matchesMeth179.definitionModule = "dialect";
    block179.methods["matches(1)"] = matchesMeth179;
    // call case 6: other requests
    if (var_cache === undefined) raiseUninitializedVariable("cache");
    var call178 = request(var_cache, "atKey(1)do(1)", [1, 1], var_node, block179);
    setLineNumber(237);    // compilenode string
    var string181 = new GraceString("cannot type non-expression ");
    var opresult182 = request(string181, "++(1)", [1], var_node);
    var string183 = new GraceString("");
    var opresult184 = request(opresult182, "++(1)", [1], string183);
    // call case 6: other requests
    if (var_DialectError === undefined) raiseUninitializedVariable("DialectError");
    var call180 = request(var_DialectError, "raise(1)with(1)", [1, 1], opresult184, var_node);
    return call180;
  };    // end of method typeOf(_)
  this.methods["typeOf(1)"] = func176;
  func176.methodName = "typeOf(1)";
  func176.paramCounts = [1];
  func176.paramNames = ["node"];
  func176.definitionLine = 234;
  func176.definitionModule = "dialect";
  var func185 = function(argcv, var_node) {    // method runRules(_), line 240
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("runRules(_)", 0, numArgs - 1);
    }
    setLineNumber(243);    // compilenode block
    var block187 = new GraceBlock(this, 243, 1);
    block187.guard = jsTrue;
    block187.real = function block187(var_value) {
      setLineNumber(243);    // compilenode return
      throw new ReturnException(var_value, returnTarget);
    };
    let applyMeth187 = function apply_1 (argcv, ...args) {
        if (this.guard.apply(this.receiver, args))
            return this.real.apply(this.receiver, args);
        badBlockArgs.apply(this, args);
    };
    applyMeth187.methodName = "apply(1)";
    applyMeth187.paramCounts = [1];
    applyMeth187.paramNames = ["value"];
    applyMeth187.definitionLine = 243;
    applyMeth187.definitionModule = "dialect";
    block187.methods["apply(1)"] = applyMeth187;
    let matchesMeth187 = function matches_1 (argcv, ...args) {
        return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
    };
    matchesMeth187.methodName = "matches(1)";
    matchesMeth187.paramCounts = [1];
    matchesMeth187.paramNames = ["value"];
    matchesMeth187.definitionLine = 243;
    matchesMeth187.definitionModule = "dialect";
    block187.methods["matches(1)"] = matchesMeth187;
    // call case 6: other requests
    if (var_cache === undefined) raiseUninitializedVariable("cache");
    var call186 = request(var_cache, "atKey(1)do(1)", [1, 1], var_node, block187);
    setLineNumber(244);    // compilenode member
    // call case 6: other requests
    var call188 = request(var_node, "line", [0]);
    var_currentLine = call188;
    setLineNumber(246);    // compilenode vardec
    var var_result = GraceFalse;
    setLineNumber(247);    // compilenode call
    if (var_rules === undefined) raiseUninitializedVariable("rules");
    var block190 = new GraceBlock(this, 247, 1);
    block190.guard = jsTrue;
    block190.real = function block190(var_each) {
      var if191 = GraceDone;
      setLineNumber(248);    // compilenode call
      // call case 6: other requests
      var call192 = request(var_each, "matches(1)", [1], var_node);
      if (Grace_isTrue(call192)) {
        setLineNumber(249);    // compilenode call
        // call case 6: other requests
        var call193 = request(var_each, "apply(1)", [1], var_node);
        var_result = call193;
        var if194 = GraceDone;
        setLineNumber(250);    // compilenode member
        // call case 6: other requests
        if (var_result === undefined) raiseUninitializedVariable("result");
        var call195 = request(var_result, "asString", [0]);
        var string196 = new GraceString("done");
        var opresult197 = request(call195, "==(1)", [1], string196);
        if (Grace_isTrue(opresult197)) {
          setLineNumber(252);    // compilenode string
          var string199 = new GraceString("rule.apply(node) has result 'done' when rule is ");
          var opresult200 = request(string199, "++(1)", [1], var_each);
          var string201 = new GraceString(" and node = ");
          var opresult202 = request(opresult200, "++(1)", [1], string201);
          var opresult203 = request(opresult202, "++(1)", [1], var_node);
          var string204 = new GraceString("");
          var opresult205 = request(opresult203, "++(1)", [1], string204);
          // call case 6: other requests
          setLineNumber(251);    // compilenode member
          // call case 2: outer request
          var call206 = selfRequest(var_prelude, "ProgrammingError", [0]);
          var call198 = request(call206, "raise(1)", [1], opresult205);
          if194 = call198;
        }
        setLineNumber(254);    // compilenode call
        if (var_result === undefined) raiseUninitializedVariable("result");
        // call case 6: other requests
        if (var_cache === undefined) raiseUninitializedVariable("cache");
        var call207 = request(var_cache, "at(1)put(1)", [1, 1], var_node, var_result);
        if191 = call207;
      }
      return if191;
    };
    let applyMeth190 = function apply_1 (argcv, ...args) {
        if (this.guard.apply(this.receiver, args))
            return this.real.apply(this.receiver, args);
        badBlockArgs.apply(this, args);
    };
    applyMeth190.methodName = "apply(1)";
    applyMeth190.paramCounts = [1];
    applyMeth190.paramNames = ["each"];
    applyMeth190.definitionLine = 247;
    applyMeth190.definitionModule = "dialect";
    block190.methods["apply(1)"] = applyMeth190;
    let matchesMeth190 = function matches_1 (argcv, ...args) {
        return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
    };
    matchesMeth190.methodName = "matches(1)";
    matchesMeth190.paramCounts = [1];
    matchesMeth190.paramNames = ["each"];
    matchesMeth190.definitionLine = 247;
    matchesMeth190.definitionModule = "dialect";
    block190.methods["matches(1)"] = matchesMeth190;
    // call case 2: outer request
    var call189 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], var_rules, block190);
    setLineNumber(257);    // compilenode return
    return var_result;
  };    // end of method runRules(_)
  this.methods["runRules(1)"] = func185;
  func185.methodName = "runRules(1)";
  func185.paramCounts = [1];
  func185.paramNames = ["node"];
  func185.definitionLine = 240;
  func185.definitionModule = "dialect";
  var func208 = function(argcv, var_module) {    // method check(_), line 264
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("check(_)", 0, numArgs - 1);
    }
    setLineNumber(266);    // compilenode call
    if (var_astVisitor === undefined) raiseUninitializedVariable("astVisitor");
    // call case 6: other requests
    var call209 = request(var_module, "accept(1)", [1], var_astVisitor);
    return call209;
  };    // end of method check(_)
  this.methods["check(1)"] = func208;
  func208.methodName = "check(1)";
  func208.paramCounts = [1];
  func208.paramNames = ["module"];
  func208.definitionLine = 264;
  func208.definitionModule = "dialect";
  setLineNumber(269);    // compilenode typedec
  // Type decl AstNode
  var func211 = function(argcv) {    // method AstNode, line 11
    var returnTarget = invocationCount;
    invocationCount++;
    setLineNumber(269);    // compilenode typeliteral
    //   Type literal 
    var typeLit212 = new GraceType("AstNode");
    typeLit212.typeMethods.push("kind");
    return typeLit212;
  };    // end of method AstNode
  function memofunc211(argcv) {
      if (! this.data["memo$AstNode"])    // parameterless memo function
          this.data["memo$AstNode"] = func211.call(this, argcv);
      return this.data["memo$AstNode"];
  };
  this.methods["AstNode"] = memofunc211;
  memofunc211.methodName = "AstNode";
  memofunc211.paramCounts = [0];
  memofunc211.paramNames = [];
  memofunc211.definitionLine = 11;
  memofunc211.definitionModule = "dialect";
  func211.methodName = "AstNode";
  func211.paramCounts = [0];
  func211.paramNames = [];
  func211.definitionLine = 11;
  func211.definitionModule = "dialect";
  var func213 = function(argcv, var_kind) {    // method aPatternMatchingNode(_), line 271
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("aPatternMatchingNode(_)", 0, numArgs - 1);
    }
    assertTypeOrMsg(var_kind, var_String, "argument to request of `aPatternMatchingNode(_)`", "String");
    var ouc = emptyGraceObject("aPatternMatchingNode(_)", "dialect", 271);
    var ouc_init = this.methods["aPatternMatchingNode(1)$build(3)"].call(this, null, var_kind, ouc, [], []);
    ouc_init.call(ouc);
    setLineNumber(271);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call214 = selfRequest(this, "Pattern", [0]);
    setLineNumber(278);    // typecheck
    assertTypeOrMsg(ouc, call214, "object returned from aPatternMatchingNode(_)", "Pattern");
    return ouc;
  };    // end of method aPatternMatchingNode(_)
  func213.paramTypes = [];
  func213.paramTypes.push([type_String, "kind"]);
  this.methods["aPatternMatchingNode(1)"] = func213;
  func213.methodName = "aPatternMatchingNode(1)";
  func213.paramCounts = [1];
  func213.paramNames = ["kind"];
  func213.definitionLine = 271;
  func213.definitionModule = "dialect";
  var func215 = function(argcv, var_kind, inheritingObject, aliases, exclusions) {    // method aPatternMatchingNode(_)$build(_,_,_), line 271
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("aPatternMatchingNode(_)", 0, numArgs - 1);
    }
    assertTypeOrMsg(var_kind, var_String, "argument to request of `aPatternMatchingNode(_)`", "String");
    var obj216_build = function(ignore, var_kind, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_dialect_271");
      this.outer_dialect_271 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(272);    // compilenode member
      // call case 2: outer request
      var call217 = selfRequest(importedModules["dialect"], "BasicPattern", [0]);
      var initFun218 = request(call217, "new$build(3)", [null], this, [], []);  // compileReuseCall
      var func219 = function(argcv, var_obj) {    // method matches(_), line 274
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("matches(_)", 0, numArgs - 1);
        }
        assertTypeOrMsg(var_obj, var_Object, "argument to request of `matches(_)`", "Object");
        setLineNumber(275);    // compilenode matchcase
        var cases220 = [];
        setLineNumber(276);    // compilenode block
        var block221 = new GraceBlock(this, 276, 1);
        // call case 2: outer request
        var call222 = selfRequest(importedModules["dialect"], "AstNode", [0]);
        block221.paramTypes = [call222];
        var matches223 = function(var_node) {
          // call case 2: outer request
          var call224 = selfRequest(importedModules["dialect"], "AstNode", [0]);
          if (!Grace_isTrue(request(call224, "matches(1)", [1], var_node)))
              return false;
          return true;
        };
        block221.guard = matches223;
        block221.real = function block221(var_node) {
          setLineNumber(276);    // compilenode member
          // call case 6: other requests
          var call225 = request(var_node, "kind", [0]);
          var opresult226 = request(var_kind, "==(1)", [1], call225);
          return opresult226;
        };
        let applyMeth221 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth221.methodName = "apply(1)";
        applyMeth221.paramCounts = [1];
        applyMeth221.paramNames = ["node"];
        applyMeth221.definitionLine = 276;
        applyMeth221.definitionModule = "dialect";
        block221.methods["apply(1)"] = applyMeth221;
        let matchesMeth221 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth221.methodName = "matches(1)";
        matchesMeth221.paramCounts = [1];
        matchesMeth221.paramNames = ["node"];
        matchesMeth221.definitionLine = 276;
        matchesMeth221.definitionModule = "dialect";
        block221.methods["matches(1)"] = matchesMeth221;
        cases220.push(block221);
        setLineNumber(277);    // compilenode block
        var block227 = new GraceBlock(this, 277, 0);
        block227.guard = jsTrue;
        block227.real = function block227() {
          setLineNumber(277);    // compileBlock
          return GraceFalse;
        };
        let applyMeth227 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth227.methodName = "apply";
        applyMeth227.paramCounts = [0];
        applyMeth227.paramNames = [];
        applyMeth227.definitionLine = 277;
        applyMeth227.definitionModule = "dialect";
        block227.methods["apply"] = applyMeth227;
        setLineNumber(275);    // compilematchcase
        var matchres220 = matchCase(var_obj,cases220,block227);
        assertTypeOrMsg(matchres220, var_Boolean, "result of method matches(_)", "Boolean");
        return matchres220;
      };    // end of method matches(_)
      func219.paramTypes = [];
      func219.paramTypes.push([type_Object, "obj"]);
      this.methods["matches(1)"] = func219;
      func219.methodName = "matches(1)";
      func219.paramCounts = [1];
      func219.paramNames = ["obj"];
      func219.definitionLine = 274;
      func219.definitionModule = "dialect";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 271;
          m.definitionModule = "dialect";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj216_init = function() {    // init of object on line 271
        initFun218.call(this);
      };
      return obj216_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj216_init = obj216_build.call(inheritingObject, null, var_kind, this, aliases, exclusions);
    return obj216_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method aPatternMatchingNode(_)$build(_,_,_)
  func215.paramTypes = [];
  func215.paramTypes.push([type_String, "kind"]);
  this.methods["aPatternMatchingNode(1)$build(3)"] = func215;
  func215.methodName = "aPatternMatchingNode(1)$build(3)";
  func215.paramCounts = [1];
  func215.paramNames = ["kind"];
  func215.definitionLine = 271;
  func215.definitionModule = "dialect";
  var func228 = function(argcv, var_methodName) {    // method RequestOf(_), line 313
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("RequestOf(_)", 0, numArgs - 1);
    }
    assertTypeOrMsg(var_methodName, var_String, "argument to request of `RequestOf(_)`", "String");
    var ouc = emptyGraceObject("RequestOf(_)", "dialect", 313);
    var ouc_init = this.methods["RequestOf(1)$build(3)"].call(this, null, var_methodName, ouc, [], []);
    ouc_init.call(ouc);
    setLineNumber(313);    // compilenode member
    // call case 4: self request with 0 args and 0 typeArgs 
    var call229 = selfRequest(this, "Pattern", [0]);
    setLineNumber(324);    // typecheck
    assertTypeOrMsg(ouc, call229, "object returned from RequestOf(_)", "Pattern");
    return ouc;
  };    // end of method RequestOf(_)
  func228.paramTypes = [];
  func228.paramTypes.push([type_String, "methodName"]);
  this.methods["RequestOf(1)"] = func228;
  func228.methodName = "RequestOf(1)";
  func228.paramCounts = [1];
  func228.paramNames = ["methodName"];
  func228.definitionLine = 313;
  func228.definitionModule = "dialect";
  var func230 = function(argcv, var_methodName, inheritingObject, aliases, exclusions) {    // method RequestOf(_)$build(_,_,_), line 313
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 4;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("RequestOf(_)", 0, numArgs - 1);
    }
    assertTypeOrMsg(var_methodName, var_String, "argument to request of `RequestOf(_)`", "String");
    var obj231_build = function(ignore, var_methodName, outerObj, aliases, exclusions) {
      this.closureKeys = this.closureKeys || [];
      this.closureKeys.push("outer_dialect_313");
      this.outer_dialect_313 = outerObj;
      const inheritedExclusions = { };
      for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
          const exMeth = exclusions[eix];
          inheritedExclusions[exMeth] = this.methods[exMeth]; };
      setLineNumber(315);    // compilenode member
      // call case 2: outer request
      var call232 = selfRequest(importedModules["dialect"], "BasicPattern", [0]);
      var initFun233 = request(call232, "new$build(3)", [null], this, [], []);  // compileReuseCall
      var func234 = function(argcv, var_obj) {    // method matches(_), line 317
        var returnTarget = invocationCount;
        invocationCount++;
        const numArgs = arguments.length - 1;
        if ((numArgs > 1) && (numArgs !== 1)) {
            raiseTypeArgError("matches(_)", 0, numArgs - 1);
        }
        assertTypeOrMsg(var_obj, var_Object, "argument to request of `matches(_)`", "Object");
        setLineNumber(318);    // compilenode matchcase
        var cases235 = [];
        setLineNumber(319);    // compilenode block
        var block236 = new GraceBlock(this, 319, 1);
        // call case 2: outer request
        var call237 = selfRequest(importedModules["dialect"], "AstNode", [0]);
        block236.paramTypes = [call237];
        var matches238 = function(var_node) {
          // call case 2: outer request
          var call239 = selfRequest(importedModules["dialect"], "AstNode", [0]);
          if (!Grace_isTrue(request(call239, "matches(1)", [1], var_node)))
              return false;
          return true;
        };
        block236.guard = matches238;
        block236.real = function block236(var_node) {
          setLineNumber(320);    // compilenode member
          // call case 6: other requests
          var call240 = request(var_node, "isCall", [0]);
          var block241 = new GraceBlock(this, 320, 0);
          block241.guard = jsTrue;
          block241.real = function block241() {
            setLineNumber(320);    // compilenode member
            // call case 6: other requests
            var call242 = request(var_node, "canonicalName", [0]);
            var opresult243 = request(call242, "==(1)", [1], var_methodName);
            return opresult243;
          };
          let applyMeth241 = function apply (argcv) {
              return this.real.apply(this.receiver);
          };
          applyMeth241.methodName = "apply";
          applyMeth241.paramCounts = [0];
          applyMeth241.paramNames = [];
          applyMeth241.definitionLine = 320;
          applyMeth241.definitionModule = "dialect";
          block241.methods["apply"] = applyMeth241;
          var opresult244 = request(call240, "&&(1)", [1], block241);
          return opresult244;
        };
        let applyMeth236 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth236.methodName = "apply(1)";
        applyMeth236.paramCounts = [1];
        applyMeth236.paramNames = ["node"];
        applyMeth236.definitionLine = 319;
        applyMeth236.definitionModule = "dialect";
        block236.methods["apply(1)"] = applyMeth236;
        let matchesMeth236 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth236.methodName = "matches(1)";
        matchesMeth236.paramCounts = [1];
        matchesMeth236.paramNames = ["node"];
        matchesMeth236.definitionLine = 319;
        matchesMeth236.definitionModule = "dialect";
        block236.methods["matches(1)"] = matchesMeth236;
        cases235.push(block236);
        setLineNumber(321);    // compilenode block
        var block245 = new GraceBlock(this, 321, 0);
        block245.guard = jsTrue;
        block245.real = function block245() {
          setLineNumber(321);    // compileBlock
          return GraceFalse;
        };
        let applyMeth245 = function apply (argcv) {
            return this.real.apply(this.receiver);
        };
        applyMeth245.methodName = "apply";
        applyMeth245.paramCounts = [0];
        applyMeth245.paramNames = [];
        applyMeth245.definitionLine = 321;
        applyMeth245.definitionModule = "dialect";
        block245.methods["apply"] = applyMeth245;
        setLineNumber(318);    // compilematchcase
        var matchres235 = matchCase(var_obj,cases235,block245);
        assertTypeOrMsg(matchres235, var_Boolean, "result of method matches(_)", "Boolean");
        return matchres235;
      };    // end of method matches(_)
      func234.paramTypes = [];
      func234.paramTypes.push([type_Object, "obj"]);
      this.methods["matches(1)"] = func234;
      func234.methodName = "matches(1)";
      func234.paramCounts = [1];
      func234.paramNames = ["obj"];
      func234.definitionLine = 317;
      func234.definitionModule = "dialect";
      const overridenByAliases = { };
      for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
          const a = aliases[aix];
          const newNm = a.newName;
          const oldNm = a.oldName;
          overridenByAliases[newNm] = this.methods[newNm];
          const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
          m.definitionLine = 313;
          m.definitionModule = "dialect";
          this.methods[newNm] = m;
      }
      for (let exName in inheritedExclusions) {
          if (inheritedExclusions.hasOwnProperty(exName)) {
              if (inheritedExclusions[exName]) {
                  this.methods[exName] = inheritedExclusions[exName];
              } else {
                  delete this.methods[exName];
              }
          }
      }
      var obj231_init = function() {    // init of object on line 313
        initFun233.call(this);
      };
      return obj231_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
    };
    var obj231_init = obj231_build.call(inheritingObject, null, var_methodName, this, aliases, exclusions);
    return obj231_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
  };    // end of method RequestOf(_)$build(_,_,_)
  func230.paramTypes = [];
  func230.paramTypes.push([type_String, "methodName"]);
  this.methods["RequestOf(1)$build(3)"] = func230;
  func230.methodName = "RequestOf(1)$build(3)";
  func230.paramCounts = [1];
  func230.paramNames = ["methodName"];
  func230.definitionLine = 313;
  func230.definitionModule = "dialect";
  var func246 = function(argcv, var_node) {    // method whileCond(_), line 330
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("whileCond(_)", 0, numArgs - 1);
    }
    setLineNumber(333);    // compilenode member
    // call case 6: other requests
    var call247 = request(var_node, "parts", [0]);
    var var_sig = call247;
    setLineNumber(334);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    // call case 6: other requests
    var call250 = request(var_sig, "first", [0]);
    var call249 = request(call250, "args", [0]);
    var call248 = request(call249, "first", [0]);
    return call248;
  };    // end of method whileCond(_)
  this.methods["whileCond(1)"] = func246;
  func246.methodName = "whileCond(1)";
  func246.paramCounts = [1];
  func246.paramNames = ["node"];
  func246.definitionLine = 330;
  func246.definitionModule = "dialect";
  var func251 = function(argcv, var_node) {    // method whileBody(_), line 337
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("whileBody(_)", 0, numArgs - 1);
    }
    setLineNumber(340);    // compilenode member
    // call case 6: other requests
    var call252 = request(var_node, "parts", [0]);
    var var_sig = call252;
    setLineNumber(341);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    // call case 6: other requests
    var call255 = request(var_sig, "second", [0]);
    var call254 = request(call255, "args", [0]);
    var call253 = request(call254, "first", [0]);
    return call253;
  };    // end of method whileBody(_)
  this.methods["whileBody(1)"] = func251;
  func251.methodName = "whileBody(1)";
  func251.paramCounts = [1];
  func251.paramNames = ["node"];
  func251.definitionLine = 337;
  func251.definitionModule = "dialect";
  var func256 = function(argcv, var_node) {    // method forCollection(_), line 344
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("forCollection(_)", 0, numArgs - 1);
    }
    setLineNumber(347);    // compilenode member
    // call case 6: other requests
    var call257 = request(var_node, "parts", [0]);
    var var_sig = call257;
    setLineNumber(348);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    // call case 6: other requests
    var call260 = request(var_sig, "first", [0]);
    var call259 = request(call260, "args", [0]);
    var call258 = request(call259, "first", [0]);
    return call258;
  };    // end of method forCollection(_)
  this.methods["forCollection(1)"] = func256;
  func256.methodName = "forCollection(1)";
  func256.paramCounts = [1];
  func256.paramNames = ["node"];
  func256.definitionLine = 344;
  func256.definitionModule = "dialect";
  var func261 = function(argcv, var_node) {    // method forBody(_), line 351
    var returnTarget = invocationCount;
    invocationCount++;
    const numArgs = arguments.length - 1;
    if ((numArgs > 1) && (numArgs !== 1)) {
        raiseTypeArgError("forBody(_)", 0, numArgs - 1);
    }
    setLineNumber(354);    // compilenode member
    // call case 6: other requests
    var call262 = request(var_node, "parts", [0]);
    var var_sig = call262;
    setLineNumber(355);    // compilenode member
    // call case 6: other requests
    // call case 6: other requests
    // call case 6: other requests
    var call265 = request(var_sig, "second", [0]);
    var call264 = request(call265, "args", [0]);
    var call263 = request(call264, "first", [0]);
    return call263;
  };    // end of method forBody(_)
  this.methods["forBody(1)"] = func261;
  func261.methodName = "forBody(1)";
  func261.paramCounts = [1];
  func261.paramNames = ["node"];
  func261.definitionLine = 351;
  func261.definitionModule = "dialect";
  initFun3.call(this);
  setLineNumber(16);    // compilenode string
  var string267 = new GraceString("CheckerFailure");
  // call case 6: other requests
  // call case 2: outer request
  var call268 = selfRequest(var_prelude, "Exception", [0]);
  var call266 = request(call268, "refine(1)", [1], string267);
  var var_CheckerFailure = call266;
  var reader269_CheckerFailure = function() {  // reader method CheckerFailure
      if (var_CheckerFailure === undefined) raiseUninitializedVariable("CheckerFailure");
      return var_CheckerFailure;
  };
  reader269_CheckerFailure.isDef = true;
  this.methods["CheckerFailure"] = reader269_CheckerFailure;
  setLineNumber(17);    // compilenode string
  var string271 = new GraceString("DialectError");
  // call case 6: other requests
  // call case 2: outer request
  var call272 = selfRequest(var_prelude, "Exception", [0]);
  var call270 = request(call272, "refine(1)", [1], string271);
  var var_DialectError = call270;
  var reader273_DialectError = function() {  // reader method DialectError
      if (var_DialectError === undefined) raiseUninitializedVariable("DialectError");
      return var_DialectError;
  };
  reader273_DialectError.isDef = true;
  this.methods["DialectError"] = reader273_DialectError;
  setLineNumber(112);    // compilenode array
  var array275 = new GraceSequence([]);
  // call case 2: outer request
  var call274 = selfRequest(importedModules["dialect"], "list(1)", [1], array275);
  var var_rules = call274;
  var reader276_rules = function() {  // reader method rules
      if (var_rules === undefined) raiseUninitializedVariable("rules");
      return var_rules;
  };
  reader276_rules.isDef = true;
  reader276_rules.confidential = true;
  this.methods["rules"] = reader276_rules;
  setLineNumber(115);    // compilenode member
  // call case 4: self request with 0 args and 0 typeArgs 
  var call277 = selfRequest(this, "aMutableMap", [0]);
  var var_cache = call277;
  var reader278_cache = function() {  // reader method cache
      if (var_cache === undefined) raiseUninitializedVariable("cache");
      return var_cache;
  };
  reader278_cache.isDef = true;
  reader278_cache.confidential = true;
  this.methods["cache"] = reader278_cache;
  setLineNumber(123);    // compilenode num
  var var_currentLine = new GraceNum(0);
  var reader279_currentLine = function() {  // reader method currentLine
      if (var_currentLine === undefined) raiseUninitializedVariable("currentLine");
      return var_currentLine;
  };
  reader279_currentLine.isVar = true;
  reader279_currentLine.confidential = true;
  this.methods["currentLine"] = reader279_currentLine;
  var writer280_currentLine = function(argcv, n) {   // writer method currentLine:=(_)
    var_currentLine = n;
    return GraceDone;
  };
  writer280_currentLine.confidential = true;
  this.methods["currentLine:=(1)"] = writer280_currentLine;
  setLineNumber(202);    // compilenode object
  var obj281_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_dialect_202");
    this.outer_dialect_202 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    this.data.variables = undefined;
    var reader282_variables = function() {  // reader method variables
        if (this.data.variables === undefined) raiseUninitializedVariable("variables");
        return this.data.variables;
    };
    reader282_variables.isDef = true;
    this.methods["variables"] = reader282_variables;
    this.data.methods = undefined;
    var reader283_methods = function() {  // reader method methods
        if (this.data.methods === undefined) raiseUninitializedVariable("methods");
        return this.data.methods;
    };
    reader283_methods.isDef = true;
    this.methods["methods"] = reader283_methods;
    this.data.types = undefined;
    var reader284_types = function() {  // reader method types
        if (this.data.types === undefined) raiseUninitializedVariable("types");
        return this.data.types;
    };
    reader284_types.isDef = true;
    this.methods["types"] = reader284_types;
    var func285 = function(argcv) {    // method size, line 207
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 0) && (numArgs !== 0)) {
          raiseTypeArgError("size", 0, numArgs - 0);
      }
      setLineNumber(208);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      // call case 4: self request with 0 args and 0 typeArgs 
      var call288 = selfRequest(this, "variables", [0]);
      var call287 = request(call288, "stack", [0]);
      var call286 = request(call287, "size", [0]);
      assertTypeOrMsg(call286, var_Number, "result of method size", "Number");
      return call286;
    };    // end of method size
    this.methods["size"] = func285;
    func285.methodName = "size";
    func285.paramCounts = [0];
    func285.paramNames = [];
    func285.definitionLine = 207;
    func285.definitionModule = "dialect";
    var func289 = function(argcv, var_bl) {    // method enter(_), line 211
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("enter(_)", 0, numArgs - 1);
      }
      setLineNumber(212);    // compilenode member
      // call case 2: outer request
      var call291 = selfRequest(importedModules["dialect"], "aMutableMap", [0]);
      // call case 6: other requests
      // call case 6: other requests
      // call case 4: self request with 0 args and 0 typeArgs 
      var call293 = selfRequest(this, "variables", [0]);
      var call292 = request(call293, "stack", [0]);
      var call290 = request(call292, "push(1)", [1], call291);
      setLineNumber(213);    // compilenode member
      // call case 2: outer request
      var call295 = selfRequest(importedModules["dialect"], "aMutableMap", [0]);
      // call case 6: other requests
      // call case 6: other requests
      // call case 4: self request with 0 args and 0 typeArgs 
      var call297 = selfRequest(this, "methods", [0]);
      var call296 = request(call297, "stack", [0]);
      var call294 = request(call296, "push(1)", [1], call295);
      setLineNumber(214);    // compilenode member
      // call case 2: outer request
      var call299 = selfRequest(importedModules["dialect"], "aMutableMap", [0]);
      // call case 6: other requests
      // call case 6: other requests
      // call case 4: self request with 0 args and 0 typeArgs 
      var call301 = selfRequest(this, "types", [0]);
      var call300 = request(call301, "stack", [0]);
      var call298 = request(call300, "push(1)", [1], call299);
      setLineNumber(216);    // compilenode member
      // call case 6: other requests
      var call302 = request(var_bl, "apply", [0]);
      var var_result = call302;
      setLineNumber(218);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      // call case 4: self request with 0 args and 0 typeArgs 
      var call305 = selfRequest(this, "variables", [0]);
      var call304 = request(call305, "stack", [0]);
      var call303 = request(call304, "pop", [0]);
      setLineNumber(219);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      // call case 4: self request with 0 args and 0 typeArgs 
      var call308 = selfRequest(this, "methods", [0]);
      var call307 = request(call308, "stack", [0]);
      var call306 = request(call307, "pop", [0]);
      setLineNumber(220);    // compilenode member
      // call case 6: other requests
      // call case 6: other requests
      // call case 4: self request with 0 args and 0 typeArgs 
      var call311 = selfRequest(this, "types", [0]);
      var call310 = request(call311, "stack", [0]);
      var call309 = request(call310, "pop", [0]);
      setLineNumber(222);    // compilenode return
      return var_result;
    };    // end of method enter(_)
    this.methods["enter(1)"] = func289;
    func289.methodName = "enter(1)";
    func289.paramCounts = [1];
    func289.paramNames = ["bl"];
    func289.definitionLine = 211;
    func289.definitionModule = "dialect";
    var func312 = function(argcv) {    // method asString, line 225
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 0) && (numArgs !== 0)) {
          raiseTypeArgError("asString", 0, numArgs - 0);
      }
      setLineNumber(226);    // compilenode string
      var string313 = new GraceString("scope<");
      // call case 4: self request with 0 args and 0 typeArgs 
      var call314 = selfRequest(this, "size", [0]);
      var opresult315 = request(string313, "++(1)", [1], call314);
      var string316 = new GraceString(">");
      var opresult317 = request(opresult315, "++(1)", [1], string316);
      assertTypeOrMsg(opresult317, var_String, "result of method asString", "String");
      return opresult317;
    };    // end of method asString
    this.methods["asString"] = func312;
    func312.methodName = "asString";
    func312.paramCounts = [0];
    func312.paramNames = [];
    func312.definitionLine = 225;
    func312.definitionModule = "dialect";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 202;
        m.definitionModule = "dialect";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj281_init = function() {    // init of object on line 202
      setLineNumber(203);    // compilenode string
      var string319 = new GraceString("variable");
      // call case 2: outer request
      var call318 = selfRequest(importedModules["dialect"], "stackOfKind(1)", [1], string319);
      this.data.variables = call318;
      setLineNumber(204);    // compilenode string
      var string321 = new GraceString("method");
      // call case 2: outer request
      var call320 = selfRequest(importedModules["dialect"], "stackOfKind(1)", [1], string321);
      this.data.methods = call320;
      setLineNumber(205);    // compilenode string
      var string323 = new GraceString("type");
      // call case 2: outer request
      var call322 = selfRequest(importedModules["dialect"], "stackOfKind(1)", [1], string323);
      this.data.types = call322;
    };
    return obj281_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj281 = emptyGraceObject("scope", "dialect", 202);
  var obj281_init = obj281_build.call(obj281, null, this, [], []);
  obj281_init.call(obj281);  // end of compileobject
  var var_scope = obj281;
  var reader324_scope = function() {  // reader method scope
      if (var_scope === undefined) raiseUninitializedVariable("scope");
      return var_scope;
  };
  reader324_scope.isDef = true;
  this.methods["scope"] = reader324_scope;
  setLineNumber(281);    // compilenode string
  var string326 = new GraceString("if");
  // call case 2: outer request
  var call325 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string326);
  var var_If = call325;
  var reader327_If = function() {  // reader method If
      if (var_If === undefined) raiseUninitializedVariable("If");
      return var_If;
  };
  reader327_If.isDef = true;
  this.methods["If"] = reader327_If;
  setLineNumber(282);    // compilenode string
  var string329 = new GraceString("block");
  // call case 2: outer request
  var call328 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string329);
  var var_BlockLiteral = call328;
  var reader330_BlockLiteral = function() {  // reader method BlockLiteral
      if (var_BlockLiteral === undefined) raiseUninitializedVariable("BlockLiteral");
      return var_BlockLiteral;
  };
  reader330_BlockLiteral.isDef = true;
  this.methods["BlockLiteral"] = reader330_BlockLiteral;
  setLineNumber(283);    // compilenode string
  var string332 = new GraceString("matchcase");
  // call case 2: outer request
  var call331 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string332);
  var var_MatchCase = call331;
  var reader333_MatchCase = function() {  // reader method MatchCase
      if (var_MatchCase === undefined) raiseUninitializedVariable("MatchCase");
      return var_MatchCase;
  };
  reader333_MatchCase.isDef = true;
  this.methods["MatchCase"] = reader333_MatchCase;
  setLineNumber(284);    // compilenode string
  var string335 = new GraceString("trycatch");
  // call case 2: outer request
  var call334 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string335);
  var var_TryCatch = call334;
  var reader336_TryCatch = function() {  // reader method TryCatch
      if (var_TryCatch === undefined) raiseUninitializedVariable("TryCatch");
      return var_TryCatch;
  };
  reader336_TryCatch.isDef = true;
  this.methods["TryCatch"] = reader336_TryCatch;
  setLineNumber(285);    // compilenode string
  var string338 = new GraceString("outer");
  // call case 2: outer request
  var call337 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string338);
  var var_Outer = call337;
  var reader339_Outer = function() {  // reader method Outer
      if (var_Outer === undefined) raiseUninitializedVariable("Outer");
      return var_Outer;
  };
  reader339_Outer.isDef = true;
  this.methods["Outer"] = reader339_Outer;
  setLineNumber(286);    // compilenode string
  var string341 = new GraceString("methodtype");
  // call case 2: outer request
  var call340 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string341);
  var var_MethodSignature = call340;
  var reader342_MethodSignature = function() {  // reader method MethodSignature
      if (var_MethodSignature === undefined) raiseUninitializedVariable("MethodSignature");
      return var_MethodSignature;
  };
  reader342_MethodSignature.isDef = true;
  this.methods["MethodSignature"] = reader342_MethodSignature;
  setLineNumber(287);    // compilenode string
  var string344 = new GraceString("typeliteral");
  // call case 2: outer request
  var call343 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string344);
  var var_TypeLiteral = call343;
  var reader345_TypeLiteral = function() {  // reader method TypeLiteral
      if (var_TypeLiteral === undefined) raiseUninitializedVariable("TypeLiteral");
      return var_TypeLiteral;
  };
  reader345_TypeLiteral.isDef = true;
  this.methods["TypeLiteral"] = reader345_TypeLiteral;
  setLineNumber(288);    // compilenode string
  var string347 = new GraceString("typedec");
  // call case 2: outer request
  var call346 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string347);
  var var_TypeDeclaration = call346;
  var reader348_TypeDeclaration = function() {  // reader method TypeDeclaration
      if (var_TypeDeclaration === undefined) raiseUninitializedVariable("TypeDeclaration");
      return var_TypeDeclaration;
  };
  reader348_TypeDeclaration.isDef = true;
  this.methods["TypeDeclaration"] = reader348_TypeDeclaration;
  setLineNumber(289);    // compilenode string
  var string350 = new GraceString("dtype");
  // call case 2: outer request
  var call349 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string350);
  var var_TypeAnnotation = call349;
  var reader351_TypeAnnotation = function() {  // reader method TypeAnnotation
      if (var_TypeAnnotation === undefined) raiseUninitializedVariable("TypeAnnotation");
      return var_TypeAnnotation;
  };
  reader351_TypeAnnotation.isDef = true;
  this.methods["TypeAnnotation"] = reader351_TypeAnnotation;
  setLineNumber(290);    // compilenode string
  var string353 = new GraceString("method");
  // call case 2: outer request
  var call352 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string353);
  var var_Method = call352;
  var reader354_Method = function() {  // reader method Method
      if (var_Method === undefined) raiseUninitializedVariable("Method");
      return var_Method;
  };
  reader354_Method.isDef = true;
  this.methods["Method"] = reader354_Method;
  setLineNumber(291);    // compilenode string
  var string356 = new GraceString("parameter");
  // call case 2: outer request
  var call355 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string356);
  var var_Parameter = call355;
  var reader357_Parameter = function() {  // reader method Parameter
      if (var_Parameter === undefined) raiseUninitializedVariable("Parameter");
      return var_Parameter;
  };
  reader357_Parameter.isDef = true;
  this.methods["Parameter"] = reader357_Parameter;
  setLineNumber(292);    // compilenode string
  var string359 = new GraceString("call");
  // call case 2: outer request
  var call358 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string359);
  var var_Request = call358;
  var reader360_Request = function() {  // reader method Request
      if (var_Request === undefined) raiseUninitializedVariable("Request");
      return var_Request;
  };
  reader360_Request.isDef = true;
  this.methods["Request"] = reader360_Request;
  setLineNumber(293);    // compilenode string
  var string362 = new GraceString("class");
  // call case 2: outer request
  var call361 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string362);
  var var_Class = call361;
  var reader363_Class = function() {  // reader method Class
      if (var_Class === undefined) raiseUninitializedVariable("Class");
      return var_Class;
  };
  reader363_Class.isDef = true;
  this.methods["Class"] = reader363_Class;
  setLineNumber(294);    // compilenode string
  var string365 = new GraceString("object");
  // call case 2: outer request
  var call364 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string365);
  var var_ObjectLiteral = call364;
  var reader366_ObjectLiteral = function() {  // reader method ObjectLiteral
      if (var_ObjectLiteral === undefined) raiseUninitializedVariable("ObjectLiteral");
      return var_ObjectLiteral;
  };
  reader366_ObjectLiteral.isDef = true;
  this.methods["ObjectLiteral"] = reader366_ObjectLiteral;
  setLineNumber(295);    // compilenode string
  var string368 = new GraceString("array");
  // call case 2: outer request
  var call367 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string368);
  var var_ArrayLiteral = call367;
  var reader369_ArrayLiteral = function() {  // reader method ArrayLiteral
      if (var_ArrayLiteral === undefined) raiseUninitializedVariable("ArrayLiteral");
      return var_ArrayLiteral;
  };
  reader369_ArrayLiteral.isDef = true;
  this.methods["ArrayLiteral"] = reader369_ArrayLiteral;
  setLineNumber(296);    // compilenode string
  var string371 = new GraceString("member");
  // call case 2: outer request
  var call370 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string371);
  var var_Member = call370;
  var reader372_Member = function() {  // reader method Member
      if (var_Member === undefined) raiseUninitializedVariable("Member");
      return var_Member;
  };
  reader372_Member.isDef = true;
  this.methods["Member"] = reader372_Member;
  setLineNumber(297);    // compilenode string
  var string374 = new GraceString("generic");
  // call case 2: outer request
  var call373 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string374);
  var var_Generic = call373;
  var reader375_Generic = function() {  // reader method Generic
      if (var_Generic === undefined) raiseUninitializedVariable("Generic");
      return var_Generic;
  };
  reader375_Generic.isDef = true;
  this.methods["Generic"] = reader375_Generic;
  setLineNumber(298);    // compilenode string
  var string377 = new GraceString("identifier");
  // call case 2: outer request
  var call376 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string377);
  var var_Identifier = call376;
  var reader378_Identifier = function() {  // reader method Identifier
      if (var_Identifier === undefined) raiseUninitializedVariable("Identifier");
      return var_Identifier;
  };
  reader378_Identifier.isDef = true;
  this.methods["Identifier"] = reader378_Identifier;
  setLineNumber(299);    // compilenode string
  var string380 = new GraceString("octets");
  // call case 2: outer request
  var call379 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string380);
  var var_OctetsLiteral = call379;
  var reader381_OctetsLiteral = function() {  // reader method OctetsLiteral
      if (var_OctetsLiteral === undefined) raiseUninitializedVariable("OctetsLiteral");
      return var_OctetsLiteral;
  };
  reader381_OctetsLiteral.isDef = true;
  this.methods["OctetsLiteral"] = reader381_OctetsLiteral;
  setLineNumber(300);    // compilenode string
  var string383 = new GraceString("string");
  // call case 2: outer request
  var call382 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string383);
  var var_StringLiteral = call382;
  var reader384_StringLiteral = function() {  // reader method StringLiteral
      if (var_StringLiteral === undefined) raiseUninitializedVariable("StringLiteral");
      return var_StringLiteral;
  };
  reader384_StringLiteral.isDef = true;
  this.methods["StringLiteral"] = reader384_StringLiteral;
  setLineNumber(301);    // compilenode string
  var string386 = new GraceString("num");
  // call case 2: outer request
  var call385 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string386);
  var var_NumberLiteral = call385;
  var reader387_NumberLiteral = function() {  // reader method NumberLiteral
      if (var_NumberLiteral === undefined) raiseUninitializedVariable("NumberLiteral");
      return var_NumberLiteral;
  };
  reader387_NumberLiteral.isDef = true;
  this.methods["NumberLiteral"] = reader387_NumberLiteral;
  setLineNumber(302);    // compilenode string
  var string389 = new GraceString("op");
  // call case 2: outer request
  var call388 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string389);
  var var_Operator = call388;
  var reader390_Operator = function() {  // reader method Operator
      if (var_Operator === undefined) raiseUninitializedVariable("Operator");
      return var_Operator;
  };
  reader390_Operator.isDef = true;
  this.methods["Operator"] = reader390_Operator;
  setLineNumber(303);    // compilenode string
  var string392 = new GraceString("bind");
  // call case 2: outer request
  var call391 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string392);
  var var_Bind = call391;
  var reader393_Bind = function() {  // reader method Bind
      if (var_Bind === undefined) raiseUninitializedVariable("Bind");
      return var_Bind;
  };
  reader393_Bind.isDef = true;
  this.methods["Bind"] = reader393_Bind;
  setLineNumber(304);    // compilenode string
  var string395 = new GraceString("defdec");
  // call case 2: outer request
  var call394 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string395);
  var var_Def = call394;
  var reader396_Def = function() {  // reader method Def
      if (var_Def === undefined) raiseUninitializedVariable("Def");
      return var_Def;
  };
  reader396_Def.isDef = true;
  this.methods["Def"] = reader396_Def;
  setLineNumber(305);    // compilenode string
  var string398 = new GraceString("vardec");
  // call case 2: outer request
  var call397 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string398);
  var var_Var = call397;
  var reader399_Var = function() {  // reader method Var
      if (var_Var === undefined) raiseUninitializedVariable("Var");
      return var_Var;
  };
  reader399_Var.isDef = true;
  this.methods["Var"] = reader399_Var;
  setLineNumber(306);    // compilenode string
  var string401 = new GraceString("import");
  // call case 2: outer request
  var call400 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string401);
  var var_Import = call400;
  var reader402_Import = function() {  // reader method Import
      if (var_Import === undefined) raiseUninitializedVariable("Import");
      return var_Import;
  };
  reader402_Import.isDef = true;
  this.methods["Import"] = reader402_Import;
  setLineNumber(307);    // compilenode string
  var string404 = new GraceString("dialect");
  // call case 2: outer request
  var call403 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string404);
  var var_Dialect = call403;
  var reader405_Dialect = function() {  // reader method Dialect
      if (var_Dialect === undefined) raiseUninitializedVariable("Dialect");
      return var_Dialect;
  };
  reader405_Dialect.isDef = true;
  this.methods["Dialect"] = reader405_Dialect;
  setLineNumber(308);    // compilenode string
  var string407 = new GraceString("return");
  // call case 2: outer request
  var call406 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string407);
  var var_Return = call406;
  var reader408_Return = function() {  // reader method Return
      if (var_Return === undefined) raiseUninitializedVariable("Return");
      return var_Return;
  };
  reader408_Return.isDef = true;
  this.methods["Return"] = reader408_Return;
  setLineNumber(309);    // compilenode string
  var string410 = new GraceString("inherit");
  // call case 2: outer request
  var call409 = selfRequest(importedModules["dialect"], "aPatternMatchingNode(1)", [1], string410);
  var var_Inherit = call409;
  var reader411_Inherit = function() {  // reader method Inherit
      if (var_Inherit === undefined) raiseUninitializedVariable("Inherit");
      return var_Inherit;
  };
  reader411_Inherit.isDef = true;
  this.methods["Inherit"] = reader411_Inherit;
  setLineNumber(327);    // compilenode string
  var string413 = new GraceString("while(_)do(_)");
  // call case 2: outer request
  var call412 = selfRequest(importedModules["dialect"], "RequestOf(1)", [1], string413);
  var var_WhileRequest = call412;
  var reader414_WhileRequest = function() {  // reader method WhileRequest
      if (var_WhileRequest === undefined) raiseUninitializedVariable("WhileRequest");
      return var_WhileRequest;
  };
  reader414_WhileRequest.isDef = true;
  this.methods["WhileRequest"] = reader414_WhileRequest;
  setLineNumber(328);    // compilenode string
  var string416 = new GraceString("for(_)do(_)");
  // call case 2: outer request
  var call415 = selfRequest(importedModules["dialect"], "RequestOf(1)", [1], string416);
  var var_ForRequest = call415;
  var reader417_ForRequest = function() {  // reader method ForRequest
      if (var_ForRequest === undefined) raiseUninitializedVariable("ForRequest");
      return var_ForRequest;
  };
  reader417_ForRequest.isDef = true;
  this.methods["ForRequest"] = reader417_ForRequest;
  setLineNumber(358);    // compilenode object
  var obj418_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_dialect_358");
    this.outer_dialect_358 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    setLineNumber(359);    // reuse call
    var initFun419 = request(var_ast, "baseVisitor$build(3)", [null], this, [], []);  // compileReuseCall
    var func420 = function(argcv, var_node) {    // method checkMatch(_), line 361
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("checkMatch(_)", 0, numArgs - 1);
      }
      setLineNumber(362);    // compilenode call
      // call case 2: outer request
      var call421 = selfRequest(importedModules["dialect"], "runRules(1)", [1], var_node);
      setLineNumber(363);    // typecheck
      assertTypeOrMsg(GraceTrue, var_Boolean, "return value", "Boolean");
      return GraceTrue;
    };    // end of method checkMatch(_)
    this.methods["checkMatch(1)"] = func420;
    func420.methodName = "checkMatch(1)";
    func420.paramCounts = [1];
    func420.paramNames = ["node"];
    func420.definitionLine = 361;
    func420.definitionModule = "dialect";
    var func422 = function(argcv, var_node) {    // method visitIf(_), line 366
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitIf(_)", 0, numArgs - 1);
      }
      setLineNumber(367);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call423 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call423, var_Boolean, "result of method visitIf(_)", "Boolean");
      return call423;
    };    // end of method visitIf(_)
    this.methods["visitIf(1)"] = func422;
    func422.methodName = "visitIf(1)";
    func422.paramCounts = [1];
    func422.paramNames = ["node"];
    func422.definitionLine = 366;
    func422.definitionModule = "dialect";
    var func424 = function(argcv, var_node) {    // method visitBlock(_), line 370
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitBlock(_)", 0, numArgs - 1);
      }
      setLineNumber(371);    // compilenode call
      // call case 2: outer request
      var call425 = selfRequest(importedModules["dialect"], "runRules(1)", [1], var_node);
      setLineNumber(373);    // compilenode member
      // call case 6: other requests
      var call427 = request(var_node, "params", [0]);
      var block428 = new GraceBlock(this, 373, 1);
      block428.guard = jsTrue;
      block428.real = function block428(var_param) {
        setLineNumber(374);    // compilenode call
        // call case 6: other requests
        if (var_aParameter === undefined) raiseUninitializedVariable("aParameter");
        var call430 = request(var_aParameter, "fromNode(1)", [1], var_param);
        // call case 2: outer request
        var call429 = selfRequest(importedModules["dialect"], "runRules(1)", [1], call430);
        return call429;
      };
      let applyMeth428 = function apply_1 (argcv, ...args) {
          if (this.guard.apply(this.receiver, args))
              return this.real.apply(this.receiver, args);
          badBlockArgs.apply(this, args);
      };
      applyMeth428.methodName = "apply(1)";
      applyMeth428.paramCounts = [1];
      applyMeth428.paramNames = ["param"];
      applyMeth428.definitionLine = 373;
      applyMeth428.definitionModule = "dialect";
      block428.methods["apply(1)"] = applyMeth428;
      let matchesMeth428 = function matches_1 (argcv, ...args) {
          return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
      };
      matchesMeth428.methodName = "matches(1)";
      matchesMeth428.paramCounts = [1];
      matchesMeth428.paramNames = ["param"];
      matchesMeth428.definitionLine = 373;
      matchesMeth428.definitionModule = "dialect";
      block428.methods["matches(1)"] = matchesMeth428;
      // call case 2: outer request
      var call426 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call427, block428);
      setLineNumber(377);    // compilenode member
      // call case 6: other requests
      var call432 = request(var_node, "body", [0]);
      var block433 = new GraceBlock(this, 377, 1);
      block433.guard = jsTrue;
      block433.real = function block433(var_stmt) {
        setLineNumber(378);    // compilenode call
        // call case 6: other requests
        var call434 = request(var_stmt, "accept(1)", [1], this);
        return call434;
      };
      let applyMeth433 = function apply_1 (argcv, ...args) {
          if (this.guard.apply(this.receiver, args))
              return this.real.apply(this.receiver, args);
          badBlockArgs.apply(this, args);
      };
      applyMeth433.methodName = "apply(1)";
      applyMeth433.paramCounts = [1];
      applyMeth433.paramNames = ["stmt"];
      applyMeth433.definitionLine = 377;
      applyMeth433.definitionModule = "dialect";
      block433.methods["apply(1)"] = applyMeth433;
      let matchesMeth433 = function matches_1 (argcv, ...args) {
          return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
      };
      matchesMeth433.methodName = "matches(1)";
      matchesMeth433.paramCounts = [1];
      matchesMeth433.paramNames = ["stmt"];
      matchesMeth433.definitionLine = 377;
      matchesMeth433.definitionModule = "dialect";
      block433.methods["matches(1)"] = matchesMeth433;
      // call case 2: outer request
      var call431 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call432, block433);
      setLineNumber(381);    // typecheck
      assertTypeOrMsg(GraceFalse, var_Boolean, "return value", "Boolean");
      return GraceFalse;
    };    // end of method visitBlock(_)
    this.methods["visitBlock(1)"] = func424;
    func424.methodName = "visitBlock(1)";
    func424.paramCounts = [1];
    func424.paramNames = ["node"];
    func424.definitionLine = 370;
    func424.definitionModule = "dialect";
    var func435 = function(argcv, var_node) {    // method visitMatchCase(_), line 384
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitMatchCase(_)", 0, numArgs - 1);
      }
      setLineNumber(385);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call436 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call436, var_Boolean, "result of method visitMatchCase(_)", "Boolean");
      return call436;
    };    // end of method visitMatchCase(_)
    this.methods["visitMatchCase(1)"] = func435;
    func435.methodName = "visitMatchCase(1)";
    func435.paramCounts = [1];
    func435.paramNames = ["node"];
    func435.definitionLine = 384;
    func435.definitionModule = "dialect";
    var func437 = function(argcv, var_node) {    // method visitTryCatch(_), line 388
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitTryCatch(_)", 0, numArgs - 1);
      }
      setLineNumber(389);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call438 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call438, var_Boolean, "result of method visitTryCatch(_)", "Boolean");
      return call438;
    };    // end of method visitTryCatch(_)
    this.methods["visitTryCatch(1)"] = func437;
    func437.methodName = "visitTryCatch(1)";
    func437.paramCounts = [1];
    func437.paramNames = ["node"];
    func437.definitionLine = 388;
    func437.definitionModule = "dialect";
    var func439 = function(argcv, var_node) {    // method visitMethodType(_), line 392
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitMethodType(_)", 0, numArgs - 1);
      }
      setLineNumber(393);    // compilenode call
      // call case 2: outer request
      var call440 = selfRequest(importedModules["dialect"], "runRules(1)", [1], var_node);
      setLineNumber(395);    // compilenode member
      // call case 6: other requests
      var call442 = request(var_node, "signature", [0]);
      var block443 = new GraceBlock(this, 395, 1);
      block443.guard = jsTrue;
      block443.real = function block443(var_part) {
        setLineNumber(396);    // compilenode member
        // call case 6: other requests
        var call445 = request(var_part, "params", [0]);
        var block446 = new GraceBlock(this, 396, 1);
        block446.guard = jsTrue;
        block446.real = function block446(var_param) {
          setLineNumber(397);    // compilenode call
          // call case 6: other requests
          if (var_aParameter === undefined) raiseUninitializedVariable("aParameter");
          var call448 = request(var_aParameter, "fromNode(1)", [1], var_param);
          // call case 2: outer request
          var call447 = selfRequest(importedModules["dialect"], "runRules(1)", [1], call448);
          return call447;
        };
        let applyMeth446 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth446.methodName = "apply(1)";
        applyMeth446.paramCounts = [1];
        applyMeth446.paramNames = ["param"];
        applyMeth446.definitionLine = 396;
        applyMeth446.definitionModule = "dialect";
        block446.methods["apply(1)"] = applyMeth446;
        let matchesMeth446 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth446.methodName = "matches(1)";
        matchesMeth446.paramCounts = [1];
        matchesMeth446.paramNames = ["param"];
        matchesMeth446.definitionLine = 396;
        matchesMeth446.definitionModule = "dialect";
        block446.methods["matches(1)"] = matchesMeth446;
        // call case 2: outer request
        var call444 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call445, block446);
        return call444;
      };
      let applyMeth443 = function apply_1 (argcv, ...args) {
          if (this.guard.apply(this.receiver, args))
              return this.real.apply(this.receiver, args);
          badBlockArgs.apply(this, args);
      };
      applyMeth443.methodName = "apply(1)";
      applyMeth443.paramCounts = [1];
      applyMeth443.paramNames = ["part"];
      applyMeth443.definitionLine = 395;
      applyMeth443.definitionModule = "dialect";
      block443.methods["apply(1)"] = applyMeth443;
      let matchesMeth443 = function matches_1 (argcv, ...args) {
          return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
      };
      matchesMeth443.methodName = "matches(1)";
      matchesMeth443.paramCounts = [1];
      matchesMeth443.paramNames = ["part"];
      matchesMeth443.definitionLine = 395;
      matchesMeth443.definitionModule = "dialect";
      block443.methods["matches(1)"] = matchesMeth443;
      // call case 2: outer request
      var call441 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call442, block443);
      setLineNumber(401);    // typecheck
      assertTypeOrMsg(GraceFalse, var_Boolean, "return value", "Boolean");
      return GraceFalse;
    };    // end of method visitMethodType(_)
    this.methods["visitMethodType(1)"] = func439;
    func439.methodName = "visitMethodType(1)";
    func439.paramCounts = [1];
    func439.paramNames = ["node"];
    func439.definitionLine = 392;
    func439.definitionModule = "dialect";
    var func449 = function(argcv, var_node) {    // method visitType(_), line 404
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitType(_)", 0, numArgs - 1);
      }
      setLineNumber(405);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call450 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call450, var_Boolean, "result of method visitType(_)", "Boolean");
      return call450;
    };    // end of method visitType(_)
    this.methods["visitType(1)"] = func449;
    func449.methodName = "visitType(1)";
    func449.paramCounts = [1];
    func449.paramNames = ["node"];
    func449.definitionLine = 404;
    func449.definitionModule = "dialect";
    var func451 = function(argcv, var_node) {    // method visitMethod(_), line 408
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitMethod(_)", 0, numArgs - 1);
      }
      setLineNumber(409);    // compilenode call
      // call case 2: outer request
      var call452 = selfRequest(importedModules["dialect"], "runRules(1)", [1], var_node);
      setLineNumber(411);    // compilenode member
      // call case 6: other requests
      var call454 = request(var_node, "signature", [0]);
      var block455 = new GraceBlock(this, 411, 1);
      block455.guard = jsTrue;
      block455.real = function block455(var_part) {
        setLineNumber(412);    // compilenode member
        // call case 6: other requests
        var call457 = request(var_part, "params", [0]);
        var block458 = new GraceBlock(this, 412, 1);
        block458.guard = jsTrue;
        block458.real = function block458(var_param) {
          setLineNumber(413);    // compilenode call
          // call case 6: other requests
          if (var_aParameter === undefined) raiseUninitializedVariable("aParameter");
          var call460 = request(var_aParameter, "fromNode(1)", [1], var_param);
          // call case 2: outer request
          var call459 = selfRequest(importedModules["dialect"], "runRules(1)", [1], call460);
          return call459;
        };
        let applyMeth458 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth458.methodName = "apply(1)";
        applyMeth458.paramCounts = [1];
        applyMeth458.paramNames = ["param"];
        applyMeth458.definitionLine = 412;
        applyMeth458.definitionModule = "dialect";
        block458.methods["apply(1)"] = applyMeth458;
        let matchesMeth458 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth458.methodName = "matches(1)";
        matchesMeth458.paramCounts = [1];
        matchesMeth458.paramNames = ["param"];
        matchesMeth458.definitionLine = 412;
        matchesMeth458.definitionModule = "dialect";
        block458.methods["matches(1)"] = matchesMeth458;
        // call case 2: outer request
        var call456 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call457, block458);
        return call456;
      };
      let applyMeth455 = function apply_1 (argcv, ...args) {
          if (this.guard.apply(this.receiver, args))
              return this.real.apply(this.receiver, args);
          badBlockArgs.apply(this, args);
      };
      applyMeth455.methodName = "apply(1)";
      applyMeth455.paramCounts = [1];
      applyMeth455.paramNames = ["part"];
      applyMeth455.definitionLine = 411;
      applyMeth455.definitionModule = "dialect";
      block455.methods["apply(1)"] = applyMeth455;
      let matchesMeth455 = function matches_1 (argcv, ...args) {
          return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
      };
      matchesMeth455.methodName = "matches(1)";
      matchesMeth455.paramCounts = [1];
      matchesMeth455.paramNames = ["part"];
      matchesMeth455.definitionLine = 411;
      matchesMeth455.definitionModule = "dialect";
      block455.methods["matches(1)"] = matchesMeth455;
      // call case 2: outer request
      var call453 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call454, block455);
      setLineNumber(417);    // compilenode member
      // call case 6: other requests
      var call462 = request(var_node, "body", [0]);
      var block463 = new GraceBlock(this, 417, 1);
      block463.guard = jsTrue;
      block463.real = function block463(var_stmt) {
        setLineNumber(418);    // compilenode call
        // call case 6: other requests
        var call464 = request(var_stmt, "accept(1)", [1], this);
        return call464;
      };
      let applyMeth463 = function apply_1 (argcv, ...args) {
          if (this.guard.apply(this.receiver, args))
              return this.real.apply(this.receiver, args);
          badBlockArgs.apply(this, args);
      };
      applyMeth463.methodName = "apply(1)";
      applyMeth463.paramCounts = [1];
      applyMeth463.paramNames = ["stmt"];
      applyMeth463.definitionLine = 417;
      applyMeth463.definitionModule = "dialect";
      block463.methods["apply(1)"] = applyMeth463;
      let matchesMeth463 = function matches_1 (argcv, ...args) {
          return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
      };
      matchesMeth463.methodName = "matches(1)";
      matchesMeth463.paramCounts = [1];
      matchesMeth463.paramNames = ["stmt"];
      matchesMeth463.definitionLine = 417;
      matchesMeth463.definitionModule = "dialect";
      block463.methods["matches(1)"] = matchesMeth463;
      // call case 2: outer request
      var call461 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call462, block463);
      setLineNumber(421);    // typecheck
      assertTypeOrMsg(GraceFalse, var_Boolean, "return value", "Boolean");
      return GraceFalse;
    };    // end of method visitMethod(_)
    this.methods["visitMethod(1)"] = func451;
    func451.methodName = "visitMethod(1)";
    func451.paramCounts = [1];
    func451.paramNames = ["node"];
    func451.definitionLine = 408;
    func451.definitionModule = "dialect";
    var func465 = function(argcv, var_node) {    // method visitCall(_), line 424
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitCall(_)", 0, numArgs - 1);
      }
      setLineNumber(425);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call466 = selfRequest(this, "checkMatch(1)", [1], var_node);
      setLineNumber(427);    // compilenode member
      // call case 6: other requests
      var call468 = request(var_node, "receiver", [0]);
      var cases467 = [];
      setLineNumber(428);    // compilenode block
      var block469 = new GraceBlock(this, 428, 1);
      if (var_Member === undefined) raiseUninitializedVariable("Member");
      block469.paramTypes = [var_Member];
      var matches470 = function(var_memb) {
        if (var_Member === undefined) raiseUninitializedVariable("Member");
        if (!Grace_isTrue(request(var_Member, "matches(1)", [1], var_memb)))
            return false;
        return true;
      };
      block469.guard = matches470;
      block469.real = function block469(var_memb) {
        setLineNumber(429);    // compilenode call
        // call case 6: other requests
        // call case 6: other requests
        var call472 = request(var_memb, "receiver", [0]);
        var call471 = request(call472, "accept(1)", [1], this);
        return call471;
      };
      let applyMeth469 = function apply_1 (argcv, ...args) {
          if (this.guard.apply(this.receiver, args))
              return this.real.apply(this.receiver, args);
          badBlockArgs.apply(this, args);
      };
      applyMeth469.methodName = "apply(1)";
      applyMeth469.paramCounts = [1];
      applyMeth469.paramNames = ["memb"];
      applyMeth469.definitionLine = 428;
      applyMeth469.definitionModule = "dialect";
      block469.methods["apply(1)"] = applyMeth469;
      let matchesMeth469 = function matches_1 (argcv, ...args) {
          return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
      };
      matchesMeth469.methodName = "matches(1)";
      matchesMeth469.paramCounts = [1];
      matchesMeth469.paramNames = ["memb"];
      matchesMeth469.definitionLine = 428;
      matchesMeth469.definitionModule = "dialect";
      block469.methods["matches(1)"] = matchesMeth469;
      cases467.push(block469);
      setLineNumber(430);    // compilenode block
      var block473 = new GraceBlock(this, 430, 0);
      block473.guard = jsTrue;
      block473.real = function block473() {
        setLineNumber(430);    // compileBlock
        return GraceDone;
      };
      let applyMeth473 = function apply (argcv) {
          return this.real.apply(this.receiver);
      };
      applyMeth473.methodName = "apply";
      applyMeth473.paramCounts = [0];
      applyMeth473.paramNames = [];
      applyMeth473.definitionLine = 430;
      applyMeth473.definitionModule = "dialect";
      block473.methods["apply"] = applyMeth473;
      setLineNumber(427);    // compilematchcase
      var matchres467 = matchCase(call468,cases467,block473);
      setLineNumber(432);    // compilenode member
      // call case 6: other requests
      var call475 = request(var_node, "parts", [0]);
      var block476 = new GraceBlock(this, 432, 1);
      block476.guard = jsTrue;
      block476.real = function block476(var_part) {
        setLineNumber(433);    // compilenode member
        // call case 6: other requests
        var call478 = request(var_part, "args", [0]);
        var block479 = new GraceBlock(this, 433, 1);
        block479.guard = jsTrue;
        block479.real = function block479(var_arg) {
          setLineNumber(434);    // compilenode call
          // call case 6: other requests
          var call480 = request(var_arg, "accept(1)", [1], this);
          return call480;
        };
        let applyMeth479 = function apply_1 (argcv, ...args) {
            if (this.guard.apply(this.receiver, args))
                return this.real.apply(this.receiver, args);
            badBlockArgs.apply(this, args);
        };
        applyMeth479.methodName = "apply(1)";
        applyMeth479.paramCounts = [1];
        applyMeth479.paramNames = ["arg"];
        applyMeth479.definitionLine = 433;
        applyMeth479.definitionModule = "dialect";
        block479.methods["apply(1)"] = applyMeth479;
        let matchesMeth479 = function matches_1 (argcv, ...args) {
            return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
        };
        matchesMeth479.methodName = "matches(1)";
        matchesMeth479.paramCounts = [1];
        matchesMeth479.paramNames = ["arg"];
        matchesMeth479.definitionLine = 433;
        matchesMeth479.definitionModule = "dialect";
        block479.methods["matches(1)"] = matchesMeth479;
        // call case 2: outer request
        var call477 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call478, block479);
        return call477;
      };
      let applyMeth476 = function apply_1 (argcv, ...args) {
          if (this.guard.apply(this.receiver, args))
              return this.real.apply(this.receiver, args);
          badBlockArgs.apply(this, args);
      };
      applyMeth476.methodName = "apply(1)";
      applyMeth476.paramCounts = [1];
      applyMeth476.paramNames = ["part"];
      applyMeth476.definitionLine = 432;
      applyMeth476.definitionModule = "dialect";
      block476.methods["apply(1)"] = applyMeth476;
      let matchesMeth476 = function matches_1 (argcv, ...args) {
          return this.guard.apply(this.receiver, args) ? GraceTrue : GraceFalse;
      };
      matchesMeth476.methodName = "matches(1)";
      matchesMeth476.paramCounts = [1];
      matchesMeth476.paramNames = ["part"];
      matchesMeth476.definitionLine = 432;
      matchesMeth476.definitionModule = "dialect";
      block476.methods["matches(1)"] = matchesMeth476;
      // call case 2: outer request
      var call474 = selfRequest(var_prelude, "for(1)do(1)", [1, 1], call475, block476);
      setLineNumber(438);    // typecheck
      assertTypeOrMsg(GraceFalse, var_Boolean, "return value", "Boolean");
      return GraceFalse;
    };    // end of method visitCall(_)
    this.methods["visitCall(1)"] = func465;
    func465.methodName = "visitCall(1)";
    func465.paramCounts = [1];
    func465.paramNames = ["node"];
    func465.definitionLine = 424;
    func465.definitionModule = "dialect";
    var func481 = function(argcv, var_node) {    // method visitObject(_), line 441
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitObject(_)", 0, numArgs - 1);
      }
      setLineNumber(442);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call482 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call482, var_Boolean, "result of method visitObject(_)", "Boolean");
      return call482;
    };    // end of method visitObject(_)
    this.methods["visitObject(1)"] = func481;
    func481.methodName = "visitObject(1)";
    func481.paramCounts = [1];
    func481.paramNames = ["node"];
    func481.definitionLine = 441;
    func481.definitionModule = "dialect";
    var func483 = function(argcv, var_node) {    // method visitArray(_), line 445
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitArray(_)", 0, numArgs - 1);
      }
      setLineNumber(446);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call484 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call484, var_Boolean, "result of method visitArray(_)", "Boolean");
      return call484;
    };    // end of method visitArray(_)
    this.methods["visitArray(1)"] = func483;
    func483.methodName = "visitArray(1)";
    func483.paramCounts = [1];
    func483.paramNames = ["node"];
    func483.definitionLine = 445;
    func483.definitionModule = "dialect";
    var func485 = function(argcv, var_node) {    // method visitMember(_), line 449
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitMember(_)", 0, numArgs - 1);
      }
      setLineNumber(450);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call486 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call486, var_Boolean, "result of method visitMember(_)", "Boolean");
      return call486;
    };    // end of method visitMember(_)
    this.methods["visitMember(1)"] = func485;
    func485.methodName = "visitMember(1)";
    func485.paramCounts = [1];
    func485.paramNames = ["node"];
    func485.definitionLine = 449;
    func485.definitionModule = "dialect";
    var func487 = function(argcv, var_node) {    // method visitGeneric(_), line 453
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitGeneric(_)", 0, numArgs - 1);
      }
      setLineNumber(454);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call488 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call488, var_Boolean, "result of method visitGeneric(_)", "Boolean");
      return call488;
    };    // end of method visitGeneric(_)
    this.methods["visitGeneric(1)"] = func487;
    func487.methodName = "visitGeneric(1)";
    func487.paramCounts = [1];
    func487.paramNames = ["node"];
    func487.definitionLine = 453;
    func487.definitionModule = "dialect";
    var func489 = function(argcv, var_node) {    // method visitIdentifier(_), line 457
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitIdentifier(_)", 0, numArgs - 1);
      }
      setLineNumber(458);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call490 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call490, var_Boolean, "result of method visitIdentifier(_)", "Boolean");
      return call490;
    };    // end of method visitIdentifier(_)
    this.methods["visitIdentifier(1)"] = func489;
    func489.methodName = "visitIdentifier(1)";
    func489.paramCounts = [1];
    func489.paramNames = ["node"];
    func489.definitionLine = 457;
    func489.definitionModule = "dialect";
    var func491 = function(argcv, var_node) {    // method visitOctets(_), line 461
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitOctets(_)", 0, numArgs - 1);
      }
      setLineNumber(462);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call492 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call492, var_Boolean, "result of method visitOctets(_)", "Boolean");
      return call492;
    };    // end of method visitOctets(_)
    this.methods["visitOctets(1)"] = func491;
    func491.methodName = "visitOctets(1)";
    func491.paramCounts = [1];
    func491.paramNames = ["node"];
    func491.definitionLine = 461;
    func491.definitionModule = "dialect";
    var func493 = function(argcv, var_node) {    // method visitString(_), line 465
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitString(_)", 0, numArgs - 1);
      }
      setLineNumber(466);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call494 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call494, var_Boolean, "result of method visitString(_)", "Boolean");
      return call494;
    };    // end of method visitString(_)
    this.methods["visitString(1)"] = func493;
    func493.methodName = "visitString(1)";
    func493.paramCounts = [1];
    func493.paramNames = ["node"];
    func493.definitionLine = 465;
    func493.definitionModule = "dialect";
    var func495 = function(argcv, var_node) {    // method visitNum(_), line 469
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitNum(_)", 0, numArgs - 1);
      }
      setLineNumber(470);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call496 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call496, var_Boolean, "result of method visitNum(_)", "Boolean");
      return call496;
    };    // end of method visitNum(_)
    this.methods["visitNum(1)"] = func495;
    func495.methodName = "visitNum(1)";
    func495.paramCounts = [1];
    func495.paramNames = ["node"];
    func495.definitionLine = 469;
    func495.definitionModule = "dialect";
    var func497 = function(argcv, var_node) {    // method visitOp(_), line 473
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitOp(_)", 0, numArgs - 1);
      }
      setLineNumber(474);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call498 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call498, var_Boolean, "result of method visitOp(_)", "Boolean");
      return call498;
    };    // end of method visitOp(_)
    this.methods["visitOp(1)"] = func497;
    func497.methodName = "visitOp(1)";
    func497.paramCounts = [1];
    func497.paramNames = ["node"];
    func497.definitionLine = 473;
    func497.definitionModule = "dialect";
    var func499 = function(argcv, var_node) {    // method visitBind(_), line 477
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitBind(_)", 0, numArgs - 1);
      }
      setLineNumber(478);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call500 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call500, var_Boolean, "result of method visitBind(_)", "Boolean");
      return call500;
    };    // end of method visitBind(_)
    this.methods["visitBind(1)"] = func499;
    func499.methodName = "visitBind(1)";
    func499.paramCounts = [1];
    func499.paramNames = ["node"];
    func499.definitionLine = 477;
    func499.definitionModule = "dialect";
    var func501 = function(argcv, var_node) {    // method visitDefDec(_), line 481
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitDefDec(_)", 0, numArgs - 1);
      }
      setLineNumber(482);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call502 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call502, var_Boolean, "result of method visitDefDec(_)", "Boolean");
      return call502;
    };    // end of method visitDefDec(_)
    this.methods["visitDefDec(1)"] = func501;
    func501.methodName = "visitDefDec(1)";
    func501.paramCounts = [1];
    func501.paramNames = ["node"];
    func501.definitionLine = 481;
    func501.definitionModule = "dialect";
    var func503 = function(argcv, var_node) {    // method visitVarDec(_), line 485
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitVarDec(_)", 0, numArgs - 1);
      }
      setLineNumber(486);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call504 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call504, var_Boolean, "result of method visitVarDec(_)", "Boolean");
      return call504;
    };    // end of method visitVarDec(_)
    this.methods["visitVarDec(1)"] = func503;
    func503.methodName = "visitVarDec(1)";
    func503.paramCounts = [1];
    func503.paramNames = ["node"];
    func503.definitionLine = 485;
    func503.definitionModule = "dialect";
    var func505 = function(argcv, var_node) {    // method visitImport(_), line 489
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitImport(_)", 0, numArgs - 1);
      }
      setLineNumber(490);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call506 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call506, var_Boolean, "result of method visitImport(_)", "Boolean");
      return call506;
    };    // end of method visitImport(_)
    this.methods["visitImport(1)"] = func505;
    func505.methodName = "visitImport(1)";
    func505.paramCounts = [1];
    func505.paramNames = ["node"];
    func505.definitionLine = 489;
    func505.definitionModule = "dialect";
    var func507 = function(argcv, var_node) {    // method visitReturn(_), line 493
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitReturn(_)", 0, numArgs - 1);
      }
      setLineNumber(494);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call508 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call508, var_Boolean, "result of method visitReturn(_)", "Boolean");
      return call508;
    };    // end of method visitReturn(_)
    this.methods["visitReturn(1)"] = func507;
    func507.methodName = "visitReturn(1)";
    func507.paramCounts = [1];
    func507.paramNames = ["node"];
    func507.definitionLine = 493;
    func507.definitionModule = "dialect";
    var func509 = function(argcv, var_node) {    // method visitInherits(_), line 497
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitInherits(_)", 0, numArgs - 1);
      }
      setLineNumber(498);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call510 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call510, var_Boolean, "result of method visitInherits(_)", "Boolean");
      return call510;
    };    // end of method visitInherits(_)
    this.methods["visitInherits(1)"] = func509;
    func509.methodName = "visitInherits(1)";
    func509.paramCounts = [1];
    func509.paramNames = ["node"];
    func509.definitionLine = 497;
    func509.definitionModule = "dialect";
    var func511 = function(argcv, var_node) {    // method visitDialect(_), line 501
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("visitDialect(_)", 0, numArgs - 1);
      }
      setLineNumber(502);    // compilenode call
      // call case 4: self request with 1 args and 0 typeArgs 
      var call512 = selfRequest(this, "checkMatch(1)", [1], var_node);
      assertTypeOrMsg(call512, var_Boolean, "result of method visitDialect(_)", "Boolean");
      return call512;
    };    // end of method visitDialect(_)
    this.methods["visitDialect(1)"] = func511;
    func511.methodName = "visitDialect(1)";
    func511.paramCounts = [1];
    func511.paramNames = ["node"];
    func511.definitionLine = 501;
    func511.definitionModule = "dialect";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 358;
        m.definitionModule = "dialect";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj418_init = function() {    // init of object on line 358
      initFun419.call(this);
    };
    return obj418_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj418 = emptyGraceObject("astVisitor", "dialect", 358);
  var obj418_init = obj418_build.call(obj418, null, this, [], []);
  obj418_init.call(obj418);  // end of compileobject
  var var_astVisitor = obj418;
  var reader513_astVisitor = function() {  // reader method astVisitor
      if (var_astVisitor === undefined) raiseUninitializedVariable("astVisitor");
      return var_astVisitor;
  };
  reader513_astVisitor.isDef = true;
  reader513_astVisitor.confidential = true;
  this.methods["astVisitor"] = reader513_astVisitor;
  setLineNumber(507);    // compilenode object
  var obj514_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_dialect_507");
    this.outer_dialect_507 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func515 = function(argcv, var_node) {    // method fromNode(_), line 508
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("fromNode(_)", 0, numArgs - 1);
      }
      var ouc = emptyGraceObject("aTypeAnnotation.fromNode(_)", "dialect", 508);
      var ouc_init = this.methods["fromNode(1)$build(3)"].call(this, null, var_node, ouc, [], []);
      ouc_init.call(ouc);
      if (var_TypeAnnotation === undefined) raiseUninitializedVariable("TypeAnnotation");
      setLineNumber(514);    // typecheck
      assertTypeOrMsg(ouc, var_TypeAnnotation, "object returned from fromNode(_)", "TypeAnnotation");
      return ouc;
    };    // end of method fromNode(_)
    this.methods["fromNode(1)"] = func515;
    func515.methodName = "fromNode(1)";
    func515.paramCounts = [1];
    func515.paramNames = ["node"];
    func515.definitionLine = 508;
    func515.definitionModule = "dialect";
    var func516 = function(argcv, var_node, inheritingObject, aliases, exclusions) {    // method fromNode(_)$build(_,_,_), line 508
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 4;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("fromNode(_)", 0, numArgs - 1);
      }
      var obj517_build = function(ignore, var_node, outerObj, aliases, exclusions) {
        this.closureKeys = this.closureKeys || [];
        this.closureKeys.push("outer_dialect_508");
        this.outer_dialect_508 = outerObj;
        const inheritedExclusions = { };
        for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
            const exMeth = exclusions[eix];
            inheritedExclusions[exMeth] = this.methods[exMeth]; };
        setLineNumber(509);    // reuse call
        var initFun518 = selfRequest(importedModules["dialect"], "identityEquality$build(3)", [null], this, [], []);  // compileReuseCall
        this.data.kind = undefined;
        var reader519_kind = function() {  // reader method kind
            if (this.data.kind === undefined) raiseUninitializedVariable("kind");
            return this.data.kind;
        };
        reader519_kind.isDef = true;
        this.methods["kind"] = reader519_kind;
        this.data.value = undefined;
        var reader520_value = function() {  // reader method value
            if (this.data.value === undefined) raiseUninitializedVariable("value");
            return this.data.value;
        };
        reader520_value.isDef = true;
        this.methods["value"] = reader520_value;
        this.data.line = undefined;
        var reader521_line = function() {  // reader method line
            if (this.data.line === undefined) raiseUninitializedVariable("line");
            return this.data.line;
        };
        reader521_line.isDef = true;
        this.methods["line"] = reader521_line;
        this.data.linePos = undefined;
        var reader522_linePos = function() {  // reader method linePos
            if (this.data.linePos === undefined) raiseUninitializedVariable("linePos");
            return this.data.linePos;
        };
        reader522_linePos.isDef = true;
        this.methods["linePos"] = reader522_linePos;
        const overridenByAliases = { };
        for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
            const a = aliases[aix];
            const newNm = a.newName;
            const oldNm = a.oldName;
            overridenByAliases[newNm] = this.methods[newNm];
            const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
            m.definitionLine = 508;
            m.definitionModule = "dialect";
            this.methods[newNm] = m;
        }
        for (let exName in inheritedExclusions) {
            if (inheritedExclusions.hasOwnProperty(exName)) {
                if (inheritedExclusions[exName]) {
                    this.methods[exName] = inheritedExclusions[exName];
                } else {
                    delete this.methods[exName];
                }
            }
        }
        var obj517_init = function() {    // init of object on line 508
          setLineNumber(510);    // compilenode string
          var string523 = new GraceString("dtype");
          this.data.kind = string523;
          this.data.value = var_node;
          setLineNumber(512);    // compilenode member
          // call case 6: other requests
          var call524 = request(var_node, "line", [0]);
          this.data.line = call524;
          setLineNumber(513);    // compilenode member
          // call case 6: other requests
          var call525 = request(var_node, "linePos", [0]);
          this.data.linePos = call525;
        };
        return obj517_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
      };
      var obj517_init = obj517_build.call(inheritingObject, null, var_node, this, aliases, exclusions);
      return obj517_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
    };    // end of method fromNode(_)$build(_,_,_)
    this.methods["fromNode(1)$build(3)"] = func516;
    func516.methodName = "fromNode(1)$build(3)";
    func516.paramCounts = [1];
    func516.paramNames = ["node"];
    func516.definitionLine = 508;
    func516.definitionModule = "dialect";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 507;
        m.definitionModule = "dialect";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj514_init = function() {    // init of object on line 507
    };
    return obj514_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj514 = emptyGraceObject("aTypeAnnotation", "dialect", 507);
  var obj514_init = obj514_build.call(obj514, null, this, [], []);
  obj514_init.call(obj514);  // end of compileobject
  var var_aTypeAnnotation = obj514;
  var reader526_aTypeAnnotation = function() {  // reader method aTypeAnnotation
      if (var_aTypeAnnotation === undefined) raiseUninitializedVariable("aTypeAnnotation");
      return var_aTypeAnnotation;
  };
  reader526_aTypeAnnotation.isDef = true;
  reader526_aTypeAnnotation.confidential = true;
  this.methods["aTypeAnnotation"] = reader526_aTypeAnnotation;
  setLineNumber(517);    // compilenode object
  var obj527_build = function(ignore, outerObj, aliases, exclusions) {
    this.closureKeys = this.closureKeys || [];
    this.closureKeys.push("outer_dialect_517");
    this.outer_dialect_517 = outerObj;
    const inheritedExclusions = { };
    for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
        const exMeth = exclusions[eix];
        inheritedExclusions[exMeth] = this.methods[exMeth]; };
    var func528 = function(argcv, var_node) {    // method fromNode(_), line 518
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 1;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("fromNode(_)", 0, numArgs - 1);
      }
      var ouc = emptyGraceObject("aParameter.fromNode(_)", "dialect", 518);
      var ouc_init = this.methods["fromNode(1)$build(3)"].call(this, null, var_node, ouc, [], []);
      ouc_init.call(ouc);
      if (var_Parameter === undefined) raiseUninitializedVariable("Parameter");
      setLineNumber(525);    // typecheck
      assertTypeOrMsg(ouc, var_Parameter, "object returned from fromNode(_)", "Parameter");
      return ouc;
    };    // end of method fromNode(_)
    this.methods["fromNode(1)"] = func528;
    func528.methodName = "fromNode(1)";
    func528.paramCounts = [1];
    func528.paramNames = ["node"];
    func528.definitionLine = 518;
    func528.definitionModule = "dialect";
    var func529 = function(argcv, var_node, inheritingObject, aliases, exclusions) {    // method fromNode(_)$build(_,_,_), line 518
      var returnTarget = invocationCount;
      invocationCount++;
      const numArgs = arguments.length - 4;
      if ((numArgs > 1) && (numArgs !== 1)) {
          raiseTypeArgError("fromNode(_)", 0, numArgs - 1);
      }
      var obj530_build = function(ignore, var_node, outerObj, aliases, exclusions) {
        this.closureKeys = this.closureKeys || [];
        this.closureKeys.push("outer_dialect_518");
        this.outer_dialect_518 = outerObj;
        const inheritedExclusions = { };
        for (var eix = 0, eLen = exclusions.length; eix < eLen; eix ++) {
            const exMeth = exclusions[eix];
            inheritedExclusions[exMeth] = this.methods[exMeth]; };
        setLineNumber(519);    // reuse call
        var initFun531 = selfRequest(importedModules["dialect"], "identityEquality$build(3)", [null], this, [], []);  // compileReuseCall
        this.data.kind = undefined;
        var reader532_kind = function() {  // reader method kind
            if (this.data.kind === undefined) raiseUninitializedVariable("kind");
            return this.data.kind;
        };
        reader532_kind.isDef = true;
        this.methods["kind"] = reader532_kind;
        this.data.value = undefined;
        var reader533_value = function() {  // reader method value
            if (this.data.value === undefined) raiseUninitializedVariable("value");
            return this.data.value;
        };
        reader533_value.isDef = true;
        this.methods["value"] = reader533_value;
        this.data.dtype = undefined;
        var reader534_dtype = function() {  // reader method dtype
            if (this.data.dtype === undefined) raiseUninitializedVariable("dtype");
            return this.data.dtype;
        };
        reader534_dtype.isDef = true;
        this.methods["dtype"] = reader534_dtype;
        this.data.line = undefined;
        var reader535_line = function() {  // reader method line
            if (this.data.line === undefined) raiseUninitializedVariable("line");
            return this.data.line;
        };
        reader535_line.isDef = true;
        this.methods["line"] = reader535_line;
        this.data.linePos = undefined;
        var reader536_linePos = function() {  // reader method linePos
            if (this.data.linePos === undefined) raiseUninitializedVariable("linePos");
            return this.data.linePos;
        };
        reader536_linePos.isDef = true;
        this.methods["linePos"] = reader536_linePos;
        const overridenByAliases = { };
        for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
            const a = aliases[aix];
            const newNm = a.newName;
            const oldNm = a.oldName;
            overridenByAliases[newNm] = this.methods[newNm];
            const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
            m.definitionLine = 518;
            m.definitionModule = "dialect";
            this.methods[newNm] = m;
        }
        for (let exName in inheritedExclusions) {
            if (inheritedExclusions.hasOwnProperty(exName)) {
                if (inheritedExclusions[exName]) {
                    this.methods[exName] = inheritedExclusions[exName];
                } else {
                    delete this.methods[exName];
                }
            }
        }
        var obj530_init = function() {    // init of object on line 518
          setLineNumber(520);    // compilenode string
          var string537 = new GraceString("parameter");
          this.data.kind = string537;
          setLineNumber(521);    // compilenode member
          // call case 6: other requests
          var call538 = request(var_node, "value", [0]);
          this.data.value = call538;
          setLineNumber(522);    // compilenode member
          // call case 6: other requests
          var call539 = request(var_node, "dtype", [0]);
          this.data.dtype = call539;
          setLineNumber(523);    // compilenode member
          // call case 6: other requests
          var call540 = request(var_node, "line", [0]);
          this.data.line = call540;
          setLineNumber(524);    // compilenode member
          // call case 6: other requests
          var call541 = request(var_node, "linePos", [0]);
          this.data.linePos = call541;
        };
        return obj530_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
      };
      var obj530_init = obj530_build.call(inheritingObject, null, var_node, this, aliases, exclusions);
      return obj530_init;      // from compileBuildMethodFor(_)withObjCon(_)inside(_)
    };    // end of method fromNode(_)$build(_,_,_)
    this.methods["fromNode(1)$build(3)"] = func529;
    func529.methodName = "fromNode(1)$build(3)";
    func529.paramCounts = [1];
    func529.paramNames = ["node"];
    func529.definitionLine = 518;
    func529.definitionModule = "dialect";
    const overridenByAliases = { };
    for (let aix = 0, aLen = aliases.length; aix < aLen; aix ++) {
        const a = aliases[aix];
        const newNm = a.newName;
        const oldNm = a.oldName;
        overridenByAliases[newNm] = this.methods[newNm];
        const m = confidentialVersion(overridenByAliases[oldNm] || this.methods[oldNm], newNm);
        m.definitionLine = 517;
        m.definitionModule = "dialect";
        this.methods[newNm] = m;
    }
    for (let exName in inheritedExclusions) {
        if (inheritedExclusions.hasOwnProperty(exName)) {
            if (inheritedExclusions[exName]) {
                this.methods[exName] = inheritedExclusions[exName];
            } else {
                delete this.methods[exName];
            }
        }
    }
    var obj527_init = function() {    // init of object on line 517
    };
    return obj527_init;   // from compileBuildAndInitFunctions(_)inMethod(_)
  };
  var obj527 = emptyGraceObject("aParameter", "dialect", 517);
  var obj527_init = obj527_build.call(obj527, null, this, [], []);
  obj527_init.call(obj527);  // end of compileobject
  var var_aParameter = obj527;
  var reader542_aParameter = function() {  // reader method aParameter
      if (var_aParameter === undefined) raiseUninitializedVariable("aParameter");
      return var_aParameter;
  };
  reader542_aParameter.isDef = true;
  reader542_aParameter.confidential = true;
  this.methods["aParameter"] = reader542_aParameter;
  return this;
}
if (typeof global !== "undefined")
  global.gracecode_dialect = gracecode_dialect;
if (typeof window !== "undefined")
  window.gracecode_dialect = gracecode_dialect;
gracecode_dialect.imports = ["ast", "errormessages", "standardGrace"];
gracecode_dialect.definitionModule = "dialect";
gracecode_dialect.definitionLine = 1;
